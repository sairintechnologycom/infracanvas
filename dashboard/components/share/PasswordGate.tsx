'use client'
/**
 * PasswordGate — placeholder shipped with Task 1 of plan 07-09.
 * Task 2 implements the real component (zero-metadata enforcement,
 * 401/410/429 handling, ShareViewer mount on success).
 */
interface Props {
  token: string
}

export function PasswordGate(_props: Props) {
  return null
}
