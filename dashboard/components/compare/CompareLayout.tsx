'use client'
import type { ResourceDiff } from '@/lib/types'
// stub — implementation in Task 2
export function CompareLayout(_props: { diff: ResourceDiff; scanAId: string; scanBId: string }) {
  return null
}
