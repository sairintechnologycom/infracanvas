'use client'
import type { NodeDiff } from '@/lib/types'
// stub — implementation in GREEN
export function DiffNodeList(_props: { nodes: NodeDiff[]; onSelect?: (id: string) => void }) {
  return null
}
