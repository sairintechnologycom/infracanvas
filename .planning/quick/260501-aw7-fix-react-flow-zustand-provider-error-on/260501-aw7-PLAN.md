---
phase: 260501-aw7
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - dashboard/components/scans/ScanViewerClient.tsx
autonomous: false
requirements:
  - QUICK-AW7-01
must_haves:
  truths:
    - "Opening /scans/[id] in dev mode (DEV_BYPASS_AUTH=1) renders the diagram canvas with no React Flow zustand provider error in the browser console"
    - "DiagramCanvas inside ScanViewerClient is wrapped by a ReactFlowProvider ancestor (mirrors viewer/src/App.tsx:111)"
    - "Existing ViewerProvider wrapping is preserved (graph store still drives the canvas)"
  artifacts:
    - path: "dashboard/components/scans/ScanViewerClient.tsx"
      provides: "ReactFlowProvider-wrapped DiagramCanvas mount"
      contains: "ReactFlowProvider"
  key_links:
    - from: "dashboard/components/scans/ScanViewerClient.tsx"
      to: "@xyflow/react"
      via: "named import"
      pattern: "import \\{[^}]*ReactFlowProvider[^}]*\\} from ['\"]@xyflow/react['\"]"
    - from: "dashboard/components/scans/ScanViewerClient.tsx"
      to: "DiagramCanvas"
      via: "JSX nesting inside ReactFlowProvider"
      pattern: "<ReactFlowProvider>[\\s\\S]*<DiagramCanvas"
---

<objective>
Fix the runtime error "[React Flow]: Seems like you have not used zustand provider as an ancestor" that fires when the user opens /scans/[id] in dev mode (DEV_BYPASS_AUTH=1, dev/local-no-auth branch).

Purpose: React Flow v12 hooks (useReactFlow, useStore for fitView, etc.) used inside DiagramCanvas require a `<ReactFlowProvider>` ancestor. The standalone viewer (viewer/src/App.tsx:111) already wraps DiagramCanvas in ReactFlowProvider; the dashboard's ScanViewerClient currently only wraps it in `<ViewerProvider>`, so the React Flow internal store is missing.

Output: ScanViewerClient.tsx renders `<DiagramCanvas />` inside both `<ViewerProvider>` and `<ReactFlowProvider>`, matching the viewer's mount pattern. No regressions to scan loading, error states, or the graph store wiring.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@dashboard/components/scans/ScanViewerClient.tsx
@viewer/src/App.tsx

<interfaces>
<!-- Key APIs the executor needs. Already verified during planning. -->

From @xyflow/react (v12.6.0, hoisted at repo root node_modules — resolvable from dashboard via workspace hoisting):
```typescript
export const ReactFlowProvider: React.FC<{ children: React.ReactNode }>;
```

From @infracanvas/viewer (already imported in ScanViewerClient.tsx):
```typescript
export const ViewerProvider: React.FC<{ store: ViewerStoreApi; children: React.ReactNode }>;
export const DiagramCanvas: React.FC;
export function createViewerStore(): ViewerStoreApi;
```

Note: The viewer package does NOT re-export ReactFlowProvider (verified: viewer/src/index.ts has no `xyflow` references), so importing directly from `@xyflow/react` is the correct approach. The package is already a transitive dep via @infracanvas/viewer, hoisted to the repo root.

Reference pattern from viewer/src/App.tsx:111 and :140:
```tsx
return (
  <ReactFlowProvider>
    <div className="flex flex-col h-screen w-screen" ...>
      ...
      <DiagramCanvas />
      ...
    </div>
  </ReactFlowProvider>
);
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Wrap DiagramCanvas with ReactFlowProvider in ScanViewerClient</name>
  <files>dashboard/components/scans/ScanViewerClient.tsx</files>
  <behavior>
    - Import ReactFlowProvider from `@xyflow/react` at the top of the file
    - In the success-path return (currently lines 93-99), wrap the existing `<DiagramCanvas />` so the JSX tree becomes:
      `<ViewerProvider store={store}><ReactFlowProvider><DiagramCanvas /></ReactFlowProvider></ViewerProvider>`
    - Preserve the outer `<div className="h-full w-full" data-testid="scan-viewer-client">` wrapper exactly as-is
    - Do NOT alter loading state, error state, fetch logic, store creation, or useEffect dependencies
    - ReactFlowProvider must be INSIDE ViewerProvider (so the viewer store is available everywhere DiagramCanvas reads from it, and React Flow's internal store is scoped to the canvas mount)
  </behavior>
  <action>
    Edit `dashboard/components/scans/ScanViewerClient.tsx`:

    1. Add a new import line after the existing `@infracanvas/viewer` imports (line 7):
       ```tsx
       import { ReactFlowProvider } from '@xyflow/react'
       ```

    2. Replace the success-path JSX block (lines 93-99). Current:
       ```tsx
       return (
         <div className="h-full w-full" data-testid="scan-viewer-client">
           <ViewerProvider store={store}>
             <DiagramCanvas />
           </ViewerProvider>
         </div>
       )
       ```
       New:
       ```tsx
       return (
         <div className="h-full w-full" data-testid="scan-viewer-client">
           <ViewerProvider store={store}>
             <ReactFlowProvider>
               <DiagramCanvas />
             </ReactFlowProvider>
           </ViewerProvider>
         </div>
       )
       ```

    Rationale for ReactFlowProvider INSIDE ViewerProvider (not outside): matches the standalone viewer's nesting intent — ViewerProvider is the app-level state context (Zustand store with graph/filters), ReactFlowProvider is the canvas-internal store. Either order works for the immediate bug fix, but inside-out preserves the conceptual hierarchy and avoids surprising the viewer package's own assumptions.

    Do NOT change anything else — no refactors, no formatter sweeps, no touching the loading/error branches.
  </action>
  <verify>
    <automated>cd /Users/bhushan/Documents/Projects/Infracanvas/dashboard && grep -E "import \{ ReactFlowProvider \} from '@xyflow/react'" components/scans/ScanViewerClient.tsx && grep -Pzo '(?s)<ViewerProvider[^>]*>\s*<ReactFlowProvider>\s*<DiagramCanvas\s*/>\s*</ReactFlowProvider>\s*</ViewerProvider>' components/scans/ScanViewerClient.tsx && npx tsc --noEmit -p tsconfig.json</automated>
  </verify>
  <done>
    - `import { ReactFlowProvider } from '@xyflow/react'` present in ScanViewerClient.tsx
    - `<DiagramCanvas />` is nested inside `<ReactFlowProvider>` which is nested inside `<ViewerProvider>`
    - `npx tsc --noEmit` from dashboard/ passes with no new errors
    - No other lines in ScanViewerClient.tsx changed
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human verification — open scan detail page in dev mode</name>
  <what-built>
    ScanViewerClient now wraps DiagramCanvas in ReactFlowProvider, mirroring viewer/src/App.tsx:111. The runtime React Flow zustand provider error should no longer fire on /scans/[id].
  </what-built>
  <how-to-verify>
    1. Ensure dev mode is active: `DEV_BYPASS_AUTH=1` in dashboard env, on branch `dev/local-no-auth`.
    2. Start the dashboard dev server: `cd dashboard && npm run dev` (default port 3001).
    3. Open http://localhost:3001 in a browser with DevTools console open.
    4. From the home dashboard, click any scan row to navigate to /scans/[id].
    5. Expected:
       - The diagram canvas renders (React Flow nodes/edges visible, fit-view applied if applicable).
       - DevTools console shows NO error matching `[React Flow]: Seems like you have not used zustand provider as an ancestor`.
       - No new console errors introduced by this change.
    6. Optional sanity: refresh the page, navigate away and back — the canvas should remount cleanly.
  </how-to-verify>
  <resume-signal>Type "approved" if the canvas renders without the React Flow zustand error, or describe any issues you see.</resume-signal>
</task>

</tasks>

<verification>
- TypeScript: `cd dashboard && npx tsc --noEmit` passes (no new type errors).
- Static check: ScanViewerClient.tsx contains exactly one `ReactFlowProvider` import and one `<ReactFlowProvider>` wrapping `<DiagramCanvas />`.
- Runtime (human): /scans/[id] renders the diagram with no React Flow zustand provider console error.
- No other files modified.
</verification>

<success_criteria>
- The runtime error "[React Flow]: Seems like you have not used zustand provider as an ancestor" no longer appears in the browser console when opening /scans/[id].
- DiagramCanvas renders correctly inside the dashboard scan detail page.
- TypeScript compilation passes.
- ViewerProvider wrapping and all surrounding logic (loading, error, fetch, store creation) are unchanged.
</success_criteria>

<output>
After completion, create `.planning/quick/260501-aw7-fix-react-flow-zustand-provider-error-on/260501-aw7-01-SUMMARY.md` describing:
- The single import added and the JSX wrap change
- Confirmation that no other logic was touched
- Result of the human verification step
</output>
