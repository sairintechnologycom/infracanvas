---
phase: 07.2
plan: 07
type: execute
wave: 2
# Wave 2 (independent of Wave 1; depends_on intentionally empty).
depends_on: []
files_modified:
  - dashboard/components/share/ShareModal.tsx
autonomous: true
requirements: [D-08]
must_haves:
  truths:
    - "ShareModal uses shadcn <Select/> instead of native <select>"
    - "The expiry label reads exactly 'Link expires in' (not 'Expires in')"
    - "The warning paragraph uses className text-amber-600 (not text-slate-700)"
    - "Existing ShareModal tests still pass with selectors updated for shadcn Select"
  artifacts:
    - path: "dashboard/components/share/ShareModal.tsx"
      provides: "ShareModal with shadcn Select, locked label, amber warning"
      contains: "Link expires in"
  key_links:
    - from: "ShareModal.tsx"
      to: "@/components/ui/select (shadcn primitive landed in plan 07.1-01)"
      via: "import statement"
      pattern: "from ['\"]@/components/ui/select['\"]"
---

<objective>
Migrate `ShareModal.tsx` away from a native `<select>` to the shadcn `<Select/>` primitive (already landed in plan 07.1-01), update the expiry label copy to spec, and change the warning paragraph color to the spec's amber.

Purpose: D-08 P1 polish — Success-criteria item 7 mandates all three: shadcn `<Select/>`, "Link expires in" label, and `text-amber-600` warning. Native select in a shadcn Dialog also breaks visual consistency.
Output: Single-file edit covering the Select primitive swap + 2 copy/color tweaks.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/07.2-ui-contract-remediation-live/07.2-CONTEXT.md
@.planning/phases/07.1-phase-7-ui-contract-remediation/07.1-LIVE-UI-REVIEW.md
@dashboard/components/share/ShareModal.tsx
@dashboard/components/ui/select.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace native select with shadcn Select + lock label + amber warning</name>
  <files>dashboard/components/share/ShareModal.tsx</files>
  <read_first>
    - dashboard/components/share/ShareModal.tsx (the file being modified — read full file; locate `<select id="share-expiry">` block (~lines 139–150) and warning `<p>` (~line 155))
    - dashboard/components/ui/select.tsx (shadcn primitive — confirm export names: Select, SelectTrigger, SelectValue, SelectContent, SelectItem)
    - .planning/phases/07.2-ui-contract-remediation-live/07.2-CONTEXT.md (D-08 section, lines 80–86)
    - tests for ShareModal (find via `grep -rn "ShareModal" dashboard --include="*.test.tsx"` — existing tests at memory 1136 expansion may need selector updates)
  </read_first>
  <action>
    In `dashboard/components/share/ShareModal.tsx`:

    Edit 1 — Add shadcn Select import at top (if not already present):
    ```tsx
    import {
      Select,
      SelectTrigger,
      SelectValue,
      SelectContent,
      SelectItem,
    } from '@/components/ui/select'
    ```

    Edit 2 — Replace the native `<select id="share-expiry">...</select>` block (~lines 139–150)
    with the shadcn equivalent. Same option values, same default state, same onChange handler
    semantics (the shadcn Select uses `onValueChange` instead of `onChange={e => e.target.value}`):

    ```tsx
    <Select value={expiry} onValueChange={setExpiry}>
      <SelectTrigger id="share-expiry">
        <SelectValue />
      </SelectTrigger>
      <SelectContent>
        <SelectItem value="1d">1 day</SelectItem>
        <SelectItem value="7d">7 days</SelectItem>
        <SelectItem value="30d">30 days</SelectItem>
        <SelectItem value="never">Never</SelectItem>
      </SelectContent>
    </Select>
    ```

    Preserve the existing state variable name (`expiry`/`setExpiry` or whatever it currently
    is) and the existing option values exactly — do NOT change the API contract sent to the
    backend.

    Edit 3 — Change the label text immediately above this Select. Find the `<label>` (or
    label element) currently containing the text "Expires in" and change it to
    "Link expires in" exactly. Do not change the `htmlFor`/`id` linkage.

    Edit 4 — Around line ~155, find the warning `<p>` element with className
    `text-xs text-slate-700 mt-1`. Change ONLY the className to
    `text-xs text-amber-600 mt-1`. Do NOT change the warning copy text itself.

    Edit 5 — If existing ShareModal tests query the native select (e.g., `getByRole('combobox')`,
    `getByLabelText('Expires in')`), update the selectors:
    - "Expires in" → "Link expires in"
    - For shadcn Select interaction patterns: tests may need to use `userEvent.click(trigger)`
      then `userEvent.click(option)` instead of `fireEvent.change`. Mirror any existing
      shadcn Select test patterns in the dashboard test suite.
  </action>
  <verify>
    <automated>! grep -n "<select" dashboard/components/share/ShareModal.tsx && grep -n "Link expires in" dashboard/components/share/ShareModal.tsx && grep -n "text-amber-600" dashboard/components/share/ShareModal.tsx && grep -n "from '@/components/ui/select'" dashboard/components/share/ShareModal.tsx</automated>
  </verify>
  <done>
    - `grep "<select" dashboard/components/share/ShareModal.tsx` returns 0 hits
    - File imports `Select` from `@/components/ui/select`
    - `grep "Link expires in" dashboard/components/share/ShareModal.tsx` returns ≥1 hit
    - `grep "Expires in" dashboard/components/share/ShareModal.tsx | grep -v "Link expires in"` returns 0 hits (no stray "Expires in" lingering)
    - `grep "text-amber-600" dashboard/components/share/ShareModal.tsx` returns ≥1 hit (on the warning paragraph)
    - `grep "text-slate-700" dashboard/components/share/ShareModal.tsx` returns 0 hits on the warning paragraph specifically
    - `cd dashboard && npm test -- ShareModal` passes
  </done>
  <acceptance_criteria>
    - `grep -c "<select" dashboard/components/share/ShareModal.tsx` returns 0
    - `grep -c "Link expires in" dashboard/components/share/ShareModal.tsx` returns ≥1
    - `grep -c "from '@/components/ui/select'" dashboard/components/share/ShareModal.tsx` returns 1
    - `grep -c "text-amber-600" dashboard/components/share/ShareModal.tsx` returns ≥1
    - `cd dashboard && npm test` passes (no test count regression vs current baseline)
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| User input ↔ ShareModal expiry value | Constrained option set (1d/7d/30d/never); no free-text |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07.2.07-01 | Tampering | Expiry value submitted to backend | accept | Option values are constrained client-side via the Select component. Backend MUST also validate (existing contract from Phase 7); this UI swap does not alter that boundary. |
| T-07.2.07-02 | Tampering | Copy/color CSS swap | accept | No executable surface. |
</threat_model>

<verification>
- All grep gates above pass
- ShareModal test suite passes (test count unchanged)
- Manual: open ShareModal in dev server; expiry dropdown is shadcn-styled (matches surrounding shadcn Dialog), label reads "Link expires in", warning paragraph is amber
</verification>

<success_criteria>
- ROADMAP success-criteria item 7 satisfied: ShareModal uses shadcn `<Select/>` (not native), warning text uses `text-amber-600`, label reads "Link expires in"
- D-08 acceptance criteria from CONTEXT.md met
</success_criteria>

<output>
After completion, create `.planning/phases/07.2-ui-contract-remediation-live/07.2-07-SUMMARY.md`
</output>
