---
phase: 07.2
plan: 09
type: execute
wave: 3
# Wave 3 (sweep / cleanup; depends_on intentionally empty — no logical dependency on Wave 1/2 plans).
depends_on: []
files_modified:
  - viewer/src/components/FilterPanel.tsx
  - viewer/src/components/FindingCard.tsx
  - viewer/src/components/DiagramCanvas.tsx
  - viewer/src/components/DetailPanel.tsx
  - viewer/src/components/SummaryBar.tsx
  - viewer/src/components/flowmap/PathDetailPanel.tsx
  - viewer/src/components/flowmap/FlowMapFilterPanel.tsx
autonomous: true
requirements: [D-10]
must_haves:
  truths:
    - "FilterPanel uses only the 4 spec-defined type sizes (12/14/16/28); no sub-spec text-[9px]/text-[10px]/text-[11px] literals remain"
    - "Section header dividers use text-xs uppercase tracking-wide consistently"
  artifacts:
    - path: "viewer/src/components/FilterPanel.tsx"
      provides: "FilterPanel typography conformant to UI spec (text-xs / text-sm / text-base / text-3xl)"
  key_links:
    - from: "FilterPanel typography"
      to: "07-UI-SPEC.md typography contract (4 sizes only)"
      via: "Tailwind text-xs / text-sm / text-base / text-3xl tokens"
      pattern: "text-xs"
---

<objective>
Replace sub-spec arbitrary-value text sizes (text-[9px], text-[10px], text-[11px]) in viewer FilterPanel with the spec's 12px (text-xs) token.

Purpose: D-10 P2 sweep — Spec defines exactly 4 type sizes (12/14/16/28). Sub-spec sizes break the typography contract. Bundled into a separate plan from the dashboard sweep (07.2-08) to keep parallel-worktree practicality (different repository directory).
Output: One file edit replacing arbitrary-value text size literals with text-xs.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/07.2-ui-contract-remediation-live/07.2-CONTEXT.md
@.planning/phases/07.1-phase-7-ui-contract-remediation/07.1-LIVE-UI-REVIEW.md
@viewer/src/components/FilterPanel.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace text-[9px]/text-[10px]/text-[11px] with text-xs in FilterPanel + sweep other viewer components</name>
  <files>viewer/src/components/FilterPanel.tsx, viewer/src/components/FindingCard.tsx, viewer/src/components/DiagramCanvas.tsx, viewer/src/components/DetailPanel.tsx, viewer/src/components/SummaryBar.tsx, viewer/src/components/flowmap/PathDetailPanel.tsx, viewer/src/components/flowmap/FlowMapFilterPanel.tsx</files>
  <read_first>
    - viewer/src/components/FilterPanel.tsx (the file being modified — read full file; locate every text-[9px], text-[10px], text-[11px] literal)
    - .planning/phases/07.2-ui-contract-remediation-live/07.2-CONTEXT.md (D-10 section, lines 95–98)
    - .planning/phases/07-saas-dashboard-history-share/07-UI-SPEC.md (find the typography section — confirms 12/14/16/28 only)
    - All other viewer components: run a sweep `grep -rn 'text-\[9px\]\|text-\[10px\]\|text-\[11px\]' viewer/src/components/` to confirm whether any other file (not just FilterPanel) has the same problem; CONTEXT.md says "and any other viewer component that uses sub-spec sizes" — so if other files have hits, they're in scope for this task
  </read_first>
  <action>
    Step 1 — Confirm enumeration (verified during planning):
    The following 7 files contain sub-spec arbitrary text-size literals. Each is already
    listed in `files_modified` above:
    1. viewer/src/components/FilterPanel.tsx
    2. viewer/src/components/FindingCard.tsx
    3. viewer/src/components/DiagramCanvas.tsx
    4. viewer/src/components/DetailPanel.tsx
    5. viewer/src/components/SummaryBar.tsx
    6. viewer/src/components/flowmap/PathDetailPanel.tsx
    7. viewer/src/components/flowmap/FlowMapFilterPanel.tsx

    Re-run the sweep at execution time as a safety check:
    `grep -rln 'text-\[9px\]\|text-\[10px\]\|text-\[11px\]' viewer/src/components/`
    The result MUST match the 7 files above. If a NEW file appears, add it to `files_modified`
    in this plan (note in SUMMARY) and migrate it the same way.

    Step 2 — Replace literals:
    In each file with hits, replace EVERY occurrence of:
    - `text-[9px]` → `text-xs`
    - `text-[10px]` → `text-xs`
    - `text-[11px]` → `text-xs`

    Step 3 — Section header consistency:
    In FilterPanel.tsx specifically, find section header dividers (filter group titles like "Severity", "Resource Types", etc.). Ensure they use `text-xs uppercase tracking-wide` consistently. If a section header uses any other small-caps treatment (e.g., `text-[10px] uppercase`), normalize it to the canonical `text-xs uppercase tracking-wide`.

    Do NOT change other typography tokens (text-sm, text-base, text-3xl) elsewhere in the file. Do NOT touch icon sizes, spacing, colors, or any non-text-size class.
  </action>
  <verify>
    <automated>! grep -rn "text-\[9px\]\|text-\[10px\]\|text-\[11px\]" viewer/src/components/</automated>
  </verify>
  <done>
    - `grep -rn 'text-\[9px\]\|text-\[10px\]\|text-\[11px\]' viewer/src/components/` returns 0 hits across all viewer/src/components/ files
    - FilterPanel section headers use `text-xs uppercase tracking-wide` consistently
    - cd viewer and npm test passes
  </done>
  <acceptance_criteria>
    - `grep -rn 'text-\[9px\]\|text-\[10px\]\|text-\[11px\]' viewer/src/components/` returns 0 hits
    - Viewer Vitest suite passes
    - Manual: load FilterPanel in dev server; section headers and option labels render at consistent 12px (text-xs)
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none in scope) | Pure CSS/typography class swap |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07.2.09-01 | Tampering | FilterPanel typography | accept | Class change is read by browser CSS engine only; no executable surface. |
</threat_model>

<verification>
- Grep gate above returns 0 hits across the entire viewer/src/components/ tree
- Viewer Vitest suite passes
- Manual: FilterPanel renders with consistent 12px typography
</verification>

<success_criteria>
- D-10 acceptance criteria from CONTEXT.md met
- Typography contract (4 sizes only) restored across viewer/src/components/
</success_criteria>

<output>
After completion, create .planning/phases/07.2-ui-contract-remediation-live/07.2-09-SUMMARY.md
</output>
