---
phase: 07.2
plan: 10
type: verify
wave: 4
depends_on: [07.2-01, 07.2-02, 07.2-03, 07.2-04, 07.2-05, 07.2-06, 07.2-07, 07.2-08, 07.2-09]
files_modified:
  - .planning/phases/07.2-ui-contract-remediation-live/07.2-LIVE-UI-REVIEW.md
autonomous: false
requirements: [D-15]
must_haves:
  truths:
    - "A new LIVE UI review report exists at .planning/phases/07.2-ui-contract-remediation-live/07.2-LIVE-UI-REVIEW.md"
    - "Overall score is ≥20/24"
    - "No pillar score regresses below 3/4"
    - "All 14 D-01..D-14 defects are confirmed remediated in the new report"
  artifacts:
    - path: ".planning/phases/07.2-ui-contract-remediation-live/07.2-LIVE-UI-REVIEW.md"
      provides: "Re-audit report confirming Phase 7.2 success criteria"
      min_lines: 30
  key_links:
    - from: "Re-audit report"
      to: "Phase 7.1 LIVE baseline (07.1-LIVE-UI-REVIEW.md)"
      via: "Same audit method (code audit + dev server inspection at http://localhost:3001 on dev/local-no-auth)"
      pattern: "audit method baseline match"
---

<objective>
Re-audit the dashboard against the same baseline that produced the 10/24 LIVE review (`07.1-LIVE-UI-REVIEW.md`), confirming Phase 7.2 closes the 14 defects and achieves ≥20/24 across all 6 pillars without regressing any pillar below 3/4.

Purpose: D-15 — Success-criteria item 8 of the phase. Phase 7.2 cannot be marked complete until the new audit confirms the targeted score and per-pillar floors. Per CONTEXT.md "Specific Ideas" — must use the SAME audit method (code audit + dev server inspection at http://localhost:3001 on dev/local-no-auth), not a code-only check.
Output: New `07.2-LIVE-UI-REVIEW.md` report at the Phase 7.2 directory with explicit pillar scores, per-defect resolution confirmations, and an overall score.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/07.2-ui-contract-remediation-live/07.2-CONTEXT.md
@.planning/phases/07.1-phase-7-ui-contract-remediation/07.1-LIVE-UI-REVIEW.md
@.planning/phases/07-saas-dashboard-history-share/07-UI-SPEC.md
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 1: Run /gsd-ui-review against Phase 7.1 baseline (LIVE re-audit)</name>
  <read_first>
    - .planning/phases/07.1-phase-7-ui-contract-remediation/07.1-LIVE-UI-REVIEW.md (the baseline — confirm pillar count is 6, max score is 24, original was 10/24)
    - .planning/phases/07.2-ui-contract-remediation-live/07.2-CONTEXT.md (D-15 section, lines 126–129)
    - All 9 prior plan SUMMARYs in this phase (07.2-01-SUMMARY.md through 07.2-09-SUMMARY.md) — confirm each defect was actually closed
  </read_first>
  <what-built>
    Plans 07.2-01 through 07.2-09 closed defects D-01..D-14:
    - D-01: viewer App.tsx h-screen → h-full
    - D-02: 6 viewer components migrated to useViewerStoreOrSingleton
    - D-03: /settings redirect page added
    - D-04: Sparkline preserveAspectRatio removed + ScoreSparkline height bumped
    - D-05: gradeInfo() shared util + adopted across 4 surfaces
    - D-06: Sidebar xl-breakpoint collapse removed
    - D-07: /api/top-findings route handler + TopFindings card rewrite
    - D-08: ShareModal shadcn Select + locked label + amber warning
    - D-09: @xyflow/react dep + globals.css CSS import
    - D-10: viewer FilterPanel typography fixed
    - D-11: homepage <header> removed + gap-12 → gap-6
    - D-12: RecentScansTable + TopFindings link colors → text-amber-600
    - D-13: ScanViewerClient retry → router.refresh()
    - D-14: /scans/compare copy → "its team"
  </what-built>
  <how-to-verify>
    Manual / human-driven re-audit (executor will trigger this):

    1. Start the dashboard dev server: `cd dashboard && npm run dev` (binds to http://localhost:3001 on dev/local-no-auth branch).
    2. In a separate terminal, run the existing UI review command:
       `/gsd-ui-review` with target = Phase 7.1 baseline (the same scope `07.1-LIVE-UI-REVIEW.md` audited).
    3. Confirm the produced report uses the SAME method (code audit + dev server inspection) as the original LIVE review. Code-only audits do not count (per CONTEXT.md "Specific Ideas").
    4. Save the report to `.planning/phases/07.2-ui-contract-remediation-live/07.2-LIVE-UI-REVIEW.md`.
    5. Read the report and verify:
       - Overall score: ≥20/24
       - All 6 pillars: each ≥3/4
       - Each of D-01..D-14: marked resolved with evidence
    6. If any of the above fails (any pillar < 3/4 or overall < 20/24), do NOT mark the
       phase complete and do NOT spawn a sub-phase. Instead:
       - Document the failing defect(s) in `07.2-LIVE-UI-REVIEW.md`
       - Re-execute the affected plan(s) from this same phase, e.g.
         `/gsd-execute-phase 07.2 --plan 07.2-NN` for each failing defect's owning plan
         (cross-reference the D-NN → plan-NN mapping in <what-built> above)
       - After re-execution, re-run this audit task (Task 1) to confirm the fix
       - Do NOT run `/gsd-plan-phase 07.2 --gaps` — gap planning would create a recursive
         sub-phase that this phase explicitly avoids; the existing plans 01–09 are the
         canonical remediation surface.
  </how-to-verify>
  <resume-signal>
    Type "approved" if:
    - 07.2-LIVE-UI-REVIEW.md exists at the Phase 7.2 directory
    - Overall score is ≥20/24
    - No pillar is below 3/4
    - All 14 defects (D-01..D-14) are marked resolved

    Type "gap: D-NN, D-NN" if any specific defect is still open (will trigger gap-closure planning).

    Type "rerun" if the audit method was wrong (code-only instead of code + dev server) and you want to redo it correctly.
  </resume-signal>
  <acceptance_criteria>
    - File `.planning/phases/07.2-ui-contract-remediation-live/07.2-LIVE-UI-REVIEW.md` exists
    - Report contains an overall score ≥20/24
    - Report contains per-pillar scores all ≥3/4
    - Report explicitly addresses each of D-01..D-14 with a resolved/open status
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Auditor ↔ dev server | Read-only inspection; no production data |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07.2.10-01 | Information Disclosure | Audit report content | accept | Report describes UI state of test data on local dev branch; no PII or production secrets surface. Stays in `.planning/` directory. |
</threat_model>

<verification>
- New audit report exists at `.planning/phases/07.2-ui-contract-remediation-live/07.2-LIVE-UI-REVIEW.md`
- Overall score ≥20/24
- All 6 pillars ≥3/4
- All 14 defects D-01..D-14 marked resolved
</verification>

<success_criteria>
- ROADMAP success-criteria item 8 satisfied: Live re-audit scores ≥20/24 across all 6 pillars
- D-15 acceptance criteria from CONTEXT.md met
- Phase 7.2 ready to be marked complete in STATE.md
</success_criteria>

<output>
After completion, create `.planning/phases/07.2-ui-contract-remediation-live/07.2-10-SUMMARY.md` and update `.planning/STATE.md` to mark Phase 7.2 complete (last_activity, progress.completed_phases++).
</output>
