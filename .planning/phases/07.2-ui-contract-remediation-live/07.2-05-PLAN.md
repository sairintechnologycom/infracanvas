---
phase: 07.2
plan: 05
type: execute
wave: 2
# Wave 2 (independent of Wave 1; depends_on intentionally empty).
depends_on: []
files_modified:
  - dashboard/components/layout/Sidebar.tsx
autonomous: true
requirements: [D-06]
must_haves:
  truths:
    - "Sidebar has no w-12 icon-only dead zone at 768–1279px viewports"
    - "Nav labels are visible at md+ (≥768px) viewports"
    - "Mobile (<768px) still relies on the TopBar hamburger (hidden md:flex pattern preserved)"
  artifacts:
    - path: "dashboard/components/layout/Sidebar.tsx"
      provides: "Desktop-only sidebar fixed at w-[220px] from md+; no xl-breakpoint collapse"
      contains: "w-[220px]"
  key_links:
    - from: "Sidebar root container"
      to: "Tailwind breakpoint system"
      via: "hidden md:flex w-[220px] (no xl: variant on width or label visibility)"
      pattern: "md:flex w-\\[220px\\]"
---

<objective>
Eliminate the unintended icon-only collapsed state of the dashboard sidebar at 768–1279px viewports by removing the `xl:`-gated label and width breakpoint qualifiers.

Purpose: D-06 P1 polish — Spec is desktop-only (1440p / 1080p targets); the `xl` breakpoint creates an unintentional 768–1279px icon-only state with no expand toggle. Success-criteria item 6 explicitly forbids the `w-12` dead zone in this range. Mobile (<768px) still relies on the TopBar hamburger via the existing `hidden md:flex` pattern — that contract MUST be preserved.
Output: Single-file edit removing `xl:w-[220px] w-12` collapse and any `xl:inline hidden` / `xl:block hidden` label qualifiers.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/07.2-ui-contract-remediation-live/07.2-CONTEXT.md
@.planning/phases/07.1-phase-7-ui-contract-remediation/07.1-LIVE-UI-REVIEW.md
@dashboard/components/layout/Sidebar.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove xl-breakpoint sidebar collapse + always-show labels at md+</name>
  <files>dashboard/components/layout/Sidebar.tsx</files>
  <read_first>
    - dashboard/components/layout/Sidebar.tsx (the file being modified — read entire file; target line ~41 root container)
    - .planning/phases/07.2-ui-contract-remediation-live/07.2-CONTEXT.md (D-06 section, lines 70–73)
    - dashboard/components/layout/TopBar.tsx (confirm mobile hamburger handles <768px; do not duplicate)
  </read_first>
  <action>
    In `dashboard/components/layout/Sidebar.tsx` around line 41:

    Edit 1 — Root container className. Find the className that currently reads
    `${visibility} md:flex xl:w-[220px] w-12 ...` (where `...` is whatever else is
    appended). Change it to `${visibility} md:flex w-[220px] ...`. Specifically:
    - Remove `xl:w-[220px]`
    - Remove `w-12`
    - Add `w-[220px]` (without breakpoint qualifier)
    - Preserve `${visibility}`, `md:flex`, and any other classes (border, padding, bg, etc.)

    Edit 2 — Label spans. For every `<span>` (or other element) that wraps nav-item label
    text and currently has `xl:inline hidden` or `xl:block hidden` (or any `xl:`-gated
    visibility class), remove the `xl:`-gated visibility classes entirely so labels are
    always visible. Be surgical — do NOT remove icon visibility classes, only label-gating.

    Mobile preservation:
    - Keep `hidden md:flex` on the root (or whatever the existing mobile-hide pattern is).
      Mobile (<768px) MUST continue to rely on the TopBar hamburger.
    - Do NOT add a mobile collapsed state. Per CONTEXT.md deferred section: a formal
      mobile breakpoint strategy is out of scope.

    Do NOT modify icon imports, route hrefs, or active-state highlighting.
  </action>
  <verify>
    <automated>! grep -n "w-12" dashboard/components/layout/Sidebar.tsx && ! grep -n "xl:w-\[220px\]" dashboard/components/layout/Sidebar.tsx && grep -n "w-\[220px\]" dashboard/components/layout/Sidebar.tsx && grep -n "md:flex" dashboard/components/layout/Sidebar.tsx</automated>
  </verify>
  <done>
    - `grep "w-12" dashboard/components/layout/Sidebar.tsx` returns 0 hits
    - `grep "xl:w-\[220px\]" dashboard/components/layout/Sidebar.tsx` returns 0 hits
    - `w-[220px]` (without `xl:` prefix) present on the root container
    - `hidden md:flex` (or equivalent mobile-hide pattern) preserved
    - `cd dashboard && npm test` passes
  </done>
  <acceptance_criteria>
    - `grep -c "w-12" dashboard/components/layout/Sidebar.tsx` returns 0
    - `grep -c "xl:w-\[220px\]" dashboard/components/layout/Sidebar.tsx` returns 0
    - `grep -c "xl:inline hidden\|xl:block hidden" dashboard/components/layout/Sidebar.tsx` returns 0
    - `grep -c "w-\[220px\]" dashboard/components/layout/Sidebar.tsx` returns ≥1
    - `grep -c "md:flex" dashboard/components/layout/Sidebar.tsx` returns ≥1
    - Manual: at 800px viewport, sidebar shows full 220px width with visible labels
    - Manual: at 600px viewport, sidebar is hidden (TopBar hamburger handles nav)
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none in scope) | Pure CSS class change in a layout component; no input |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07.2.05-01 | Tampering | Sidebar.tsx className | accept | Layout-only CSS change. No data flow, no executable surface. |
</threat_model>

<verification>
- All grep gates above pass
- Dashboard test suite passes
- Manual viewport tests at 600px / 800px / 1280px / 1440px confirm correct behavior
</verification>

<success_criteria>
- ROADMAP success-criteria item 6 satisfied: Sidebar has no `w-12` icon-only dead zone at 768–1279px viewports
- D-06 acceptance criteria from CONTEXT.md met
</success_criteria>

<output>
After completion, create `.planning/phases/07.2-ui-contract-remediation-live/07.2-05-SUMMARY.md`
</output>
