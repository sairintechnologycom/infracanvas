---
phase: 07.2
plan: 08
status: complete
date: 2026-05-03
requirements: [D-09, D-11, D-12, D-13, D-14]
---

# Plan 07.2-08 — Dashboard Wave 3 sweep (D-09 + D-11..D-14)

## What was built

Five small dashboard polish edits, one commit per defect:

- **D-09:** `@xyflow/react@12.10.2` pinned in `dashboard/package.json` and ReactFlow base CSS imported via `dashboard/app/globals.css` so MiniMap renders with the correct light background.
- **D-11:** Removed the homepage header dead zone (`Latest scan from main` line); outer container rhythm tightened from `gap-12` → `gap-6`.
- **D-12:** `RecentScansTable` "View all" link uses `text-amber-600` per spec amber-CTA rule. TopFindings "Open scan" link landed `text-amber-600` already in plan 07.2-06.
- **D-13:** `ScanViewerClient` error-state retry now uses `router.refresh()` (preserves sidebar state) instead of `window.location.reload()`.
- **D-14:** `/scans/compare` 404 copy "you may not have access to it." → "you may not have access to its team."

## Files changed

- `dashboard/package.json` — `@xyflow/react` dep
- `dashboard/app/globals.css` — `@import '@xyflow/react/dist/style.css';`
- `package-lock.json` — refreshed
- `dashboard/app/(dashboard)/page.tsx` — removed `<header>` + `gap-12` → `gap-6`
- `dashboard/components/home/RecentScansTable.tsx` — `text-amber-600` on "View all"
- `dashboard/components/scans/ScanViewerClient.tsx` — `useRouter` import + `router.refresh()`
- `dashboard/app/(dashboard)/scans/compare/page.tsx` — `its team` copy fix
- `dashboard/__tests__/polish-drift.test.ts` — gutter assertion `gap-12` → `gap-6`

## Verification

- `grep -c "@xyflow/react" dashboard/package.json` → 1
- `grep -c "@xyflow/react/dist/style.css" dashboard/app/globals.css` → 1
- `! grep -q "<header" dashboard/app/(dashboard)/page.tsx` → 0 hits
- `grep -c "gap-6" dashboard/app/(dashboard)/page.tsx` → 1
- `grep -c "text-amber-600" dashboard/components/home/RecentScansTable.tsx` → 1
- `grep -c "text-amber-600" dashboard/components/home/TopFindings.tsx` → 1
- `grep -c "useRouter" dashboard/components/scans/ScanViewerClient.tsx` → 2
- `grep -c "router.refresh()" dashboard/components/scans/ScanViewerClient.tsx` → 1
- `grep -c "window.location.reload" dashboard/components/scans/ScanViewerClient.tsx` → 0
- `grep -c "its team" dashboard/app/(dashboard)/scans/compare/page.tsx` → 1
- `cd dashboard && npm test` → 183/183 passing

## Commits

- `5b14d27` fix(07.2-08): D-09 — pin @xyflow/react dep + import ReactFlow CSS (D-09)
- `6098540` fix(07.2-08): D-11 — drop homepage header dead zone + gap-12 → gap-6
- `f81b5c1` fix(07.2-08): D-12 — RecentScansTable 'View all' link uses text-amber-600
- `e88fa3a` fix(07.2-08): D-13 — ScanViewerClient retry uses router.refresh()
- `e1ffafb` fix(07.2-08): D-14 — /scans/compare 404 copy reads 'its team'

## Deviations

1. **`@xyflow/react` pinned to `12.10.2`** — plan suggested `12.6.0` (taken from CONTEXT.md memory), but the workspace-hoisted version in `node_modules/@xyflow/react/package.json` was `12.10.2`. Pinned to the actually-resolved version per the plan's read_first guidance ("confirm by reading that file before pinning").
2. **`polish-drift.test.ts` modified** — not in `files_modified`, but the existing `gap-12` gutter assertion would otherwise fail after D-11. Updated to assert `gap-6`.
3. **`package-lock.json` benign churn** — `npm install` regenerated several native-binary entries and dropped some `"libc": ["glibc"]` constraints that npm has since stopped emitting. Standard npm-internal cleanup, not a dependency change.

## Self-Check: PASSED
