---
phase: 07.2
plan: 01
type: execute
wave: 1
# Wave 1 (no dependencies on other phase 07.2 plans).
depends_on: []
files_modified:
  - viewer/src/App.tsx
autonomous: true
requirements: [D-01]
must_haves:
  truths:
    - "Canvas tab on /scans/[id] renders the diagram inside the dashboard shell without overflowing the viewport"
    - "Standalone HTML export still renders full-viewport (body/root IS the viewport)"
  artifacts:
    - path: "viewer/src/App.tsx"
      provides: "Root container with embed-safe sizing"
      contains: "h-full w-full"
  key_links:
    - from: "viewer/src/App.tsx (root container)"
      to: "ScanViewerClient parent (h-full w-full) and scan detail page (flex-1 min-h-0)"
      via: "CSS flex sizing — child must consume parent height/width, not viewport"
      pattern: "flex flex-col h-full w-full"
---

<objective>
Fix viewer embed layout so Canvas tab renders inside the dashboard shell instead of breaking out via 100vw / 100vh.

Purpose: D-01 P0 unblocker — `App.tsx` root currently uses `h-screen w-screen` (100vh/100vw) which overflows the dashboard sidebar's flex container and makes the Canvas tab visually broken on `/scans/[id]`. The container hierarchy already supplies the constrained height (`flex-1 min-h-0` on scan detail page) and width (`h-full w-full` on `ScanViewerClient`), so swapping the root sizing tokens is sufficient.
Output: A 2-character class swap (`h-screen w-screen` → `h-full w-full`) on `viewer/src/App.tsx:95`, verified by grep + manual viewport check.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/07.2-ui-contract-remediation-live/07.2-CONTEXT.md
@.planning/phases/07.1-phase-7-ui-contract-remediation/07.1-LIVE-UI-REVIEW.md
@viewer/src/App.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Swap App.tsx root sizing from viewport to parent-relative</name>
  <files>viewer/src/App.tsx</files>
  <read_first>
    - viewer/src/App.tsx (the file being modified — read entire file, target line ~95)
    - .planning/phases/07.2-ui-contract-remediation-live/07.2-CONTEXT.md (D-01 section, lines 43–46)
    - .planning/phases/07.1-phase-7-ui-contract-remediation/07.1-LIVE-UI-REVIEW.md (D-01 evidence + fix sketch)
  </read_first>
  <action>
    In `viewer/src/App.tsx` around line 95, change the root container className from
    `flex flex-col h-screen w-screen` to `flex flex-col h-full w-full`.

    Do NOT change any other classes on that element. Do NOT modify any other line in the file.

    Rationale (from D-01): the standalone HTML export template's body/root IS the viewport,
    so `h-full w-full` will still render full-viewport when used standalone; when embedded
    in the dashboard, the parent (`ScanViewerClient` with `h-full w-full` inside a
    `flex-1 min-h-0` scan detail page) supplies the constrained box.
  </action>
  <verify>
    <automated>grep -n "flex flex-col h-full w-full" viewer/src/App.tsx && ! grep -n "h-screen\|w-screen" viewer/src/App.tsx</automated>
  </verify>
  <done>
    - `viewer/src/App.tsx` root className contains exactly `flex flex-col h-full w-full`
    - `grep "h-screen" viewer/src/App.tsx` returns 0 hits
    - `grep "w-screen" viewer/src/App.tsx` returns 0 hits
    - Existing viewer Vitest suite still passes (`npm test --prefix viewer`)
  </done>
  <acceptance_criteria>
    - `grep -c "flex flex-col h-full w-full" viewer/src/App.tsx` returns 1 (or more)
    - `grep -c "h-screen" viewer/src/App.tsx` returns 0
    - `grep -c "w-screen" viewer/src/App.tsx` returns 0
    - Manual: `cd viewer && npm test` exits 0
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none in scope) | Pure CSS/layout class change — no input, no data crossing trust boundary |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07.2.01-01 | Tampering | viewer/src/App.tsx | accept | Class change is read by browser CSS engine only; no executable surface. Standalone HTML export still works because body/root IS the viewport. |
</threat_model>

<verification>
- `grep "h-screen\|w-screen" viewer/src/App.tsx` returns 0 hits
- Viewer Vitest suite passes
- Manual: load `/scans/[id]` in dev server, Canvas tab renders inside the dashboard shell (sidebar + topbar visible, no horizontal scroll)
- Manual: `cd cli && infracanvas export ...` produced HTML still renders full-viewport when opened directly in a browser
</verification>

<success_criteria>
- ROADMAP success-criteria item 1 satisfied: Canvas tab on `/scans/[id]` renders the diagram in the dashboard shell (not overflowing viewport, not pushed off-screen)
- D-01 acceptance criteria from CONTEXT.md met
</success_criteria>

<output>
After completion, create `.planning/phases/07.2-ui-contract-remediation-live/07.2-01-SUMMARY.md`
</output>
