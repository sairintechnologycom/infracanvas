---
phase: 07.2
plan: 08
type: execute
wave: 3
# Wave 3 — depends_on plan 07.2-06 because Task 3 patches text-amber-600 onto a link in
# dashboard/components/home/TopFindings.tsx, which plan 06 rewrites end-to-end. Plan 06
# MUST land first; if plan 06 already applies text-amber-600 to that link, Task 3's edit
# on TopFindings.tsx is a no-op (grep gate still passes).
depends_on: ['07.2-06']
files_modified:
  - dashboard/package.json
  - package-lock.json
  - dashboard/app/globals.css
  - dashboard/app/(dashboard)/page.tsx
  - dashboard/components/home/RecentScansTable.tsx
  - dashboard/components/home/TopFindings.tsx
  - dashboard/components/scans/ScanViewerClient.tsx
  - dashboard/app/(dashboard)/scans/compare/page.tsx
autonomous: true
requirements: [D-09, D-11, D-12, D-13, D-14]
must_haves:
  truths:
    - "@xyflow/react is an explicit dashboard dependency (no workspace-hoist reliance)"
    - "ReactFlow CSS is imported via dashboard/app/globals.css so MiniMap renders correctly"
    - "Homepage no longer contains a header dead-zone block"
    - "Homepage outer container uses gap-6 rhythm (gap-12 reserved for explicit hero/strip break)"
    - "View all and Open scan links use text-amber-600 per spec amber-CTA rule"
    - "ScanViewerClient retry uses router.refresh() (preserves sidebar state); no window.location.reload()"
    - "/scans/compare 404 copy reads 'you may not have access to its team.'"
  artifacts:
    - path: "dashboard/package.json"
      provides: "@xyflow/react explicit dep"
      contains: "@xyflow/react"
    - path: "dashboard/app/globals.css"
      provides: "ReactFlow base CSS imported"
      contains: "@xyflow/react/dist/style.css"
    - path: "dashboard/app/(dashboard)/page.tsx"
      provides: "Homepage without header element + gap-6 outer container"
    - path: "dashboard/components/home/RecentScansTable.tsx"
      provides: "View all link with text-amber-600"
      contains: "text-amber-600"
    - path: "dashboard/components/home/TopFindings.tsx"
      provides: "Open scan link with text-amber-600"
      contains: "text-amber-600"
    - path: "dashboard/components/scans/ScanViewerClient.tsx"
      provides: "Retry button using router.refresh()"
      contains: "router.refresh()"
    - path: "dashboard/app/(dashboard)/scans/compare/page.tsx"
      provides: "404 copy referencing its team"
      contains: "its team"
  key_links:
    - from: "dashboard/app/globals.css"
      to: "@xyflow/react base styles (MiniMap background-color rule)"
      via: "@import statement"
      pattern: "@import .@xyflow/react/dist/style.css."
    - from: "ScanViewerClient retry button"
      to: "Next.js App Router server-component refresh"
      via: "useRouter().refresh()"
      pattern: "router.refresh"
---

<objective>
Bundle the 5 dashboard-only Wave 3 sweep defects: D-09 ReactFlow CSS + dep, D-11 homepage header + gap, D-12 link colors, D-13 router.refresh, D-14 compare copy. All touch dashboard files, no shared file conflicts internally, all are small mechanical edits.

Purpose: P2/P3 sweep — CONTEXT.md explicitly says "Wave 3 (P2/P3 sweep): D-09, D-10, D-11, D-12, D-13, D-14. Bundle into 1 or 2 plans". This plan handles the 5 dashboard-only items; viewer typography (D-10) is in plan 07.2-09 to keep parallel-worktree practicality (separate dashboard/ vs viewer/ directories).
Output: One commit per defect (recommended) covering 7 file edits plus a dashboard/package.json install.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/07.2-ui-contract-remediation-live/07.2-CONTEXT.md
@.planning/phases/07.1-phase-7-ui-contract-remediation/07.1-LIVE-UI-REVIEW.md
@dashboard/package.json
@dashboard/app/globals.css
</context>

<tasks>

<task type="auto">
  <name>Task 1: D-09 — Add @xyflow/react dep + ReactFlow CSS import</name>
  <files>dashboard/package.json, dashboard/app/globals.css</files>
  <read_first>
    - dashboard/package.json (read full file — confirm @xyflow/react is not currently in dependencies)
    - dashboard/app/globals.css (read full file — confirm import location)
    - root package.json + node_modules/@xyflow/react/package.json (find the exact workspace-hoisted version; CONTEXT.md says 12.6.0)
    - .planning/phases/07.2-ui-contract-remediation-live/07.2-CONTEXT.md (D-09 section, lines 90–93)
  </read_first>
  <action>
    Edit 1 — dashboard/package.json: Add "@xyflow/react": "12.6.0" (or whatever the exact version currently in root node_modules/@xyflow/react/package.json is — confirm by reading that file before pinning) to the dependencies block. Maintain alphabetical sort order if the file currently uses one.

    Edit 2 — dashboard/app/globals.css: Add the line
    @import '@xyflow/react/dist/style.css';
    near the top of the file (after any reset/Tailwind directives but before custom rules). Globals.css is CSS — keep the trailing semicolon.

    Lockfile note: this repo is an npm workspaces monorepo. The single lockfile at
    `package-lock.json` (repo root) — verified during planning by reading
    `head -3 package-lock.json` (`"name": "infracanvas-monorepo"`, `"lockfileVersion": 3`) —
    is the one that gets updated by `npm install`. There is NO `dashboard/package-lock.json`.

    Run `npm install` from the repo root (NOT `cd dashboard && npm install` — workspaces
    install from the root) to materialize the dep into `package-lock.json`. Confirm no
    warnings on resolution. Both `dashboard/package.json` and the root `package-lock.json`
    must be staged + committed together.

    Do NOT change any other dependency or CSS rule.
  </action>
  <verify>
    <automated>grep -q "@xyflow/react" dashboard/package.json && grep -q "@xyflow/react/dist/style.css" dashboard/app/globals.css</automated>
  </verify>
  <done>
    - dashboard/package.json dependencies contains "@xyflow/react"
    - dashboard/app/globals.css contains @import '@xyflow/react/dist/style.css';
    - npm install (run from repo root) succeeds without warnings; root package-lock.json updated
    - Manual: MiniMap renders with light background on /scans/[id] (was solid black)
  </done>
  <acceptance_criteria>
    - grep -c "@xyflow/react" dashboard/package.json returns ≥1
    - grep -c "@xyflow/react/dist/style.css" dashboard/app/globals.css returns 1
    - test -d node_modules/@xyflow/react exits 0 after npm install (workspace-hoisted to repo-root node_modules)
    - grep -q '"@xyflow/react"' package-lock.json (root lockfile updated)
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: D-11 — Remove homepage header dead zone + change gap-12 to gap-6</name>
  <files>dashboard/app/(dashboard)/page.tsx</files>
  <read_first>
    - dashboard/app/(dashboard)/page.tsx (the file being modified — read full file; locate the header element at ~lines 40–44 and outer container className `flex flex-col gap-12`)
    - .planning/phases/07.2-ui-contract-remediation-live/07.2-CONTEXT.md (D-11 section, lines 101–105)
  </read_first>
  <action>
    In dashboard/app/(dashboard)/page.tsx:

    Edit 1 — Remove the entire header block (the element with className `flex items-baseline justify-end`, currently around lines 40–44). The branch context already lives in ScoreCard's right column. Remove only this header element; do not touch other elements.

    Edit 2 — Find the outer container element. Change className from `flex flex-col gap-12` to `flex flex-col gap-6`. Preserve any other classes.

    Edit 3 (conditional, only if needed): If after the gap-6 change the sparkline strip visually merges into the hero card (looks too tight), add explicit `mt-12` to the sparkline strip wrapper alone. CONTEXT.md says default to plain gap-6 and only re-add the larger break if the sparkline visually merges into the hero. Try gap-6 first, only add mt-12 if your manual verification shows merging. If you do add mt-12, document it in the SUMMARY.

    Do NOT modify ScoreCard, RecentScansTable, TopFindings, or any other imported component.
  </action>
  <verify>
    <automated>! grep -nE "<header" "dashboard/app/(dashboard)/page.tsx" && grep -n "gap-6" "dashboard/app/(dashboard)/page.tsx"</automated>
  </verify>
  <done>
    - grep for opening header tag in dashboard/app/(dashboard)/page.tsx returns 0 hits
    - Outer container className contains gap-6 (not gap-12)
    - Homepage renders without the right-aligned "Latest scan from main" muted line
    - cd dashboard and npm test passes
  </done>
  <acceptance_criteria>
    - Grep for header opening tag in page.tsx returns 0 (CONTEXT.md acceptance: `grep "<header" dashboard/app/(dashboard)/page.tsx returns 0 hits`)
    - Grep for "gap-6" returns ≥1
    - Grep for "gap-12" on the outer container returns 0
    - Dashboard test suite passes
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: D-12 — Link colors text-slate-900 to text-amber-600 in RecentScansTable + TopFindings</name>
  <files>dashboard/components/home/RecentScansTable.tsx, dashboard/components/home/TopFindings.tsx</files>
  <read_first>
    - dashboard/components/home/RecentScansTable.tsx (find the "View all" link element)
    - dashboard/components/home/TopFindings.tsx (find the "Open scan" link element)
    - .planning/phases/07.2-ui-contract-remediation-live/07.2-CONTEXT.md (D-12 section, lines 107–110)
  </read_first>
  <action>
    For each of the two component files:

    1. Find the link element that currently has className `text-sm text-slate-900 hover:text-slate-700 hover:underline` (D-12 specifies this exact existing class string).

    2. Change it to `text-sm text-amber-600 hover:underline`. (Drop the slate hover variant — amber doesn't need a hover-color shift; underline is enough.)

    3. Apply ONLY to the navigational link spans described in CONTEXT.md:
       - RecentScansTable.tsx: the "View all" link
       - TopFindings.tsx: the "Open scan" link inside each card (this may be the same link added by plan 07.2-06; if plan 07.2-06 already used `text-amber-600` on those links, this task is a no-op for TopFindings.tsx — the grep gate will still pass)

    4. Do NOT change any other link color in either file (severity badges, table row hovers, etc. are out of scope).
  </action>
  <verify>
    <automated>grep -q "text-amber-600" dashboard/components/home/RecentScansTable.tsx && grep -q "text-amber-600" dashboard/components/home/TopFindings.tsx && ! grep -n "text-sm text-slate-900 hover:text-slate-700" dashboard/components/home/RecentScansTable.tsx && ! grep -n "text-sm text-slate-900 hover:text-slate-700" dashboard/components/home/TopFindings.tsx</automated>
  </verify>
  <done>
    - Both files contain text-amber-600 on those link elements
    - Neither file contains "text-sm text-slate-900 hover:text-slate-700" anywhere (the targeted class string)
    - cd dashboard and npm test passes
  </done>
  <acceptance_criteria>
    - grep -c "text-amber-600" dashboard/components/home/RecentScansTable.tsx returns ≥1
    - grep -c "text-amber-600" dashboard/components/home/TopFindings.tsx returns ≥1
    - grep -c "text-sm text-slate-900 hover:text-slate-700" on either file returns 0
    - Dashboard test suite passes
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 4: D-13 — ScanViewerClient retry uses router.refresh() not window.location.reload()</name>
  <files>dashboard/components/scans/ScanViewerClient.tsx</files>
  <read_first>
    - dashboard/components/scans/ScanViewerClient.tsx (the file being modified — read full file; target line ~88 retry button)
    - .planning/phases/07.2-ui-contract-remediation-live/07.2-CONTEXT.md (D-13 section, lines 114–117)
  </read_first>
  <action>
    In dashboard/components/scans/ScanViewerClient.tsx:

    1. Add or extend the import from 'next/navigation' to include useRouter:
       `import { useRouter } from 'next/navigation'`
       (If an import from 'next/navigation' already exists, add useRouter to its named imports. No duplicate import statement.)

    2. Inside the component function body, near the top of the function (before any conditional returns), call:
       `const router = useRouter()`

    3. On the error-state retry button (~line 88), find the onClick handler that currently calls `window.location.reload()`. Replace with `router.refresh()`.

    4. Do NOT change any other line. Do NOT change the button's className, copy, or surrounding JSX.

    Note: ScanViewerClient is a "use client" component (per its name). useRouter from next/navigation is a client hook — confirm by checking the existing "use client" directive at the top of the file. Do NOT remove that directive.
  </action>
  <verify>
    <automated>grep -q "useRouter" dashboard/components/scans/ScanViewerClient.tsx && grep -q "router.refresh()" dashboard/components/scans/ScanViewerClient.tsx && ! grep -n "window.location.reload" dashboard/components/scans/ScanViewerClient.tsx</automated>
  </verify>
  <done>
    - grep "import { useRouter" dashboard/components/scans/ScanViewerClient.tsx returns ≥1 hit (or useRouter inside an existing import line)
    - grep "router.refresh()" returns ≥1 hit
    - grep "window.location.reload" returns 0 hits
    - cd dashboard and npm test passes
  </done>
  <acceptance_criteria>
    - grep -c "useRouter" dashboard/components/scans/ScanViewerClient.tsx returns ≥1
    - grep -c "router.refresh()" dashboard/components/scans/ScanViewerClient.tsx returns ≥1
    - grep -c "window.location.reload" dashboard/components/scans/ScanViewerClient.tsx returns 0
    - Manual: trigger an error state on /scans/[id]; click retry; sidebar state persists (no full-page reload flash)
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 5: D-14 — /scans/compare 404 copy fix "it." to "its team."</name>
  <files>dashboard/app/(dashboard)/scans/compare/page.tsx</files>
  <read_first>
    - dashboard/app/(dashboard)/scans/compare/page.tsx (the file being modified — read full file; target line ~64 trailing copy)
    - .planning/phases/07.2-ui-contract-remediation-live/07.2-CONTEXT.md (D-14 section, lines 119–122)
  </read_first>
  <action>
    In dashboard/app/(dashboard)/scans/compare/page.tsx around line 64:

    Change the literal string "you may not have access to it." to "you may not have access to its team." Exact one-character (well, 5-character) edit.

    Do NOT change any other copy on the page. Do NOT modify imports, layout, or component logic.
  </action>
  <verify>
    <automated>grep -q "its team" "dashboard/app/(dashboard)/scans/compare/page.tsx" && ! grep -n "access to it\." "dashboard/app/(dashboard)/scans/compare/page.tsx"</automated>
  </verify>
  <done>
    - grep "its team" dashboard/app/(dashboard)/scans/compare/page.tsx returns ≥1 hit
    - grep "access to it\." dashboard/app/(dashboard)/scans/compare/page.tsx returns 0 hits
    - cd dashboard and npm test passes
  </done>
  <acceptance_criteria>
    - grep -c "its team" "dashboard/app/(dashboard)/scans/compare/page.tsx" returns ≥1
    - grep -c "access to it\." "dashboard/app/(dashboard)/scans/compare/page.tsx" returns 0
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser ↔ MiniMap CSS | Bundled CSS from a known npm package |
| User ↔ retry button | Hardcoded behavior; no input |
| User ↔ /scans/compare 404 page | Static copy; no input |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07.2.08-01 | Tampering / Supply chain | @xyflow/react explicit dep | accept | Same package version already loaded transitively via the viewer; pinning to the workspace-hoisted version surface-level reduces resolution drift but adds no new supply-chain surface. lockfile changes will be reviewed in commit. |
| T-07.2.08-02 | Tampering | router.refresh() vs window.location.reload | accept | router.refresh re-runs RSC server-side; same auth context as the original render. No new attack surface. |
| T-07.2.08-03 | Tampering | Copy/className edits | accept | Static-string edits, no executable surface. |
</threat_model>

<verification>
- All 5 task grep gates pass
- cd dashboard and npm install succeeds
- cd dashboard and npm test passes (no regression)
- Manual checks per task: MiniMap light background, homepage gap-6 spacing, amber link colors, retry preserves sidebar, compare 404 reads "its team"
</verification>

<success_criteria>
- D-09, D-11, D-12, D-13, D-14 acceptance criteria from CONTEXT.md all met
- Dashboard test suite passes with no regressions
- npm install in dashboard/ succeeds without warnings
</success_criteria>

<output>
After completion, create .planning/phases/07.2-ui-contract-remediation-live/07.2-08-SUMMARY.md
</output>
