---
phase: 07.2
plan: 03
type: execute
wave: 1
# Wave 1 (no dependencies on other phase 07.2 plans).
depends_on: []
files_modified:
  - dashboard/app/(dashboard)/settings/page.tsx
  - dashboard/components/scans/Sparkline.tsx
  - dashboard/components/home/ScoreSparkline.tsx
autonomous: true
requirements: [D-03, D-04]
must_haves:
  truths:
    - "Visiting /settings lands on /settings/members with the dashboard sidebar intact"
    - "Sparkline data points render as circles (not stretched ovals)"
    - "ScoreSparkline container is at least 80px tall to match spec"
    - "The Sparkline width-bug regression test (added at 1281d8a) still passes"
  artifacts:
    - path: "dashboard/app/(dashboard)/settings/page.tsx"
      provides: "Server component redirect from /settings to /settings/members"
      contains: "redirect('/settings/members')"
      min_lines: 3
    - path: "dashboard/components/scans/Sparkline.tsx"
      provides: "SVG sparkline with default xMidYMid aspect ratio (no preserveAspectRatio='none')"
    - path: "dashboard/components/home/ScoreSparkline.tsx"
      provides: "ScoreSparkline container with min height 80px"
      contains: "h-[80px]"
  key_links:
    - from: "dashboard/app/(dashboard)/settings/page.tsx"
      to: "/settings/members route"
      via: "next/navigation redirect()"
      pattern: "redirect\\(['\"]\\/settings\\/members['\"]\\)"
    - from: "dashboard/components/scans/Sparkline.tsx <svg>"
      to: "Browser SVG renderer (default xMidYMid meet)"
      via: "Removal of preserveAspectRatio='none' attribute"
      pattern: "(no preserveAspectRatio|preserveAspectRatio=[\"']xMidYMid)"
---

<objective>
Bundle two tiny P0 fixes: D-03 (`/settings` 404) and D-04 (sparkline ovals + container height). Both are independent of every other Wave 1 plan and both fit within a single small PR.

Purpose: D-03 — Settings layout currently has only sub-routes; root `/settings` returns Next.js's bare 404. D-04 — `preserveAspectRatio="none"` stretches r=2.5 circles into 37px ovals at wide-narrow containers; the home `ScoreSparkline` container is also too short (`h-[64px]` vs spec's ≥80px). Must NOT regress the recent wrapper-width fix (`w-full h-full` on the wrapper) committed at `e6135f3` / `1281d8a`.
Output: New `settings/page.tsx` redirect (~3 lines), `preserveAspectRatio` attribute removed from `Sparkline.tsx`, `ScoreSparkline` container height bumped to `h-[80px]`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/07.2-ui-contract-remediation-live/07.2-CONTEXT.md
@.planning/phases/07.1-phase-7-ui-contract-remediation/07.1-LIVE-UI-REVIEW.md
@dashboard/components/scans/Sparkline.tsx
@dashboard/components/home/ScoreSparkline.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create /settings redirect page (D-03)</name>
  <files>dashboard/app/(dashboard)/settings/page.tsx</files>
  <read_first>
    - dashboard/app/(dashboard)/settings/ (list contents — confirm no page.tsx currently exists)
    - dashboard/app/(dashboard)/settings/members/page.tsx (confirm target route exists)
    - dashboard/app/(dashboard)/settings/layout.tsx (existing layout — must NOT be modified; read to understand current shape so the new page.tsx integrates with it)
    - .planning/phases/07.2-ui-contract-remediation-live/07.2-CONTEXT.md (D-03 section, lines 53–56)
  </read_first>
  <action>
    Create new file `dashboard/app/(dashboard)/settings/page.tsx` with EXACTLY this content
    (server component, no "use client"):

    ```tsx
    import { redirect } from 'next/navigation'

    export default function SettingsIndexPage() {
      redirect('/settings/members')
    }
    ```

    No semicolons (project style). 2-space indent. PascalCase component name.

    Do NOT modify the existing `settings/layout.tsx` or `settings/members/page.tsx`.
  </action>
  <verify>
    <automated>test -f "dashboard/app/(dashboard)/settings/page.tsx" && grep -q "redirect('/settings/members')" "dashboard/app/(dashboard)/settings/page.tsx" && grep -q "from 'next/navigation'" "dashboard/app/(dashboard)/settings/page.tsx"</automated>
  </verify>
  <done>
    - File `dashboard/app/(dashboard)/settings/page.tsx` exists
    - Contains `import { redirect } from 'next/navigation'`
    - Contains `redirect('/settings/members')`
    - `cd dashboard && npm run typecheck` (or `next build` if no typecheck script) passes
  </done>
  <acceptance_criteria>
    - `test -f "dashboard/app/(dashboard)/settings/page.tsx"` exits 0
    - `grep -c "redirect('/settings/members')" "dashboard/app/(dashboard)/settings/page.tsx"` returns ≥1
    - `grep -c "from 'next/navigation'" "dashboard/app/(dashboard)/settings/page.tsx"` returns ≥1
    - Manual dev-server check: visiting `http://localhost:3001/settings` redirects to `/settings/members` with sidebar intact
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Remove preserveAspectRatio="none" from Sparkline + raise ScoreSparkline container to h-[80px] (D-04)</name>
  <files>dashboard/components/scans/Sparkline.tsx, dashboard/components/home/ScoreSparkline.tsx</files>
  <read_first>
    - dashboard/components/scans/Sparkline.tsx (the file being modified — read full file; do NOT remove the wrapper div with `w-full h-full` added at commit 1281d8a)
    - dashboard/components/home/ScoreSparkline.tsx (the file being modified — read full file; current container uses `h-[64px]`)
    - .planning/phases/07.2-ui-contract-remediation-live/07.2-CONTEXT.md (D-04 section, lines 58–61)
    - .planning/quick/260502-tra-fix-sparkline-width-bug-wrapper-div-in-s/ (regression-test history if present — must remain green)
  </read_first>
  <action>
    Edit 1 — `dashboard/components/scans/Sparkline.tsx`:
    - Remove the attribute `preserveAspectRatio="none"` from the `<svg>` tag.
    - Do NOT add a different value — let it default to `xMidYMid meet`.
    - Keep `className="w-full"` on the `<svg>`.
    - Do NOT modify the wrapper `<div>` (it has `w-full h-full` from commit 1281d8a — do not touch).
    - Do NOT modify viewBox, width, height, or any other attributes.

    Edit 2 — `dashboard/components/home/ScoreSparkline.tsx`:
    - Change the container className from `h-[64px]` to `h-[80px]`.
    - Do NOT change any other classes on that container.
    - If the container has additional classes like `w-full overflow-hidden`, preserve them.
  </action>
  <verify>
    <automated>! grep -n 'preserveAspectRatio="none"' dashboard/components/scans/Sparkline.tsx && grep -n 'h-\[80px\]' dashboard/components/home/ScoreSparkline.tsx && ! grep -n 'h-\[64px\]' dashboard/components/home/ScoreSparkline.tsx && grep -n 'w-full h-full' dashboard/components/scans/Sparkline.tsx</automated>
  </verify>
  <done>
    - `grep "preserveAspectRatio" dashboard/components/scans/Sparkline.tsx` returns 0 hits OR matches `xMidYMid meet`
    - `dashboard/components/home/ScoreSparkline.tsx` container className contains `h-[80px]`
    - `dashboard/components/home/ScoreSparkline.tsx` container className does NOT contain `h-[64px]`
    - `dashboard/components/scans/Sparkline.tsx` wrapper div still contains `w-full h-full` (regression boundary from 1281d8a)
    - `cd dashboard && npm test` passes (171/171 baseline; sparkline width-bug regression test specifically still green)
  </done>
  <acceptance_criteria>
    - `grep -c 'preserveAspectRatio="none"' dashboard/components/scans/Sparkline.tsx` returns 0
    - `grep -c 'h-\[80px\]' dashboard/components/home/ScoreSparkline.tsx` returns ≥1
    - `grep -c 'h-\[64px\]' dashboard/components/home/ScoreSparkline.tsx` returns 0
    - `grep -c 'w-full h-full' dashboard/components/scans/Sparkline.tsx` returns ≥1 (wrapper preserved)
    - Dashboard test suite passes with no regressions
    - Manual: load home page, sparkline data-point dots render as circles (not ovals); container is visibly taller (~80px)
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser ↔ /settings route | Standard Next.js server-component redirect — no untrusted input |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07.2.03-01 | Tampering | settings/page.tsx redirect | accept | Hardcoded redirect target `/settings/members` — no user-controlled input. Next.js's `redirect()` server-side; no XSS surface. |
| T-07.2.03-02 | Tampering | Sparkline SVG attribute removal | accept | CSS/SVG-only change. No executable surface. |
</threat_model>

<verification>
- `grep "preserveAspectRatio" dashboard/components/scans/Sparkline.tsx` returns 0 hits
- `grep "h-\[80px\]" dashboard/components/home/ScoreSparkline.tsx` returns ≥1
- `grep "redirect" "dashboard/app/(dashboard)/settings/page.tsx"` returns ≥1
- `cd dashboard && npm test` exits 0 (no regression)
- Manual: dev server check confirms `/settings` redirects, sparkline dots are circles, container is taller
</verification>

<success_criteria>
- ROADMAP success-criteria item 3 satisfied: `/settings` resolves to a real page (redirect to `/settings/members`)
- ROADMAP success-criteria item 4 satisfied: Sparkline data points render as circles, not stretched ovals; container height matches spec (≥80px)
- D-03 + D-04 acceptance criteria from CONTEXT.md met
- Sparkline width-bug regression test (1281d8a) still passes
</success_criteria>

<output>
After completion, create `.planning/phases/07.2-ui-contract-remediation-live/07.2-03-SUMMARY.md`
</output>
