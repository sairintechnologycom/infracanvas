---
phase: 07.2
plan: 04
type: execute
wave: 2
# Wave 2 (independent of Wave 1; depends_on intentionally empty).
depends_on: []
files_modified:
  - dashboard/lib/grade.ts
  - dashboard/lib/grade.test.ts
  - dashboard/components/home/ScoreCard.tsx
  - dashboard/components/scans/ScansTable.tsx
  - dashboard/components/scans/MetadataHeader.tsx
  - dashboard/components/share/ShareViewer.tsx
autonomous: true
requirements: [D-05]
must_haves:
  truths:
    - "Grade letter shown for any given score is identical across ScoreCard, ScansTable, MetadataHeader, and ShareViewer"
    - "Boundary scores at thresholds 95/90/85/80/70/60 produce the canonical letter (A+/A/B+/B/C/D respectively)"
  artifacts:
    - path: "dashboard/lib/grade.ts"
      provides: "Single source of truth for score → grade letter mapping"
      exports: ["gradeInfo"]
    - path: "dashboard/lib/grade.test.ts"
      provides: "Boundary-score unit tests for gradeInfo"
      contains: "describe"
    - path: "dashboard/components/home/ScoreCard.tsx"
      provides: "ScoreCard using gradeInfo()"
      contains: "gradeInfo"
    - path: "dashboard/components/scans/ScansTable.tsx"
      provides: "ScoreGradePill in ScansTable using gradeInfo()"
      contains: "gradeInfo"
    - path: "dashboard/components/scans/MetadataHeader.tsx"
      provides: "ScoreGradePill in MetadataHeader using gradeInfo()"
      contains: "gradeInfo"
    - path: "dashboard/components/share/ShareViewer.tsx"
      provides: "ShareViewer using gradeInfo()"
      contains: "gradeInfo"
  key_links:
    - from: "ScoreCard, ScansTable, MetadataHeader, ShareViewer"
      to: "dashboard/lib/grade.ts gradeInfo()"
      via: "import statement"
      pattern: "from ['\"]@/lib/grade['\"]"
---

<objective>
Extract a single canonical grade-letter helper and adopt it across all 4 surfaces that currently inline their own thresholds, so a scan scoring 82 shows the same letter everywhere on the same page load.

Purpose: D-05 P1 polish — A scan scoring 82 currently shows "B" on the home hero card and "B+" in the table/header for the same scan on the same page load. Success-criteria item 5 mandates identical grade across all 4 surfaces. Canonical thresholds (locked): A+ ≥95, A ≥90, B+ ≥85, B ≥80, C ≥70, D ≥60, F <60.
Output: New `dashboard/lib/grade.ts` exporting `gradeInfo()`, accompanying unit test for boundary scores, and 4 component files updated to import + use the helper for the letter (pill styling stays inline).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/07.2-ui-contract-remediation-live/07.2-CONTEXT.md
@.planning/phases/07.1-phase-7-ui-contract-remediation/07.1-LIVE-UI-REVIEW.md
@dashboard/components/home/ScoreCard.tsx
@dashboard/components/scans/ScansTable.tsx
@dashboard/components/scans/MetadataHeader.tsx
@dashboard/components/share/ShareViewer.tsx
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create dashboard/lib/grade.ts with canonical thresholds + boundary tests</name>
  <files>dashboard/lib/grade.ts, dashboard/lib/grade.test.ts</files>
  <read_first>
    - dashboard/lib/ (list directory — confirm no existing grade.ts; check naming conventions of sibling utils)
    - dashboard/components/home/ScoreCard.tsx (read inline grade logic to capture current letters/styling)
    - dashboard/components/scans/ScansTable.tsx (read ScoreGradePill block)
    - dashboard/components/scans/MetadataHeader.tsx (read ScoreGradePill block)
    - dashboard/components/share/ShareViewer.tsx (read inline grade logic)
    - .planning/phases/07.2-ui-contract-remediation-live/07.2-CONTEXT.md (D-05 section, lines 65–68)
  </read_first>
  <behavior>
    - `gradeInfo(95)` → `{ letter: 'A+' }`
    - `gradeInfo(94)` → `{ letter: 'A' }`
    - `gradeInfo(90)` → `{ letter: 'A' }`
    - `gradeInfo(89)` → `{ letter: 'B+' }`
    - `gradeInfo(85)` → `{ letter: 'B+' }`
    - `gradeInfo(84)` → `{ letter: 'B' }`
    - `gradeInfo(80)` → `{ letter: 'B' }`
    - `gradeInfo(79)` → `{ letter: 'C' }`
    - `gradeInfo(70)` → `{ letter: 'C' }`
    - `gradeInfo(69)` → `{ letter: 'D' }`
    - `gradeInfo(60)` → `{ letter: 'D' }`
    - `gradeInfo(59)` → `{ letter: 'F' }`
    - `gradeInfo(0)` → `{ letter: 'F' }`
    - `gradeInfo(100)` → `{ letter: 'A+' }`
  </behavior>
  <action>
    Step 1 (RED) — Create `dashboard/lib/grade.test.ts` first with boundary cases:

    ```ts
    import { describe, it, expect } from 'vitest'
    import { gradeInfo } from './grade'

    describe('gradeInfo', () => {
      it.each([
        [100, 'A+'], [95, 'A+'],
        [94, 'A'], [90, 'A'],
        [89, 'B+'], [85, 'B+'],
        [84, 'B'], [80, 'B'],
        [79, 'C'], [70, 'C'],
        [69, 'D'], [60, 'D'],
        [59, 'F'], [0, 'F'],
      ])('score %i → %s', (score, letter) => {
        expect(gradeInfo(score).letter).toBe(letter)
      })
    })
    ```

    Run: `cd dashboard && npm test -- grade.test.ts` — MUST fail (no grade.ts yet).

    Step 2 (GREEN) — Create `dashboard/lib/grade.ts`:

    ```ts
    export interface GradeInfo {
      letter: string
    }

    export function gradeInfo(score: number): GradeInfo {
      if (score >= 95) return { letter: 'A+' }
      if (score >= 90) return { letter: 'A' }
      if (score >= 85) return { letter: 'B+' }
      if (score >= 80) return { letter: 'B' }
      if (score >= 70) return { letter: 'C' }
      if (score >= 60) return { letter: 'D' }
      return { letter: 'F' }
    }
    ```

    Run: `cd dashboard && npm test -- grade.test.ts` — MUST pass (14/14).

    No semicolons. 2-space indent. The `GradeInfo` interface is intentionally extensible
    (D-05 says "letter: string; ..." — leave room for future fields without ETA-locking).
  </action>
  <verify>
    <automated>cd dashboard && npm test -- grade.test.ts</automated>
  </verify>
  <done>
    - `dashboard/lib/grade.ts` exists and exports `gradeInfo`
    - `dashboard/lib/grade.test.ts` exists with 14 boundary cases (100, 95, 94, 90, 89, 85, 84, 80, 79, 70, 69, 60, 59, 0)
    - All 14 grade test cases pass
  </done>
  <acceptance_criteria>
    - `test -f dashboard/lib/grade.ts && test -f dashboard/lib/grade.test.ts` exits 0
    - `grep -c "export function gradeInfo" dashboard/lib/grade.ts` returns 1
    - `cd dashboard && npm test -- grade.test.ts` passes 14 cases
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Adopt gradeInfo() in ScoreCard, ScansTable, MetadataHeader, ShareViewer</name>
  <files>dashboard/components/home/ScoreCard.tsx, dashboard/components/scans/ScansTable.tsx, dashboard/components/scans/MetadataHeader.tsx, dashboard/components/share/ShareViewer.tsx</files>
  <read_first>
    - dashboard/lib/grade.ts (created in Task 1 — confirm export shape)
    - dashboard/components/home/ScoreCard.tsx (find inline letter derivation block)
    - dashboard/components/scans/ScansTable.tsx (find ScoreGradePill block)
    - dashboard/components/scans/MetadataHeader.tsx (find ScoreGradePill block)
    - dashboard/components/share/ShareViewer.tsx (find inline letter derivation block)
    - .planning/phases/07.2-ui-contract-remediation-live/07.2-CONTEXT.md (D-05 section)
  </read_first>
  <action>
    For each of the 4 component files:

    1. Add import at top: `import { gradeInfo } from '@/lib/grade'`. The dashboard
       project uses the `@/` alias (verified by `grep -rln "from '@/" dashboard/components/`).
       Do NOT fall back to a relative path; the alias is the locked convention.

    2. Replace the inline letter-derivation logic. Pattern to find: any `if (score >= 80)`
       / `score >= 90` ladder OR ternary chain that produces a letter string.
       Replace with: `const letter = gradeInfo(score).letter` (or inline:
       `gradeInfo(score).letter`).

    3. PRESERVE pill styling — D-05 explicitly says "pill styling can stay inline but the
       letter MUST come from `gradeInfo()`". Do not change colors, padding, or rounded
       classes on the pill element.

    4. After the swap, no file outside `dashboard/lib/grade.ts` should contain a
       `score >= 80` (or 90, 85, 70, 60) literal in any of the 4 modified files.

    File-specific notes:
    - `ScansTable.tsx` and `MetadataHeader.tsx` both define a local `ScoreGradePill`
      component. Update each to call `gradeInfo()` for the letter. Do NOT extract the
      pill into a shared component — that's out of scope; only the letter source unifies.
    - `ScoreCard.tsx` and `ShareViewer.tsx` may inline the grade in JSX or in a const.
      Either way, swap the source.

    Do NOT modify any test file's import paths or other unrelated tests.
  </action>
  <verify>
    <automated>grep -l "gradeInfo" dashboard/components/home/ScoreCard.tsx dashboard/components/scans/ScansTable.tsx dashboard/components/scans/MetadataHeader.tsx dashboard/components/share/ShareViewer.tsx | wc -l | grep -q 4 && ! grep -nE "score\s*>=\s*(95|90|85|80|70|60)" dashboard/components/home/ScoreCard.tsx dashboard/components/scans/ScansTable.tsx dashboard/components/scans/MetadataHeader.tsx dashboard/components/share/ShareViewer.tsx</automated>
  </verify>
  <done>
    - All 4 files import `gradeInfo` from `@/lib/grade` (the alias form — no relative fallback)
    - All 4 files call `gradeInfo()` for the letter
    - No `score >= {95|90|85|80|70|60}` literals remain in any of the 4 files
    - Pill styling unchanged (colors, padding, rounded classes preserved)
    - `cd dashboard && npm test` passes
  </done>
  <acceptance_criteria>
    - For each of the 4 files: `grep -c "gradeInfo" {file}` returns ≥1
    - `grep -rnE "score\s*>=\s*(95|90|85|80|70|60)" dashboard/components/home/ScoreCard.tsx dashboard/components/scans/ScansTable.tsx dashboard/components/scans/MetadataHeader.tsx dashboard/components/share/ShareViewer.tsx` returns 0
    - Dashboard test suite passes with no regressions
    - Manual: load home page; the same scan's letter on ScoreCard hero, ScansTable row, MetadataHeader, and a share view all match
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none in scope) | Pure-function helper + read-only score input |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07.2.04-01 | Tampering | gradeInfo() input | accept | `score: number` is consumed only for branching; no eval, no DOM injection. NaN/negative inputs fall through to F (deterministic). |
| T-07.2.04-02 | Information Disclosure | Pill styling | accept | No PII; grade letter is derived from already-displayed score. |
</threat_model>

<verification>
- `grep -c "export function gradeInfo" dashboard/lib/grade.ts` returns 1
- All 14 boundary tests pass
- All 4 surface files import + use `gradeInfo`
- No threshold literals leak outside `lib/grade.ts`
- Manual cross-surface check on a single scan ID confirms identical letter
</verification>

<success_criteria>
- ROADMAP success-criteria item 5 satisfied: Grade letter shown for any given score is identical across ScoreCard, ScansTable, MetadataHeader, and ShareViewer
- D-05 acceptance criteria from CONTEXT.md met (gradeInfo extracted, all 4 surfaces use it, boundary tests for 95/94/90/89/85/84/80/79/70/69/60/59 all pass)
</success_criteria>

<output>
After completion, create `.planning/phases/07.2-ui-contract-remediation-live/07.2-04-SUMMARY.md`
</output>
