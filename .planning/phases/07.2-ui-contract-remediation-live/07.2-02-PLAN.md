---
phase: 07.2
plan: 02
type: execute
wave: 1
# Wave 1 (no dependencies on other phase 07.2 plans).
depends_on: []
files_modified:
  - viewer/src/components/FilterPanel.tsx
  - viewer/src/components/DetailPanel.tsx
  - viewer/src/components/SummaryBar.tsx
  - viewer/src/components/SearchBar.tsx
  - viewer/src/components/flowmap/FlowMapFilterPanel.tsx
  - viewer/src/components/flowmap/PathDetailPanel.tsx
  - viewer/src/components/flowmap/FlowMapCanvas.tsx
  - viewer/src/components/flowmap/nodes/RouterNode.tsx
  - viewer/src/components/flowmap/nodes/CloudHubNode.tsx
  - viewer/src/components/flowmap/nodes/FirewallNode.tsx
autonomous: true
requirements: [D-02]
must_haves:
  truths:
    - "FilterPanel, DetailPanel, SummaryBar, SearchBar render and react to per-scan state when embedded in the dashboard"
    - "FlowMap-variant panels and nodes (FlowMapFilterPanel, PathDetailPanel, FlowMapCanvas, RouterNode, CloudHubNode, FirewallNode) read from the per-scan store, not the singleton"
  artifacts:
    - path: "viewer/src/components/FilterPanel.tsx"
      provides: "FilterPanel reading from useViewerStoreOrSingleton"
      contains: "useViewerStoreOrSingleton"
    - path: "viewer/src/components/DetailPanel.tsx"
      provides: "DetailPanel reading from useViewerStoreOrSingleton"
      contains: "useViewerStoreOrSingleton"
    - path: "viewer/src/components/SummaryBar.tsx"
      provides: "SummaryBar reading from useViewerStoreOrSingleton"
      contains: "useViewerStoreOrSingleton"
    - path: "viewer/src/components/SearchBar.tsx"
      provides: "SearchBar reading from useViewerStoreOrSingleton"
      contains: "useViewerStoreOrSingleton"
    - path: "viewer/src/components/flowmap/FlowMapFilterPanel.tsx"
      provides: "FlowMap variant reading from useViewerStoreOrSingleton"
      contains: "useViewerStoreOrSingleton"
    - path: "viewer/src/components/flowmap/PathDetailPanel.tsx"
      provides: "FlowMap variant reading from useViewerStoreOrSingleton"
      contains: "useViewerStoreOrSingleton"
    - path: "viewer/src/components/flowmap/FlowMapCanvas.tsx"
      provides: "FlowMap canvas reading from useViewerStoreOrSingleton"
      contains: "useViewerStoreOrSingleton"
    - path: "viewer/src/components/flowmap/nodes/RouterNode.tsx"
      provides: "FlowMap RouterNode reading from useViewerStoreOrSingleton"
      contains: "useViewerStoreOrSingleton"
    - path: "viewer/src/components/flowmap/nodes/CloudHubNode.tsx"
      provides: "FlowMap CloudHubNode reading from useViewerStoreOrSingleton"
      contains: "useViewerStoreOrSingleton"
    - path: "viewer/src/components/flowmap/nodes/FirewallNode.tsx"
      provides: "FlowMap FirewallNode reading from useViewerStoreOrSingleton"
      contains: "useViewerStoreOrSingleton"
  key_links:
    - from: "FilterPanel/DetailPanel/SummaryBar/SearchBar (+ all FlowMap variants and nodes)"
      to: "Per-scan Zustand store via useViewerStoreOrSingleton factory"
      via: "Hook factory selects context-bound store when wrapped in ViewerStoreProvider, else singleton"
      pattern: "useViewerStoreOrSingleton\\("
---

<!-- Revision iteration 1: Added 4 missing flowmap files (FlowMapCanvas, RouterNode, CloudHubNode, FirewallNode) discovered via `grep -rln 'useStore' viewer/src/components/flowmap/`. Decoded HTML entities in shell verify gates. -->

<objective>
Migrate the 4 viewer components (+ all 6 FlowMap-variant files) that still call the singleton `useStore` to the `useViewerStoreOrSingleton` factory hook, so they bind to the per-scan store provided by the dashboard's `ViewerStoreProvider`.

Purpose: D-02 P0 unblocker — the dual-store fix from the 2026-05-01 debug session migrated `DiagramCanvas` and `ResourceNode` only; `FilterPanel`, `DetailPanel`, `SummaryBar`, `SearchBar` AND every FlowMap-variant sibling (`FlowMapFilterPanel`, `PathDetailPanel`, `FlowMapCanvas`, plus the three flowmap node components `RouterNode`, `CloudHubNode`, `FirewallNode`) were deferred. Phase 7.1's tab shell now renders all of them; with singleton bindings they read from a `graph: null` global and stay invisible inside the dashboard embed (works fine in standalone HTML where the singleton IS the only store).
Output: Ten `.tsx` files updated to import `useViewerStoreOrSingleton` and replace each `useStore(...)` call site, verified by recursive grep gate.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/07.2-ui-contract-remediation-live/07.2-CONTEXT.md
@.planning/phases/07.1-phase-7-ui-contract-remediation/07.1-LIVE-UI-REVIEW.md
@viewer/src/store.ts
@viewer/src/components/FilterPanel.tsx
@viewer/src/components/DetailPanel.tsx
@viewer/src/components/SummaryBar.tsx
@viewer/src/components/SearchBar.tsx
@viewer/src/components/flowmap/FlowMapFilterPanel.tsx
@viewer/src/components/flowmap/PathDetailPanel.tsx
@viewer/src/components/flowmap/FlowMapCanvas.tsx
@viewer/src/components/flowmap/nodes/RouterNode.tsx
@viewer/src/components/flowmap/nodes/CloudHubNode.tsx
@viewer/src/components/flowmap/nodes/FirewallNode.tsx

<interfaces>
<!-- From viewer/src/store.ts — the executor will read this file to confirm the exact factory signature. -->
<!-- Key contract: useViewerStoreOrSingleton<T>(selector: (state: StoreState) => T): T -->
<!-- It returns the context-bound store when used inside <ViewerStoreProvider>, else the module singleton. -->
<!-- Drop-in replacement for useStore — same selector signature. -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migrate FilterPanel + DetailPanel + SummaryBar + SearchBar to useViewerStoreOrSingleton</name>
  <files>viewer/src/components/FilterPanel.tsx, viewer/src/components/DetailPanel.tsx, viewer/src/components/SummaryBar.tsx, viewer/src/components/SearchBar.tsx</files>
  <read_first>
    - viewer/src/store.ts (confirm `useViewerStoreOrSingleton` is exported and matches `useStore` signature)
    - viewer/src/components/FilterPanel.tsx
    - viewer/src/components/DetailPanel.tsx
    - viewer/src/components/SummaryBar.tsx
    - viewer/src/components/SearchBar.tsx
    - .planning/phases/07.2-ui-contract-remediation-live/07.2-CONTEXT.md (D-02 section, lines 48–51)
    - viewer/src/components/DiagramCanvas.tsx (reference — already-migrated component for the pattern)
  </read_first>
  <action>
    For each of the four files (`FilterPanel.tsx`, `DetailPanel.tsx`, `SummaryBar.tsx`, `SearchBar.tsx`):

    1. Replace the import:
       `import { useStore } from '../store'` (or whatever the relative path is in that file)
       with
       `import { useViewerStoreOrSingleton } from '../store'`
       (preserve the relative path — do not change `..` to `../..` etc.)

    2. Replace EVERY call site `useStore(` with `useViewerStoreOrSingleton(`. The selector
       argument is unchanged — this is a drop-in replacement.

    3. Do NOT change selectors, render logic, JSX, or any other code.

    4. If a file already imports `useViewerStoreOrSingleton` (mixed usage), only swap the
       remaining `useStore` references; do not duplicate the import.

    Reference pattern: see `viewer/src/components/DiagramCanvas.tsx` and
    `viewer/src/components/ResourceNode.tsx` — both already migrated in the 2026-05-01 fix.
  </action>
  <verify>
    <automated>! grep -n "useStore" viewer/src/components/FilterPanel.tsx viewer/src/components/DetailPanel.tsx viewer/src/components/SummaryBar.tsx viewer/src/components/SearchBar.tsx && grep -l "useViewerStoreOrSingleton" viewer/src/components/FilterPanel.tsx viewer/src/components/DetailPanel.tsx viewer/src/components/SummaryBar.tsx viewer/src/components/SearchBar.tsx | wc -l | grep -q 4</automated>
  </verify>
  <done>
    - `grep -rn "useStore" viewer/src/components/FilterPanel.tsx viewer/src/components/DetailPanel.tsx viewer/src/components/SummaryBar.tsx viewer/src/components/SearchBar.tsx` returns 0 hits
    - Each of the 4 files contains ≥1 `useViewerStoreOrSingleton` reference
    - `cd viewer && npm test` exits 0
  </done>
  <acceptance_criteria>
    - `grep -c "useStore" viewer/src/components/FilterPanel.tsx` returns 0 (and same for the other 3 files)
    - `grep -c "useViewerStoreOrSingleton" viewer/src/components/FilterPanel.tsx` returns ≥1 (and same for the other 3 files)
    - Viewer Vitest suite passes
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Migrate ALL FlowMap variants and nodes to useViewerStoreOrSingleton</name>
  <files>viewer/src/components/flowmap/FlowMapFilterPanel.tsx, viewer/src/components/flowmap/PathDetailPanel.tsx, viewer/src/components/flowmap/FlowMapCanvas.tsx, viewer/src/components/flowmap/nodes/RouterNode.tsx, viewer/src/components/flowmap/nodes/CloudHubNode.tsx, viewer/src/components/flowmap/nodes/FirewallNode.tsx</files>
  <read_first>
    - viewer/src/components/flowmap/FlowMapFilterPanel.tsx
    - viewer/src/components/flowmap/PathDetailPanel.tsx
    - viewer/src/components/flowmap/FlowMapCanvas.tsx
    - viewer/src/components/flowmap/nodes/RouterNode.tsx
    - viewer/src/components/flowmap/nodes/CloudHubNode.tsx
    - viewer/src/components/flowmap/nodes/FirewallNode.tsx
    - .planning/phases/07.2-ui-contract-remediation-live/07.2-CONTEXT.md (D-02 section — explicitly mentions FlowMap variants)
  </read_first>
  <action>
    The complete enumeration of FlowMap files that import `useStore` (verified during planning via
    `grep -rln 'useStore' viewer/src/components/flowmap/`) is:

    1. `viewer/src/components/flowmap/FlowMapFilterPanel.tsx`
    2. `viewer/src/components/flowmap/PathDetailPanel.tsx`
    3. `viewer/src/components/flowmap/FlowMapCanvas.tsx`
    4. `viewer/src/components/flowmap/nodes/RouterNode.tsx`
    5. `viewer/src/components/flowmap/nodes/CloudHubNode.tsx`
    6. `viewer/src/components/flowmap/nodes/FirewallNode.tsx`

    For EACH of the six files above:

    1. Replace the import. The relative path differs by directory depth:
       - Files directly under `flowmap/` (FlowMapFilterPanel, PathDetailPanel, FlowMapCanvas):
         `import { useStore } from '../../store'` → `import { useViewerStoreOrSingleton } from '../../store'`
       - Files under `flowmap/nodes/` (RouterNode, CloudHubNode, FirewallNode):
         `import { useStore } from '../../../store'` → `import { useViewerStoreOrSingleton } from '../../../store'`
       Preserve whatever the actual relative path is in each file — do not invent a different prefix.

    2. Replace EVERY call site `useStore(` with `useViewerStoreOrSingleton(`. Selector unchanged.

    3. Do NOT change render logic, JSX, props, ReactFlow handle/edge wiring, or any other code.

    4. If a file already imports `useViewerStoreOrSingleton` (mixed usage), only swap the
       remaining `useStore` references; do not duplicate the import.
  </action>
  <verify>
    <automated>! grep -rn "useStore" viewer/src/components/flowmap/ | grep -v useViewerStoreOrSingleton</automated>
  </verify>
  <done>
    - `grep -rn "useStore" viewer/src/components/flowmap/ | grep -v useViewerStoreOrSingleton` returns 0 hits
    - Each of the 6 migrated files contains ≥1 `useViewerStoreOrSingleton` reference
    - Viewer Vitest suite passes
  </done>
  <acceptance_criteria>
    - `grep -rn "useStore" viewer/src/components/flowmap/ | grep -v useViewerStoreOrSingleton` returns 0 hits
    - For each of the 6 files: `grep -c "useViewerStoreOrSingleton" {file}` returns ≥1
    - Viewer Vitest suite passes
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Dashboard ↔ Viewer state context | Per-scan store provided via `ViewerStoreProvider`; isolation preserves scan-A vs scan-B data separation |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07.2.02-01 | Information Disclosure | Per-scan store boundary | mitigate | `useViewerStoreOrSingleton` reads context-bound store first; cross-scan leakage prevented by React context scoping. Verified by existing tests for DiagramCanvas/ResourceNode migration. |
| T-07.2.02-02 | Tampering | Hook swap correctness | accept | Mechanical refactor; viewer Vitest suite catches selector signature regressions. |
</threat_model>

<verification>
- `grep -rn "useStore" viewer/src/components/ | grep -v useViewerStoreOrSingleton` returns 0 hits across all files
- `cd viewer && npm test` exits 0 (currently 171 tests; no count regression)
- Manual: open `/scans/[id]` in dev server with two different scans; FilterPanel/DetailPanel/SummaryBar/SearchBar AND the FlowMap tab render scan-specific data (no `graph: null` empty state, no FlowMap blank canvas)
</verification>

<success_criteria>
- ROADMAP success-criteria item 2 satisfied: FilterPanel / DetailPanel / SummaryBar / SearchBar render and react to per-scan state when embedded in dashboard
- FlowMap tab also renders per-scan data (canvas, nodes, filter panel, path detail) when embedded in dashboard
- D-02 acceptance criteria from CONTEXT.md met
</success_criteria>

<output>
After completion, create `.planning/phases/07.2-ui-contract-remediation-live/07.2-02-SUMMARY.md`
</output>
