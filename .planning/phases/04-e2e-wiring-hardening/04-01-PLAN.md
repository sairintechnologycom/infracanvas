---
phase: 04-e2e-wiring-hardening
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - cli/infracanvas/main.py
autonomous: true
requirements:
  - WRG-01
tags:
  - cli
  - exit-codes
  - typer

must_haves:
  truths:
    - "infracanvas export returns exit code 0 on success"
    - "infracanvas export returns exit code 1 when input report file is missing"
    - "infracanvas export returns exit code 2 when report file fails parse/validation"
    - "infracanvas export accepts --gate-mode/--no-gate-mode flag, default true, and passes it to export_html()"
    - "All error messages from scan/score/plan/export go to stderr, not stdout"
    - "scan/score/plan/export commands apply the 0/1/2 exit-code contract uniformly"
  artifacts:
    - path: "cli/infracanvas/main.py"
      provides: "Module-level _err_console = Console(stderr=True), --gate-mode option on export command, normalized exit codes, stderr-routed error prints"
      contains: "_err_console = Console(stderr=True)"
      contains_also: "--gate-mode/--no-gate-mode"
      contains_also_2: "export_html(graph, out_path, gate_mode=gate_mode)"
  key_links:
    - from: "cli/infracanvas/main.py (export command)"
      to: "cli/infracanvas/export/html.py:export_html"
      via: "keyword arg gate_mode=gate_mode"
      pattern: "export_html\\(.*gate_mode=gate_mode"
    - from: "cli/infracanvas/main.py error paths"
      to: "stderr"
      via: "_err_console.print"
      pattern: "_err_console\\.print"
---

<objective>
Close WRG-01: make `infracanvas export` (and scan/score/plan) obey a deterministic exit-code contract, route errors to stderr, and expose `gate_mode` as a CLI flag so CI/scripts can invoke it without interpretation.

Purpose: Phase 5+ and downstream SaaS work rely on deterministic CLI behavior. Today `export` silently ignores `gate_mode`, mixes errors into stdout, and `score`/`plan` use inconsistent exit codes for the same failure class.

Output: `cli/infracanvas/main.py` updated with `_err_console` module-level constant, `--gate-mode/--no-gate-mode` flag on `export`, call-site fix passing `gate_mode=gate_mode` to `export_html()`, and normalized `typer.Exit(code=…)` values across all four commands per D-02 + D-04.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-e2e-wiring-hardening/04-CONTEXT.md
@.planning/phases/04-e2e-wiring-hardening/04-PATTERNS.md
@cli/infracanvas/main.py
@cli/infracanvas/export/html.py

<interfaces>
<!-- Key contracts extracted from existing code. Use these directly — no discovery needed. -->

From cli/infracanvas/export/html.py:
```python
def export_html(graph: ResourceGraph, output_path: Path, gate_mode: bool = True) -> None
```
Signature already accepts gate_mode. The bug is purely at the call site in main.py:763.

From cli/infracanvas/main.py (existing module-level consoles, ~line 39-40):
```python
console = Console()
_ci_console = Console(stderr=True)  # for CI mode: diagnostics go to stderr
```
Phase 4 adds `_err_console = Console(stderr=True)` as a third alongside.

Typer boolean flag idiom (for --gate-mode/--no-gate-mode):
```python
gate_mode: Annotated[
    bool,
    typer.Option("--gate-mode/--no-gate-mode",
                 help="Enable free-tier resource gating (default: true)"),
] = True,
```

Exit-code triplet (D-02, reified by existing main.py:89, 142, 338, 366, 390, 752):
| Code | Meaning                                  |
|------|------------------------------------------|
| 0    | success                                   |
| 1    | missing input file (report/planfile/dir)  |
| 2    | parse / Pydantic validation error         |
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add _err_console, --gate-mode flag, and fix export call site</name>
  <files>cli/infracanvas/main.py</files>

  <read_first>
    - cli/infracanvas/main.py (entire file — must see current state of all four command bodies: scan, score, plan, export; and the existing `console` / `_ci_console` module-level constants)
    - cli/infracanvas/export/html.py (confirm `export_html(graph, output_path, gate_mode=True)` signature is unchanged — no edits here)
    - .planning/phases/04-e2e-wiring-hardening/04-CONTEXT.md §Implementation Decisions WRG-01 (D-01..D-04 — exact flag name, exact exit-code table, stderr routing rule)
    - .planning/phases/04-e2e-wiring-hardening/04-PATTERNS.md §`cli/infracanvas/main.py` (lines covering the 7-site migration table and the Typer Annotated idiom)
  </read_first>

  <action>
    Make four concrete edits to `cli/infracanvas/main.py`:

    **Edit 1 — Add module-level error console (near line 40, right after `_ci_console`):**
    ```python
    _err_console = Console(stderr=True)  # for error messages (WRG-01 D-03)
    ```
    Keep the existing `console` and `_ci_console` lines as-is. The comment must reference `WRG-01 D-03` so future readers can trace.

    **Edit 2 — Add `--gate-mode/--no-gate-mode` option to the `export` command signature at `main.py:735-748`.** The Typer parameter list today ends with `format: Annotated[str, typer.Option("--format", "-f", help="Export format (json, html)")] = "html"`. Insert this parameter immediately after `format` (order: after format, before the function body opens):
    ```python
    gate_mode: Annotated[
        bool,
        typer.Option(
            "--gate-mode/--no-gate-mode",
            help="Enable free-tier resource gating (default: true)",
        ),
    ] = True,
    ```

    **Edit 3 — Fix the export call site at `main.py:763` per D-01.** Change:
    ```python
    export_html(graph, out_path)
    ```
    to:
    ```python
    export_html(graph, out_path, gate_mode=gate_mode)
    ```

    **Edit 4 — Migrate 7 error-print sites from `console` → `_err_console` and normalize their exit codes per D-02/D-04.** Exact table (copied verbatim from PATTERNS.md §cli/infracanvas/main.py — CLI error/exit contract):

    | Line (pre-edit) | Current | New                                                               |
    |-----------------|---------|-------------------------------------------------------------------|
    | 337–338         | `console.print("[red]Error:[/red] …"); raise typer.Exit(code=2)` | `_err_console.print(…); raise typer.Exit(code=2)` |
    | 464–465         | same (serve dir-check)                                            | `_err_console.print(…); raise typer.Exit(code=2)` |
    | 565–566         | `… (score dir-check); raise typer.Exit(code=1)`                   | `_err_console.print(…); raise typer.Exit(code=2)` **(NORMALIZE code 1→2)** |
    | 626–627         | `… (plan dir-check); raise typer.Exit(code=1)`                    | `_err_console.print(…); raise typer.Exit(code=2)` **(NORMALIZE code 1→2)** |
    | 630–631         | `… (plan planfile-missing); raise typer.Exit(code=1)`             | `_err_console.print(…); raise typer.Exit(code=1)` |
    | 751–752         | `… ({report} not found); raise typer.Exit(code=1)`                | `_err_console.print(…); raise typer.Exit(code=1)` |
    | 758–759         | `… (Invalid report file); raise typer.Exit(code=1)`               | `_err_console.print(…); raise typer.Exit(code=2)` **(NORMALIZE code 1→2 — parse/validation)** |

    **Critical normalization rule:** "not-a-directory" and "invalid-JSON/Pydantic-validation-failure" are parse/validation errors → exit code 2. "File literally does not exist at the given path" is a missing-input error → exit code 1. Do not confuse.

    **Do NOT migrate** `_ci_console.print(...)` calls in `_run_scan()` — those are CI-mode diagnostic routes and stay on `_ci_console`. Only migrate calls currently on plain `console` that precede a `typer.Exit(code>=1)`.

    **Do NOT change** any success-path `console.print(…)` (e.g., the Rich tables printed on normal exits, the `Report saved to …` confirmations). Those stay on stdout.

    After editing, the grep `_err_console.print` should match exactly 7 sites; the grep `raise typer.Exit(code=1)` should remain (missing-input cases); `raise typer.Exit(code=2)` should be the dominant exit for parse/validation errors.
  </action>

  <verify>
    <automated>cd cli &amp;&amp; python -c "import infracanvas.main" &amp;&amp; ruff check infracanvas/main.py &amp;&amp; mypy infracanvas/main.py</automated>
  </verify>

  <acceptance_criteria>
    - `grep -n "_err_console = Console(stderr=True)" cli/infracanvas/main.py` returns exactly 1 match (module level, near other consoles)
    - `grep -n "WRG-01 D-03" cli/infracanvas/main.py` returns exactly 1 match (the new console's comment)
    - `grep -n '"--gate-mode/--no-gate-mode"' cli/infracanvas/main.py` returns exactly 1 match inside the `export` command signature
    - `grep -n "export_html(graph, out_path, gate_mode=gate_mode)" cli/infracanvas/main.py` returns exactly 1 match
    - `grep -c "_err_console.print" cli/infracanvas/main.py` returns `7` (the 7 migrated sites)
    - `cd cli &amp;&amp; ruff check infracanvas/main.py` exits 0
    - `cd cli &amp;&amp; mypy infracanvas/main.py` exits 0
    - `cd cli &amp;&amp; python -m infracanvas.main export /nonexistent/file.json 2>/dev/null; echo $?` outputs `1`
    - `cd cli &amp;&amp; python -m infracanvas.main export /nonexistent/file.json >/dev/null 2>&amp;1; echo $?` outputs `1` (exit propagates even when stderr is redirected)
    - `cd cli &amp;&amp; python -m infracanvas.main export /nonexistent/file.json 2>&amp;1 >/dev/null | grep -c "Error"` returns `>=1` (error text reaches stderr, not stdout)
    - Running `cd cli &amp;&amp; python -m infracanvas.main export --help` shows both `--gate-mode` and `--no-gate-mode` in the options listing
    - Existing pytest suite still passes: `cd cli &amp;&amp; pytest tests/ -x` exits 0
  </acceptance_criteria>

  <done>All 7 error-path sites route through `_err_console`; `export` command signature carries `--gate-mode/--no-gate-mode` default True; `export_html()` call site passes the flag through; `infracanvas export` on a missing file exits 1 with error on stderr; `infracanvas export` on a malformed JSON file exits 2; ruff + mypy clean; existing pytests still pass.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

No new trust boundaries. All changes are internal to the CLI process; no new external inputs, no new auth paths, no secrets introduced. The `--gate-mode` flag is a boolean parsed by Typer's built-in validation.

## STRIDE Threat Register (minimal — no new attack surface)

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01   | Tampering | `typer.Exit(code=N)` semantics | accept | Exit codes are script-visible only; no integrity claim. Consistency is a correctness property, not a security property. |
| T-04-02   | Information Disclosure | stderr-routed error messages | accept | Errors contain only filenames + Pydantic validation messages; no secrets are ever loaded into memory by `export` (it reads a user-provided JSON and writes HTML). ASVS L1 satisfied. |
</threat_model>

<verification>
- `python -m infracanvas.main export --help` shows `--gate-mode/--no-gate-mode`
- `python -m infracanvas.main export /nonexistent/path.json` exits 1, error on stderr
- `python -m infracanvas.main export tests/fixtures/malformed.json` exits 2 (or create a throwaway malformed JSON inline in the verify step)
- `python -m infracanvas.main export <valid-report>.json -o /tmp/out.html` exits 0, HTML written, stdout clean of error markers
- `ruff check` and `mypy --strict` both pass on `main.py`
</verification>

<success_criteria>
- `_err_console` constant exists and is used at exactly 7 error sites
- `--gate-mode/--no-gate-mode` flag appears in `export --help`
- `export_html()` receives `gate_mode` from the command layer
- All four commands (`scan`, `score`, `plan`, `export`) use 0/1/2 exit codes per D-02
- Error messages go to stderr; success output goes to stdout
- Existing 79+ pytests still pass
</success_criteria>

<output>
After completion, create `.planning/phases/04-e2e-wiring-hardening/04-01-SUMMARY.md` documenting the 7 migrated sites, the normalized exit codes, and any minor deviations from the table above.
</output>
