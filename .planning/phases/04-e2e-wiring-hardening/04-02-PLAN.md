---
phase: 04-e2e-wiring-hardening
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - cli/infracanvas/drift/analyzer.py
  - cli/infracanvas/graph/models.py
autonomous: true
requirements:
  - WRG-02
tags:
  - drift
  - summary
  - invariant

must_haves:
  truths:
    - "summary.drift contains exactly 5 keys: added, changed, deleted, unchanged, shadow"
    - "sum(graph.summary.drift.values()) == len(graph.nodes) after DriftAnalyzer runs"
    - "Every ResourceNode.drift is exactly one of DriftStatus.{unchanged, added, changed, deleted, shadow}"
    - "GraphSummary.drift default_factory produces all 5 keys initialized to 0 on a freshly constructed summary"
  artifacts:
    - path: "cli/infracanvas/drift/analyzer.py"
      provides: "drift_counts dict literal with all 5 keys"
      contains: '{"added": 0, "changed": 0, "deleted": 0, "unchanged": 0, "shadow": 0}'
    - path: "cli/infracanvas/graph/models.py"
      provides: "GraphSummary.drift default_factory with all 5 keys"
      contains: '"unchanged": 0'
      contains_also: '"shadow": 0'
  key_links:
    - from: "cli/infracanvas/drift/analyzer.py:drift_counts accumulator"
      to: "cli/infracanvas/graph/models.py:DriftStatus enum values"
      via: "if node.drift in drift_counts"
      pattern: "if node\\.drift in drift_counts"
---

<objective>
Close WRG-02: make `summary.drift_counts` a complete census of all `ResourceNode.drift` states so `sum(counts.values()) == len(graph.nodes)` always holds.

Purpose: Downstream consumers (Phase 6 API, Phase 9 CostLens dashboards) will read the drift summary as authoritative. The current 3-key literal (`added/changed/deleted`) silently drops `unchanged` and `shadow` nodes, breaking the invariant and producing misleading summary totals.

Output: `drift/analyzer.py` accumulates all 5 states; `graph/models.py` default factory for `GraphSummary.drift` initializes all 5 keys to zero. The invariant is mechanical and testable. Per D-08, viewer filter/badge changes are explicitly out of scope — this is a CLI summary fix only.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-e2e-wiring-hardening/04-CONTEXT.md
@.planning/phases/04-e2e-wiring-hardening/04-PATTERNS.md
@cli/infracanvas/drift/analyzer.py
@cli/infracanvas/graph/models.py

<interfaces>
<!-- Key contracts extracted from existing code. Use these directly — no discovery needed. -->

From cli/infracanvas/graph/models.py (lines 34–39) — DriftStatus enum already contains all 5 values:
```python
class DriftStatus(StrEnum):
    unchanged = "unchanged"
    added = "added"
    changed = "changed"
    deleted = "deleted"
    shadow = "shadow"
```
No enum change needed (CONTEXT.md line 64 was conservative; all values already exist per PATTERNS.md).

From cli/infracanvas/graph/models.py:61 — ResourceNode.drift default is `DriftStatus.unchanged`.

From cli/infracanvas/graph/models.py:73-75 — GraphSummary.drift default (TO EXTEND):
```python
drift: dict[str, int] = Field(
    default_factory=lambda: {"added": 0, "changed": 0, "deleted": 0}
)
```

From cli/infracanvas/drift/analyzer.py:41-44 — drift_counts init + accumulator loop (TO EXTEND):
```python
drift_counts = {"added": 0, "changed": 0, "deleted": 0}
for node in graph.nodes:
    if node.drift in drift_counts:
        drift_counts[node.drift] += 1
```

Invariant (D-05, D-09): after the loop, `sum(drift_counts.values()) == len(graph.nodes)`.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend drift_counts to 5 keys in both analyzer and models defaults</name>
  <files>cli/infracanvas/drift/analyzer.py, cli/infracanvas/graph/models.py</files>

  <read_first>
    - cli/infracanvas/drift/analyzer.py (entire file — must see the accumulator loop at ~line 41-44 and any downstream consumers of `graph.summary.drift` inside this module)
    - cli/infracanvas/graph/models.py (lines 1–90 minimum — must confirm DriftStatus enum has all 5 values and locate the GraphSummary.drift Field default_factory)
    - .planning/phases/04-e2e-wiring-hardening/04-CONTEXT.md §WRG-02 (D-05..D-09 — 5-key contract, mutual exclusivity, shadow semantics, deferred UI scope, property-test invariant)
    - .planning/phases/04-e2e-wiring-hardening/04-PATTERNS.md §`cli/infracanvas/drift/analyzer.py` and §`cli/infracanvas/graph/models.py` — exact before/after dict literals
  </read_first>

  <action>
    Two edits, one per file. Both are mechanical dict-literal extensions.

    **Edit 1 — `cli/infracanvas/drift/analyzer.py` (around line 41):** Replace the 3-key dict literal with the 5-key literal. The existing accumulator loop uses `if node.drift in drift_counts` — growing the dict automatically opts `unchanged` and `shadow` nodes into counting without any loop-body change.

    Before:
    ```python
    drift_counts = {"added": 0, "changed": 0, "deleted": 0}
    for node in graph.nodes:
        if node.drift in drift_counts:
            drift_counts[node.drift] += 1
    ```

    After (copy exactly — key order matches D-05 / CONTEXT.md):
    ```python
    drift_counts = {"added": 0, "changed": 0, "deleted": 0, "unchanged": 0, "shadow": 0}
    for node in graph.nodes:
        if node.drift in drift_counts:
            drift_counts[node.drift] += 1
    ```

    Keep everything else in `analyzer.py` unchanged. Do not touch the `graph.summary.drift = drift_counts` or `graph.summary.total_resources = len(graph.nodes)` lines that follow.

    **Edit 2 — `cli/infracanvas/graph/models.py` (GraphSummary.drift field, lines 73–75):** Extend the `default_factory` to match the 5-key contract so freshly constructed `GraphSummary()` instances have all keys present at zero.

    Before:
    ```python
    drift: dict[str, int] = Field(
        default_factory=lambda: {"added": 0, "changed": 0, "deleted": 0}
    )
    ```

    After (copy exactly):
    ```python
    drift: dict[str, int] = Field(
        default_factory=lambda: {
            "added": 0, "changed": 0, "deleted": 0, "unchanged": 0, "shadow": 0,
        }
    )
    ```

    **Do NOT modify** the `DriftStatus` StrEnum at lines 34–39 — all 5 values already exist.
    **Do NOT add** an `is_shadow` boolean or any orthogonal flag on `ResourceNode` (per D-06 the 5 drift states are mutually exclusive).
    **Do NOT touch** the viewer, FilterPanel, or summary badges (per D-08 — deferred).

    After both edits, the invariant `sum(graph.summary.drift.values()) == len(graph.nodes)` holds for any graph that has run through `DriftAnalyzer.apply()` (and also for any graph that has only default-constructed summary state, since unchanged is the default for every node per models.py:61 but unchanged nodes on a default summary simply sum to 0 until the analyzer runs — the invariant is a post-condition of the analyzer).
  </action>

  <verify>
    <automated>cd cli &amp;&amp; ruff check infracanvas/drift/analyzer.py infracanvas/graph/models.py &amp;&amp; mypy infracanvas/drift/analyzer.py infracanvas/graph/models.py &amp;&amp; pytest tests/test_drift.py tests/test_shadow.py -x</automated>
  </verify>

  <acceptance_criteria>
    - `grep -n '"added": 0, "changed": 0, "deleted": 0, "unchanged": 0, "shadow": 0' cli/infracanvas/drift/analyzer.py` returns exactly 1 match
    - `grep -n '"unchanged": 0' cli/infracanvas/graph/models.py` returns exactly 1 match inside the GraphSummary.drift default_factory
    - `grep -n '"shadow": 0' cli/infracanvas/graph/models.py` returns exactly 1 match inside the GraphSummary.drift default_factory
    - `grep -c 'class DriftStatus' cli/infracanvas/graph/models.py` returns `1` (enum untouched)
    - `grep -E 'is_shadow' cli/infracanvas/graph/models.py` returns no matches (no orthogonal flag introduced)
    - `cd cli &amp;&amp; ruff check infracanvas/drift/analyzer.py infracanvas/graph/models.py` exits 0
    - `cd cli &amp;&amp; mypy infracanvas/drift/analyzer.py infracanvas/graph/models.py` exits 0
    - `cd cli &amp;&amp; pytest tests/test_drift.py tests/test_shadow.py -x` exits 0 (existing tests may need updates in Plan 04 — if any pre-existing assertion in these files asserts `len(drift) == 3`, update it to `len(drift) == 5` as part of this edit since it's the same file under test)
    - Python one-liner check: `cd cli &amp;&amp; python -c "from infracanvas.graph.models import GraphSummary; s = GraphSummary(); assert sorted(s.drift.keys()) == ['added','changed','deleted','shadow','unchanged'], s.drift; assert all(v == 0 for v in s.drift.values()); print('OK')"` prints `OK`
  </acceptance_criteria>

  <done>`analyzer.py` drift_counts literal has 5 keys; `models.py` GraphSummary.drift default_factory produces 5 keys at zero; DriftStatus enum unchanged; no orthogonal flag introduced; existing test_drift + test_shadow suites still pass (with any 3-key assertion updated in-place to 5-key); ruff + mypy clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

No new trust boundaries. This is a structural change to an internal dict — the data shape is read-only output consumed by trusted CLI writers (HTML exporter, JSON exporter).

## STRIDE Threat Register (minimal — no new attack surface)

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-03   | Tampering | `graph.summary.drift` dict content | accept | Drift counts are derived from already-trusted `ResourceNode.drift` enum values. Pydantic validation on the enum is the existing boundary check; no new input channel is opened. |
| T-04-04   | Information Disclosure | `shadow` key now surfaced in summary JSON | accept | "Shadow" indicates a resource exists in plan/state but not current scan — not sensitive by itself. Consumers of the JSON are already trusted (user's own infrastructure snapshots). ASVS L1 satisfied. |
</threat_model>

<verification>
- `python -c "from infracanvas.graph.models import GraphSummary; print(GraphSummary().drift)"` prints a dict with 5 keys, all zero
- After running `DriftAnalyzer.apply(graph, changes)` on any mixed graph, `sum(graph.summary.drift.values()) == len(graph.nodes)` evaluates True
- `ruff check` and `mypy --strict` both pass on both files
- `pytest tests/test_drift.py tests/test_shadow.py -x` exits 0
</verification>

<success_criteria>
- drift_counts literal grows from 3 keys to 5 in exactly one place in `analyzer.py`
- GraphSummary.drift default_factory grows from 3 keys to 5 in exactly one place in `models.py`
- Invariant `sum(summary.drift.values()) == len(graph.nodes)` holds after analyzer runs (asserted by property test to be added in Plan 04)
- No change to DriftStatus enum (already complete)
- No change to viewer code (per D-08 deferral)
</success_criteria>

<output>
After completion, create `.planning/phases/04-e2e-wiring-hardening/04-02-SUMMARY.md` documenting the two edits, the invariant now satisfied, and any test files whose 3-key assertions were updated in-place to 5-key.
</output>
