---
phase: 03-flowmap-v1-0
plan: 05
type: execute
wave: 2
depends_on: [03-01]
files_modified:
  - cli/infracanvas/security/rules/network/aws_tgw.yaml
  - cli/infracanvas/security/rules/network/aws_vpc.yaml
  - cli/infracanvas/security/rules/network/aws_dx.yaml
  - cli/infracanvas/security/rules/network/azure_vwan.yaml
  - cli/infracanvas/security/rules/network/azure_vnet.yaml
  - cli/infracanvas/security/rules/network/azure_expressroute.yaml
  - cli/tests/test_flowmap_network_rules.py
  - cli/tests/fixtures/flowmap/rules/aws_net_fixtures.json
  - cli/tests/fixtures/flowmap/rules/azure_net_fixtures.json
autonomous: true
requirements: [FDM-03, NFN-01]
user_setup: []
tags: [yaml-rules, security-engine, net-findings, cloud-only, path-independent]

must_haves:
  truths:
    - "Security rule loader discovers all new NET-* YAML files under security/rules/network/ without any engine code changes (rglob auto-discovery per PATTERNS.md Loader discovery)"
    - "12 cloud-only, path-independent NET rules land: NET-001..NET-006 AWS + NET-007..NET-012 Azure (6 + 6 = 12 per RESEARCH.md minimum 4-6 per cloud; 6 chosen for breadth)"
    - "Each rule has grep-verifiable id, title, severity, resource_types, framework_ids, condition.attribute, condition.operator, condition.value/values, remediation, description"
    - "All 12 rules pass a parse-time YAML→SecurityRule validation (load_rules() returns them as SecurityRule instances)"
    - "Each rule produces a Finding when evaluated against a crafted positive fixture node; each rule produces NO finding against a crafted negative fixture node"
    - "Operator values are exclusively from the documented set: equals, not_equals, in, not_in, exists, not_exists, contains, matches_cidr, list_contains_cidr, any_equals (per security/engine.py lines 49-75)"
    - "NET-010 (stateful firewall on only one path — ASY-03) is NOT shipped in 3a — path-dependent — documented in SUMMARY output"
  artifacts:
    - path: "cli/infracanvas/security/rules/network/aws_tgw.yaml"
      provides: "NET-001 (TGW route to 0.0.0.0/0 blackhole), NET-002 (TGW attachment without route propagation)"
      contains: "- id: NET-001"
    - path: "cli/infracanvas/security/rules/network/aws_vpc.yaml"
      provides: "NET-003 (NACL allows 0.0.0.0/0 inbound), NET-004 (VPC without flow logs)"
      contains: "- id: NET-003"
    - path: "cli/infracanvas/security/rules/network/aws_dx.yaml"
      provides: "NET-005 (Direct Connect VIF down), NET-006 (DX connection without redundancy indicator)"
      contains: "- id: NET-005"
    - path: "cli/infracanvas/security/rules/network/azure_vwan.yaml"
      provides: "NET-007 (vWAN hub without internet-security on connection), NET-008 (hub connection in failed provisioning state)"
      contains: "- id: NET-007"
    - path: "cli/infracanvas/security/rules/network/azure_vnet.yaml"
      provides: "NET-009 (vNet peering with UseRemoteGateways true but AllowGatewayTransit false — misconfig), NET-011 (NSG rule allows wildcard destination)"
      contains: "- id: NET-009"
    - path: "cli/infracanvas/security/rules/network/azure_expressroute.yaml"
      provides: "NET-012 (ExpressRoute circuit in not-provisioned state)"
      contains: "- id: NET-012"
    - path: "cli/tests/test_flowmap_network_rules.py"
      provides: "Parse-time + evaluate-time pytest coverage for all 12 rules"
      contains: "class TestNetworkRuleEvaluation"
    - path: "cli/tests/fixtures/flowmap/rules/aws_net_fixtures.json"
      provides: "Crafted AWS positive/negative node fixtures for each NET-00x rule"
      contains: "NET-001"
    - path: "cli/tests/fixtures/flowmap/rules/azure_net_fixtures.json"
      provides: "Crafted Azure positive/negative node fixtures for each NET-0xx rule"
      contains: "NET-007"
  key_links:
    - from: "cli/infracanvas/security/rules/network/*.yaml"
      to: "cli/infracanvas/security/loader.py"
      via: "rglob('*.yaml') auto-discovery; zero engine code changes"
      pattern: "rglob"
    - from: "cli/infracanvas/security/rules/network/*.yaml"
      to: "cli/infracanvas/security/engine.py"
      via: "evaluate_all iterates graph.nodes, matches by resource_types, calls _evaluate_rule"
      pattern: "_evaluate_rule"
---

<objective>
Land the Phase 3a cloud-only, path-independent network rule catalogue: 12 NET-* YAML rules (6 AWS + 6 Azure) that evaluate against the resource types collected by Plans 03-03 (AWS) and 03-04 (Azure). Zero engine code changes — the existing `security/engine.py` (Phase 1 + Phase 2 POL extensions) already handles generic condition/operator/value over arbitrary ResourceNode.attributes.

Purpose: Deliver FDM-03 + NFN-01-partial. Gives users a meaningful findings panel the first time they run `infracanvas scan --flowmap` against a real cloud, surfacing common misconfigurations (orphaned TGW routes, unrestricted NSG rules, VIF down, ER circuit unprovisioned). Path-dependent rules (NET-010 asymmetric routing impact) defer to 3b per CONTEXT.md D-11.

Output: Six YAML files under `cli/infracanvas/security/rules/network/` + a pytest suite that proves each rule fires on a crafted positive fixture and stays silent on a negative fixture.

NET-id allocation (Claude's Discretion per D-11 — documented for 3b handoff):
- 3a (ships): NET-001..NET-009 + NET-011 + NET-012 (11 rules). NET-010 reserved.
- 3b (defers): NET-010 (stateful firewall on one path only — ASY-03 requires path math).

Note: to keep contiguous numbering the plan ships NET-001..NET-012 MINUS NET-010. SUMMARY output will state this explicitly so Plan 3b-01 picks NET-010 correctly.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-flowmap-v1-0/03-CONTEXT.md
@.planning/phases/03-flowmap-v1-0/03-RESEARCH.md
@.planning/phases/03-flowmap-v1-0/03-PATTERNS.md
@.planning/phases/03-flowmap-v1-0/03-01-SUMMARY.md
@.planning/phases/03-flowmap-v1-0/03-03-SUMMARY.md
@.planning/phases/03-flowmap-v1-0/03-04-SUMMARY.md
@cli/infracanvas/security/engine.py
@cli/infracanvas/security/loader.py
@cli/infracanvas/security/models.py
@cli/infracanvas/security/rules/aws/networking.yaml
@cli/infracanvas/security/rules/azure/network.yaml

<interfaces>
<!-- Existing operator set (engine.py lines 49-75) -->
equals, not_equals, in, not_in, exists, not_exists, contains, matches_cidr,
list_contains_cidr, any_equals

<!-- Existing YAML rule shape (SEC-003 from aws/networking.yaml — exact template) -->
- id: SEC-003
  title: "..."
  severity: critical
  resource_types: ["aws_security_group"]
  framework_ids: ["CIS-5.2", "NIST-AC-17", "SOC2-CC6.6", "PCI-DSS-1.2"]
  condition:
    attribute: "ingress.cidr_blocks"
    operator: "list_contains_cidr"
    values: ["0.0.0.0/0"]
  remediation: "..."
  description: "..."

<!-- Node type strings (from Plan 03-03 + 03-04 SUMMARYs) -->
AWS: aws_ec2_transit_gateway, aws_ec2_transit_gateway_attachment,
     aws_ec2_transit_gateway_route_table, aws_vpn_connection,
     aws_route_table, aws_network_acl, aws_dx_connection, aws_dx_virtual_interface,
     aws_vpc  (pre-existing from Phase 2 parser — used for flow-log NET-004)

Azure: azurerm_virtual_wan, azurerm_virtual_hub, azurerm_virtual_hub_connection,
       azurerm_virtual_network, azurerm_virtual_network_peering,
       azurerm_network_security_group, azurerm_express_route_circuit,
       azurerm_express_route_circuit_peering
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author 6 AWS NET-* YAML rules (NET-001..NET-006)</name>

  <read_first>
    - cli/infracanvas/security/rules/aws/networking.yaml (lines 1-11 — exact rule schema template)
    - cli/infracanvas/security/rules/aws/networking_advanced.yaml (for variety of operator usage)
    - cli/infracanvas/security/engine.py (lines 49-75 — canonical operator list)
    - cli/infracanvas/security/models.py (SecurityRule + RuleCondition Pydantic schema — for field name parity)
    - .planning/phases/03-flowmap-v1-0/03-PATTERNS.md §`cli/infracanvas/security/rules/network/aws_*.yaml` (YAML rule pattern)
    - .planning/phases/03-flowmap-v1-0/03-03-SUMMARY.md (exact AWS node.type strings and attribute field names)
    - .planning/phases/03-flowmap-v1-0/03-CONTEXT.md §D-11 (path-independent only)
  </read_first>

  <files>cli/infracanvas/security/rules/network/aws_tgw.yaml, cli/infracanvas/security/rules/network/aws_vpc.yaml, cli/infracanvas/security/rules/network/aws_dx.yaml</files>

  <action>
    Create three YAML files following `cli/infracanvas/security/rules/aws/networking.yaml` schema exactly. Only use operators from the documented set.

    **`cli/infracanvas/security/rules/network/aws_tgw.yaml`** (NET-001, NET-002):
    ```yaml
    - id: NET-001
      title: "Transit Gateway Route Table Has Blackhole Route"
      severity: high
      resource_types: ["aws_ec2_transit_gateway_route_table"]
      framework_ids: ["CIS-3.4", "NIST-SC-7", "SOC2-CC6.6"]
      condition:
        attribute: "routes"
        operator: "any_equals"
        value: "blackhole"
      remediation: "Remove blackhole routes or replace with a valid attachment; blackholes silently drop traffic and confuse asymmetric-routing analysis."
      description: "A TGW route table entry is in blackhole state. Traffic matching this destination is dropped without notification."

    - id: NET-002
      title: "Transit Gateway Attachment Not In 'available' State"
      severity: medium
      resource_types: ["aws_ec2_transit_gateway_attachment"]
      framework_ids: ["CIS-3.4", "NIST-CM-6", "SOC2-CC7.1"]
      condition:
        attribute: "state"
        operator: "not_equals"
        value: "available"
      remediation: "Investigate the attachment state — pending/failed/deleting attachments indicate an incomplete or broken integration."
      description: "A Transit Gateway attachment is not in the 'available' state. Traffic through this attachment may be dropped."
    ```

    NOTE on NET-001: `any_equals` operator reads the first element of the `routes` list, then matches against its `value` field... actually `any_equals` checks if any dict in the list has value === condition.value for a named path. Confirm by reading engine.py lines 73-74 + `_check_any_equals` helper. If `any_equals` signature does not fit, fall back to `contains` with attribute `routes[].State` or restructure. **Before writing the final YAML, read `_check_any_equals` in engine.py to confirm the exact semantics**, then adapt the condition shape (may need `attribute: "routes"` + `operator: "contains"` + `value: "blackhole"` depending on how the helper treats nested lists). If the current operators cannot cleanly express "any route has state=blackhole", restructure NET-001 to evaluate at collection time by extracting `has_blackhole_route: bool` into the TGW route-table node attributes during Plan 03-03 (that refactor requires an amendment — prefer a simpler rule form first).

    If the operator semantics don't line up cleanly, use this alternate form for NET-001:
    ```yaml
    condition:
      attribute: "state"
      operator: "not_equals"
      value: "available"
    # With resource_types: ["aws_ec2_transit_gateway_route_table"]
    # catches route-table-level state issues — still path-independent
    ```

    **`cli/infracanvas/security/rules/network/aws_vpc.yaml`** (NET-003, NET-004):
    ```yaml
    - id: NET-003
      title: "Network ACL Allows Unrestricted Ingress From Internet"
      severity: high
      resource_types: ["aws_network_acl"]
      framework_ids: ["CIS-5.1", "NIST-SC-7", "SOC2-CC6.6", "PCI-DSS-1.3"]
      condition:
        attribute: "entries"
        operator: "any_equals"
        value: "0.0.0.0/0"
      remediation: "Restrict NACL ingress entries to specific CIDR ranges or layer SGs for fine-grained control."
      description: "A Network ACL entry allows inbound traffic from 0.0.0.0/0. Combined with a permissive security group, this exposes the subnet to the public internet."

    - id: NET-004
      title: "VPC Without Flow Logs Enabled"
      severity: medium
      resource_types: ["aws_vpc"]
      framework_ids: ["CIS-3.9", "NIST-AU-2", "SOC2-CC7.2", "PCI-DSS-10.1"]
      condition:
        attribute: "flow_log"
        operator: "not_exists"
      remediation: "Enable VPC Flow Logs targeting CloudWatch Logs or S3 to capture accepted/rejected traffic for forensics."
      description: "This VPC has no associated flow log. Network traffic is not being recorded; incident response and asymmetric-routing analysis will be blind."
    ```

    **`cli/infracanvas/security/rules/network/aws_dx.yaml`** (NET-005, NET-006):
    ```yaml
    - id: NET-005
      title: "Direct Connect Virtual Interface Not In 'available' State"
      severity: critical
      resource_types: ["aws_dx_virtual_interface"]
      framework_ids: ["CIS-3.4", "NIST-CM-6", "SOC2-A1.2"]
      condition:
        attribute: "state"
        operator: "not_equals"
        value: "available"
      remediation: "Investigate the VIF state (pending, confirming, down, rejected). Hybrid connectivity is degraded while a VIF is not available."
      description: "A Direct Connect virtual interface is not in the 'available' state. On-prem↔AWS traffic through this interface is impaired."

    - id: NET-006
      title: "Direct Connect Connection Not In 'available' State"
      severity: high
      resource_types: ["aws_dx_connection"]
      framework_ids: ["CIS-3.4", "NIST-CM-6", "SOC2-A1.2"]
      condition:
        attribute: "state"
        operator: "not_equals"
        value: "available"
      remediation: "Investigate connection state; coordinate with the DX provider if the port is in a non-available state."
      description: "A Direct Connect connection is not in 'available' state. All VIFs on this connection are offline."
    ```

    Before finalising NET-001 and NET-003 (which use `any_equals`), open `cli/infracanvas/security/engine.py` and read the `_check_any_equals` helper implementation. If the helper's semantics don't fit (e.g., it expects a dotted path into a list of dicts with a specific nested key), restructure those two rules to use the simpler `not_equals` / `exists` forms that DO fit — adjust the rule title/description accordingly. The safety net: NET-001 and NET-003 BOTH have simpler fallback forms (state-based on the route-table-level `state` field, and `entries` existence check) — use them if `any_equals` doesn't fit cleanly.

    Author note on CIS/NIST/SOC2/PCI-DSS IDs: use plausible mappings consistent with Phase 2 precedent; exact catalog lookup not required for 3a (Phase 5 CMP-* does the authoritative mapping). Reviewer can correct in code review.
  </action>

  <verify>
    <automated>cd /Users/bhushan/Documents/Projects/Infracanvas/cli && python -c "from infracanvas.security.loader import load_rules; rules = load_rules(); ids = {r.id for r in rules}; assert 'NET-001' in ids and 'NET-002' in ids and 'NET-003' in ids and 'NET-004' in ids and 'NET-005' in ids and 'NET-006' in ids; print('ok')"</automated>
  </verify>

  <done>
    - `grep -c "^- id: NET-001" cli/infracanvas/security/rules/network/aws_tgw.yaml` returns 1
    - `grep -c "^- id: NET-002" cli/infracanvas/security/rules/network/aws_tgw.yaml` returns 1
    - `grep -c "^- id: NET-003" cli/infracanvas/security/rules/network/aws_vpc.yaml` returns 1
    - `grep -c "^- id: NET-004" cli/infracanvas/security/rules/network/aws_vpc.yaml` returns 1
    - `grep -c "^- id: NET-005" cli/infracanvas/security/rules/network/aws_dx.yaml` returns 1
    - `grep -c "^- id: NET-006" cli/infracanvas/security/rules/network/aws_dx.yaml` returns 1
    - All 6 rules include `framework_ids:` line
    - All 6 rules use operators ONLY from the documented set (equals / not_equals / in / not_in / exists / not_exists / contains / matches_cidr / list_contains_cidr / any_equals) — verified by: `grep "operator:" cli/infracanvas/security/rules/network/aws_*.yaml | awk -F'"' '{print $2}' | sort -u` returns only documented operator names
    - `load_rules()` returns all 6 NET-00x rules without raising
  </done>
</task>

<task type="auto">
  <name>Task 2: Author 6 Azure NET-* YAML rules (NET-007..NET-012 minus NET-010)</name>

  <read_first>
    - cli/infracanvas/security/rules/azure/network.yaml (lines 1-11 — Azure rule template)
    - cli/infracanvas/security/engine.py (operator semantics recheck)
    - .planning/phases/03-flowmap-v1-0/03-04-SUMMARY.md (Azure node.type strings + attribute fields)
    - .planning/phases/03-flowmap-v1-0/03-PATTERNS.md §`azure_*.yaml` (Azure YAML variant)
  </read_first>

  <files>cli/infracanvas/security/rules/network/azure_vwan.yaml, cli/infracanvas/security/rules/network/azure_vnet.yaml, cli/infracanvas/security/rules/network/azure_expressroute.yaml</files>

  <action>
    **`cli/infracanvas/security/rules/network/azure_vwan.yaml`** (NET-007, NET-008):
    ```yaml
    - id: NET-007
      title: "vWAN Hub Connection Missing Internet Security"
      severity: high
      resource_types: ["azurerm_virtual_hub_connection"]
      framework_ids: ["CIS-Azure-6.5", "NIST-SC-7", "SOC2-CC6.6"]
      condition:
        attribute: "enable_internet_security"
        operator: "equals"
        value: false
      remediation: "Enable 'Propagate to None' + internet security on the hub connection so egress traffic is inspected by Azure Firewall in Secure Hub mode."
      description: "A vWAN hub-to-vNet connection has internet security disabled. Outbound traffic from this vNet is not inspected by a Secure Hub firewall."

    - id: NET-008
      title: "vWAN Hub Connection Not In Succeeded State"
      severity: medium
      resource_types: ["azurerm_virtual_hub_connection"]
      framework_ids: ["CIS-Azure-6.1", "NIST-CM-6", "SOC2-A1.2"]
      condition:
        attribute: "provisioning_state"
        operator: "not_equals"
        value: "Succeeded"
      remediation: "Investigate the connection provisioning state (Creating / Failed / Updating). Traffic may be incorrectly routed until provisioning completes."
      description: "A vWAN hub connection is not in 'Succeeded' state. Routing through this connection may be impaired."
    ```

    **`cli/infracanvas/security/rules/network/azure_vnet.yaml`** (NET-009, NET-011):
    ```yaml
    - id: NET-009
      title: "vNet Peering Misconfigured Gateway Transit"
      severity: medium
      resource_types: ["azurerm_virtual_network_peering"]
      framework_ids: ["CIS-Azure-6.2", "NIST-CM-6", "SOC2-CC7.1"]
      condition:
        attribute: "use_remote_gateways"
        operator: "equals"
        value: true
      remediation: "If UseRemoteGateways is true on this side, the REMOTE peering MUST set AllowGatewayTransit true. Otherwise, gateway routing fails silently."
      description: "A vNet peering asserts UseRemoteGateways. Verify the corresponding peering on the remote vNet has AllowGatewayTransit enabled."

    - id: NET-011
      title: "NSG Rule Uses Wildcard Destination"
      severity: high
      resource_types: ["azurerm_network_security_group"]
      framework_ids: ["CIS-Azure-6.1", "NIST-SC-7", "SOC2-CC6.6", "PCI-DSS-1.2"]
      condition:
        attribute: "security_rules"
        operator: "any_equals"
        value: "*"
      remediation: "Replace wildcard destinationAddressPrefix with specific IP ranges, service tags (VirtualNetwork, AzureLoadBalancer), or ASGs."
      description: "A network security group rule uses '*' as the destination address prefix. This is overly permissive and bypasses network segmentation."
    ```

    If `any_equals` doesn't fit NET-011 cleanly (given `security_rules` is a list of dicts with nested `properties.destinationAddressPrefix`), fall back to:
    ```yaml
    # Alternate form if any_equals over nested-dict-list is not supported:
    condition:
      attribute: "security_rules"
      operator: "exists"
    # And document in SUMMARY that NET-011 is "triggers on any NSG with at least one explicit security_rule — follow-up SEC-NEXT refines"
    ```
    The fallback still catches the shape in 3a; Plan 3b or a later cleanup plan can refine.

    **`cli/infracanvas/security/rules/network/azure_expressroute.yaml`** (NET-012):
    ```yaml
    - id: NET-012
      title: "ExpressRoute Circuit Not Provisioned"
      severity: critical
      resource_types: ["azurerm_express_route_circuit"]
      framework_ids: ["CIS-Azure-6.1", "NIST-CM-6", "SOC2-A1.2"]
      condition:
        attribute: "service_provider_provisioning_state"
        operator: "not_equals"
        value: "Provisioned"
      remediation: "Coordinate with the ExpressRoute service provider to complete provisioning. Hybrid connectivity through this circuit is non-functional until provisioned."
      description: "An ExpressRoute circuit is not in 'Provisioned' state (e.g., NotProvisioned, Provisioning, Deprovisioning). On-prem ↔ Azure traffic through this circuit is impaired."
    ```

    **NET-010 intentionally NOT shipped in this file** — reserved for Phase 3b (ASY-03 — stateful firewall on one path). Document in Plan 03-05 SUMMARY.
  </action>

  <verify>
    <automated>cd /Users/bhushan/Documents/Projects/Infracanvas/cli && python -c "from infracanvas.security.loader import load_rules; rules = load_rules(); ids = {r.id for r in rules}; assert 'NET-007' in ids and 'NET-008' in ids and 'NET-009' in ids and 'NET-011' in ids and 'NET-012' in ids; assert 'NET-010' not in ids; print('ok, NET-010 correctly absent')"</automated>
  </verify>

  <done>
    - `grep -c "^- id: NET-007" cli/infracanvas/security/rules/network/azure_vwan.yaml` returns 1
    - `grep -c "^- id: NET-008" cli/infracanvas/security/rules/network/azure_vwan.yaml` returns 1
    - `grep -c "^- id: NET-009" cli/infracanvas/security/rules/network/azure_vnet.yaml` returns 1
    - `grep -c "^- id: NET-011" cli/infracanvas/security/rules/network/azure_vnet.yaml` returns 1
    - `grep -c "^- id: NET-012" cli/infracanvas/security/rules/network/azure_expressroute.yaml` returns 1
    - `grep -rc "NET-010" cli/infracanvas/security/rules/network/` returns 0 (intentionally absent — reserved for 3b)
    - `load_rules()` returns all 5 new Azure NET rules successfully (plus the 6 AWS from Task 1 = 11 total)
    - All Azure rules use compliance tags prefixed `CIS-Azure-` (matching Phase 2 convention in azure/network.yaml)
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Pytest suite for all 11 NET-* rules + crafted positive/negative fixtures</name>

  <read_first>
    - cli/tests/test_security.py (entire file — PATTERNS.md §test_flowmap_network_rules.py analog, rule-count + rule-id + severity assertion patterns)
    - cli/infracanvas/security/engine.py (evaluate_all + _evaluate_rule signatures — reuse directly in tests)
    - cli/infracanvas/security/rules/network/ (Tasks 1+2 output — drives the test matrix)
    - .planning/phases/03-flowmap-v1-0/03-PATTERNS.md §Pytest fixture scaffold + §YAML rule conventions
  </read_first>

  <files>cli/tests/test_flowmap_network_rules.py, cli/tests/fixtures/flowmap/rules/aws_net_fixtures.json, cli/tests/fixtures/flowmap/rules/azure_net_fixtures.json</files>

  <behavior>
    - `test_loader_finds_all_net_rules`: `load_rules()` returns ≥11 rules whose id starts with `NET-`, covering NET-001..NET-009, NET-011, NET-012; NOT NET-010
    - `test_net_rule_severities_are_valid`: every NET-* rule has severity in {critical, high, medium, info}
    - `test_net_rule_operators_are_documented`: every NET-* rule uses an operator from the documented set
    - `test_net_rule_has_framework_ids`: every NET-* rule has non-empty framework_ids list
    - For each of the 11 NET rules: a `test_net_00X_fires_on_positive_fixture` test that builds a ResourceGraph with a crafted positive node, runs `evaluate_all`, asserts the rule's Finding appears on that node
    - For each of the 11 NET rules: a `test_net_00X_silent_on_negative_fixture` test that builds a ResourceGraph with a crafted negative node, runs `evaluate_all`, asserts the rule's Finding does NOT appear
  </behavior>

  <action>
    Create `cli/tests/fixtures/flowmap/rules/aws_net_fixtures.json`:
    ```json
    {
      "NET-001_positive": {
        "id": "aws_ec2_transit_gateway_route_table.broken",
        "type": "aws_ec2_transit_gateway_route_table",
        "name": "broken",
        "provider": "aws",
        "region": "us-east-1",
        "attributes": {"state": "pending", "routes": []}
      },
      "NET-001_negative": {
        "id": "aws_ec2_transit_gateway_route_table.healthy",
        "type": "aws_ec2_transit_gateway_route_table",
        "name": "healthy",
        "provider": "aws",
        "region": "us-east-1",
        "attributes": {"state": "available", "routes": []}
      },
      "NET-002_positive": {
        "id": "aws_ec2_transit_gateway_attachment.pending",
        "type": "aws_ec2_transit_gateway_attachment",
        "name": "pending",
        "provider": "aws",
        "region": "us-east-1",
        "attributes": {"state": "pending"}
      },
      "NET-002_negative": {
        "id": "aws_ec2_transit_gateway_attachment.ok",
        "type": "aws_ec2_transit_gateway_attachment",
        "name": "ok",
        "provider": "aws",
        "region": "us-east-1",
        "attributes": {"state": "available"}
      },
      "NET-003_positive": {
        "id": "aws_network_acl.open",
        "type": "aws_network_acl",
        "name": "open",
        "provider": "aws",
        "region": "us-east-1",
        "attributes": {"entries": [{"cidr_block": "0.0.0.0/0", "rule_action": "allow"}]}
      },
      "NET-003_negative": {
        "id": "aws_network_acl.closed",
        "type": "aws_network_acl",
        "name": "closed",
        "provider": "aws",
        "region": "us-east-1",
        "attributes": {"entries": [{"cidr_block": "10.0.0.0/16", "rule_action": "allow"}]}
      },
      "NET-004_positive": {
        "id": "aws_vpc.no_flowlog",
        "type": "aws_vpc",
        "name": "no_flowlog",
        "provider": "aws",
        "region": "us-east-1",
        "attributes": {"vpc_id": "vpc-x"}
      },
      "NET-004_negative": {
        "id": "aws_vpc.with_flowlog",
        "type": "aws_vpc",
        "name": "with_flowlog",
        "provider": "aws",
        "region": "us-east-1",
        "attributes": {"vpc_id": "vpc-y", "flow_log": {"log_group": "/aws/vpc/prod"}}
      },
      "NET-005_positive": {
        "id": "aws_dx_virtual_interface.down",
        "type": "aws_dx_virtual_interface",
        "name": "down",
        "provider": "aws",
        "region": "us-east-1",
        "attributes": {"state": "down"}
      },
      "NET-005_negative": {
        "id": "aws_dx_virtual_interface.up",
        "type": "aws_dx_virtual_interface",
        "name": "up",
        "provider": "aws",
        "region": "us-east-1",
        "attributes": {"state": "available"}
      },
      "NET-006_positive": {
        "id": "aws_dx_connection.down",
        "type": "aws_dx_connection",
        "name": "down",
        "provider": "aws",
        "region": "us-east-1",
        "attributes": {"state": "down"}
      },
      "NET-006_negative": {
        "id": "aws_dx_connection.up",
        "type": "aws_dx_connection",
        "name": "up",
        "provider": "aws",
        "region": "us-east-1",
        "attributes": {"state": "available"}
      }
    }
    ```

    Create `cli/tests/fixtures/flowmap/rules/azure_net_fixtures.json`:
    ```json
    {
      "NET-007_positive": {
        "id": "azurerm_virtual_hub_connection.insecure",
        "type": "azurerm_virtual_hub_connection",
        "name": "insecure",
        "provider": "azure",
        "region": "eastus",
        "attributes": {"enable_internet_security": false, "provisioning_state": "Succeeded"}
      },
      "NET-007_negative": {
        "id": "azurerm_virtual_hub_connection.secure",
        "type": "azurerm_virtual_hub_connection",
        "name": "secure",
        "provider": "azure",
        "region": "eastus",
        "attributes": {"enable_internet_security": true, "provisioning_state": "Succeeded"}
      },
      "NET-008_positive": {
        "id": "azurerm_virtual_hub_connection.failed",
        "type": "azurerm_virtual_hub_connection",
        "name": "failed",
        "provider": "azure",
        "region": "eastus",
        "attributes": {"enable_internet_security": true, "provisioning_state": "Failed"}
      },
      "NET-008_negative": {
        "id": "azurerm_virtual_hub_connection.succeeded",
        "type": "azurerm_virtual_hub_connection",
        "name": "succeeded",
        "provider": "azure",
        "region": "eastus",
        "attributes": {"enable_internet_security": true, "provisioning_state": "Succeeded"}
      },
      "NET-009_positive": {
        "id": "azurerm_virtual_network_peering.broken",
        "type": "azurerm_virtual_network_peering",
        "name": "broken",
        "provider": "azure",
        "region": "eastus",
        "attributes": {"use_remote_gateways": true, "allow_gateway_transit": false}
      },
      "NET-009_negative": {
        "id": "azurerm_virtual_network_peering.ok",
        "type": "azurerm_virtual_network_peering",
        "name": "ok",
        "provider": "azure",
        "region": "eastus",
        "attributes": {"use_remote_gateways": false, "allow_gateway_transit": false}
      },
      "NET-011_positive": {
        "id": "azurerm_network_security_group.wildcard",
        "type": "azurerm_network_security_group",
        "name": "wildcard",
        "provider": "azure",
        "region": "eastus",
        "attributes": {"security_rules": [{"name": "open", "properties": {"destinationAddressPrefix": "*"}}]}
      },
      "NET-011_negative": {
        "id": "azurerm_network_security_group.scoped",
        "type": "azurerm_network_security_group",
        "name": "scoped",
        "provider": "azure",
        "region": "eastus",
        "attributes": {"security_rules": []}
      },
      "NET-012_positive": {
        "id": "azurerm_express_route_circuit.notprov",
        "type": "azurerm_express_route_circuit",
        "name": "notprov",
        "provider": "azure",
        "region": "eastus",
        "attributes": {"service_provider_provisioning_state": "NotProvisioned", "circuit_provisioning_state": "Enabled"}
      },
      "NET-012_negative": {
        "id": "azurerm_express_route_circuit.prov",
        "type": "azurerm_express_route_circuit",
        "name": "prov",
        "provider": "azure",
        "region": "eastus",
        "attributes": {"service_provider_provisioning_state": "Provisioned", "circuit_provisioning_state": "Enabled"}
      }
    }
    ```

    Create `cli/tests/test_flowmap_network_rules.py`:
    ```python
    """Tests for FlowMap NET-* network findings (FDM-03, NFN-01 partial — 3a cloud-only)."""
    from __future__ import annotations

    import json
    from pathlib import Path

    import pytest

    from infracanvas.graph.models import ResourceGraph, ResourceNode, Severity
    from infracanvas.security.engine import evaluate_all
    from infracanvas.security.loader import load_rules

    FIXTURES = Path(__file__).parent / "fixtures" / "flowmap" / "rules"
    AWS_FIX = json.load(open(FIXTURES / "aws_net_fixtures.json"))
    AZ_FIX = json.load(open(FIXTURES / "azure_net_fixtures.json"))

    ALL_NET_IDS_3A = [
        "NET-001", "NET-002", "NET-003", "NET-004", "NET-005", "NET-006",
        "NET-007", "NET-008", "NET-009", "NET-011", "NET-012",
    ]
    DOCUMENTED_OPERATORS = {
        "equals", "not_equals", "in", "not_in", "exists", "not_exists",
        "contains", "matches_cidr", "list_contains_cidr", "any_equals",
    }


    def _node_from_fixture(data: dict) -> ResourceNode:
        return ResourceNode(**data)


    class TestRuleLoader:
        def test_loads_all_net_rules(self):
            rules = load_rules()
            ids = {r.id for r in rules}
            for nid in ALL_NET_IDS_3A:
                assert nid in ids, f"Missing rule {nid}"
            assert "NET-010" not in ids, "NET-010 should be deferred to Phase 3b"

        def test_net_rules_have_valid_severities(self):
            net = [r for r in load_rules() if r.id.startswith("NET-")]
            for r in net:
                assert r.severity in Severity, f"{r.id} has invalid severity {r.severity}"

        def test_net_rules_use_documented_operators(self):
            net = [r for r in load_rules() if r.id.startswith("NET-")]
            for r in net:
                assert r.condition.operator in DOCUMENTED_OPERATORS, (
                    f"{r.id} uses undocumented operator '{r.condition.operator}'"
                )

        def test_net_rules_have_framework_ids(self):
            net = [r for r in load_rules() if r.id.startswith("NET-")]
            for r in net:
                assert r.framework_ids, f"{r.id} missing framework_ids"


    class TestNetworkRuleEvaluation:
        @pytest.mark.parametrize("rule_id", [
            "NET-001", "NET-002", "NET-003", "NET-004", "NET-005", "NET-006",
        ])
        def test_aws_rule_fires_on_positive(self, rule_id):
            fix = AWS_FIX[f"{rule_id}_positive"]
            g = ResourceGraph(nodes=[_node_from_fixture(fix)])
            g = evaluate_all(g)
            findings = [f for f in g.nodes[0].findings if f.rule_id == rule_id]
            assert findings, f"{rule_id} did not fire on positive fixture"

        @pytest.mark.parametrize("rule_id", [
            "NET-001", "NET-002", "NET-003", "NET-004", "NET-005", "NET-006",
        ])
        def test_aws_rule_silent_on_negative(self, rule_id):
            fix = AWS_FIX[f"{rule_id}_negative"]
            g = ResourceGraph(nodes=[_node_from_fixture(fix)])
            g = evaluate_all(g)
            findings = [f for f in g.nodes[0].findings if f.rule_id == rule_id]
            assert not findings, f"{rule_id} fired on negative fixture (false positive)"

        @pytest.mark.parametrize("rule_id", [
            "NET-007", "NET-008", "NET-009", "NET-011", "NET-012",
        ])
        def test_azure_rule_fires_on_positive(self, rule_id):
            fix = AZ_FIX[f"{rule_id}_positive"]
            g = ResourceGraph(nodes=[_node_from_fixture(fix)])
            g = evaluate_all(g)
            findings = [f for f in g.nodes[0].findings if f.rule_id == rule_id]
            assert findings, f"{rule_id} did not fire on positive fixture"

        @pytest.mark.parametrize("rule_id", [
            "NET-007", "NET-008", "NET-009", "NET-011", "NET-012",
        ])
        def test_azure_rule_silent_on_negative(self, rule_id):
            fix = AZ_FIX[f"{rule_id}_negative"]
            g = ResourceGraph(nodes=[_node_from_fixture(fix)])
            g = evaluate_all(g)
            findings = [f for f in g.nodes[0].findings if f.rule_id == rule_id]
            assert not findings, f"{rule_id} fired on negative fixture (false positive)"
    ```

    **Iteration note:** if Task 1/Task 2 used fallback rule forms (e.g., NET-001 checks route-table state vs. routes list), adjust the positive/negative fixture JSON to match. The fixture is authored to match the YAML — update both together if the author opts for a fallback form.
  </action>

  <verify>
    <automated>cd /Users/bhushan/Documents/Projects/Infracanvas/cli && python -m pytest tests/test_flowmap_network_rules.py -x -v</automated>
  </verify>

  <done>
    - `grep -c "class TestRuleLoader" cli/tests/test_flowmap_network_rules.py` returns 1
    - `grep -c "class TestNetworkRuleEvaluation" cli/tests/test_flowmap_network_rules.py` returns 1
    - `pytest cli/tests/test_flowmap_network_rules.py -v` — all tests pass (≥26 tests: 4 loader + 11 positive + 11 negative)
    - `pytest cli/tests/` — no regressions (existing Phase 1/2 SEC-* + AZ-* + POL-* tests unaffected; load_rules() now returns 11 more rules but existing tests assert on specific IDs, not counts)
    - `ruff check cli/tests/test_flowmap_network_rules.py` exits 0
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| YAML rule files → security/loader.py | Parsed at runtime by loader.py; malformed YAML would raise at rule-load time; blessed loader is PyYAML safe_load per Phase 1 precedent |
| Rule evaluation → Finding evidence in graph.nodes.findings | Evidence dict embedded in graph serialised to HTML; already a Phase 1 precedent (Finding.evidence) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-05-01 | T (Tampering) | Malicious YAML in rules/network/ | accept | Rules are check-in artifacts under CODEOWNERS/PR review. Runtime parses via PyYAML safe_load (verified in loader.py). Not a dynamic-injection surface. |
| T-03-05-02 | I (Information Disclosure) | NET rule Finding.evidence exposing raw node attributes | accept | Existing Phase 1+2 pattern — security engine sanitizes via `_sanitize_evidence` helper (engine.py). NET-* inherits automatically. |
| T-03-05-03 | D (Denial of Service) | Rule evaluation cost over 500+ nodes × 40+ rules | accept | Phase 1 perf budget (< 10s for 500 nodes) proven in Phase 1 SUMMARY. NET-* adds 11 rules → ~20% evaluation increase, well within budget. |
| T-03-05-04 | S (Spoofing) | Rule author forges framework_ids to imply compliance | mitigate | 3a documents framework_ids as "plausible mappings" in SUMMARY. Phase 5 CMP-* introduces authoritative CIS/NIST/SOC2/PCI-DSS mapping database. Phase 4 dashboard MUST NOT claim compliance certification based on these IDs. |
| T-03-05-05 | R (Repudiation) | Findings disappearing from viewer silently (rule regression) | mitigate | Task 3 `TestNetworkRuleEvaluation` pytest parametrized suite locks positive fixtures — any rule regression fails CI. |
</threat_model>

<verification>
Phase-level checks:
1. `cd cli && pytest tests/test_flowmap_network_rules.py -v` — 26+ tests pass
2. `cd cli && pytest tests/` — no regressions to Phase 1/2 security/policy test suites
3. `cd cli && python -c "from infracanvas.security.loader import load_rules; n=[r for r in load_rules() if r.id.startswith('NET-')]; print(f'NET rules loaded: {len(n)}')"` prints `NET rules loaded: 11`
4. Manual: create a minimal HCL fixture that collects (via Plans 03-03/04) the rule-positive shapes and run end-to-end `_run_scan(flowmap=True)` (with mocked SDKs) — findings surface in `graph.nodes[*].findings` with `source == "security"` and `rule_id` starting with `NET-`
</verification>

<success_criteria>
- 11 NET-* rules land under `cli/infracanvas/security/rules/network/` (NET-001..009, 011, 012). NET-010 reserved for 3b.
- Zero changes to `security/engine.py` or `security/loader.py` — existing rglob discovery picks up new YAML files automatically
- All rules use documented operators; all have framework_ids; all have valid severities
- Pytest matrix (22 positive/negative tests + 4 loader tests) locks rule behavior
- `evaluate_all(graph)` with a 3a-collected cloud graph produces NET-* findings in `node.findings` through the existing unified pipeline (D-12)
- Fixture JSON files are hand-crafted, sanitized, no real customer data
</success_criteria>

<output>
Create `.planning/phases/03-flowmap-v1-0/03-05-SUMMARY.md` listing:
- Each shipped NET-id + final resource_types + final operator (if fallback was used)
- NET-010 explicit reservation note for Phase 3b-01 author
- Framework-id-mapping caveat (plausible, not authoritative)
- Count of rules loaded after 3a merge (Phase 2 count + 11)
</output>
