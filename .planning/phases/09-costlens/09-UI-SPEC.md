---
phase: 9
slug: costlens
status: approved
reviewed_at: 2026-05-05
shadcn_initialized: true
preset: new-york / slate / cssVariables
created: 2026-05-05
---

# Phase 9 — UI Design Contract

> Visual and interaction contract for the CostLens phase.
> Generated by gsd-ui-researcher. Verified by gsd-ui-checker.
>
> This phase touches TWO rendering surfaces:
> - **Dashboard** (`dashboard/`) — shadcn/ui new-york, Tailwind v4 CSS vars, Next.js RSC
> - **Viewer** (`viewer/`) — custom Tailwind v4 theme, standalone HTML report

---

## Design System

| Property | Value | Source |
|----------|-------|--------|
| Tool | shadcn/ui | `dashboard/components.json` |
| Style | new-york | `components.json` → `style` |
| Base color | slate | `components.json` → `tailwind.baseColor` |
| CSS variables | true | `globals.css` — all tokens as `--color-*` |
| Component library | Radix UI (via shadcn) | `dashboard/components/ui/` |
| Icon library | lucide-react | `components.json` → `iconLibrary` |
| Font (dashboard) | System sans via shadcn slate preset | `globals.css` → `font-sans` |
| Font (viewer) | Inter (with JetBrains Mono for mono) | `viewer/src/index.css` |
| Viewer design system | Custom Tailwind v4 theme (no shadcn) | `viewer/src/index.css` |

**shadcn Gate result:** `components.json` found — pre-populated from existing preset. No re-initialization needed.

**Registry Safety:** shadcn official only. No third-party registries. Gate not required.

---

## Spacing Scale

Declared values (multiples of 4 only):

| Token | Value | Usage |
|-------|-------|-------|
| xs | 4px | Icon gaps, badge internal padding, inline separators |
| sm | 8px | Compact element spacing, card header icon-label gap |
| md | 16px | Default card padding, table cell horizontal padding, section gaps |
| lg | 24px | Card-to-card gap in workload grid, section vertical spacing |
| xl | 32px | Panel top padding, idle-recommendations section top margin |
| 2xl | 48px | Major section breaks between workload grid and recommendations |
| 3xl | 64px | Page-level vertical breathing (unused in this phase) |

Exceptions:
- Dollar amount column in dashboard table: `min-w-[120px]` to prevent wrapping of `$1,234.56/mo`
- Percentage column: `min-w-[64px]`
- Workload card line-item rows: `py-1` (4px top + 4px bottom) for compact breakdown lists

---

## Typography

Both surfaces share the same conceptual scale. Implementation tokens differ per surface.

**Weight constraint:** 2 weights per surface maximum. All roles map to either 400 (regular) or 600 (semibold).

### Dashboard surface (shadcn/Tailwind CSS vars)

| Role | Size | Weight | Line Height | Tailwind class |
|------|------|--------|-------------|----------------|
| Body | 14px | 400 | 1.5 | `text-sm` |
| Label | 12px | 400 | 1.4 | `text-xs` |
| Heading | 16px | 600 | 1.25 | `text-base font-semibold` |
| Display | 24px | 600 | 1.2 | `text-2xl font-semibold` |

- Body (14px/400/1.5) — table cells, card body text, line-item descriptions
- Label (12px/400/1.4) — column headers, badge text, metadata chips
- Heading (16px/600/1.25) — section headings ("Idle & Oversized Recommendations"), card workload names
- Display (24px/600/1.2) — total allocated cost per workload in the dashboard table summary row

Dollar amounts in the workload column: `font-mono text-sm` (`text-sm` + `font-mono`) to align decimal points. Use `tabular-nums` (`font-variant-numeric: tabular-nums`).

### Viewer surface (custom Tailwind theme, Inter)

| Role | Size | Weight | Line Height | CSS |
|------|------|--------|-------------|-----|
| Body | 14px | 400 | 1.5 | `text-sm` |
| Label | 12px | 400 | 1.4 | `text-xs` |
| Heading | 16px | 600 | 1.25 | `text-base font-semibold` |
| Display | 20px | 600 | 1.2 | `text-xl font-semibold` |

Dollar amounts in viewer workload cards: `font-mono tabular-nums text-sm`.

---

## Color

### Dashboard surface

| Role | Token | Computed (light) | Computed (dark) | Usage |
|------|-------|-----------------|-----------------|-------|
| Dominant (60%) | `--background` | `oklch(1 0 0)` ≈ #FFFFFF | `oklch(0.145 0 0)` ≈ #1C1C1C | Page background |
| Secondary (30%) | `--card` / `--muted` | `oklch(0.97 0 0)` ≈ #F7F7F7 | `oklch(0.205 0 0)` ≈ #282828 | Workload table rows, section containers |
| Accent (10%) | `--primary` | `oklch(0.205 0 0)` ≈ #282828 | `oklch(0.922 0 0)` ≈ #EBEBEB | Active tab indicator, "Cost" tab label when selected |
| Destructive | `--destructive` | `oklch(0.577 0.245 27)` ≈ #DC2626 | `oklch(0.704 0.191 22)` ≈ #EF4444 | Idle waste amount badge (high-waste >$100/mo) |
| Success / positive | Tailwind `green-600` (#16A34A) | — | — | Shared cost % bar fills, clean allocation indicator |

**Accent reserved for:** active "Cost" tab underline, keyboard focus ring. NOT used for cost amounts, workload names, or table row hover.

**Semantic cost colors (new for this phase):**

| Semantic | Value | Usage |
|----------|-------|-------|
| Cost positive (waste) | `text-destructive` | Monthly waste amount when resource is idle |
| Cost neutral (shared) | `text-muted-foreground` | Shared resource contribution line items |
| Cost total | `text-foreground` | Workload total allocated cost, dedicated resource costs |

### Viewer surface

| Role | Token | Value | Usage |
|------|-------|-------|-------|
| Dominant (60%) | `--color-canvas-bg` | `#FAFBFC` | CostLens tab background |
| Secondary (30%) | `--color-card-bg` / `--color-card-border` | `#FFFFFF` / `#E2E8F0` | Workload cards, recommendations section |
| Accent (10%) | `#3B82F6` (matches `--color-flow-forward`) | — | Active workload card left border, percentage fill bars |
| Waste indicator | `--color-sev-high` (`#f97316`) | — | Idle/oversized recommendation badge |
| Foreground | `#0f172a` | — | All body text |

**Accent reserved for:** workload card left-border accent stripe (4px solid), percentage allocation bar fill. NOT used for headings or general text.

---

## Component Inventory

### Dashboard — new components for this phase

| Component | shadcn base | File path |
|-----------|-------------|-----------|
| `CostTab` | `Tabs` (existing `components/ui/tabs.tsx`) | `app/(dashboard)/scans/[id]/CostTab.tsx` |
| `WorkloadTable` | `Table` (existing `components/ui/table.tsx`) | `components/scans/WorkloadTable.tsx` |
| `IdleRecommendationsList` | `Card` (existing `components/ui/card.tsx`) | `components/scans/IdleRecommendationsList.tsx` |
| `WasteBadge` | none — `<span>` with `bg-destructive/10 text-destructive` | inline in `IdleRecommendationsList` |

### Dashboard — reused existing components

| Component | File | Usage in this phase |
|-----------|------|---------------------|
| `Tabs` / `TabsList` / `TabsTrigger` | `components/ui/tabs.tsx` | Tab switcher on scan detail page (Viewer / Cost) |
| `Table` / `TableRow` / `TableHead` / `TableCell` | `components/ui/table.tsx` | Workload allocation table |
| `Card` / `CardHeader` / `CardContent` | `components/ui/card.tsx` | Idle recommendations wrapper |
| `Skeleton` | `components/ui/skeleton.tsx` | Loading state while scan JSON fetched |
| `Badge` | `components/ui/badge.tsx` (add if not present) | "idle" / "oversized" resource type label |

### Viewer — new components for this phase

| Component | File path | Notes |
|-----------|-----------|-------|
| `CostLensPanel` | `viewer/src/components/costlens/CostLensPanel.tsx` | Root panel, replaces `null` in tab render branch |
| `WorkloadCard` | `viewer/src/components/costlens/WorkloadCard.tsx` | Per-workload card with line-item breakdown |
| `IdleRecommendations` | `viewer/src/components/costlens/IdleRecommendations.tsx` | Bottom section of CostLens tab |

---

## Layout Contract

### Dashboard — "Cost" tab on scan detail page

```
┌─ ScanDetailPage ─────────────────────────────────────────────────────┐
│  <MetadataHeader />                                                   │
│  ┌─ Tabs (shadcn) ─────────────────────────────────────────────────┐ │
│  │  [Viewer]  [Cost]                                                │ │
│  └─────────────────────────────────────────────────────────────────┘ │
│  ┌─ TabsContent value="cost" ──────────────────────────────────────┐ │
│  │  <WorkloadTable />                                               │ │
│  │    workload        allocated/mo   shared resources   details     │ │
│  │    payments-svc    $412.00        TGW + NAT GW        ›          │ │
│  │    auth-svc        $234.00        TGW                 ›          │ │
│  │    untagged        $178.00        TGW                 ›          │ │
│  │  ─────────────────────────────────────────────────────────────  │ │
│  │  <IdleRecommendationsList />                                     │ │
│  │    [!] aws_nat_gateway.main  idle  $32.85/mo waste               │ │
│  └─────────────────────────────────────────────────────────────────┘ │
└──────────────────────────────────────────────────────────────────────┘
```

- Tab switcher uses shadcn `Tabs` with `TabsList` + `TabsTrigger`. Tab values: `"viewer"` and `"cost"`.
- `WorkloadTable` fills available width. Columns: Workload (flex), Allocated/mo (120px min, right-align), Shared Resources (labels only, flex), Details chevron (32px).
- `IdleRecommendationsList` is separated from the table by a 48px top margin (`mt-12`) and a full-width `<hr className="border-border" />`.
- Expandable detail row: clicking the chevron in a workload row expands an inline sub-row showing per-shared-resource line items (name, $amount, % share). Uses `aria-expanded` on the trigger row. No modal — inline accordion pattern.

**Details chevron ("›") trigger behavior:** The chevron is intentionally icon-only. The "Details" column header text serves as the label context for the entire column. On hover, show a tooltip with text `"View cost breakdown"` via shadcn `Tooltip` (`TooltipProvider` > `Tooltip` > `TooltipTrigger` > `TooltipContent`). The trigger is a `<button>` with `aria-label="View cost breakdown for {workload name}"` and `aria-expanded` reflecting the row's expanded state.

### Viewer — CostLens tab layout

```
┌─ CostLens tab ───────────────────────────────────────────────────────┐
│  p-4 grid cols-1 gap-4 (md: cols-2, lg: cols-3)                      │
│                                                                       │
│  ┌─ WorkloadCard ────────────────────────────────── $412/mo ───────┐ │
│  │  border-l-4 border-[#3B82F6] rounded-lg bg-white p-4            │ │
│  │  payments-svc                               $412.00/mo           │ │
│  │  ────────────────────────────────────────────────────────────── │ │
│  │  TGW attachment         $89.00     25%                           │ │
│  │  NAT Gateway share      $11.00      8%                           │ │
│  │  EC2 (dedicated)       $312.00     —                             │ │
│  └─────────────────────────────────────────────────────────────────┘ │
│                                                                       │
│  [more workload cards…]                                               │
│                                                                       │
│  ── Idle & Oversized Recommendations ──────────────────────────────── │
│                                                                       │
│  aws_nat_gateway.main   idle   $32.85/mo waste                        │
│  Zero route entries reference this NAT Gateway in the Terraform graph │
│                                                                       │
└──────────────────────────────────────────────────────────────────────┘
```

- Grid: `grid grid-cols-1 gap-4 p-4 md:grid-cols-2 lg:grid-cols-3`
- `WorkloadCard`: `rounded-lg border border-[#E2E8F0] bg-white p-4 border-l-4 border-l-[#3B82F6]`
- Line item rows within card: `flex justify-between items-center py-1 text-sm`
- Percentage value: `text-muted-foreground text-xs ml-2` (right side, dimmer than $ amount)
- `untagged` card renders identically but with label `untagged` in italic (`italic text-muted-foreground`)
- Recommendations section: `mt-12` separator + heading `text-base font-semibold mb-4` + flat list rows

---

## Interaction States

### Dashboard WorkloadTable

| State | Visual |
|-------|--------|
| Default row | `bg-background` |
| Hover | `bg-muted/50` (shadcn TableRow default) |
| Expanded | `bg-muted/50 has-aria-expanded:bg-muted/50` (shadcn pattern — already in `table.tsx`) |
| Loading | `<Skeleton>` — full table replaced with 3 skeleton rows of height `h-10` |
| Empty (no costlens block) | Empty state component (see Copywriting) |
| Error (fetch fail) | Error state component (see Copywriting) |

### Viewer WorkloadCard

| State | Visual |
|-------|--------|
| Default | White card, blue left border |
| Hover | `bg-[#F1F5F9]` (matches `--color-card-hover`) — CSS `hover:bg-[#F1F5F9]` |
| No workloads | Empty state (see Copywriting) |

### Dashboard "Cost" tab trigger

| State | Visual |
|-------|--------|
| Inactive | `text-muted-foreground` |
| Active | `text-foreground` + bottom border via shadcn `TabsTrigger` default |
| Focus | `ring-2 ring-ring ring-offset-2` (shadcn default) |

---

## Copywriting Contract

| Element | Copy | Surface |
|---------|------|---------|
| Tab label | Cost | Dashboard scan detail tabs |
| CostLens tab label | CostLens | Viewer TabBar (already in code) |
| CostLens tab tooltip | Shared infrastructure cost allocation | Viewer TabBar (remove `soon: true`, keep tooltip) |
| WorkloadTable heading | Cost Allocation | Dashboard section heading above table |
| Column: workload | Workload | Dashboard table header |
| Column: allocated | Allocated / mo | Dashboard table header |
| Column: shared | Shared Resources | Dashboard table header |
| Column: details | Details | Dashboard table header (provides label context for icon-only chevron) |
| Details chevron tooltip | View cost breakdown | Shown on hover via shadcn Tooltip |
| Recommendations heading | Idle & Oversized Recommendations | Dashboard + Viewer section heading |
| Idle badge | idle | `WasteBadge` text — all lowercase |
| Oversized badge | oversized | `WasteBadge` text — all lowercase |
| Untagged workload label | untagged | Workload card/row name — italic, muted |
| Empty state heading | No cost data yet | Dashboard + Viewer |
| Empty state body | Run `infracanvas scan` to generate cost allocation. Make sure `costlens.workload_tag_key` is set in `infracanvas.yaml`. | Dashboard + Viewer |
| Error state | Failed to load cost data. Try refreshing the page. | Dashboard Cost tab (fetch fail) |
| Zero allocations | No shared resources detected for this workload. | WorkloadCard line-item area when `line_items` is empty |
| Waste estimate label | est. monthly waste | Idle recommendation row, beside dollar amount |

**Primary CTA for this phase:** none — this is a read-only data surface. No destructive actions.

**Destructive actions:** none. No confirmation dialogs required.

**Currency formatting:** All dollar amounts as `$N,NNN.NN` (USD, 2 decimal places, thousands separator). Use `Intl.NumberFormat('en-US', { style: 'currency', currency: 'USD' })` in TypeScript. Append `/mo` suffix inline: `$412.00/mo`.

---

## Accessibility Contract

| Requirement | Implementation |
|-------------|----------------|
| Tab keyboard navigation | Viewer TabBar already uses arrow-key nav (existing code in `TabBar.tsx`); dashboard uses shadcn `Tabs` which is Radix — keyboard nav is built in |
| Expandable table rows | `aria-expanded` on chevron button, `aria-controls` pointing to detail row `id` |
| Details chevron button | `aria-label="View cost breakdown for {workload name}"` — removes reliance on icon alone for screen readers |
| Cost amounts | Do NOT use color alone to convey cost magnitude — always pair color with text label (e.g., "idle" badge + dollar amount) |
| Screen reader workload card | `aria-label="Workload: payments-svc, $412.00 per month"` on each card root |
| Idle recommendation | Each row has `role="listitem"` within a `role="list"` wrapper |
| Focus visible | shadcn default `ring-2 ring-ring` already applied; viewer uses Tailwind `focus-visible:outline-2` |
| Empty state | `aria-live="polite"` on the container so screen readers announce when data arrives |

---

## Registry Safety

| Registry | Blocks Used | Safety Gate |
|----------|-------------|-------------|
| shadcn official | `tabs`, `table`, `card`, `skeleton`, `badge`, `button`, `tooltip` | not required |

No third-party registries declared. Gate not applicable.

---

## Checker Sign-Off

- [ ] Dimension 1 Copywriting: PASS
- [ ] Dimension 2 Visuals: PASS
- [ ] Dimension 3 Color: PASS
- [ ] Dimension 4 Typography: PASS
- [ ] Dimension 5 Spacing: PASS
- [ ] Dimension 6 Registry Safety: PASS

**Approval:** pending
