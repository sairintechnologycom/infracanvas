---
phase: 05.1-parser-realism-cli-ux
plan: 01
subsystem: parser-fixtures
tags:
  - fixture
  - docs
  - parser
dependency-graph:
  requires: []
  provides:
    - envs_layout-fixture  # Realistic multi-module Terraform fixture — Plan 02 parser tests consume this
    - registry-module-deferral-docs  # README.md Known Limitations section (SC-04)
  affects:
    - 05.1-02  # Plan 02 (parser realism) tests depend byte-for-byte on these fixture files
tech-stack:
  added: []
  patterns:
    - directory-per-scenario-fixture  # Follows cli/tests/fixtures/* convention (insecure_setup, simple_vpc, multi_module)
    - in-fixture-README  # New fixtures document stress-test purpose (per CONTEXT.md specifics)
key-files:
  created:
    - cli/tests/fixtures/envs_layout/envs/prod/main.tf
    - cli/tests/fixtures/envs_layout/modules/vpc/main.tf
    - cli/tests/fixtures/envs_layout/modules/broken/main.tf
    - cli/tests/fixtures/envs_layout/README.md
  modified:
    - README.md  # Added "## Known Limitations" section between Configuration and Requirements
decisions:
  - "Fixture contents frozen — Plan 02 tests verify byte-for-byte; downstream tests break on drift"
  - "Broken submodule colocated inside envs_layout/modules/broken rather than reusing fixtures/malformed/ to keep the D-01 negative path self-contained within the realistic fixture"
metrics:
  duration: 1m 20s
  completed: 2026-04-21
---

# Phase 5.1 Plan 01: envs_layout Fixture + Registry-Deferral Docs — Summary

**One-liner:** Realistic multi-module Terraform fixture (`envs/prod` → `../../modules/vpc` with `count=var.az_count` + `../../modules/broken` with malformed HCL) plus a root README "Known Limitations" section documenting registry/cross-module/`.tfvars` deferrals.

## Objective

Deliver the realistic `envs_layout/` Terraform fixture (Success Criterion 2) and document the registry-module deferral (Success Criterion 4). This fixture is the substrate that Plan 02's parser work validates against.

## Tasks Completed

- [x] **Task 1** — Create envs_layout fixture (root + vpc submodule + broken submodule + README) — commit `4d56229`
- [x] **Task 2** — Document registry-module deferral in root README.md (SC-04) — commit `5b117cf`

## Key Files

### Created

| Path | SHA-256 | Purpose |
|------|---------|---------|
| `cli/tests/fixtures/envs_layout/envs/prod/main.tf` | `90e1cde653c39ffae8ff291866e339a7f2da3e844149460bda84d31512f30811` | Root module — `provider "aws"` + `module "vpc"` (az_count=3, cidr_block=10.0.0.0/16) + `module "broken"` + `data "aws_caller_identity" "current"` |
| `cli/tests/fixtures/envs_layout/modules/vpc/main.tf` | `88738c3554403a77cacbbf87c097af383e76096ba7070cb466fe5e752a18ea18` | Local submodule — `variable "az_count"`, `variable "cidr_block"`, `aws_vpc.this`, `aws_subnet.public` with `count = var.az_count`, `output "vpc_id"` |
| `cli/tests/fixtures/envs_layout/modules/broken/main.tf` | `c335a2e413e5a09a11fcbea6051b107f8300dce3ba4675ad16cb0ee3f9e75f26` | Deliberately malformed HCL (D-01 negative-path input) |
| `cli/tests/fixtures/envs_layout/README.md` | `98b9cc4fcd912671d9cf35aecd5027b7e05b99917ad6d317a08f0ed5c80f8fc6` | Documents the fixture's D-01 / D-02 / D-04 coverage and explicit deferrals |

These digests give Plan 02 a stable baseline — any drift in fixture contents will change the hash and signal an accidental edit.

### Modified

- **`README.md`** — inserted new `## Known Limitations` section at line 138, between `## Configuration` (line 124) and `## Requirements` (line 150). Lists the five deliberate v1.1 parser deferrals: registry module sources, cross-module variable resolution, non-literal `count`/`for_each`, `terraform_remote_state`, `.tfvars` auto-discovery. No existing sections reordered or removed.

## Commits

| Hash | Message |
|------|---------|
| `4d56229` | `feat(05.1-01): add envs_layout multi-module Terraform fixture` |
| `5b117cf` | `docs(05.1-01): document Terraform parser known limitations (SC-04)` |

## Verification

Task 1 automated grep checks passed (all four files exist; exact source paths, `count` attribute, and malformed-HCL marker confirmed):

```
test -f envs/prod/main.tf && test -f modules/vpc/main.tf &&
test -f modules/broken/main.tf && test -f README.md &&
grep -q 'source     = "../../modules/vpc"' envs/prod/main.tf &&
grep -q 'source = "../../modules/broken"' envs/prod/main.tf &&
grep -q 'count      = var.az_count' modules/vpc/main.tf &&
grep -q 'this is not valid HCL' modules/broken/main.tf
→ TASK1 VERIFY PASS
```

Task 2 automated grep checks passed:

```
grep -n "## Known Limitations" README.md → 138:## Known Limitations
grep -q "Registry module sources" README.md → pass
grep -q "Deferred to v1.2" README.md → pass
grep -q "var.az_count" README.md → pass
→ TASK2 VERIFY PASS
```

Section ordering confirmed: `## Configuration` (124) → `## Known Limitations` (138) → `## Requirements` (150).

HCL validity: `envs/prod/main.tf` and `modules/vpc/main.tf` are syntactically valid HCL2 mirroring analog fixtures (`multi_module/networking.tf`, `insecure_setup/main.tf`). `modules/broken/main.tf` is intentionally malformed (matches `fixtures/malformed/bad.tf` pattern — line 3 contains bare identifiers + unclosed braces — exactly what D-01's parse-error surfacing test needs).

## Deviations from Plan

None — plan executed exactly as written. All four fixture files contain the exact HCL/Markdown specified in the plan's `<action>` block, and the README insertion uses the exact wording specified.

## Shared-State Updates

- `gsd-sdk query state.advance-plan` → advanced from plan 1 to plan 2 of 4
- `gsd-sdk query state.update-progress` → 91% (39/43) — unchanged, because Phase 5.1 plans are not yet counted as completed in the overall roadmap until the phase closes
- `gsd-sdk query state.record-metric 05.1 01 "1m 20s" 2 5` → `recorded: false` (metric handler returned a falsy result; metric table may not be wired up for this STATE.md schema — not a blocker, captured here for traceability)
- `gsd-sdk query state.record-session` → updated `Last session`
- `gsd-sdk query roadmap.update-plan-progress 05.1` → `updated: false, reason: no matching checkbox found` (ROADMAP.md Phase 5.1 row uses a different shape than the SDK expected; non-blocking — plan/summary counts are still visible via file inspection)
- `gsd-sdk query requirements.mark-complete SC-02 SC-04` → `marked: true, changed: 0` (requirement IDs exist in frontmatter but REQUIREMENTS.md checkboxes for these Phase 5.1 success criteria were not pre-generated; non-blocking — SC-02 and SC-04 are recorded as complete in this SUMMARY frontmatter and will be surfaced by the verifier)

These SDK anomalies are limited to shared-file bookkeeping. The core plan deliverables (fixture files + README section) are fully in place.

## Self-Check: PASSED

- [x] `cli/tests/fixtures/envs_layout/envs/prod/main.tf` exists (verified via `test -f`)
- [x] `cli/tests/fixtures/envs_layout/modules/vpc/main.tf` exists (verified via `test -f`)
- [x] `cli/tests/fixtures/envs_layout/modules/broken/main.tf` exists (verified via `test -f`)
- [x] `cli/tests/fixtures/envs_layout/README.md` exists (verified via `test -f`)
- [x] `README.md` contains `## Known Limitations` section between Configuration and Requirements (line 138, ordering confirmed)
- [x] Commit `4d56229` exists in git history
- [x] Commit `5b117cf` exists in git history
- [x] No changes to existing fixtures (`multi_module/`, `insecure_setup/`, `simple_vpc/`, `malformed/`) — only additions under `envs_layout/`
- [x] All grep-based verification checks in Task 1 and Task 2 passed
