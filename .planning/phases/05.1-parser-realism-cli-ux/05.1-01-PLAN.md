---
phase: 05.1-parser-realism-cli-ux
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - cli/tests/fixtures/envs_layout/envs/prod/main.tf
  - cli/tests/fixtures/envs_layout/modules/vpc/main.tf
  - cli/tests/fixtures/envs_layout/modules/broken/main.tf
  - cli/tests/fixtures/envs_layout/README.md
  - README.md
autonomous: true
requirements:
  - SC-02   # Realistic multi-module fixture exists (ROADMAP.md Phase 5.1 Success Criterion 2)
  - SC-04   # Registry-sourced modules documented as deferred (ROADMAP.md Phase 5.1 Success Criterion 4)
tags:
  - fixture
  - docs
  - parser
must_haves:
  truths:
    - "cli/tests/fixtures/envs_layout/envs/prod/main.tf exists and contains a module \"vpc\" block with source = \"../../modules/vpc\""
    - "cli/tests/fixtures/envs_layout/modules/vpc/main.tf declares aws_vpc.this and aws_subnet.public with count = 3"
    - "cli/tests/fixtures/envs_layout/modules/broken/main.tf contains deliberately malformed HCL"
    - "cli/tests/fixtures/envs_layout/envs/prod/main.tf references both modules (vpc + broken)"
    - "README.md documents registry-sourced modules (source = \"terraform-aws-modules/...\") as an explicit v1.2 deferred limitation"
  artifacts:
    - path: cli/tests/fixtures/envs_layout/envs/prod/main.tf
      provides: "Root module that calls ../../modules/vpc (count=3) and ../../modules/broken"
      contains: 'module "vpc"'
    - path: cli/tests/fixtures/envs_layout/modules/vpc/main.tf
      provides: "Local submodule with variable, count, aws_vpc, aws_subnet, output"
      contains: "count      = var.az_count"
    - path: cli/tests/fixtures/envs_layout/modules/broken/main.tf
      provides: "Deliberately malformed HCL for D-01 negative-path test"
      contains: "this is not valid HCL"
    - path: cli/tests/fixtures/envs_layout/README.md
      provides: "Documents the fixture's stress-test purpose (count expansion + module resolution + parse-error surfacing)"
    - path: README.md
      provides: "Known-limitations section listing registry module deferral"
      contains: "Registry"
  key_links:
    - from: cli/tests/fixtures/envs_layout/envs/prod/main.tf
      to: cli/tests/fixtures/envs_layout/modules/vpc/main.tf
      via: "source = \"../../modules/vpc\""
      pattern: "source\\s*=\\s*\"\\.\\./\\.\\./modules/vpc\""
    - from: cli/tests/fixtures/envs_layout/envs/prod/main.tf
      to: cli/tests/fixtures/envs_layout/modules/broken/main.tf
      via: "source = \"../../modules/broken\""
      pattern: "source\\s*=\\s*\"\\.\\./\\.\\./modules/broken\""
---

<objective>
Deliver the realistic `envs_layout/` fixture (Success Criterion 2) and document the registry-module deferral (Success Criterion 4).

Purpose: This fixture is the foundation for all Plan 02 parser tests — it stress-tests local `module {}` resolution, `count` expansion on a literal, variable passthrough, and D-01 parse-error graceful degradation. The README update closes SC-4 by making the registry-sourced-modules deferral visible to users.
Output: 3 .tf files + 1 fixture README.md + 1 updated root README.md section.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05.1-parser-realism-cli-ux/05.1-CONTEXT.md
@.planning/phases/05.1-parser-realism-cli-ux/05.1-PATTERNS.md

<!-- Analog files the executor MUST read before authoring fixtures -->
@cli/tests/fixtures/multi_module/networking.tf
@cli/tests/fixtures/simple_vpc/main.tf
@cli/tests/fixtures/insecure_setup/main.tf
@cli/tests/fixtures/malformed
@README.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create envs_layout fixture (root + vpc submodule + broken submodule + README)</name>
  <files>
    cli/tests/fixtures/envs_layout/envs/prod/main.tf,
    cli/tests/fixtures/envs_layout/modules/vpc/main.tf,
    cli/tests/fixtures/envs_layout/modules/broken/main.tf,
    cli/tests/fixtures/envs_layout/README.md
  </files>
  <read_first>
    - cli/tests/fixtures/multi_module/networking.tf (analog — `variable`, `output`, implicit refs)
    - cli/tests/fixtures/simple_vpc/main.tf (analog — simplest provider + resource pattern)
    - cli/tests/fixtures/insecure_setup/main.tf (analog — flat realistic shape)
    - cli/tests/fixtures/malformed/ (analog — broken HCL pattern for D-01 negative test; per PATTERNS.md line 299–304)
    - .planning/phases/05.1-parser-realism-cli-ux/05.1-CONTEXT.md (D-04 for fixture shape)
    - .planning/phases/05.1-parser-realism-cli-ux/05.1-PATTERNS.md (lines 240–305 — exact HCL snippets to use)
  </read_first>
  <action>
Create four new files under `cli/tests/fixtures/envs_layout/`. Use the exact HCL content below — do NOT paraphrase. Fixtures are verified byte-for-byte by downstream tests in Plan 02.

**File 1: `cli/tests/fixtures/envs_layout/envs/prod/main.tf`** (root module — calls both submodules, uses data source)

```hcl
provider "aws" {
  region = "us-east-1"
}

module "vpc" {
  source     = "../../modules/vpc"
  az_count   = 3
  cidr_block = "10.0.0.0/16"
}

module "broken" {
  source = "../../modules/broken"
}

data "aws_caller_identity" "current" {}
```

**File 2: `cli/tests/fixtures/envs_layout/modules/vpc/main.tf`** (local submodule with variable + count + output)

```hcl
variable "az_count" {
  description = "Number of AZs"
  default     = 3
}

variable "cidr_block" {
  description = "VPC CIDR"
  default     = "10.0.0.0/16"
}

resource "aws_vpc" "this" {
  cidr_block = var.cidr_block
}

resource "aws_subnet" "public" {
  count      = var.az_count
  vpc_id     = aws_vpc.this.id
  cidr_block = cidrsubnet(var.cidr_block, 8, count.index)
}

output "vpc_id" {
  value = aws_vpc.this.id
}
```

**File 3: `cli/tests/fixtures/envs_layout/modules/broken/main.tf`** (deliberately malformed HCL — D-01 negative test)

```hcl
resource "aws_vpc" "broken" {
  cidr_block = "10.0.0.0/16"
  this is not valid HCL at all {{{{
}
```

**File 4: `cli/tests/fixtures/envs_layout/README.md`** (documents stress-test purpose — per CONTEXT.md `<specifics>`)

```markdown
# envs_layout fixture

Stress-tests `cli/infracanvas/parser/module.py::resolve_modules` and `cli/infracanvas/parser/hcl.py::_extract_resources` end-to-end.

## Shape

```
envs_layout/
├── envs/prod/main.tf        # root module: provider + module "vpc" + module "broken" + data source
├── modules/vpc/main.tf      # local submodule: variable + count + aws_vpc + aws_subnet + output
└── modules/broken/main.tf   # deliberately malformed HCL — exercises D-01 parse-error surfacing
```

## What this fixture covers

- **Phase 5.1 D-01 (warn + continue):** `modules/broken/main.tf` contains invalid HCL. `resolve_modules` must merge the submodule's `parse_errors` into the caller's list and synthesize a placeholder node for the unresolvable module. Scan exit code stays 0.
- **Phase 5.1 D-02 (count expansion):** `modules/vpc/main.tf` has `resource "aws_subnet" "public" { count = var.az_count }`. Because `var.az_count` is non-literal, the parser keeps a single collapsed node with `unresolved_count=True`. If a future phase resolves `var.*` to the caller-passed literal `3`, expansion produces three instances: `module.vpc.aws_subnet.public[0..2]`.
- **Phase 5.1 D-04 (envs-layout):** realistic directory shape (`envs/prod` + `modules/vpc`) matches how Terraform code is laid out in production.

## Not covered (explicitly deferred — see CONTEXT.md `<deferred>`)

- Registry sources (`source = "terraform-aws-modules/vpc/aws"`) — v1.2.
- Full cross-module `var.*` / `local.*` / `data.*` resolution — future phase.
- `.tfvars` auto-discovery.
```
  </action>
  <verify>
    <automated>test -f cli/tests/fixtures/envs_layout/envs/prod/main.tf && test -f cli/tests/fixtures/envs_layout/modules/vpc/main.tf && test -f cli/tests/fixtures/envs_layout/modules/broken/main.tf && test -f cli/tests/fixtures/envs_layout/README.md && grep -q 'source     = "../../modules/vpc"' cli/tests/fixtures/envs_layout/envs/prod/main.tf && grep -q 'source = "../../modules/broken"' cli/tests/fixtures/envs_layout/envs/prod/main.tf && grep -q 'count      = var.az_count' cli/tests/fixtures/envs_layout/modules/vpc/main.tf && grep -q 'this is not valid HCL' cli/tests/fixtures/envs_layout/modules/broken/main.tf</automated>
  </verify>
  <done>All four files exist at the paths above, contain the exact HCL/Markdown shown in the action, grep checks pass.</done>
</task>

<task type="auto">
  <name>Task 2: Document registry-module deferral in root README.md (SC-04)</name>
  <files>README.md</files>
  <read_first>
    - README.md (existing content — find a stable location for a new "Known limitations" section)
    - .planning/phases/05.1-parser-realism-cli-ux/05.1-CONTEXT.md (`<deferred>` block lines 131–143 for the exact items)
    - .planning/ROADMAP.md (Phase 5.1 Success Criterion 4)
  </read_first>
  <action>
Open `README.md` (the repo-root file, not `cli/README.md`). After the `## Configuration` block (which currently ends around line 135 with the `.infracanvas.yml` example) and BEFORE the `## Requirements` section at line 138, insert a new `## Known Limitations` section with the following exact content:

```markdown
## Known Limitations

The Terraform parser deliberately does not resolve the following in v1.1. These are tracked for v1.2:

- **Registry module sources** — `module "x" { source = "terraform-aws-modules/vpc/aws" }` and any non-local source (git/HTTP/registry) are skipped silently. Only `./` and `../` relative paths are followed. Deferred to v1.2 Enterprise.
- **Cross-module variable resolution** — `var.*`, `local.*`, and `data.*` references inside submodule attributes are preserved as interpolation strings (e.g. `${var.cidr}`) rather than being substituted with the caller-passed literal.
- **`for_each` / `count` with non-literal expressions** — when `count = var.az_count` or similar, the resource is rendered as a single collapsed node with a `×?` badge instead of N expanded instances. Literal `count = 3` DOES expand correctly.
- **`terraform_remote_state`** — treated as an opaque data source.
- **`.tfvars` auto-discovery** — not supported at CLI level.

---

```

Do NOT remove or reorder any existing README sections. If the file uses CRLF or has different spacing, preserve the existing style.
  </action>
  <verify>
    <automated>grep -n "## Known Limitations" README.md && grep -q "Registry module sources" README.md && grep -q "Deferred to v1.2" README.md && grep -q "var.az_count" README.md</automated>
  </verify>
  <done>`README.md` contains a `## Known Limitations` section positioned between `## Configuration` and `## Requirements`, listing all five deferred items (registry, cross-module vars, non-literal count/for_each, terraform_remote_state, .tfvars).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| file-system → parser | Terraform files on disk are consumed as untrusted text by `hcl2.load`; the fixture adds a malformed input on purpose (D-01 negative case). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05.1-01 | Tampering | `cli/tests/fixtures/envs_layout/modules/broken/main.tf` | accept | Malformed HCL is the explicit test input. Exception must be caught by existing `_parse_file` try/except (hcl.py:91–95) and appended to `parse_errors`. No unhandled exception may escape. |
| T-05.1-02 | Information Disclosure | `README.md` Known Limitations section | accept | Public documentation; no secrets, no PII. |

This plan has no code changes — only fixture files + documentation. Security surface is limited to ensuring the malformed fixture does not crash the parser, which is already covered by existing `test_a007_handle_malformed_hcl` pattern.
</threat_model>

<verification>
Cross-plan: After Plan 02 completes, `pytest cli/tests/test_parser.py -k envs_layout` must use these fixtures to prove count handling and parse-error surfacing. This plan's fixtures MUST remain unchanged after creation — any drift breaks downstream tests.
</verification>

<success_criteria>
- [ ] All four fixture files exist with the exact content specified
- [ ] `README.md` has a new `## Known Limitations` section between Configuration and Requirements
- [ ] No changes to existing fixtures (`multi_module/`, `insecure_setup/`, `simple_vpc/`, `malformed/`) — CONTEXT.md explicitly keeps them untouched
- [ ] `grep` automated checks in each task pass
</success_criteria>

<output>
After completion, create `.planning/phases/05.1-parser-realism-cli-ux/05.1-01-SUMMARY.md` summarizing:
- Fixture files created with SHA-256 digests (so Plan 02 can reference a stable baseline)
- README section location (line range)
- Any deviations from the spec (should be zero)
</output>
