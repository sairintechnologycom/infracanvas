---
phase: 07.5-github-repo-connector
plan: 11
type: execute
wave: 6
depends_on: [01, 02, 03, 04, 05, 06, 07, 08, 09, 10]
files_modified:
  - .planning/phases/07.5-github-repo-connector/07.5-PHASE-VERIFICATION.md
  - .planning/phases/07.5-github-repo-connector/07.5-MANUAL-SMOKE.md
autonomous: false
requirements: [GH-01, GH-02, GH-03, GH-04, GH-05]
must_haves:
  truths:
    - "Full backend pytest suite passes: pytest backend/tests/ exits 0"
    - "Full dashboard test suite passes: cd dashboard && npm test -- --run exits 0"
    - "TypeScript typecheck clean: cd dashboard && npx tsc --noEmit exits 0"
    - "ruff check + mypy --strict on backend/app/ exits 0"
    - "alembic upgrade head applies cleanly on a fresh local Postgres"
    - "Manual smoke checklist completed against a real fixture repo in a sandbox GitHub org"
    - "Stripe meter event observed for a github-sourced scan in the Stripe test dashboard"
    - "Token never appears in any structlog emission across the full happy-path scan"
    - "All five requirements (GH-01..GH-05) traced through their implementing plans + tests"
  artifacts:
    - path: ".planning/phases/07.5-github-repo-connector/07.5-PHASE-VERIFICATION.md"
      provides: "Sign-off document covering automated + manual gates per VALIDATION.md"
    - path: ".planning/phases/07.5-github-repo-connector/07.5-MANUAL-SMOKE.md"
      provides: "Step-by-step manual smoke checklist (GitHub App register, install, scan, observe meter)"
  key_links:
    - from: "PHASE-VERIFICATION.md"
      to: "07.5-VALIDATION.md"
      via: "Per-task verification map → automated commands run + recorded"
    - from: "MANUAL-SMOKE.md"
      to: "07.5-RESEARCH.md §Manual-Only Verifications"
      via: "Real-GitHub flow checklist tied to ROADMAP success criteria"
---

<rationale>
Final verification gate. The whole phase only ships when both automated and manual gates pass. Includes the human checkpoint for the real-GitHub smoke test (GH-01 + GH-03 + GH-05 cannot be fully verified without a real GitHub App registration and a real fixture repo). Wave 6 because depends on every other plan.

Two artifacts:
1. PHASE-VERIFICATION.md — automated checklist run by Claude.
2. MANUAL-SMOKE.md — step-by-step user checklist for the human-required flows (GitHub App registration, real install, real fixture-repo scan, Stripe meter observation in test dashboard, Fly secrets PEM newline preservation).

The plan ends with a `checkpoint:human-verify` task pausing for the user to confirm the manual smoke passed before phase close.
</rationale>

<objective>
Run the full automated test suite, lint+typecheck, alembic upgrade, then walk through the manual GitHub smoke test. Record results in two artifact files. Phase closes only after the user signs off on the manual smoke.

Output: PHASE-VERIFICATION.md filled in with command outputs + green/red status per requirement; MANUAL-SMOKE.md as a freshly-rendered checklist; user approval at the human-verify checkpoint.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/07.5-github-repo-connector/07.5-VALIDATION.md
@.planning/phases/07.5-github-repo-connector/07.5-RESEARCH.md
@.planning/phases/07.5-github-repo-connector/07.5-CONTEXT.md
@.planning/phases/07.5-github-repo-connector/07.5-01-SUMMARY.md
@.planning/phases/07.5-github-repo-connector/07.5-02-SUMMARY.md
@.planning/phases/07.5-github-repo-connector/07.5-03-SUMMARY.md
@.planning/phases/07.5-github-repo-connector/07.5-04-SUMMARY.md
@.planning/phases/07.5-github-repo-connector/07.5-05-SUMMARY.md
@.planning/phases/07.5-github-repo-connector/07.5-06-SUMMARY.md
@.planning/phases/07.5-github-repo-connector/07.5-07-SUMMARY.md
@.planning/phases/07.5-github-repo-connector/07.5-08-SUMMARY.md
@.planning/phases/07.5-github-repo-connector/07.5-09-SUMMARY.md
@.planning/phases/07.5-github-repo-connector/07.5-10-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Automated phase verification — run full suites and record</name>
  <files>.planning/phases/07.5-github-repo-connector/07.5-PHASE-VERIFICATION.md</files>
  <read_first>
    - .planning/phases/07.5-github-repo-connector/07.5-VALIDATION.md (per-task verification map; sampling rate; sign-off checklist)
    - All ten 07.5-NN-SUMMARY.md files
    - .planning/phases/07.5-github-repo-connector/07.5-RESEARCH.md (§Validation Architecture)
  </read_first>
  <behavior>
    - All listed automated commands execute and capture pass/fail + count
    - Output recorded in PHASE-VERIFICATION.md as a table mapping each VALIDATION.md row to result
    - Any red row blocks phase closure (escalate to user with diff)
  </behavior>
  <action>
1. Run the following and record exit codes + summary lines:
   - `cd backend && alembic upgrade head` (must exit 0)
   - `cd backend && pytest tests/ -x` (full backend suite; record pass count, fail list)
   - `cd backend && ruff check app/` (record any errors)
   - `cd backend && mypy app/` (record any errors)
   - `cd dashboard && npx tsc --noEmit` (record any TS errors)
   - `cd dashboard && npm test -- --run` (full dashboard suite; record pass count, fail list)
   - `cd dashboard && npm run build` (verify production build succeeds — Phase 7.1 deferred-items lesson)
2. Write .planning/phases/07.5-github-repo-connector/07.5-PHASE-VERIFICATION.md with:
   - Header: phase name, date, environment.
   - Section 1: per-VALIDATION.md row, result column (✅ green / ❌ red / ⚠️ flaky) + the exact command output line.
   - Section 2: Requirements traceability matrix (GH-01..GH-05 → implementing plans → tests run).
   - Section 3: Cross-cutting truth checks (T1..T9 from phase guidance — manually grep that each invariant exists in code):
     - T1: `grep -rE "Depends\(require_role\|Depends\(resolve_team_from_clerk_org" backend/app/routes/github.py backend/app/routes/scans_from_github.py | wc -l` ≥ 6
     - T2: `grep -rc "set_config\('app.current_team_id'" backend/app/routes/github.py backend/app/routes/scans_from_github.py backend/app/queue/tasks/scan_repo.py` ≥ 5
     - T3: no PEM bytes in any file under backend/app: `grep -rE "BEGIN (RSA )?PRIVATE KEY" backend/app/ | wc -l` == 0
     - T4: token never bound — gating criterion is the behavioral test:
       `pytest backend/tests/jobs/test_scan_repo.py::test_token_redacted_in_stderr_log -x  # exits 0`
       Complementary smoke (defense-in-depth, non-gating): `grep -rE "_log\.bind\([^)]*token=|_log\.info\([^)]*token=" backend/app/queue/tasks/scan_repo.py | wc -l` == 0
     - T5: traversal guard present: `grep -c "relative_to" backend/app/queue/tasks/scan_repo.py` ≥ 1
     - T6: team_id is a kwarg, no SECURITY DEFINER lookup: `grep -c "scan_team_id\|installation_team_id" backend/app/queue/tasks/scan_repo.py` == 0
     - T7: meter via finalize_scan: `grep -c "finalize_scan\|fire_scan_meter" backend/app/queue/tasks/scan_repo.py` ≥ 1
     - T8: state vs clerk_org_id: `grep -c "state != .*clerk_org_id\|state.*team\.clerk_org_id" backend/app/routes/github.py` ≥ 1
     - T9: no react-query in dashboard: `grep -c "@tanstack/react-query" dashboard/package.json` == 0
   - Section 4: Sign-off — leave UNCHECKED until manual smoke (Task 2) is also green.
  </action>
  <verify>
    <automated>cd backend && pytest tests/ -x 2>&1 | tail -5 && cd ../dashboard && npm test -- --run 2>&1 | tail -5 && npx tsc --noEmit 2>&1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - File .planning/phases/07.5-github-repo-connector/07.5-PHASE-VERIFICATION.md exists
    - All four sections present (per-row results, traceability matrix, T1-T9 checks, sign-off)
    - `cd backend && pytest tests/ -x` exits 0
    - `cd dashboard && npm test -- --run` exits 0
    - `cd dashboard && npx tsc --noEmit` exits 0
    - `cd backend && alembic upgrade head` exits 0
    - All T1-T9 cross-cutting checks have the expected grep results
    - File contains a row for every requirement (GH-01..GH-05) with ✅ status
  </acceptance_criteria>
  <done>All automated gates green; PHASE-VERIFICATION.md written with command outputs + traceability + cross-cutting checks.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Manual GitHub smoke test — real install + real scan + Stripe meter check</name>
  <what-built>
    Full Phase 7.5 integration: GitHub App registered, install flow live, scan flow live, polling UI, Retry on failure, meter fires.
  </what-built>
  <how-to-verify>
    Open .planning/phases/07.5-github-repo-connector/07.5-MANUAL-SMOKE.md (created by this checkpoint task) and follow the steps:
    
    **Pre-flight (one-time setup):**
    1. Register the InfraCanvas DEV GitHub App at https://github.com/settings/apps/new with:
       - Name: "InfraCanvas Dev"
       - Homepage URL: dashboard.infracanvas.dev (or your dev URL)
       - Callback URL: https://api.infracanvas.dev/v1/github/install-callback (your backend URL)
       - Setup URL: leave blank (per RESEARCH §GitHub Apps Auth Flow — we use Callback URL only)
       - Webhook URL: https://api.infracanvas.dev/v1/github/webhooks (Phase 8; configure now, no handler yet)
       - Webhook Secret: openssl rand -hex 32 — copy to clipboard
       - Repository permissions: Contents=Read, Metadata=Read (nothing else!)
       - Account permissions: none
       - Webhook events: subscribe to Push (for Phase 8; not handled now)
       - Where can this GitHub App be installed: Any account
    2. Generate a private key (Settings → Generate a private key) — download the .pem file
    3. Set Fly secrets on infracanvas-api-dev:
       - flyctl secrets set GITHUB_APP_ID="<numeric app id from app settings>" --app infracanvas-api-dev
       - flyctl secrets set GITHUB_APP_PRIVATE_KEY="$(cat infracanvas-dev.private-key.pem)" --app infracanvas-api-dev
       - flyctl secrets set GITHUB_APP_WEBHOOK_SECRET="<hex from step 1>" --app infracanvas-api-dev
       - flyctl secrets set GITHUB_APP_SLUG="<your-app-slug>" --app infracanvas-api-dev
    4. Set NEXT_PUBLIC_GITHUB_APP_SLUG on Vercel (dashboard env) to match the GitHub App slug
    
    **Smoke test:**
    1. Sign in to dashboard at /sign-in.
    2. Navigate to /settings/integrations.
    3. EXPECTED: "Install InfraCanvas GitHub App" button visible (no installations yet).
    4. Click button — opens new tab to https://github.com/apps/{slug}/installations/new?state={clerk_org_id}.
    5. Pick "Only select repositories" → choose a sandbox fixture repo (e.g., a small Terraform repo you control, like infracanvas-fixtures/terraform-sample).
    6. Click Install.
    7. EXPECTED: Browser redirects to /settings/integrations?install=success. After 0-3s, the installation row appears (post-install hydration poll picked it up).
    8. EXPECTED: ScanTriggerForm renders below the installation row.
    9. Pick the fixture repo from RepoCombobox (type to filter — typeahead must debounce, not fire per-keystroke).
    10. Pick a branch (must default to repo's default_branch, e.g., main).
    11. Leave path as "." OR set to a subdirectory you know exists in the repo.
    12. Click Scan.
    13. EXPECTED: Browser navigates to /scans/{scan_id} with skeleton + "Scanning {repo}@{branch}..." copy.
    14. Wait 10-60s. EXPECTED: page automatically transitions to the viewer (status='ready', polling stopped).
    15. Open the Stripe test dashboard → Customers → your team's customer → Usage → confirm one new metered usage event with timestamp matching the scan.
    16. Open Cloudflare R2 dashboard → infracanvas-scans bucket → confirm teams/{team_id}/scans/{scan_id}.json exists.
    
    **Negative path (failure handling):**
    17. Trigger a scan with path="nonexistent" (a directory that doesn't exist in the repo).
    18. EXPECTED: page shows status='failed' + error_message about missing subpath + Retry button.
    19. Click Retry — dashboard navigates to a NEW /scans/{new_scan_id}; this one also fails the same way (subpath still wrong) — but the second scan's row exists with its own error_message.
    
    **Token leakage check:**
    20. fly logs -a infracanvas-api-dev | grep -E "ghs_|x-access-token" — must return zero matches across the entire smoke test window.
    21. fly logs --app infracanvas-api-dev | grep "scan_repo.failed" | tail -5 — error messages must contain "***" not the raw token.
  </how-to-verify>
  <resume-signal>
    Type "approved" if all 21 checkpoints pass (the install URL flow, the scan-and-poll flow, the Stripe meter observation, the R2 object existence, the failure+Retry flow, and the token-leakage grep).
    
    Describe any failure with: which step number, what was expected, what actually happened. Common issues:
    - State mismatch 403 → check NEXT_PUBLIC_GITHUB_APP_SLUG on Vercel matches the GitHub App slug; check clerk_org_id resolution path.
    - 401 on install-callback → user's Clerk session expired during install; re-sign-in.
    - PEM decode error → verify flyctl secrets set used "$(cat key.pem)" (preserves newlines).
    - clone failed (Repository not found) → installation lacks access to the fixture repo; re-grant in GitHub App settings.
    - Stripe meter missing → check stripe_customer_id is set on the team row; verify the meter event ID format matches Phase 6 D-09 expected shape.
  </resume-signal>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Phase close — mark requirements complete in PHASE-VERIFICATION.md, update STATE.md</name>
  <files>.planning/phases/07.5-github-repo-connector/07.5-PHASE-VERIFICATION.md, .planning/STATE.md</files>
  <read_first>
    - .planning/STATE.md (current Position + Pending Todos)
    - .planning/ROADMAP.md (Phase 7.5 row)
  </read_first>
  <behavior>
    - PHASE-VERIFICATION.md sign-off section is filled in with date + green status across all 5 requirements
    - STATE.md updated to reflect Phase 7.5 complete
    - ROADMAP.md plan checkboxes flipped to checked
  </behavior>
  <action>
1. After human approval at Task 2, edit .planning/phases/07.5-github-repo-connector/07.5-PHASE-VERIFICATION.md:
   - Section 4 Sign-off: check all boxes, add date, add summary sentence "All 5 requirements (GH-01..GH-05) verified. Manual smoke green on YYYY-MM-DD."
2. Edit .planning/STATE.md:
   - Update last_updated and last_activity to today's date.
   - Update progress: completed_phases += 1; completed_plans += 11; recompute percent.
   - Update Current Position section to next phase (Phase 8 — GitHub Webhook + Auto-scan).
   - Append a new Decisions row(s) for any planning-time decisions worth carrying:
     - "07.5: install-callback uses GET (not POST per CONTEXT D-10d typo); state==clerk_org_id (not team_id) per dashboard's session-knowable identity. Backend resolve_team_from_clerk_org closes the loop."
     - "07.5: Mint-per-scan installation token; never persisted. PEM key in Fly secrets per env."
     - "07.5: scan_repo job extracts _finalize_scan helper so CLI commit + worker scan share the meter+row-flip code path (CC-7 invariant)."
3. Edit .planning/ROADMAP.md — Phase 7.5 section:
   - Update Plans count to 11
   - Mark each `- [ ] 07.5-NN-PLAN.md` checkbox as `- [x]`
4. Run a final `git status` and report files changed; do NOT auto-commit (user runs /gsd-execute-phase post-actions).
  </action>
  <verify>
    <automated>grep -c "All 5 requirements" .planning/phases/07.5-github-repo-connector/07.5-PHASE-VERIFICATION.md && grep -c "Phase 7.5" .planning/STATE.md && grep -cE "\\[x\\] 07\\.5-[0-9]+-PLAN\\.md" .planning/ROADMAP.md</automated>
  </verify>
  <acceptance_criteria>
    - PHASE-VERIFICATION.md sign-off filled in
    - STATE.md last_activity updated
    - STATE.md progress numbers updated
    - ROADMAP.md Phase 7.5 plan checkboxes all checked
    - At least 11 `[x]` entries under Phase 7.5 plan list
  </acceptance_criteria>
  <done>Phase 7.5 closed; STATE.md and ROADMAP.md reflect completion.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Smoke-test environment to production | Smoke runs on dev env; production deploy is gated by Phase 7.5 success criteria |
| Human checkpoint to phase close | Mandatory user sign-off prevents silent close on red gates |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07.5-11-01 | Repudiation | Phase closed without manual smoke verification | mitigate | checkpoint:human-verify (gate=blocking) prevents Task 3 from running until user types "approved". The checkpoint resume signal explicitly enumerates the 21 expected outcomes. |
| T-07.5-11-02 | Information Disclosure | Smoke checklist embeds tokens in writeup | mitigate | MANUAL-SMOKE.md includes a "fly logs ... grep ghs_" step that ASSERTS no token leakage; the checklist text itself never contains real tokens. |
</threat_model>

<verification>
- All automated gates green (Task 1)
- All manual smoke checkpoints green (Task 2 — human approval)
- STATE.md + ROADMAP.md reflect closure (Task 3)
</verification>

<success_criteria>
- PHASE-VERIFICATION.md sign-off complete with all 5 requirements ✅
- MANUAL-SMOKE.md executed end-to-end with user approval
- STATE.md updated; Phase 7.5 marked complete in ROADMAP.md
- Token leakage grep returns zero matches across the smoke window
- Stripe meter event observed
</success_criteria>

<output>
After completion, append a final SUMMARY at .planning/phases/07.5-github-repo-connector/07.5-11-SUMMARY.md noting: total tests run, total tests passed, manual smoke timestamp, link to first GitHub App install in dev env.
</output>
