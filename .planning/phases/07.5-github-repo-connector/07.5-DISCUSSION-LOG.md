# Phase 7.5: GitHub Repo Connector — Discussion Log

**Date:** 2026-05-03
**Mode:** discuss (default, single-question-per-turn)
**Audience:** human reference for audits / retrospectives. Downstream agents do NOT read this file — they read CONTEXT.md.

## Areas user selected to discuss

- GitHub auth model
- Clone-and-scan execution
- Scan-triggering UX surface
- Token storage + clone auth

## Q&A

### Q1 — GitHub auth model
**Options:** GitHub App (recommended) / OAuth App / Both
**Selected:** GitHub App
**→ CONTEXT D-01**

### Q2 — Scan execution
**Options:** Taskiq worker (recommended) / FastAPI inline subprocess / Fly Machines API ephemeral VMs
**Selected:** Taskiq worker
**→ CONTEXT D-02**

### Q3 — Scan progress UX
**Options:** Optimistic redirect + poll / Server-sent events / Toast + stay on settings
**Selected:** Optimistic redirect + poll
**→ CONTEXT D-03**

### Q4 — Scan trigger UX surface
**Options:** /settings/integrations all-in-one (recommended) / connect+/scans/new split / connect+modal-from-/scans
**Selected:** /settings/integrations all-in-one
**→ CONTEXT D-04**

### Q5 — Repo+branch picker UX
**Options:** Searchable combobox (recommended) / two-step paginated table / free-text URL input
**Selected:** Searchable combobox
**→ CONTEXT D-05, D-10b, D-10c**

### Q6 — Token strategy
**Options:** Mint-per-scan, never persist (recommended) / cache in Redis / encrypted in Postgres
**Selected:** Mint-per-scan, never persist
**→ CONTEXT D-06**

### Q7 — Token handoff to subprocess
**Options:** env var + URL embedding (recommended) / GIT_ASKPASS helper / ~/.netrc
**Selected:** env var + URL embedding
**→ CONTEXT D-07**

### Q8 — Concurrency / dedup
**Options:** No dedup, accept user intent (recommended) / strict dedup by (repo, branch, sha) / per-team rate limit
**Selected:** No dedup, accept user intent
**→ CONTEXT D-08**

### Q9 — Subdir scope at scan time
**Options:** cd-and-scan only (recommended) / pass --root flag to CLI
**Selected:** cd-and-scan only
**→ CONTEXT D-09**

### Q10 — Repo-list endpoint caching
**Options:** Cache 60s in Redis (recommended) / no caching, live every request
**Selected:** Cache 60s in Redis
**→ CONTEXT D-10b**

## Deferred ideas raised (not in scope)

(None during this discussion — scope was already tight per ROADMAP. Standard deferred list captured in CONTEXT.md `<deferred_ideas>`.)

## Notes for next time

- All 10 sub-decisions converged on the recommended option without back-and-forth — phase is well-bounded by Phase 6/7's locked decisions, leaving Phase 7.5 mostly mechanical glue.
- Phase 8 webhook delivery shares the `scan_repo` taskiq job — planner should call this out as a key cross-phase contract.
