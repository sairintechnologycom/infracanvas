---
phase: 07.5-github-repo-connector
plan: 10
type: execute
wave: 5
depends_on: [05, 07]
files_modified:
  - dashboard/components/scans/ScanPendingClient.tsx
  - dashboard/app/(dashboard)/scans/[id]/page.tsx
  - dashboard/app/api/scan-status/route.ts
  - dashboard/__tests__/scan-pending-client.test.tsx
  - dashboard/__tests__/scan-detail-polling.test.tsx
autonomous: true
requirements: [GH-05]
must_haves:
  truths:
    - "GET /api/scan-status?id={scan_id} proxies to backend GET /v1/scans/{id}"
    - "ScanPendingClient polls /api/scan-status every 2s while scan.status === 'pending' (CC-14 useEffect+setInterval+cancelled flag)"
    - "On scan.status==='ready' → polling stops, calls router.refresh() so the parent RSC re-renders the viewer"
    - "On scan.status==='failed' → polling stops, renders error_message + Retry button"
    - "Scan detail page: when initial scan.status is pending or failed, render ScanPendingClient; when ready, render existing viewer (Phase 7 D-08 path) — UNCHANGED for ready path"
    - "Polling NEVER uses react-query (PATTERNS A1 correction); uses setInterval + cancelled-flag teardown"
  artifacts:
    - path: "dashboard/components/scans/ScanPendingClient.tsx"
      provides: "Client polling shell for pending/failed scan rows"
      exports: ["ScanPendingClient"]
    - path: "dashboard/app/(dashboard)/scans/[id]/page.tsx"
      provides: "Status-gated page shell — pending/failed → ScanPendingClient; ready → existing viewer"
    - path: "dashboard/app/api/scan-status/route.ts"
      provides: "Polling-friendly proxy returning the extended ScanGetResp"
  key_links:
    - from: "ScanPendingClient.tsx"
      to: "/api/scan-status?id=..."
      via: "setInterval(fetch, 2000) + cancelled flag teardown"
      pattern: "setInterval.*2000"
    - from: "ScanPendingClient.tsx (failed state)"
      to: "/api/scans/from-github (POST retry)"
      via: "Retry button rebuilds the (installation_id, repo, branch, path) payload from scan row's github_* columns"
      pattern: "/api/scans/from-github"
---

<rationale>
Polling shell for pending/failed scans + the small proxy route the polling fetches against. Splitting from Plans 08-09 because this UI is in the /scans/[id] route tree (not /settings/integrations) and the lifecycle pattern is different (long-running poll vs one-shot fetch). Wave 5 because depends on Plan 07 (proxies — though we add a new one here) + Plan 05 (extended GET response).

Plan 11 originally suggested by guidance is rolled into this plan since both concerns (page guard + ScanPendingClient + proxy + Retry) are tightly coupled — the page can't render the polling component without the component existing, and the component can't refresh status without the proxy.
</rationale>

<objective>
Add a polling-friendly /api/scan-status proxy, a ScanPendingClient that polls every 2s while pending and renders a Retry CTA on failed, and a guard at the top of /scans/[id] page that routes to ScanPendingClient when status is pending/failed. The "ready" rendering path is unchanged.

Output: one new component, one new proxy route, modified scan detail page, two new test files.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/07.5-github-repo-connector/07.5-CONTEXT.md
@.planning/phases/07.5-github-repo-connector/07.5-PATTERNS.md
@dashboard/components/scans/ScanPickerModal.tsx
@dashboard/app/(dashboard)/scans/[id]/page.tsx
@dashboard/components/scans/ScanViewerClient.tsx
</context>

<interfaces>
From dashboard/lib/types.ts (Plan 07 — extended in this phase):
- ScanGetResp { ...existing fields..., status, error_message?: string | null, source_path?, github_installation_id?, github_repo?, github_branch?, github_sha?, presigned_get_url?: string | null }

From next/navigation: useRouter().refresh(), router.push(...).

From dashboard/components/ui/skeleton.tsx (existing): Skeleton component for the pending state.
</interfaces>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: /api/scan-status proxy route</name>
  <files>dashboard/app/api/scan-status/route.ts</files>
  <read_first>
    - dashboard/app/api/scan-presigned/route.ts (similar single-id proxy if present)
    - dashboard/app/api/scans-list/route.ts (CC-13 GET pattern)
    - dashboard/lib/backend.ts
    - .planning/phases/07.5-github-repo-connector/07.5-PATTERNS.md (CC-13)
  </read_first>
  <behavior>
    - GET /api/scan-status?id=<uuid> → backendFetch /v1/scans/{id} → returns the extended ScanGetResp JSON
    - Missing id query param → 400
    - 404 from backend → 404 from proxy
    - Other errors → 500
  </behavior>
  <action>
1. Create dashboard/app/api/scan-status/route.ts:
   ```typescript
   import { NextRequest, NextResponse } from 'next/server'
   import { backendFetch } from '@/lib/backend'
   import type { ScanGetResp } from '@/lib/types'
   
   export async function GET(req: NextRequest) {
     const id = req.nextUrl.searchParams.get('id')
     if (!id) {
       return NextResponse.json({ error: 'missing_id' }, { status: 400 })
     }
     try {
       const data = await backendFetch<ScanGetResp>(`/v1/scans/${id}`)
       return NextResponse.json(data)
     } catch (err) {
       const status = err instanceof Error && err.message === '404' ? 404 : 500
       return NextResponse.json(
         { error: status === 404 ? 'Not found' : 'Internal error' },
         { status },
       )
     }
   }
   ```
2. Run cd dashboard && npx tsc --noEmit — must pass.
  </action>
  <verify>
    <automated>cd dashboard && test -f app/api/scan-status/route.ts && grep -c "/v1/scans/" app/api/scan-status/route.ts && npx tsc --noEmit 2>&1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - File dashboard/app/api/scan-status/route.ts exists
    - `grep -c "^export async function GET" dashboard/app/api/scan-status/route.ts` returns 1
    - `grep -c "backendFetch" dashboard/app/api/scan-status/route.ts` returns 1
    - `grep -c "/v1/scans/" dashboard/app/api/scan-status/route.ts` returns ≥ 1
    - `cd dashboard && npx tsc --noEmit 2>&1 | grep -cE "error TS"` returns 0
  </acceptance_criteria>
  <done>Proxy route exists; tsc clean.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: ScanPendingClient.tsx — poll every 2s; render skeleton/failed/Retry</name>
  <files>dashboard/components/scans/ScanPendingClient.tsx, dashboard/__tests__/scan-pending-client.test.tsx</files>
  <read_first>
    - dashboard/components/scans/ScanPickerModal.tsx (CC-14 cancelled-flag pattern)
    - dashboard/components/ui/skeleton.tsx
    - dashboard/components/ui/button.tsx
    - .planning/phases/07.5-github-repo-connector/07.5-PATTERNS.md (CC-14 polling pattern; ScanPendingClient.tsx assignment)
    - .planning/phases/07.5-github-repo-connector/07.5-CONTEXT.md (D-03)
  </read_first>
  <behavior>
    - Test 1 (renders_pending_skeleton): initial scan.status === 'pending' renders Skeleton + repo/branch text + "Scanning…" copy.
    - Test 2 (polls_every_2s): vi.useFakeTimers(); advance 2000ms — fetch /api/scan-status?id=<id> fires once; advance another 2000ms — fires again.
    - Test 3 (stops_polling_on_ready): mock fetch returns status='ready' on second poll → setInterval cleared (no third fetch); router.refresh() called.
    - Test 4 (stops_polling_on_failed): mock fetch returns status='failed' → setInterval cleared; renders error_message; renders Retry button.
    - Test 5 (retry_button_reposts): click Retry → fetch /api/scans/from-github with body matching the original github_installation_id, github_repo, github_branch, source_path from the scan row; on success router.push to new scan id.
    - Test 6 (cleanup_on_unmount): unmount during poll — interval cleared (no fetch fires after unmount).
    - Test 7 (no_poll_when_initial_ready): pass scan with status='ready' as initialScan — no setInterval started (component immediately calls router.refresh OR returns null).
  </behavior>
  <action>
1. Create dashboard/components/scans/ScanPendingClient.tsx (~100 lines):

   Component signature: ScanPendingClient({ initialScan: ScanGetResp }).

   State:
   - scan (ScanGetResp, init initialScan)
   - retrying (boolean)

   useEffect on mount keyed on scan.id (or initialScan.id):
   - If scan.status !== 'pending' → no-op (but if it's 'ready', call router.refresh() once).
   - Otherwise: let cancelled = false; const tick = async () => { if cancelled return; try res = await fetch('/api/scan-status?id=' + scan.id); if !res.ok return; data = await res.json(); if cancelled return; setScan(data); if (data.status === 'ready') router.refresh(); }; const id = setInterval(tick, 2000); return () => { cancelled = true; clearInterval(id) }.

   Render:
   - status === 'pending': render skeleton + branch/repo/sha header. Copy: "Scanning {scan.github_repo}@{scan.github_branch}…".
   - status === 'failed': render error_message + Retry button. Retry handler:
     - If scan.github_installation_id and scan.github_repo and scan.github_branch present, POST /api/scans/from-github body { installation_id: scan.github_installation_id, repo: scan.github_repo, branch: scan.github_branch, path: scan.source_path || '.' }; on success router.push('/scans/' + new_scan_id).
     - If any github_* field missing (e.g., scan was triggered via CLI), show "Cannot retry from here — re-scan from /settings/integrations".
   - status === 'ready': call router.refresh() (already in the effect); render minimal placeholder while RSC re-renders.

2. Create dashboard/__tests__/scan-pending-client.test.tsx with the 7 tests in the behavior block. Use:
   - vi.mock for next/navigation returning useRouter with push + refresh spies.
   - vi.useFakeTimers + advanceTimersByTime(2000) for poll cadence tests.
   - Sequential vi.fn().mockResolvedValueOnce(...) for the polling fetch mock.

3. Run vitest.
  </action>
  <verify>
    <automated>cd dashboard && test -f components/scans/ScanPendingClient.tsx && grep -c "setInterval" components/scans/ScanPendingClient.tsx && grep -c "2000" components/scans/ScanPendingClient.tsx && grep -c "router.refresh" components/scans/ScanPendingClient.tsx && npm test -- --run scan-pending-client 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - File dashboard/components/scans/ScanPendingClient.tsx exists
    - `grep -c "'use client'" dashboard/components/scans/ScanPendingClient.tsx` returns 1
    - `grep -c "setInterval" dashboard/components/scans/ScanPendingClient.tsx` returns 1
    - `grep -c "clearInterval" dashboard/components/scans/ScanPendingClient.tsx` returns ≥ 1
    - `grep -c "let cancelled = false" dashboard/components/scans/ScanPendingClient.tsx` returns 1
    - `grep -c "router.refresh" dashboard/components/scans/ScanPendingClient.tsx` returns ≥ 1
    - `grep -c "/api/scan-status" dashboard/components/scans/ScanPendingClient.tsx` returns ≥ 1
    - `grep -c "Retry" dashboard/components/scans/ScanPendingClient.tsx` returns ≥ 1
    - `grep -c "/api/scans/from-github" dashboard/components/scans/ScanPendingClient.tsx` returns ≥ 1 (Retry POST)
    - `grep -c "^  it(" dashboard/__tests__/scan-pending-client.test.tsx` returns ≥ 7
    - `cd dashboard && npm test -- --run scan-pending-client 2>&1 | grep -cE "7 passed|passed"` returns ≥ 1
  </acceptance_criteria>
  <done>ScanPendingClient polls 2s, stops on terminal status, surfaces Retry that re-POSTs with derived params; seven tests pass.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Modify scan detail page to gate on status — pending/failed → ScanPendingClient, ready → existing viewer</name>
  <files>dashboard/app/(dashboard)/scans/[id]/page.tsx, dashboard/__tests__/scan-detail-polling.test.tsx</files>
  <read_first>
    - dashboard/app/(dashboard)/scans/[id]/page.tsx (current RSC — read in full)
    - dashboard/components/scans/ScanViewerClient.tsx
    - dashboard/components/scans/ScanPendingClient.tsx (Task 2 just-created)
    - .planning/phases/07.5-github-repo-connector/07.5-PATTERNS.md ("scans/[id]/page.tsx modify" pattern assignment)
    - dashboard/lib/backend.ts (backendFetch)
  </read_first>
  <behavior>
    - Test 1 (status_pending_renders_pending_client): backendFetch returns scan with status='pending' → page renders ScanPendingClient (mock-asserted via data-testid).
    - Test 2 (status_failed_renders_pending_client): scan with status='failed' → renders ScanPendingClient (so user sees error_message + Retry).
    - Test 3 (status_ready_renders_viewer): scan with status='ready' + r2_presigned → renders existing viewer client (data-testid="scan-viewer-mock").
    - Test 4 (404_returns_not_found): backendFetch raises 404 → notFound() called (or whatever the existing 404 path uses).
  </behavior>
  <action>
1. Read dashboard/app/(dashboard)/scans/[id]/page.tsx in full. Identify the scan-fetch step and the "render viewer" branch.

2. Modify the page to add a status guard. Approach (least-invasive):
   - After backendFetch returns the scan, check scan.status:
     - if (scan.status === 'pending' || scan.status === 'failed') return <ScanPendingClient initialScan={scan} />
     - else: render the existing viewer path unchanged.
   - Import ScanPendingClient at the top.

3. Create dashboard/__tests__/scan-detail-polling.test.tsx (or extend an existing scan-detail test if present).
   - Note: this file tests the page module. RSC pages are tricky to render in vitest; use the same mock-style as existing dashboard RSC tests if any (check dashboard/__tests__/ for prior RSC test patterns). If RSC testing isn't already a pattern in the project, restrict this test file to logic-only assertions:
     - Mock backendFetch to return each status variant.
     - Render the page component with await-rendering helper (next/server-aware).
     - Assert data-testid markers from mocked ScanPendingClient and ScanViewerClient.
   - Acceptable alternative: write the gate logic into a tiny pure helper renderScanByStatus(scan): JSX.Element exported from the page, then unit-test the helper.

4. Run vitest + npx tsc.
  </action>
  <verify>
    <automated>cd dashboard && grep -c "ScanPendingClient" "app/(dashboard)/scans/[id]/page.tsx" && grep -c "status === 'pending'" "app/(dashboard)/scans/[id]/page.tsx" && npm test -- --run scan-detail-polling 2>&1 | tail -10 && npx tsc --noEmit 2>&1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "ScanPendingClient" "dashboard/app/(dashboard)/scans/[id]/page.tsx"` returns ≥ 1
    - `grep -c "status === 'pending'\|scan.status ===" "dashboard/app/(dashboard)/scans/[id]/page.tsx"` returns ≥ 1
    - `grep -c "^  it(" dashboard/__tests__/scan-detail-polling.test.tsx` returns ≥ 4
    - `cd dashboard && npm test -- --run scan-detail-polling 2>&1 | grep -cE "passed"` returns ≥ 1
    - `cd dashboard && npx tsc --noEmit 2>&1 | grep -cE "error TS"` returns 0
  </acceptance_criteria>
  <done>Scan detail page routes pending/failed scans to ScanPendingClient; ready path unchanged; four tests pass.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser to /api/scan-status | Same-origin Clerk-authed fetch every 2s |
| ScanPendingClient retry path to /api/scans/from-github | Re-uses backend's RLS-scoped endpoint; same trust posture as initial submit |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07.5-10-01 | DoS | Polling continues after tab idle for hours | mitigate | Polling is gated on scan.status === 'pending'; once terminal, interval cleared. Pending scans terminate within ~10 min (worker timeouts CLONE_TIMEOUT_S+SCAN_TIMEOUT_S). Worst case: 600s × 0.5 fetch/s × 1 user = 300 fetches per scan tail; well within Vercel/Cloudflare proxy budgets. |
| T-07.5-10-02 | Information Disclosure | error_message could embed token (paranoia check) | mitigate | scan_repo job (Plan 06) runs _redact_token on stderr before storing in error_message column; verified by Plan 06 test_token_redacted_in_stderr_log. Display path here just renders the column value; no re-introduction risk. |
| T-07.5-10-03 | Tampering | Retry button posts to a different installation | mitigate | Retry payload sourced exclusively from the scan row's github_* columns (server-fetched); user cannot edit them client-side. |
</threat_model>

<verification>
- cd dashboard && npm test -- --run scan-pending-client passes (≥ 7 tests)
- cd dashboard && npm test -- --run scan-detail-polling passes (≥ 4 tests)
- cd dashboard && npx tsc --noEmit clean
- Full dashboard test suite: cd dashboard && npm test -- --run passes
</verification>

<success_criteria>
- /api/scan-status proxy works
- ScanPendingClient polls 2s, stops on terminal, surfaces Retry
- Scan detail page gates on status
- All required tests passing; tsc clean
</success_criteria>

<output>
After completion, append a SUMMARY noting the polling cadence (2s), terminal-state stop condition, and Retry POST shape.
</output>
