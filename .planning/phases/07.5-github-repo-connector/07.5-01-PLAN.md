---
phase: 07.5-github-repo-connector
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - backend/pyproject.toml
  - backend/Dockerfile
  - backend/app/settings.py
  - backend/tests/conftest.py
  - dashboard/components/ui/command.tsx
autonomous: true
requirements: [GH-01, GH-02, GH-03, GH-04]
must_haves:
  truths:
    - "git CLI available in backend Docker worker image"
    - "fakeredis dev dep installable; respx already present"
    - "settings.github_app_id, github_app_private_key, github_app_webhook_secret, github_app_slug exist as Settings fields with safe test defaults"
    - "shadcn command primitive exists at dashboard/components/ui/command.tsx"
  artifacts:
    - path: "backend/Dockerfile"
      provides: "git installed in apt step"
      contains: "git"
    - path: "backend/pyproject.toml"
      provides: "fakeredis>=2.26 in dev extras; redis~=5.2.0 + httpx~=0.27 explicit deps"
      contains: "fakeredis"
    - path: "backend/app/settings.py"
      provides: "github_app_* settings fields"
      contains: "github_app_id"
    - path: "dashboard/components/ui/command.tsx"
      provides: "shadcn Command primitive (Popover+Command recipe foundation)"
  key_links:
    - from: "backend/Dockerfile"
      to: "scan_repo job (Wave 2 plan 06)"
      via: "git binary on PATH for asyncio.create_subprocess_exec for git clone"
      pattern: "RUN apt-get install.*git"
    - from: "backend/app/settings.py"
      to: "backend/app/integrations/github/auth.py (Wave 1 plan 03)"
      via: "settings.github_app_private_key / github_app_id read in mint_app_jwt()"
      pattern: "settings\\.github_app_"
    - from: "dashboard/components/ui/command.tsx"
      to: "RepoCombobox.tsx (Wave 3 plan 09)"
      via: "import Command, CommandInput, CommandList, CommandItem from @/components/ui/command"
      pattern: "from '@/components/ui/command'"
---

<rationale>
Wave 0 foundation: dependency, image, settings, and shadcn primitive bootstrap. Isolated as its own plan so every later plan can assume git, fakeredis, redis, httpx, GitHub App settings fields, and the shadcn Command primitive exist without ordering risk. No DB changes here (those are Plan 02). No test fixtures here (those are Plan 02 too — they require the migration). Splitting this avoids merge contention in Wave 1+ on pyproject.toml and settings.py.
</rationale>

<objective>
Bootstrap Phase 7.5 environment: install Python deps (fakeredis, explicit redis, explicit httpx), add git to backend Docker image, register GitHub App settings fields, add shadcn command primitive to dashboard.

Purpose: All later plans (auth helpers, taskiq job, frontend combobox, test fixtures) depend on these foundations. Without them, code added in Wave 1 cannot be exercised or rendered.

Output: pyproject + Dockerfile + settings.py edits, new shadcn command.tsx component file.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/07.5-github-repo-connector/07.5-CONTEXT.md
@.planning/phases/07.5-github-repo-connector/07.5-RESEARCH.md
@.planning/phases/07.5-github-repo-connector/07.5-PATTERNS.md
@backend/pyproject.toml
@backend/Dockerfile
@backend/app/settings.py
@backend/tests/conftest.py
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Add git to backend Dockerfile + extend pyproject.toml deps</name>
  <files>backend/Dockerfile, backend/pyproject.toml</files>
  <read_first>
    - backend/Dockerfile (current apt-install block, lines 19-24)
    - backend/pyproject.toml (dependencies + dev extras, lines 28-62)
    - .planning/phases/07.5-github-repo-connector/07.5-PATTERNS.md (CC-18, "Backend test fixtures" + "pyproject.toml modify" sections)
    - .planning/phases/07.5-github-repo-connector/07.5-RESEARCH.md (Environment Availability table; A2 assumption)
  </read_first>
  <behavior>
    - After change, docker build backend/ succeeds and git --version returns 2.x in image
    - pip install -e backend/[dev] resolves with fakeredis>=2.26 and redis~=5.2.0
    - httpx pinned to ~=0.27.0 (per CC-PATTERNS pyproject.toml modify)
  </behavior>
  <action>
1. Edit backend/Dockerfile: in the existing apt-get install block (around line 19-24), append `git \` to the package list. Final block must include: `build-essential libpq-dev ca-certificates curl git`.
2. Edit backend/pyproject.toml:
   - In `dependencies` array, add `"httpx~=0.27.0"` and `"redis~=5.2.0"` (alphabetically sorted with existing entries). Verify `pyjwt[crypto]~=2.9.0` already present (PATTERNS confirms line 41) — do NOT add a duplicate.
   - In `[project.optional-dependencies] dev` array, add `"fakeredis~=2.26.0"` (alphabetically next to existing respx).
3. Do NOT change versions of any other dep.
4. Run `pip install -e .[dev]` from backend/ to verify resolution (no commit if it fails).
  </action>
  <verify>
    <automated>cd backend && grep -E '^\s+git \\$' Dockerfile && grep -E 'fakeredis~=' pyproject.toml && grep -E 'redis~=5\.2' pyproject.toml && grep -E 'httpx~=0\.27' pyproject.toml && pip install -e ".[dev]" --quiet --dry-run 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c '^\s\+git \\\\\?$' backend/Dockerfile` returns ≥ 1 (git on its own line in apt list)
    - `grep -c 'fakeredis~=' backend/pyproject.toml` returns 1
    - `grep -c 'redis~=5\.2' backend/pyproject.toml` returns 1
    - `grep -c 'httpx~=0\.27' backend/pyproject.toml` returns 1
    - `grep -c 'pyjwt\[crypto\]~=2\.9' backend/pyproject.toml` returns 1 (unchanged from before)
    - `cd backend && python -c "import fakeredis, redis, httpx, jwt; print('ok')"` prints `ok`
  </acceptance_criteria>
  <done>git installed in backend image; fakeredis + redis + httpx pinned in pyproject; smoke import works.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add GitHub App settings fields + test env stubs</name>
  <files>backend/app/settings.py, backend/tests/conftest.py</files>
  <read_first>
    - backend/app/settings.py (current Settings class — Pydantic BaseSettings shape)
    - backend/tests/conftest.py:33-46 (env-stub block)
    - .planning/phases/07.5-github-repo-connector/07.5-PATTERNS.md (auth.py pattern assignment, Settings extension subsection)
    - .planning/phases/07.5-github-repo-connector/07.5-CONTEXT.md (D-06, D-15)
  </read_first>
  <behavior>
    - Test 1: `from app.settings import settings; assert hasattr(settings, 'github_app_id')` — pass
    - Test 2: `assert hasattr(settings, 'github_app_private_key')` — pass
    - Test 3: `assert hasattr(settings, 'github_app_webhook_secret')` — pass
    - Test 4: `assert hasattr(settings, 'github_app_slug')` — pass
    - Test 5: defaults are empty strings (so test env doesn't crash on import)
    - Test 6: under tests/conftest.py env stubs, `settings.github_app_id == "12345"`
  </behavior>
  <action>
1. Edit backend/app/settings.py: add four fields to the Settings class (matching existing snake_case + type-hint pattern):
   ```
   github_app_id: str = ""
   github_app_private_key: str = ""        # multi-line PEM, set via Fly secret
   github_app_webhook_secret: str = ""     # provisioned now (D-15), unused until Phase 8
   github_app_slug: str = ""               # for constructing install URLs in dashboard env
   ```
   Place them next to other service-config fields (after redis_url or similar grouping). Add a leading comment line `# GitHub App (Phase 7.5, D-06/D-15)` for navigability.
2. Edit backend/tests/conftest.py: extend the env-stub block (lines 33-46) with:
   ```
   os.environ.setdefault("GITHUB_APP_ID", "12345")
   os.environ.setdefault("GITHUB_APP_PRIVATE_KEY", "")
   os.environ.setdefault("GITHUB_APP_WEBHOOK_SECRET", "test-webhook-secret")
   os.environ.setdefault("GITHUB_APP_SLUG", "infracanvas-dev")
   ```
   The real PEM private key is generated per-test in tests/integrations/github/conftest.py (Plan 02).
3. Run `pytest backend/tests/ --collect-only -q 2>&1 | tail -20` to confirm no import errors from settings change.
  </action>
  <verify>
    <automated>cd backend && python -c "from app.settings import settings; assert all(hasattr(settings, f) for f in ['github_app_id','github_app_private_key','github_app_webhook_secret','github_app_slug']); print('ok')" && pytest tests/ --collect-only -q 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'github_app_id: str' backend/app/settings.py` returns ≥ 1
    - `grep -c 'github_app_private_key: str' backend/app/settings.py` returns ≥ 1
    - `grep -c 'github_app_webhook_secret: str' backend/app/settings.py` returns ≥ 1
    - `grep -c 'github_app_slug: str' backend/app/settings.py` returns ≥ 1
    - `grep -c 'GITHUB_APP_ID' backend/tests/conftest.py` returns ≥ 1
    - `cd backend && python -c "from app.settings import settings; print(settings.github_app_id)"` exits 0
    - `cd backend && pytest tests/ --collect-only -q 2>&1` ends with "tests collected" (no errors line)
  </acceptance_criteria>
  <done>Settings fields present, defaults safe, conftest stubs added, full test collection still clean.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Add shadcn Command primitive to dashboard</name>
  <files>dashboard/components/ui/command.tsx</files>
  <read_first>
    - dashboard/components/ui/popover.tsx (existing primitive; same shadcn install style)
    - dashboard/components/ui/dialog.tsx (existing primitive)
    - .planning/phases/07.5-github-repo-connector/07.5-PATTERNS.md (CC-16, RepoCombobox pattern assignment)
    - dashboard/package.json (existing shadcn-related deps; cmdk is the upstream package)
    - dashboard/lib/utils.ts (cn() helper — shadcn add may overwrite, per PATTERNS Plan 07.1-01 lesson)
  </read_first>
  <behavior>
    - File dashboard/components/ui/command.tsx exists
    - Exports Command, CommandInput, CommandList, CommandItem, CommandEmpty, CommandGroup (shadcn Command surface)
    - Imports from cmdk (shadcn's underlying lib)
    - npx tsc --noEmit passes
  </behavior>
  <action>
1. From dashboard/, run: `npx shadcn@latest add command --yes` (auto-installs the cmdk npm dep + writes dashboard/components/ui/command.tsx).
2. If the --yes flag isn't supported on the installed shadcn version, run with stdin redirection: `printf 'y\n' | npx shadcn@latest add command`.
3. The shadcn add command step may also overwrite dashboard/lib/utils.ts (PATTERNS notes shadcn init does this). After install:
   - Run `git diff dashboard/lib/utils.ts` — if cn() was already present and the install only re-adds the same export, accept. If the install removed prior helpers (e.g., isUUID()), re-merge them manually (PATTERNS Plan 07.1-01 documents this exact behavior).
4. Run `cd dashboard && npx tsc --noEmit` — must pass.
5. Run `cd dashboard && npm test -- --run` — existing tests must still pass (smoke).
6. If shadcn adds cmdk to package.json automatically, verify it landed in dependencies (not devDependencies).
  </action>
  <verify>
    <automated>cd dashboard && test -f components/ui/command.tsx && grep -cE "^export" components/ui/command.tsx && grep -c "cmdk" package.json && npx tsc --noEmit 2>&1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - `test -f dashboard/components/ui/command.tsx` returns 0 (file exists)
    - `grep -c '"cmdk"' dashboard/components/ui/command.tsx` returns ≥ 1
    - `grep -cE "^export (const|function|\{) ?(Command|CommandInput|CommandList|CommandItem|CommandEmpty|CommandGroup)" dashboard/components/ui/command.tsx` returns ≥ 4
    - `grep -c '"cmdk"' dashboard/package.json` returns ≥ 1
    - `cd dashboard && npx tsc --noEmit 2>&1 | grep -cE "error TS"` returns 0
    - `cd dashboard && npm test -- --run 2>&1 | grep -cE "Tests.*passed|Tests.*[0-9]+ passed"` returns ≥ 1 (existing tests still green)
  </acceptance_criteria>
  <done>Command primitive lives at components/ui/command.tsx, cmdk dep installed, typecheck and existing tests pass.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Container build to runtime | Dockerfile change adds git binary; trust assumed for apt-installed package |
| Test env to Settings | Test env vars feed Pydantic settings; no production secrets touched |
| npm registry to dashboard | shadcn pulls cmdk; supply-chain-trust assumed at parity with existing shadcn deps |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07.5-01-01 | Information Disclosure | github_app_private_key default in settings.py | mitigate | Default is empty string (not a real key); real key sourced from Fly secret only. Test env stubs use empty string, not a real PEM. |
| T-07.5-01-02 | Tampering | shadcn npx install pulls remote code | accept | Same trust posture as existing shadcn primitives in dashboard/components/ui/ (Phase 7.1 baseline). Lockfile pins resolved versions. |
| T-07.5-01-03 | Elevation of Privilege | git binary in worker image enables clone-as-shellout | mitigate | Worker runs as non-root in Fly VM (Phase 6 D-12). git invoked only via asyncio.create_subprocess_exec in scan_repo job (Plan 06) with a tmpdir cwd; no shell=True. |
</threat_model>

<verification>
- git --version works inside docker-build-produced image (manual verify after Plan 02 migration so worker boots end-to-end)
- cd backend && pip install -e ".[dev]" resolves cleanly
- cd dashboard && npx tsc --noEmit passes
- pytest backend/tests/ --collect-only reports zero collection errors
</verification>

<success_criteria>
- All four GitHub App settings fields exist with empty defaults
- git, fakeredis, redis, httpx available
- shadcn Command primitive exported and typechecked
- No regressions to existing test suites
</success_criteria>

<output>
After completion, create .planning/phases/07.5-github-repo-connector/07.5-01-SUMMARY.md summarizing exact deps added, settings field names, and shadcn version installed.
</output>
