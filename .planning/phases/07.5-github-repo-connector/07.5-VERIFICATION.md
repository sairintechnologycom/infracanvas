---
phase: 07.5-github-repo-connector
verified: 2026-05-05T07:42:00+05:30
status: human_needed
score: 5/5 must-haves verified (1 test-only warning, 0 blockers)
overrides_applied: 0
re_verification:
  previous_status: passed
  previous_score: 5/5 (self-verification by Plan 11)
  gaps_closed: []
  gaps_remaining: []
  regressions:
    - "tests/test_services_scans.py: 3 unit tests for finalize_scan fail to connect to testcontainer DB (test wiring gap — get_sessionmaker() not monkeypatched). Production code is exercised end-to-end by tests/jobs/test_scan_repo.py (13/13 pass), so functional coverage is intact, but unit-level tests do not run."
human_verification:
  - test: "Operator already walked 16-step manual smoke (S1–S16 in 07.5-MANUAL-SMOKE.md) end-to-end against a real InfraCanvas DEV App + sandbox Terraform repo on 2026-05-05; resume signal 'approved'. Re-confirm at next deploy or env change."
    expected: "All 16 numbered checkpoints green: install URL flow, post-install hydration poll, repo+branch+path picking, /scans/{id} pending → ready, Stripe meter event in test dashboard, R2 object existence at teams/{team_id}/scans/{scan_id}.json, negative-path Retry, token-leakage grep returns zero matches."
    why_human: "GitHub App registration UI is interactive (P1 pre-flight); private key handoff via clipboard never touches git (P2); Fly secrets must be set by operator (P3); Stripe meter UI confirmation is human-only (S13); R2 console object check is UI-based (S14); fly logs grep over a live smoke window (S16) cannot be automated programmatically. Operator confirmed once on 2026-05-05; this VERIFICATION file records that confirmation."
---

# Phase 7.5: GitHub Repo Connector Verification Report

**Phase Goal:** Let authenticated dashboard users connect a GitHub repo, browse repos/branches, and trigger an on-demand scan from the dashboard end-to-end (per ROADMAP.md Phase 7.5 row + .planning/REQUIREMENTS.md GH-* IDs).
**Verified:** 2026-05-05T07:42:00+05:30 (codebase audit independent of Plan 11 self-verification)
**Status:** human_needed (1 already-completed manual smoke recorded; functional verification PASSED)
**Re-verification:** Yes — cross-checks Plan 11's `07.5-PHASE-VERIFICATION.md` self-sign-off against the live working tree

## Goal Achievement

### Observable Truths (from ROADMAP.md Phase 7.5 success criteria + 07.5-CONTEXT.md GH-01..GH-05)

| # | Truth | Status | Evidence |
|---|-------|--------|----------|
| 1 | User installs the InfraCanvas GitHub App and sees their accessible repos in the dashboard (SC1, GH-01, GH-02) | ✓ VERIFIED | `dashboard/components/integrations/InstallButton.tsx` (35 LoC, opens install URL with `state==clerkOrgId`); `backend/app/routes/github.py:218-279` (install-callback verifies state, App-JWT reverify, ON CONFLICT upsert into `github_installations`); `dashboard/app/(dashboard)/settings/integrations/page.tsx` 3-state machine (preinstall/postinstall) with bounded post-install hydration poll. 4 install-callback tests + 4 installations-list tests pass. |
| 2 | User picks a repo + branch + optional subdirectory path, clicks "Scan" (SC2, GH-04) | ✓ VERIFIED | `dashboard/components/integrations/RepoCombobox.tsx` (148 LoC, shadcn Popover+Command, 250ms debounce, cancellable fetch); `BranchPicker.tsx` (113 LoC, lazy-loaded on selectedRepo, default-branch fallback); `ScanTriggerForm.tsx` (117 LoC, composes the three + path Input + Scan Button → POST /api/scans/from-github → router.push). 4+7+6+7 = 24 component tests pass. |
| 3 | Backend performs a read-only shallow clone, runs `infracanvas scan`, stores result in Neon + R2 under the team (SC3, GH-03) | ✓ VERIFIED | `backend/app/queue/tasks/scan_repo.py` (362 LoC, `@broker.task` pipeline: `mint_installation_token` → `git clone --depth=1 --single-branch` with `GIT_TERMINAL_PROMPT=0` + `asyncio.wait_for(CLONE_TIMEOUT_S=60)` → traversal guard via `Pathlib.relative_to` (line 199) → `infracanvas scan --ci` subprocess with `rc∈(0,1)` success → sha256 → `r2.put_bytes` → `finalize_scan`); 3-layer token redaction; failure-path UPDATE with `WHERE status='pending'` clobber-guard. 13/13 scan_repo job tests pass. |
| 4 | The resulting scan appears in Phase 7 history/detail views (status pending → ready, polled every 2s) (SC4, GH-05) | ✓ VERIFIED | `backend/migrations/versions/20260503_008_scan_github_columns.py` adds `pending` status enum value + 6 github_* columns; `backend/app/routes/scans_from_github.py:73-180` (POST creates `pending` row + .kiq dispatch); `dashboard/app/api/scan-status/route.ts` proxy; `dashboard/components/scans/ScanPendingClient.tsx` (187 LoC, CC-14 useEffect+setInterval(2000)+cancelled-flag teardown — explicitly NOT react-query per PATTERNS A1; renders `error_message` + Retry CTA on `failed`); `dashboard/app/(dashboard)/scans/[id]/renderScanByStatus.tsx` status-gate helper. 7+4 = 11 polling tests pass. |
| 5 | GitLab, Bitbucket, and Azure DevOps explicitly out of scope — deferred to v1.2 Enterprise (SC5) | ✓ VERIFIED | 07.5-CONTEXT.md `<deferred_ideas>` section explicitly defers; ROADMAP.md row says "GitHub-only for MVP". No GitLab/Bitbucket/Azure DevOps modules under `backend/app/integrations/` (only `github/`). |

**Score:** 5/5 truths verified

### Required Artifacts (sampled via wc -l + grep against live tree)

| Artifact | Expected | Status | Details |
|----------|----------|--------|---------|
| `backend/app/integrations/github/auth.py` | App-JWT + installation-token mint | ✓ VERIFIED | 77 LoC; defines `mint_app_jwt`, `mint_installation_token`, `get_installation_metadata` |
| `backend/app/integrations/github/client.py` | Repos/branches/HEAD-sha + 60s Redis cache | ✓ VERIFIED | 179 LoC; `list_installation_repos` with `gh:repos:*` cache, `list_branches`, `get_head_sha` |
| `backend/app/routes/github.py` | 4 endpoints (installations/repos/branches/install-callback) | ✓ VERIFIED | 312 LoC; 4 `@router.{get,get,get,get}` decorators (CONTEXT D-10d's POST was a typo, corrected to GET — documented in code) |
| `backend/app/routes/scans_from_github.py` | POST /v1/scans/from-github | ✓ VERIFIED | 204 LoC; RLS membership probe + ON CONFLICT INSERT pending + lazy import + 7-kwarg .kiq dispatch |
| `backend/app/queue/tasks/scan_repo.py` | Taskiq job (clone+scan+R2+finalize) | ✓ VERIFIED | 362 LoC; broker.task with mint→clone→traversal-guard→scan→put_bytes→finalize_scan |
| `backend/app/services/scans.py` (new helpers) | finalize_scan + fire_scan_meter_or_502 | ✓ VERIFIED | 173 LoC; both helpers present; UPDATE WHERE status != 'ready' RETURNING idempotency lock |
| `backend/app/storage/r2.py` (new helpers) | put_bytes + get_bytes async | ✓ VERIFIED | 159 LoC; `put_bytes` + `get_bytes` present (line 130+) |
| `backend/migrations/versions/20260503_007_github_installations.py` | github_installations table + RLS | ✓ VERIFIED | File exists in `backend/migrations/versions/` |
| `backend/migrations/versions/20260503_008_scan_github_columns.py` | scans columns + pending status | ✓ VERIFIED | File exists in `backend/migrations/versions/` |
| `dashboard/components/integrations/{InstallButton,RepoCombobox,BranchPicker,ScanTriggerForm}.tsx` | 3 reusable components + composer | ✓ VERIFIED | 35+148+113+117 = 413 LoC across 4 files; all present |
| `dashboard/components/scans/ScanPendingClient.tsx` | Polling shell | ✓ VERIFIED | 187 LoC; setInterval(2000) + cancelled flag + Retry on failed |
| `dashboard/app/api/github/{installations,repos,branches}/route.ts` + `from-github/route.ts` + `scan-status/route.ts` | 5 Next.js Route Handlers | ✓ VERIFIED | 27+35+32+37+37 = 168 LoC across 5 files; all present |
| `dashboard/app/(dashboard)/settings/integrations/page.tsx` | Live state machine page | ✓ VERIFIED | Present (page.tsx exists, replaces Phase 7 D-03 placeholder) |
| `dashboard/app/(dashboard)/scans/[id]/renderScanByStatus.tsx` | Extracted status-gate helper | ✓ VERIFIED | Present (Next.js 15 page-export rule fix) |

### Key Link Verification (Wiring)

| From | To | Via | Status | Details |
|------|-----|-----|--------|---------|
| `InstallButton` → GitHub install URL | window.open(state==clerkOrgId) | direct call | ✓ WIRED | T-07.5-08-01 mitigation (state from active org, not team_id) |
| `install-callback` → DB upsert | ON CONFLICT (team_id, github_installation_id) | route handler | ✓ WIRED | github.py:248-249 verifies `state==team.clerk_org_id`; ON CONFLICT upsert at tx scope |
| `RepoCombobox` → backend `/v1/github/repos` | dashboard `/api/github/repos` proxy → backendFetch | fetch w/ AbortController | ✓ WIRED | RepoCombobox.tsx fetches via debounced `/api/github/repos`; proxy forwards Clerk JWT |
| `ScanTriggerForm` → POST `/v1/scans/from-github` | dashboard `/api/scans/from-github` proxy | fetch + router.push | ✓ WIRED | scans_from_github.py:79 `Depends(resolve_team_from_clerk_org)` → INSERT pending → .kiq |
| `scans_from_github` → `scan_repo` taskiq job | broker.task .kiq() with 7 kwargs | lazy import | ✓ WIRED | route imports task lazily to avoid import cycle; CC-4 contract |
| `scan_repo` → R2 PUT | r2.put_bytes via run_in_threadpool | async wrapper | ✓ WIRED | put_bytes added in Plan 06 (r2.py); awaited in scan_repo |
| `scan_repo` → DB UPDATE pending→ready | finalize_scan helper | atomic UPDATE+meter | ✓ WIRED | scan_repo:281; finalize_scan in services/scans.py |
| `ScanPendingClient` → `/api/scan-status` | setInterval(2000) + cancelled flag | useEffect | ✓ WIRED | CC-14 polling; explicitly NOT react-query (T9 grep = 0) |
| `scan-detail page` → `renderScanByStatus` | helper module | import | ✓ WIRED | renderScanByStatus.tsx routes pending/failed → ScanPendingClient, ready+URL → ScanViewerClient |

### Cross-Cutting Truth Checks (T1–T9, re-run independently)

| ID | Invariant | Expected | Actual | Status |
|----|-----------|----------|--------|--------|
| T1 | Auth-coverage on github.py + scans_from_github.py routes | ≥ 6 (PHASE-VERIFICATION used "Depends(require_role" OR "Depends(resolve_team_from_clerk_org") | **5** with original grep, **6** when including `Depends(require_principal)` (install-callback uses both) | ⚠️ MINOR — PHASE-VERIFICATION's threshold of 6 was based on miscount; live shows 4 in github.py + 1 in scans_from_github.py for `resolve_team_from_clerk_org`. ALL 5 routes ARE auth-gated. Intent satisfied. |
| T2 | RLS GUC set on every team-scoped tx | ≥ 5 | **10** (5 in github.py, 3 in scans_from_github.py, 2 in scan_repo.py) | ✓ |
| T3 | No PEM material in source under `backend/app/` | == 0 | **0** | ✓ |
| T4 | Token never bound on structlog logger in scan_repo.py | == 0 | **0** | ✓ defense-in-depth |
| T5 | Path-traversal guard present in scan_repo.py | ≥ 1 | **2** | ✓ |
| T6 | `team_id` is a kwarg, not SECURITY DEFINER lookup | == 0 callable references | **1** doc-comment mention only (no callable) | ✓ intent satisfied |
| T7 | Meter goes through `finalize_scan`, not direct Stripe SDK | ≥ 1 | **8** | ✓ |
| T8 | Install-callback compares `state` against `clerk_org_id` | ≥ 1 | **1** (line 249) | ✓ |
| T9 | No `@tanstack/react-query` in dashboard | == 0 | **0** | ✓ PATTERNS A1 lock |

### Quality Gates (Backend + Dashboard)

| Gate | Command | Result | Notes |
|------|---------|--------|-------|
| Dashboard tests | `cd dashboard && npm test -- --run` | ✓ **225/225 PASS** | 27 test files, 11.5s; matches PHASE-VERIFICATION claim |
| Dashboard typecheck | `cd dashboard && npx tsc --noEmit` | ⚠️ 1 error (TS6133 in `__tests__/scan-filters.test.tsx`) | **Pre-existing**, explicitly deferred in `deferred-items.md` (Plan 01 carryover) |
| Backend tests (full suite) | `cd backend && pytest tests/ --no-cov` | ⚠️ **169/172 PASS, 3 fail** | All 3 failures in `tests/test_services_scans.py` (`finalize_scan` unit tests) — testcontainer DB wiring gap (test calls `get_sessionmaker()` without monkeypatching the engine; uses static `infracanvas_test` URL on `localhost:5432`). Production code paths covered by `test_scan_repo.py` (13/13 pass). |
| Backend Phase 7.5 tests only | `pytest tests/api/test_github_* tests/api/test_scans_from_github tests/jobs/test_scan_repo tests/integrations/github/` | ✓ **71/71 PASS** | Branches(5) + callback(7) + installations(4) + repos(5) + scans_from_github(10) + scan_repo(13) + auth(27) |
| Backend ruff | `cd backend && ruff check app/` | ⚠️ 11 errors | **All in pre-existing files** (`db/models.py`, `db/session.py`, `queue/tasks/indexing.py`, `routes/scans.py`, `routes/share.py`). 4 of these (`routes/scans.py:411,422,436,441`) are explicitly deferred per `deferred-items.md` (Plan 05 scope). **Zero ruff errors in any Phase 7.5 file** (`auth.py`, `client.py`, `github.py`, `scans_from_github.py`, `scan_repo.py`). |
| Backend mypy --strict | `cd backend && mypy app/` | ⚠️ 30 errors across 11 files | Most are missing-stubs warnings on third-party libs (boto3/botocore — pre-existing). 2 minor regressions in Phase 7.5 territory: `app/services/scans.py:134,162` "Unused 'type: ignore' comment" (added in Plan 05); `app/storage/r2.py:139` "Returning Any from function declared to return bytes" (line 139 = `get_bytes` added in Plan 06). Not blocking — production code works. |

### Requirements Coverage

| Requirement | Source Plan | Description | Status | Evidence |
|-------------|-------------|-------------|--------|----------|
| **GH-01** | Plans 01, 02, 03, 04 | InfraCanvas GitHub App registered + install-callback upserts to `github_installations` keyed by `(team_id, github_installation_id)` | ✓ SATISFIED | `app/routes/github.py:218-279` (callback handler with state==clerk_org_id check at L249, App-JWT reverify, ON CONFLICT upsert); `app/integrations/github/auth.py` (`mint_app_jwt`); `migrations/versions/20260503_007_github_installations.py` (table + RLS); 7 tests in `test_github_callback.py` + `test_auth.py` |
| **GH-02** | Plans 01, 02, 03, 04 | Three repo-discovery endpoints; 60s Redis cache on /v1/github/repos | ✓ SATISFIED | `app/routes/github.py:80,122,168` (3 GET handlers); `app/integrations/github/client.py` (httpx + Redis cache); 14 tests in `test_github_installations|repos|branches.py`; rate-limit 403/429 → 503+Retry-After:60 confirmed |
| **GH-03** | Plans 01, 03, 05, 06 | `scan_repo(install_id, repo, branch, sha, path)` taskiq job; clones, scans, uploads, commits via Phase 6 D-09 | ✓ SATISFIED | `app/queue/tasks/scan_repo.py` 362 LoC pipeline; calls `app/services/scans.py:finalize_scan`; `r2.put_bytes`; 13 tests pass; T4+T5 grep gates green |
| **GH-04** | Plans 07, 08, 09 | `/settings/integrations` is the live connect+browse+scan surface (replaces Phase 7 D-03 placeholder) | ✓ SATISFIED | `dashboard/app/(dashboard)/settings/integrations/page.tsx` (3-state machine); 4 components in `dashboard/components/integrations/`; 4 proxy routes in `dashboard/app/api/github/{installations,repos,branches}` + `from-github`; 24+7 = 31 component+page tests pass |
| **GH-05** | Plans 02, 05, 06, 07, 10 | New `scans.status='pending'` value; row created at enqueue; dashboard polls every 2s until ready | ✓ SATISFIED | `migrations/versions/20260503_008_scan_github_columns.py` (status enum + 6 github columns); `app/routes/scans_from_github.py:79-180` (POST handler); `dashboard/app/api/scan-status/route.ts` proxy; `ScanPendingClient.tsx` (CC-14 setInterval(2000) + cancelled flag); `renderScanByStatus.tsx` status-gate; 11 polling tests pass |

**Note on requirement IDs:** GH-01..GH-05 are **phase-internal** requirement IDs that live in `07.5-CONTEXT.md` and PLAN frontmatters only. They are deliberately NOT promoted to `.planning/REQUIREMENTS.md` — Plan 11 SUMMARY.md decision-3 documents this: "the global requirements taxonomy already covers GitHub integration via the WBH-* category (Phase 8 webhook). Phase-internal IDs stay scoped to the phase artifacts to avoid double-bookkeeping." The cross-link is `07.5-PHASE-VERIFICATION.md` Section 2. This is an intentional design choice, recorded transparently. No orphaned requirements.

### Anti-Patterns Found

| File | Line | Pattern | Severity | Impact |
|------|------|---------|----------|--------|
| `backend/tests/test_services_scans.py` | 122,182,243 (3 tests) | Test calls `get_sessionmaker()` without monkeypatching `app.db.session._engine` to point at the testcontainer | ⚠️ Warning | 3 unit tests fail to connect (`InvalidCatalogNameError: database "infracanvas_test" does not exist`). Production code is fine; same `finalize_scan` paths are exercised end-to-end by `tests/jobs/test_scan_repo.py` (13/13 pass), so functional coverage is preserved. **Not a regression in shipped code; a test-wiring miss in Plan 05.** |
| `backend/app/storage/r2.py` | 139 | `get_bytes` returns `obj["Body"].read()` typed as Any → declared bytes (mypy `no-any-return`) | ℹ️ Info | Mypy strict warning; runtime behavior is correct (returns bytes). Same pattern as 3 pre-existing functions in r2.py (lines 62, 81, 95). |
| `backend/app/services/scans.py` | 134, 162 | Unused `# type: ignore[attr-defined]` comments on `stripe.error.StripeError` | ℹ️ Info | Stripe SDK type stubs evolved; comments could be removed. No runtime impact. |

### Behavioral Spot-Checks

| Behavior | Command | Result | Status |
|----------|---------|--------|--------|
| Backend Phase 7.5 test surface | `pytest tests/api/test_github_* tests/api/test_scans_from_github tests/jobs/test_scan_repo tests/integrations/github/ --no-cov` | 71 passed in 30.5s | ✓ PASS |
| Dashboard full test suite | `cd dashboard && npm test -- --run` | 225 passed (27 files) in 11.5s | ✓ PASS |
| Dashboard typecheck | `cd dashboard && npx tsc --noEmit` | 1 error (deferred TS6133, pre-existing) | ⚠️ ACCEPTED (deferred-items.md) |
| T1–T9 cross-cutting greps | manually re-run all 9 | 8/9 green; T1 threshold pedantic but intent satisfied | ✓ PASS |
| Migration files exist | `ls backend/migrations/versions/20260503_*` | 007 + 008 both present | ✓ PASS |
| GitHub App secrets reference (no PEM in source) | `grep -rE "BEGIN (RSA )?PRIVATE KEY" backend/app/` | 0 | ✓ PASS |

### Human Verification Required

#### 1. Manual Smoke Re-run (already completed once on 2026-05-05)

**Test:** Walk all 16 numbered checkpoints in `07.5-MANUAL-SMOKE.md` (S1–S16) end-to-end against a real GitHub App installation, real GitHub org, sandbox Terraform fixture repo, live Fly worker, and live Stripe test dashboard.
**Expected:** All 16 pass; install URL flow → callback redirect → repo+branch picking → POST /v1/scans/from-github → /scans/{id} pending → polling → ready; Stripe meter event in test dashboard; R2 object at `teams/{team_id}/scans/{scan_id}.json`; `fly logs | grep -E "ghs_|x-access-token"` returns zero matches.
**Why human:** GitHub App registration UI is interactive (P1 pre-flight), private key handoff via clipboard (P2), `flyctl secrets set` requires operator auth (P3), Stripe meter UI confirmation is human-only (S13), R2 console object check is UI-based (S14), `fly logs` grep over a live smoke window (S16) cannot be reliably automated.
**Status:** **Already confirmed on 2026-05-05 by operator** — resume signal "approved" recorded at Plan 11 Task 2 `checkpoint:human-verify` gate. Re-walk required only if a future deploy or env change happens.

### Gaps Summary

**Functional verification: PASSED.** All 5 phase truths are satisfied with concrete file:line evidence in the live working tree. All 5 phase-internal requirement IDs (GH-01..GH-05) trace cleanly to implementation files and tests. All 9 cross-cutting truth checks (T1–T9) are green or have a documented intent-satisfied note. The dashboard ships 225/225 green tests; the Phase 7.5 backend test surface ships 71/71 green tests; backend full suite is 169/172 (3 known test-only failures in `test_services_scans.py` due to a test-wiring gap in Plan 05 — production code works).

**Quality gate notes (none blocking):**
1. **Dashboard typecheck:** 1 pre-existing TS6133 in `scan-filters.test.tsx` is documented in `deferred-items.md` and was NOT introduced by Phase 7.5 — accepted carryover.
2. **Backend ruff:** 11 errors, all in **pre-existing files** untouched by Phase 7.5 (`db/models.py`, `db/session.py`, `queue/tasks/indexing.py`, `routes/scans.py`, `routes/share.py`); 4 of these are explicitly deferred in `deferred-items.md` (Plan 05 scope boundary).
3. **Backend mypy:** 30 errors across 11 files; 28 are pre-existing or third-party-stub issues; 2 minor regressions in Phase 7.5 territory (unused `type: ignore` in `services/scans.py`; `no-any-return` on r2.get_bytes — same pattern as 3 pre-existing functions in same file). Not blocking.
4. **Test wiring miss:** `tests/test_services_scans.py` calls `app.db.session.get_sessionmaker()` without monkeypatching the engine to the testcontainer URL, causing 3 unit tests to fail with `InvalidCatalogNameError`. The `finalize_scan` production code IS exercised end-to-end by `tests/jobs/test_scan_repo.py` (13/13 pass), so functional coverage is preserved. **Recommend: a follow-up cleanup plan to apply the same `monkeypatch.setattr(sess_mod, "_engine", ...)` shim that `tests/api/conftest.py:128` uses, so the unit tests can run.**

**Status rationale:** Marked `human_needed` (not `passed`) because the goal-achievement model requires recording the manual smoke as a human-verification artifact. The smoke was completed by the operator on 2026-05-05 ("approved"); re-walk is required only on env or deploy changes. No blockers; no functional gaps.

---

_Verified: 2026-05-05T07:42:00+05:30_
_Verifier: Claude (gsd-verifier)_
_Cross-check: PHASE-VERIFICATION.md self-sign-off (2026-05-05) confirmed against live working tree — all file:line references trace; T1–T9 grep counts match within ±1 (T1's "8 hits" claim was a miscount of "5 + 3" — live shows 5 + 1 = 6 when including require_principal); all 5 routes ARE auth-gated as intended._
