---
phase: 11-firewall-integration
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - agent/internal/asa/testdata/asa-rest-acl.json
  - agent/internal/asa/testdata/asa-rest-nat.json
  - agent/internal/asa/testdata/asa-rest-objects.json
  - agent/internal/asa/testdata/show-running-config.txt
  - agent/internal/asa/rest_test.go
  - agent/internal/asa/ssh_test.go
  - agent/internal/fmc/testdata/fmc-token.json
  - agent/internal/fmc/testdata/fmc-access-policy.json
  - agent/internal/fmc/testdata/fmc-nat-policy.json
  - agent/internal/fmc/testdata/fmc-network-objects.json
  - agent/internal/fmc/client_test.go
  - agent/internal/checkpoint/testdata/ckp-login.json
  - agent/internal/checkpoint/testdata/ckp-access-rulebase.json
  - agent/internal/checkpoint/testdata/ckp-access-rulebase-import.json
  - agent/internal/checkpoint/testdata/ckp-nat-rulebase.json
  - agent/internal/checkpoint/testdata/ckp-objects.json
  - agent/internal/checkpoint/parser_test.go
  - agent/internal/checkpoint/live_test.go
  - agent/internal/checkpoint/import_test.go
  - agent/cmd/infracanvas-agent/main_test.go
  - backend/tests/test_routes_firewall.py
  - backend/tests/test_routes_firewall_read.py
  - backend/tests/test_schemas_firewall.py
  - backend/tests/conftest.py
autonomous: true
requirements: [ASA-01, ASA-02, ASA-03, CKP-01, CKP-02]
must_haves:
  truths:
    - "All Wave 0 fixtures from VALIDATION.md exist on disk and match real vendor JSON shapes"
    - "Every Phase 11 test command returns RED (compiles, references non-existent symbols, fails) — ready for downstream waves to GREEN"
    - "Paired live + import checkpoint fixtures lock D-12 shared-parser premise via TestParser_LiveImportEquivalence test stub"
    - "Existing TestDefaultIntervals (Phase 10) is updated to assert all 4 intervals (Routes/BGP/Flow/Firewall) — not replaced, EXTENDED"
  artifacts:
    - path: "agent/internal/checkpoint/testdata/ckp-access-rulebase.json"
      provides: "Live Checkpoint show access-rulebase response fixture"
    - path: "agent/internal/checkpoint/testdata/ckp-access-rulebase-import.json"
      provides: "Offline mgmt_cli --format json fixture (paired with live for D-12 equivalence)"
    - path: "agent/internal/checkpoint/parser_test.go"
      provides: "TestParser_LiveImportEquivalence locking shared-parser premise"
    - path: "backend/tests/test_routes_firewall.py"
      provides: "Push endpoint test stubs (snapshot_id idempotency, RLS, three endpoints)"
    - path: "backend/tests/test_routes_firewall_read.py"
      provides: "Read endpoint stubs (latest-per-device, cross-team isolation)"
  key_links:
    - from: "agent/internal/checkpoint/parser_test.go::TestParser_LiveImportEquivalence"
      to: "agent/internal/checkpoint/testdata/{ckp-access-rulebase,ckp-access-rulebase-import}.json"
      via: "Both fixtures parsed → assert equivalent Rules slice"
      pattern: "TestParser_LiveImportEquivalence"
---

<objective>
Wave 0 test scaffold: land every failing test and every fixture file referenced in VALIDATION.md and RESEARCH.md §"Wave 0 Gaps" so all downstream waves operate against compiling-but-RED tests. No production code is written here — only `_test.go` files, JSON/text fixtures, pytest stubs, and one targeted update to the existing TestDefaultIntervals to extend (not replace) the assertion to all 4 intervals.

Purpose: Nyquist test-first scaffold. Mirrors Phase 10 plan 10-01. Without this wave, every later task's `<verify>` would say "MISSING — Wave 0 must create {test_file} first."
Output: 25 new files (24 fixtures/tests + 1 conftest extension) and 1 modified file (main_test.go updated for D-02 4-ticker shape).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/11-firewall-integration/11-CONTEXT.md
@.planning/phases/11-firewall-integration/11-RESEARCH.md
@.planning/phases/11-firewall-integration/11-PATTERNS.md
@.planning/phases/11-firewall-integration/11-VALIDATION.md
@.planning/phases/10-dc-agent-core/10-01-SUMMARY.md
@agent/cmd/infracanvas-agent/main_test.go
@agent/internal/netconf/collector_test.go
@agent/internal/ssh/collector_test.go
@backend/tests/conftest.py

<interfaces>
<!-- Phase 10 Pusher interface that test stubs reference (will gain 3 firewall methods in 11-05) -->
From agent/cmd/infracanvas-agent/main.go (Phase 10 final state):
```go
type Intervals struct {
    Routes time.Duration
    BGP    time.Duration
    Flow   time.Duration
    // PHASE 11: Firewall time.Duration  ← test asserts this exists
}

type Pusher interface {
    PushRoutes(ctx context.Context, p push.RoutesPayload) error
    PushFlows(ctx context.Context, p push.FlowsPayload) error
    // PHASE 11: PushFirewallRules / PushFirewallNAT / PushFirewallObjects
}
```

From backend/app/routes/agent.py (Phase 10):
```python
async def push_routes(
    body: RoutesPushBody,
    principal: DCSitePrincipal = Depends(require_site_token),
) -> dict[str, bool]: ...
```

From agent/internal/netconf/collector_test.go (analog for httptest pattern):
- Uses httptest.Server with stubbed Dialer/Session interfaces
- Fixture-driven: testdata/*.xml read from disk
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Land all agent-side fixture files + RED test stubs (asa, fmc, checkpoint, main_test extension)</name>
  <files>agent/internal/asa/testdata/asa-rest-acl.json, agent/internal/asa/testdata/asa-rest-nat.json, agent/internal/asa/testdata/asa-rest-objects.json, agent/internal/asa/testdata/show-running-config.txt, agent/internal/asa/rest_test.go, agent/internal/asa/ssh_test.go, agent/internal/fmc/testdata/fmc-token.json, agent/internal/fmc/testdata/fmc-access-policy.json, agent/internal/fmc/testdata/fmc-nat-policy.json, agent/internal/fmc/testdata/fmc-network-objects.json, agent/internal/fmc/client_test.go, agent/internal/checkpoint/testdata/ckp-login.json, agent/internal/checkpoint/testdata/ckp-access-rulebase.json, agent/internal/checkpoint/testdata/ckp-access-rulebase-import.json, agent/internal/checkpoint/testdata/ckp-nat-rulebase.json, agent/internal/checkpoint/testdata/ckp-objects.json, agent/internal/checkpoint/parser_test.go, agent/internal/checkpoint/live_test.go, agent/internal/checkpoint/import_test.go, agent/cmd/infracanvas-agent/main_test.go</files>

  <read_first>
    - .planning/phases/11-firewall-integration/11-VALIDATION.md (Wave 0 Requirements list — fixture inventory)
    - .planning/phases/11-firewall-integration/11-RESEARCH.md §"Phase Requirements → Test Map" (line 779) — exact `-run TestXxx` names
    - .planning/phases/11-firewall-integration/11-PATTERNS.md (httptest + fixture-driven test analogs)
    - agent/internal/netconf/collector_test.go (httptest + Dialer/Session mock pattern — exact analog)
    - agent/internal/ssh/collector_test.go (fixture-from-disk pattern)
    - agent/cmd/infracanvas-agent/main_test.go (current TestDefaultIntervals shape — assert 3 intervals)
    - .planning/phases/10-dc-agent-core/10-01-SUMMARY.md (Wave 0 scaffold pattern from Phase 10)
  </read_first>

  <behavior>
    - rest_test.go: `TestRESTCollector_Pull` — httptest.Server returns asa-rest-acl.json/asa-rest-nat.json/asa-rest-objects.json on `/api/access/in/.../rules`, `/api/nat`, `/api/objects/networkobjects`; references `asa.NewRESTCollector(...).Pull(...)` (does not exist → compile RED)
    - rest_test.go: `TestRESTCollector_DisabledAPI` — httptest returns 401 → asserts non-retryable error
    - ssh_test.go: `TestSSHCollector_DisablesPager` — mock Session asserts first command in payload is `terminal pager 0` (or `terminal length 0` per ssh/collector.go line 96)
    - ssh_test.go: `TestSSHParser_RealConfig` — reads testdata/show-running-config.txt → asserts ≥1 access-list rule + ≥1 nat rule + ≥1 object parsed
    - client_test.go (fmc): `TestClient_TokenRefresh` — httptest serves fmc-token.json with X-auth-access-token header, then 401, then refresh succeeds; asserts exactly one refresh attempt before bail
    - client_test.go (fmc): `TestClient_PaginatedAccessRules` — httptest serves multi-page response (Link header or `next` cursor); asserts all rules collected
    - parser_test.go (checkpoint): `TestParser_LiveImportEquivalence` — calls `checkpoint.Parse(liveJSON, natJSON, objsJSON)` with both ckp-access-rulebase.json AND ckp-access-rulebase-import.json fixtures; asserts `reflect.DeepEqual` on resulting Rules/NATs/Objects (D-12 lock)
    - live_test.go: `TestLiveCollector_LoginPullLogout` — httptest sequence: POST /web_api/login → 200 + sid; GET show-* → 200 + fixture; POST /web_api/logout → 200; asserts exact 3-call ordering, asserts SID never appears in any zap log field
    - live_test.go: `TestLiveCollector_Paginates` — httptest returns offset/limit pages of 500 each
    - import_test.go: `TestImport_MatchesLiveShape` — reads three import fixture files; asserts output identical to TestParser_LiveImportEquivalence (proves D-12 import path goes through shared parser)
    - main_test.go EXTENSION: Update existing `TestDefaultIntervals` to assert `iv.Firewall == 1*time.Hour` alongside existing 3 assertions; ADD `TestRunDaemon_FirewallTick` stub that asserts the 4th ticker fires `collectAndPushFirewall` on tick (currently undefined symbol → RED)
    - All fixtures: minimal but realistic. ckp-access-rulebase.json should have ≥3 rules; ckp-access-rulebase-import.json must produce identical parser output (paired). show-running-config.txt should have ≥5 access-list lines + ≥3 nat lines + ≥3 object-group lines.
  </behavior>

  <action>
Mirror agent/internal/netconf/collector_test.go and agent/internal/ssh/collector_test.go exactly. Each `_test.go` MUST:
1. Live in `package <pkg>` (asa, fmc, checkpoint — not `<pkg>_test` external)
2. Import `testing` + `github.com/stretchr/testify/require` + `net/http/httptest` (where applicable) + `os` (for fixture reads via `os.ReadFile("testdata/...")`)
3. Reference symbols that DO NOT YET EXIST — compile errors are EXPECTED (RED). Use the exact names from PATTERNS.md per D-04 nomenclature: `asa.NewRESTCollector`, `asa.NewSSHCollector`, `fmc.NewClient`, `checkpoint.Parse`, `checkpoint.NewLiveCollector`, `checkpoint.LoadImport`.

Fixture content shape (use vendor doc references in RESEARCH §Sources):
- `asa-rest-acl.json` → ASA REST `/api/access/in/<iface>/rules` shape: `{"items": [{"position": 1, "sourceAddress": {...}, "destinationAddress": {...}, "permit": true, "protocol": {...}, "ruleId": ...}], "rangeInfo": {...}}`
- `asa-rest-nat.json` → ASA REST `/api/nat` shape with `originalSourceNetworkObject`, `translatedSourceNetworkObject`, etc.
- `fmc-token.json` → response body for `POST /api/fmc_platform/v1/auth/generatetoken` (note: real FMC returns headers not body; in fixture, store both as `{"X-auth-access-token": "tok-abc", "X-auth-refresh-token": "ref-xyz", "DOMAIN_UUID": "uuid-1"}` so the test stub serves them as headers)
- `ckp-login.json` → `{"sid": "sid-test-12345", "uid": "uid-1", "session-timeout": 3600}`
- `ckp-access-rulebase.json` → live API shape: `{"rulebase": [{"uid": "...", "rule-number": 1, "name": "...", "source": [{"uid": "..."}], "destination": [{"uid": "..."}], "service": [{"uid": "..."}], "action": {"name": "Accept"}}], "total": N, "to": N}`
- `ckp-access-rulebase-import.json` → mgmt_cli `--format json` shape (same fields, may differ in envelope): MUST produce identical parser output as live fixture
- `show-running-config.txt` → ASA running config with at least:
  ```
  object network web-server
   host 10.1.1.10
  object-group network DMZ-NETS
   network-object 10.2.0.0 255.255.0.0
  access-list outside_in extended permit tcp any host web-server eq www
  access-list outside_in extended deny ip any any log
  nat (inside,outside) source dynamic any interface
  ```

For `main_test.go`, locate the existing `TestDefaultIntervals` and modify the assertion block — DO NOT delete the existing 3 assertions. Append:
```go
require.Equal(t, 1*time.Hour, iv.Firewall, "Phase 11 D-02: 4th interval Firewall=1h")
```
Then APPEND a new test:
```go
func TestRunDaemon_FirewallTick(t *testing.T) {
    // RED until Plan 11-07 lands collectAndPushFirewall + Intervals.Firewall + 4th ticker
    // Asserts: ticker with Firewall=10ms fires collectAndPushFirewall ≥1× within 100ms,
    //          mock Pusher.PushFirewallRules called with snapshot_id non-empty,
    //          ctx cancel drains the in-flight goroutine before run() returns.
    t.Skip("Wave 0 stub — fill in once 4th ticker exists (Plan 11-07)")
}
```
The `t.Skip` keeps the suite RED-but-runnable. Plan 11-07 will remove the skip and write the real assertion.
  </action>

  <verify>
    <automated>cd agent &amp;&amp; go test -race ./internal/asa/... ./internal/fmc/... ./internal/checkpoint/... ./cmd/infracanvas-agent/... 2>&amp;1 | grep -v '^#' | grep -E '(FAIL|undefined|cannot find|ok)' | head -40</automated>
  </verify>

  <acceptance_criteria>
    - All 16 fixture files exist: `find agent/internal/{asa,fmc,checkpoint}/testdata -type f | wc -l` returns 13 (asa: 4, fmc: 4, checkpoint: 5)
    - All 7 new `_test.go` files exist under agent/internal/{asa,fmc,checkpoint}/
    - `cd agent && go vet ./internal/asa/... ./internal/fmc/... ./internal/checkpoint/... 2>&1` reports "undefined" for `NewRESTCollector`, `NewSSHCollector`, `NewClient`, `Parse`, `NewLiveCollector`, `LoadImport` (RED — production code does not exist yet, this is correct)
    - `grep -l 'TestParser_LiveImportEquivalence' agent/internal/checkpoint/parser_test.go` returns the file
    - `grep -c 'iv.Firewall' agent/cmd/infracanvas-agent/main_test.go` ≥ 1 (extension landed, not replacement)
    - `grep -c 'func TestDefaultIntervals' agent/cmd/infracanvas-agent/main_test.go` == 1 (still the same single function — extended in place, not duplicated)
    - `grep -c 'func TestRunDaemon_FirewallTick' agent/cmd/infracanvas-agent/main_test.go` == 1 (new stub added)
    - `cd agent && go build ./...` exits non-zero (production code does not yet exist for the test references — RED is the goal)
    - Paired fixtures: `ls agent/internal/checkpoint/testdata/ckp-access-rulebase.json agent/internal/checkpoint/testdata/ckp-access-rulebase-import.json` both exist
    - `show-running-config.txt` contains the strings `access-list`, `nat (`, `object network`, `object-group network` (grep returns lines for each)
  </acceptance_criteria>

  <done>All 23 fixture/test files committed. Test compile-RED status verified (undefined-symbol errors point at production symbols Plan 11-05/06/08/09/10/11 will create). main_test.go extended in place with the 4th interval assertion + skipped TestRunDaemon_FirewallTick stub. No production code touched.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Land backend-side pytest stubs + conftest extension</name>
  <files>backend/tests/test_routes_firewall.py, backend/tests/test_routes_firewall_read.py, backend/tests/test_schemas_firewall.py, backend/tests/conftest.py</files>

  <read_first>
    - .planning/phases/11-firewall-integration/11-VALIDATION.md (Wave 0 Requirements — backend test list)
    - .planning/phases/11-firewall-integration/11-RESEARCH.md §"Phase Requirements → Test Map" lines 793-797 (D-08, D-09, D-11, D-18 test names)
    - .planning/phases/11-firewall-integration/11-PATTERNS.md (Pattern A site-token, Pattern B Clerk JWT, Pattern E idempotent snapshot_id)
    - backend/tests/conftest.py (existing fixtures — testcontainers Postgres, mock_clerk, bypass_role.sql, dc_site_token fixture from Phase 10)
    - backend/app/routes/agent.py (existing push_routes / push_flows handlers — analog signatures)
    - backend/app/routes/github.py (existing list_installations_endpoint — Clerk JWT analog for read API)
    - backend/app/schemas/agent.py (existing RoutesPushBody — Pydantic Field max_length=10000 pattern, T-10-02-06)
  </read_first>

  <behavior>
    - test_schemas_firewall.py: `test_firewall_rules_push_body_max_length` — instantiating `FirewallRulesPushBody(rules=[<50001 items>])` raises ValidationError (T-11-04-01 pattern, max_length=50000)
    - test_schemas_firewall.py: `test_firewall_rule_hybrid_shape` — asserts `FirewallRule` has BOTH normalized columns (`src_cidr`, `dst_cidr`, `action`, `protocol`, `ports`, `src_zone`, `dst_zone`, `position`) AND `raw_blob` (D-08 hybrid)
    - test_schemas_firewall.py: `test_firewall_nat_push_body_normalized_columns` — asserts `FirewallNATRule` has `src_translation`, `dst_translation`, `interface_in`, `interface_out` (D-15 Phase 12 forward-feed)
    - test_routes_firewall.py: `test_push_firewall_rules_writes_snapshot_and_rules` — POSTs body to `/v1/agent/firewall-rules` with valid site_token → 202, then SQL probes `firewall_ruleset_snapshots` and `firewall_rules` tables show the row (D-08, D-18)
    - test_routes_firewall.py: `test_idempotent_snapshot_id` — POST same `snapshot_id` twice → both return 202, `firewall_ruleset_snapshots` row count == 1 (Pattern E)
    - test_routes_firewall.py: `test_three_endpoints_share_snapshot_id` — POST to all three endpoints in any order with same `snapshot_id` → parent row created exactly once, all three child tables populated
    - test_routes_firewall.py: `test_push_rejects_missing_bearer` — POST without Authorization header → 401
    - test_routes_firewall.py: `test_push_firewall_objects_persists` — POST to `/v1/agent/firewall-objects` writes `firewall_objects` rows with `kind` enum (`host`/`network`/`group`/`service`) and `value` JSONB (D-09)
    - test_routes_firewall_read.py: `test_returns_latest_per_device` — seed two snapshots for same firewall_id at different snapshot_ts → `GET /v1/sites/{site_id}/firewall-rules` returns only the newer one (D-11)
    - test_routes_firewall_read.py: `test_cross_team_isolation` — Clerk JWT for team A → query for site owned by team B → returns empty list (RLS)
    - test_routes_firewall_read.py: `test_requires_clerk_jwt` — request without Clerk Bearer → 401
    - conftest.py extension: ADD a `firewall_snapshot` fixture that takes a session + site_id + firewall_id and inserts one parent + N child rows for downstream test reuse
  </behavior>

  <action>
Mirror backend/tests/test_routes_dc_agent.py (Phase 10) for the push-endpoint stubs and backend/tests/test_routes_github.py for the read-endpoint stubs.

Each test file MUST:
1. Use `pytest.mark.asyncio` and `httpx.AsyncClient` (project convention)
2. Reference symbols that do not exist yet — `FirewallRulesPushBody`, `FirewallNATPushBody`, `FirewallObjectsPushBody`, `firewalls_routes` — collection-RED is expected
3. Use the existing `dc_site_token` fixture (Phase 10 conftest) for site-token auth
4. Use the existing `mock_clerk` fixture for Clerk JWT in read-endpoint tests
5. Wrap DB probes in `async with sm() as session, session.begin(): await session.execute(text("SELECT set_config('app.current_team_id', :t, true)"), {"t": str(team_id)})` BEFORE any SELECT (Pattern B — RLS context-set)

Fixture skeleton in `conftest.py`:
```python
@pytest_asyncio.fixture
async def firewall_snapshot(db_session, dc_site):
    """Seed one populated firewall snapshot for read-endpoint tests."""
    snapshot_id = uuid.uuid4()
    # INSERT firewall_ruleset_snapshots, firewall_rules, firewall_nat_rules, firewall_objects
    # Returns dict: {"snapshot_id": ..., "site_id": ..., "firewall_id": ...}
    ...
```
This fixture will fail at collection until Plan 11-02 lands the migration + ORM models — that is the intended RED state.

For `test_push_rejects_missing_bearer`, omit the `Authorization` header entirely; expected 401 from `require_site_token` (Phase 10 dependency, already in place — this is the only test that should be collection-GREEN even before Plan 11-02 since it just hits a non-existent route → 404).

For `test_idempotent_snapshot_id`, the assertion shape:
```python
async with sm() as session, session.begin():
    await session.execute(text("SELECT set_config('app.current_team_id', :t, true)"), {"t": str(team_id)})
    count = (await session.execute(
        text("SELECT count(*) FROM firewall_ruleset_snapshots WHERE snapshot_id = :sid"),
        {"sid": snapshot_id},
    )).scalar()
assert count == 1, "Pattern E: ON CONFLICT DO NOTHING — second push must not duplicate parent"
```
  </action>

  <verify>
    <automated>cd backend &amp;&amp; pytest tests/test_schemas_firewall.py tests/test_routes_firewall.py tests/test_routes_firewall_read.py --collect-only 2>&amp;1 | tail -20</automated>
  </verify>

  <acceptance_criteria>
    - All 3 backend test files exist: `ls backend/tests/test_routes_firewall.py backend/tests/test_routes_firewall_read.py backend/tests/test_schemas_firewall.py`
    - `grep -c 'def test_' backend/tests/test_routes_firewall.py` ≥ 5 (push tests)
    - `grep -c 'def test_' backend/tests/test_routes_firewall_read.py` ≥ 3 (read tests)
    - `grep -c 'def test_' backend/tests/test_schemas_firewall.py` ≥ 3 (schema tests)
    - `grep -c 'firewall_snapshot' backend/tests/conftest.py` ≥ 1 (fixture added — search excludes comments via `grep -v '^#' backend/tests/conftest.py | grep -c firewall_snapshot` ≥ 1)
    - `cd backend && pytest tests/test_routes_firewall.py --collect-only 2>&1 | grep -E 'ImportError|ModuleNotFoundError'` returns lines (RED — `app.schemas.firewall` does not exist yet)
    - `grep -c 'set_config.*app.current_team_id' backend/tests/test_routes_firewall.py` ≥ 1 (Pattern B applied to DB probes)
    - `grep -c 'max_length=50000' backend/tests/test_schemas_firewall.py` ≥ 1 (T-11-04-01 bound asserted)
  </acceptance_criteria>

  <done>All 3 backend test files + conftest extension committed. Tests are collection-RED: import errors for non-existent `app.schemas.firewall` and `app.routes.firewalls` modules confirm tests reference future Plan 11-02/03/04 symbols. The `firewall_snapshot` fixture is in place for downstream test reuse.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| TB-1 (test→fixture-disk) | Test reads JSON fixture from disk — no untrusted input crosses the boundary in test runtime |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-01-01 | Tampering | adversary commits malformed fixture to repo | accept | Code review + CI test failure surface; fixtures live in repo, not from external source |
| T-11-01-02 | Information Disclosure | fixture accidentally contains real customer data | mitigate | All fixtures hand-crafted with synthetic IPs (10.x, 192.168.x, RFC1918), synthetic hostnames (example.com), synthetic UIDs (uuid-test-XXX); reviewer checks no real serial/cert/secret strings |
</threat_model>

<verification>
After both tasks complete:
- `cd agent && go test -race ./... 2>&1 | head -30` shows compile errors / undefined-symbol errors for production symbols (RED is correct)
- `cd backend && pytest --collect-only 2>&1 | tail -10` shows ImportError for `app.schemas.firewall` and `app.routes.firewalls` (RED is correct)
- All Wave 0 fixture files from VALIDATION.md exist
- `git log --oneline | head -2` shows two commits: `test(11-01): land agent fixtures + RED test stubs` and `test(11-01): land backend pytest stubs`
</verification>

<success_criteria>
- 25 new files + 1 modified file (main_test.go) committed
- `go test ./... -race` is RED with undefined-symbol errors for the production symbols Wave 1-3 will create
- `pytest --collect-only` is RED with ImportError for the modules Wave 1 will create
- Paired live + import Checkpoint fixtures exist (D-12 lock)
- TestDefaultIntervals updated in place (not duplicated, not replaced) to assert all 4 intervals
</success_criteria>

<output>
After completion, create `.planning/phases/11-firewall-integration/11-01-SUMMARY.md` per template.
</output>
