---
phase: 00-validation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - landing/package.json
  - landing/tsconfig.json
  - landing/next.config.ts
  - landing/tailwind.config.ts
  - landing/app/layout.tsx
  - landing/app/page.tsx
  - landing/app/globals.css
  - landing/components/Nav.tsx
  - landing/components/Hero.tsx
  - landing/components/DemoVideo.tsx
  - landing/components/ValueProps.tsx
  - landing/components/FoundingMember.tsx
  - landing/components/TypeformCTA.tsx
  - landing/components/Footer.tsx
  - landing/.env.example
  - landing/.gitignore
autonomous: true
requirements:
  - VAL-03
must_haves:
  truths:
    - "Landing page renders all 7 sections in correct order: Nav, Hero, DemoVideo, ValueProps, FoundingMember, TypeformCTA, Footer"
    - "Primary CTA links to Stripe Payment Link via NEXT_PUBLIC_STRIPE_PAYMENT_LINK env var"
    - "Secondary CTA links to Typeform via NEXT_PUBLIC_TYPEFORM_URL env var"
    - "Spot counter displays spotsRemaining in both Hero and FoundingMember sections"
    - "Page builds as static Next.js site with zero client-side data fetching"
  artifacts:
    - path: "landing/app/page.tsx"
      provides: "Main landing page assembling all sections"
      contains: "SPOTS_REMAINING"
    - path: "landing/app/layout.tsx"
      provides: "Root layout with Inter font, meta tags, og tags"
      contains: "InfraCanvas"
    - path: "landing/components/FoundingMember.tsx"
      provides: "Stripe CTA section with spot counter"
      contains: "founding-member"
    - path: "landing/components/Hero.tsx"
      provides: "Hero section with pain hook, headline, CTAs"
      contains: "5 tabs open. 0 clarity."
  key_links:
    - from: "landing/app/page.tsx"
      to: "landing/components/Hero.tsx"
      via: "import and render with spotsRemaining prop"
      pattern: "spotsRemaining={SPOTS_REMAINING}"
    - from: "landing/components/Hero.tsx"
      to: "NEXT_PUBLIC_STRIPE_PAYMENT_LINK"
      via: "process.env.NEXT_PUBLIC_STRIPE_PAYMENT_LINK"
      pattern: "process\\.env\\.NEXT_PUBLIC_STRIPE_PAYMENT_LINK"
---

<objective>
Build the InfraCanvas landing page (infracanvas.dev) as a Next.js 15 App Router static site with all 7 sections specified in the UI-SPEC.

Purpose: This is the soft-funnel destination for all community posts (per D-06). It converts traffic into Typeform completions and Stripe founding member checkouts. All copy is locked per the UI-SPEC copywriting contract.

Output: A complete, buildable Next.js project in `landing/` with all components, ready for Vercel deployment.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/00-validation/00-CONTEXT.md
@.planning/phases/00-validation/00-RESEARCH.md
@.planning/phases/00-validation/00-UI-SPEC.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Scaffold Next.js landing page project with layout and global styles</name>
  <files>
    landing/package.json,
    landing/tsconfig.json,
    landing/next.config.ts,
    landing/app/layout.tsx,
    landing/app/globals.css,
    landing/.env.example,
    landing/.gitignore
  </files>
  <read_first>
    .planning/phases/00-validation/00-UI-SPEC.md,
    .planning/phases/00-validation/00-RESEARCH.md,
    viewer/package.json
  </read_first>
  <action>
    Create a new Next.js 15 App Router project in `landing/` directory. Do NOT use `create-next-app` — scaffold manually for minimal footprint.

    **package.json**: Create with these dependencies:
    - `next`: `^15.0.0`
    - `react`: `^18.3.1`
    - `react-dom`: `^18.3.1`
    - `tailwindcss`: `^4.1.0`
    - `@tailwindcss/postcss`: `^4.1.0`
    - `typescript`: `^5.8.0`
    - `@types/react`: `^18.3.0`
    - `@types/react-dom`: `^18.3.0`
    Scripts: `dev`, `build`, `start`, `lint`

    **next.config.ts**: Static export config:
    ```typescript
    const nextConfig = { output: 'export' }
    ```
    Note: Remove `output: 'export'` if Vercel deployment — Vercel handles SSG natively without export mode. Include a comment noting this.

    **tsconfig.json**: Target ES2020, strict mode, JSX preserve, paths alias `@/*` to `./`.

    **app/layout.tsx**: Root layout with:
    - Inter font loaded via `next/font/google` with `display: 'swap'`, `subsets: ['latin']`
    - `<html lang="en" className="dark">` with `antialiased` on body
    - `<title>`: `InfraCanvas — Your infrastructure, visualised`
    - `<meta name="description">`: `One command gives you a complete, annotated picture of your hybrid infrastructure — security blind spots, cost, and drift — across AWS, Azure, and physical data centres.`
    - `og:title`: `InfraCanvas — One command. Your entire infrastructure.`
    - `og:description`: same as meta description
    - `og:image`: placeholder path `/og-image.png` (actual image created separately)
    - Body classes: `bg-slate-950 text-slate-50`
    - Wrap children in `<header>` (Nav) imported at layout level, `<main>`, `<Footer>` imported at layout level
    - Per UI-SPEC: `<header>` wraps Nav, `<main>` wraps page content, `<footer>` wraps Footer

    **app/globals.css**: Import Tailwind v4:
    ```css
    @import "tailwindcss";
    ```

    **postcss.config.mjs**: Required for Tailwind v4:
    ```javascript
    export default { plugins: { '@tailwindcss/postcss': {} } }
    ```

    **.env.example**: Three env vars with placeholder values:
    ```
    NEXT_PUBLIC_STRIPE_PAYMENT_LINK=https://pay.stripe.com/your-link-here
    NEXT_PUBLIC_TYPEFORM_URL=https://form.typeform.com/to/your-form-id
    NEXT_PUBLIC_DEMO_VIDEO_URL=https://www.loom.com/embed/your-video-id
    ```

    **.gitignore**: Standard Next.js gitignore (node_modules, .next, out, .env.local)
  </action>
  <verify>
    <automated>cd landing && npm install && npx next build 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `landing/package.json` contains `"next"` and `"tailwindcss"` as dependencies
    - `landing/app/layout.tsx` contains `InfraCanvas — Your infrastructure, visualised` as title text
    - `landing/app/layout.tsx` contains `next/font/google` import for Inter
    - `landing/app/layout.tsx` contains `og:title` with value `InfraCanvas — One command. Your entire infrastructure.`
    - `landing/app/globals.css` contains `@import "tailwindcss"`
    - `landing/.env.example` contains `NEXT_PUBLIC_STRIPE_PAYMENT_LINK=`
    - `landing/.env.example` contains `NEXT_PUBLIC_TYPEFORM_URL=`
    - `landing/.env.example` contains `NEXT_PUBLIC_DEMO_VIDEO_URL=`
    - `npx next build` in landing/ exits with code 0
  </acceptance_criteria>
  <done>Next.js 15 project scaffolded, builds successfully, Inter font loads, meta/OG tags set per UI-SPEC SEO contract</done>
</task>

<task type="auto">
  <name>Task 2: Implement all 7 landing page components and assemble page</name>
  <files>
    landing/app/page.tsx,
    landing/components/Nav.tsx,
    landing/components/Hero.tsx,
    landing/components/DemoVideo.tsx,
    landing/components/ValueProps.tsx,
    landing/components/FoundingMember.tsx,
    landing/components/TypeformCTA.tsx,
    landing/components/Footer.tsx
  </files>
  <read_first>
    .planning/phases/00-validation/00-UI-SPEC.md,
    landing/app/layout.tsx
  </read_first>
  <action>
    Implement all 7 components per the UI-SPEC section specifications. All copy is from the locked Copywriting Contract in UI-SPEC — do not rephrase any text.

    **landing/app/page.tsx**:
    - Define `const SPOTS_REMAINING = 50` at module level (not in a component)
    - Import and render sections in order: Hero, DemoVideo, ValueProps, FoundingMember, TypeformCTA
    - Nav and Footer are rendered in layout.tsx, NOT in page.tsx
    - Pass `spotsRemaining={SPOTS_REMAINING}` to Hero and FoundingMember
    - Pass `embedUrl={process.env.NEXT_PUBLIC_DEMO_VIDEO_URL || ''}` to DemoVideo

    **components/Nav.tsx** (per D-06):
    - Fixed top: `fixed top-0 left-0 right-0 z-50 backdrop-blur-sm bg-slate-950/80`
    - Height: `h-14` with `px-6 flex items-center justify-between`
    - Left: `InfraCanvas` in `font-bold text-slate-50 text-lg`
    - Right: `<a href="#founding-member">Get Early Access</a>` styled as small amber link/button
    - No hamburger, no nav links

    **components/Hero.tsx** (per D-01, D-04, D-06, D-08, D-09):
    - Props: `{ spotsRemaining: number }`
    - Layout: `pt-32 pb-20 max-w-3xl mx-auto text-center px-6`
    - Pain hook: `<p className="text-amber-400 text-sm font-mono uppercase tracking-widest">5 tabs open. 0 clarity.</p>`
    - Headline: `<h1 className="text-5xl lg:text-6xl font-extrabold text-slate-50 leading-tight mt-4">One command. Your entire infrastructure — visualised, scored, explained.</h1>`
    - Subtext: `<p className="text-lg text-slate-300 mt-4 max-w-2xl mx-auto">InfraCanvas scans your Terraform directory and opens an interactive diagram with security findings, cost estimates, and drift detection. AWS, Azure, and physical data centres — in a single view.</p>`
    - CLI block: `<div className="mt-8 bg-slate-900 border border-slate-800 rounded-lg px-6 py-4 font-mono text-sm text-slate-50 inline-block">$ infracanvas scan ./terraform</div>`
    - CTA buttons: `<div className="mt-8 flex flex-col sm:flex-row gap-4 justify-center">`
      - Primary: `<a href={process.env.NEXT_PUBLIC_STRIPE_PAYMENT_LINK} className="bg-amber-400 text-slate-950 font-bold px-8 py-3 rounded-lg">Claim Founding Member Spot — $49/mo</a>`
      - Secondary: `<a href={process.env.NEXT_PUBLIC_TYPEFORM_URL} className="border border-slate-600 text-slate-50 font-medium px-8 py-3 rounded-lg hover:bg-slate-800">Tell us what you need</a>`
    - Urgency: `<p className="text-sm text-amber-400 font-mono mt-4">{spotsRemaining} of 50 founding member spots remaining</p>`

    **components/DemoVideo.tsx**:
    - Props: `{ embedUrl: string }`
    - Layout: `py-16 max-w-4xl mx-auto px-6`
    - Label: `<p className="text-sm font-mono text-amber-400 uppercase tracking-widest mb-4">See it in action</p>`
    - If embedUrl is truthy: render `<iframe src={embedUrl} className="w-full aspect-video rounded-xl border border-slate-800" title="InfraCanvas demo video" allowFullScreen />`
    - If embedUrl is falsy: render a placeholder div with `bg-slate-900 w-full aspect-video rounded-xl border border-slate-800 flex items-center justify-center` and text "Video coming soon" in `text-slate-500`
    - Caption: `<p className="text-sm text-slate-400 mt-3 text-center">2-minute walkthrough: scan → diagram → security findings → score card</p>`

    **components/ValueProps.tsx**:
    - No props (static content)
    - Layout: `py-16 max-w-4xl mx-auto px-6`
    - Section heading: `<h2 className="text-3xl font-bold text-slate-50 text-center mb-12">Everything you need to understand your infrastructure</h2>`
    - Grid: `<div className="grid grid-cols-1 md:grid-cols-3 gap-6">`
    - Card 1 (Canvas): icon in `text-amber-400` (inline SVG map icon), title "Visual infrastructure map", body "One command generates an interactive diagram of your AWS and Azure resources, grouped by VPC and subnet, with dependency edges. No manual drawing.", badge "AWS + Azure" in `text-xs font-mono text-slate-400`
    - Card 2 (Security): icon in `text-red-400` (inline SVG shield icon), title "Security blind spots, surfaced", body "10 built-in AWS security rules (S3 public access, IAM wildcards, unencrypted RDS) with severity ratings and remediation steps. No config required.", badge "10 rules at launch"
    - Card 3 (Cost+Drift): icon in `text-green-400` (inline SVG chart icon), title "Cost and drift in the same view", body "See cost estimates per resource and flag drift between your Terraform state and what's actually deployed. No more surprises on the AWS bill.", badge "Coming in v1.0" in `text-xs font-mono text-orange-400`
    - Each card: `bg-slate-900 border border-slate-800 rounded-xl p-6`
    - Title: `text-xl font-semibold text-slate-50 mt-4`
    - Body: `text-slate-300 text-sm mt-2`

    **components/FoundingMember.tsx** (per D-08, D-09):
    - Props: `{ spotsRemaining: number }`
    - ID: `id="founding-member"` on the outermost `<section>`
    - Layout: `py-16 max-w-2xl mx-auto text-center px-6`
    - Container: `bg-slate-900 border border-amber-400/20 rounded-2xl p-10`
    - Counter: `<p className="text-amber-400 font-mono text-sm mb-2">{spotsRemaining} of 50 founding member spots remaining</p>`
    - Heading: `<h2 className="text-3xl font-bold text-slate-50 mb-2">Lock in $49/mo — forever</h2>`
    - Subtext: `<p className="text-slate-300 text-base mb-6">Price locks at $49/mo for founding members. When we launch publicly, pricing goes up. You'll also get a private Discord channel for direct roadmap input.</p>`
    - Benefits list: text-left, max-w-sm mx-auto, mb-8, space-y-2, each item has a checkmark character and text in `text-slate-300 text-sm`:
      - "$49/mo locked forever (no price increases, ever)"
      - "Private Discord channel — direct access to the founder"
      - "Input on which features ship first"
      - "Early access to every phase as it ships"
    - CTA: `<a href={process.env.NEXT_PUBLIC_STRIPE_PAYMENT_LINK} className="block bg-amber-400 text-slate-950 font-bold text-lg px-10 py-4 rounded-xl w-full" aria-label="Claim your founding member spot for $49 per month">Claim Your Founding Member Spot</a>`
    - Disclaimer: `<p className="text-slate-500 text-xs mt-4">Secure checkout via Stripe. Cancel anytime. No questions asked.</p>`

    **components/TypeformCTA.tsx**:
    - No props (reads env var directly)
    - Layout: `py-16 text-center max-w-2xl mx-auto px-6`
    - Heading: `<h2 className="text-2xl font-bold text-slate-50 mb-3">Not ready to pay yet? Help us build this right.</h2>`
    - Body: `<p className="text-slate-300 text-base mb-6">We're talking to engineers who manage Terraform-managed infrastructure. 2 minutes. No sales call.</p>`
    - CTA: `<a href={process.env.NEXT_PUBLIC_TYPEFORM_URL} className="inline-block border border-slate-600 text-slate-50 font-medium px-8 py-3 rounded-lg hover:bg-slate-800">Answer 7 questions →</a>`
    - Note: `<p className="text-slate-500 text-sm mt-3">We read every response. High-signal respondents get invited for a 15-min call.</p>`

    **components/Footer.tsx**:
    - Wrap in `<footer>` tag
    - Layout: `border-t border-slate-800 py-8 text-center`
    - Text: `<p className="text-slate-500 text-sm">© 2026 InfraCanvas. No spam. No sales calls. Just infrastructure clarity.</p>`

    **Accessibility per UI-SPEC**:
    - All external `<a>` CTAs: add `target="_blank" rel="noopener noreferrer"` and descriptive `aria-label`
    - Video iframe: `title="InfraCanvas demo video"`
    - Single `<h1>` in Hero. All other headings use `<h2>`
    - Layout wraps Nav in `<header>`, page content in `<main>`, Footer in `<footer>`

    **Icons**: Use inline SVG elements for the 3 value prop icons (map/grid for Canvas, shield for Security, bar-chart for Cost). Do NOT install any icon package. Simple 24x24 SVGs.
  </action>
  <verify>
    <automated>cd landing && npx next build 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `landing/app/page.tsx` contains `const SPOTS_REMAINING = 50`
    - `landing/app/page.tsx` contains `spotsRemaining={SPOTS_REMAINING}` (passed to components)
    - `landing/components/Hero.tsx` contains exact string `5 tabs open. 0 clarity.`
    - `landing/components/Hero.tsx` contains exact string `One command. Your entire infrastructure`
    - `landing/components/Hero.tsx` contains `process.env.NEXT_PUBLIC_STRIPE_PAYMENT_LINK`
    - `landing/components/Hero.tsx` contains `process.env.NEXT_PUBLIC_TYPEFORM_URL`
    - `landing/components/Hero.tsx` contains `Claim Founding Member Spot`
    - `landing/components/FoundingMember.tsx` contains `id="founding-member"`
    - `landing/components/FoundingMember.tsx` contains `Lock in $49/mo`
    - `landing/components/FoundingMember.tsx` contains `border-amber-400/20`
    - `landing/components/FoundingMember.tsx` contains `process.env.NEXT_PUBLIC_STRIPE_PAYMENT_LINK`
    - `landing/components/Nav.tsx` contains `#founding-member`
    - `landing/components/DemoVideo.tsx` contains `title="InfraCanvas demo video"`
    - `landing/components/DemoVideo.tsx` contains `Video coming soon` (fallback)
    - `landing/components/ValueProps.tsx` contains `Visual infrastructure map`
    - `landing/components/ValueProps.tsx` contains `Security blind spots, surfaced`
    - `landing/components/ValueProps.tsx` contains `Cost and drift in the same view`
    - `landing/components/TypeformCTA.tsx` contains `Answer 7 questions`
    - `landing/components/TypeformCTA.tsx` contains `process.env.NEXT_PUBLIC_TYPEFORM_URL`
    - `landing/components/Footer.tsx` contains `2026 InfraCanvas`
    - No `@radix-ui`, `framer-motion`, or `shadcn` strings in `landing/package.json`
    - `npx next build` exits with code 0
  </acceptance_criteria>
  <done>All 7 sections render with exact copy from UI-SPEC Copywriting Contract. CTA buttons link to env-var URLs. Spot counter displays in Hero and FoundingMember. Page builds successfully as static site. No external UI library dependencies added.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Landing page -> Stripe | User clicks CTA, redirected to Stripe-hosted checkout. No payment data touches our site. |
| Landing page -> Typeform | User clicks CTA, redirected to Typeform-hosted survey. No form data touches our site. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-00-01 | Information Disclosure | Environment variables | mitigate | All env vars use NEXT_PUBLIC_ prefix (client-side only). No secrets in env vars — only public URLs (Stripe Payment Link, Typeform URL). Stripe secret keys are never in the landing page. |
| T-00-02 | Tampering | Landing page content | accept | Static site deployed on Vercel with immutable deployments. Content tampering requires Vercel account compromise, which is out of scope for a marketing page. |
| T-00-03 | Spoofing | Stripe Payment Link | accept | Link points to pay.stripe.com — Stripe's domain. Phishing risk is standard for any external link. No mitigation beyond using the correct URL. |
</threat_model>

<verification>
1. `cd landing && npx next build` completes without errors
2. `cd landing && npx next dev` renders all 7 sections visually
3. All copy matches UI-SPEC Copywriting Contract verbatim
4. Primary CTA links to NEXT_PUBLIC_STRIPE_PAYMENT_LINK env var value
5. Secondary CTA links to NEXT_PUBLIC_TYPEFORM_URL env var value
6. Nav "Get Early Access" scrolls to #founding-member
7. Spot counter shows "50 of 50" in both Hero and FoundingMember sections
</verification>

<success_criteria>
Landing page builds and renders all 7 sections per UI-SPEC with locked copy, env-var-driven CTAs, and no external UI dependencies beyond Tailwind CSS.
</success_criteria>

<output>
After completion, create `.planning/phases/00-validation/00-01-SUMMARY.md`
</output>
