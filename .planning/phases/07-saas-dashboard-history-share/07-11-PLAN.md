---
id: 07-11
phase: 07-saas-dashboard-history-share
plan: 11
type: execute
wave: 4
depends_on: ["07-02", "07-05"]
files_modified:
  - dashboard/app/(dashboard)/page.tsx
  - dashboard/components/home/ScoreCard.tsx
  - dashboard/components/home/ScoreSparkline.tsx
  - dashboard/components/home/TopFindings.tsx
  - dashboard/components/home/RecentScansTable.tsx
  - dashboard/app/(dashboard)/settings/layout.tsx
  - dashboard/app/(dashboard)/settings/members/page.tsx
  - dashboard/app/(dashboard)/settings/billing/page.tsx
  - dashboard/app/(dashboard)/settings/integrations/page.tsx
  - dashboard/__tests__/home-dashboard.test.tsx
  - dashboard/__tests__/settings-routes.test.tsx
autonomous: true
requirements: [DSH-05]
status: pending
must_haves:
  truths:
    - "/ renders latest scan score+grade card with finding counts and scan metadata (branch, commit, date)"
    - "/ renders a score sparkline (last 10 scans) using the handrolled SVG polyline from 07-06's Sparkline component"
    - "/ renders top 3 critical findings from the latest scan's summary_json.findings[] sorted by severity"
    - "/ renders a recent scans table (5 rows, columns: Date/Branch/Score/Crit/High) with View all → link to /scans"
    - "/ empty state (no scans) shows a CLI install hint card — no error, no crash"
    - "/settings/members renders Clerk <OrganizationProfile/> restyled with slate palette + amber primary"
    - "/settings/billing renders a Billing & invoices card with Open billing portal amber CTA (stub for v1 — backend endpoint deferred)"
    - "/settings/integrations renders two integration cards: Slack webhook URL input + disabled Connect GitHub button"
    - "Settings sub-nav uses shadcn <Tabs/> with URL-persisted active tab (not internal state)"
  artifacts:
    - path: "dashboard/components/home/ScoreCard.tsx"
      provides: "Latest scan score+grade+finding-counts card"
      contains: "ScoreCard"
    - path: "dashboard/components/home/ScoreSparkline.tsx"
      provides: "10-point SVG polyline sparkline with min/max dots"
      contains: "ScoreSparkline"
    - path: "dashboard/components/home/TopFindings.tsx"
      provides: "Top-3 critical findings list from summary_json"
      contains: "TopFindings"
    - path: "dashboard/components/home/RecentScansTable.tsx"
      provides: "5-row recent scans table with View all link"
      contains: "RecentScansTable"
    - path: "dashboard/app/(dashboard)/page.tsx"
      provides: "RSC home dashboard — fetches /v1/scans?limit=10, composes home components"
      contains: "export default"
    - path: "dashboard/app/(dashboard)/settings/layout.tsx"
      provides: "Settings sub-nav layout with shadcn Tabs routing to /settings/{members,billing,integrations}"
      contains: "settings"
    - path: "dashboard/app/(dashboard)/settings/members/page.tsx"
      provides: "Members page — Clerk OrganizationProfile"
      contains: "OrganizationProfile"
    - path: "dashboard/app/(dashboard)/settings/billing/page.tsx"
      provides: "Billing page — card with Open billing portal CTA"
      contains: "Open billing portal"
    - path: "dashboard/app/(dashboard)/settings/integrations/page.tsx"
      provides: "Integrations page — Slack + GitHub integration cards"
      contains: "Connect GitHub"
    - path: "dashboard/__tests__/home-dashboard.test.tsx"
      provides: "Unit + integration tests for ScoreCard, TopFindings, home RSC fixture"
      contains: "home-dashboard"
    - path: "dashboard/__tests__/settings-routes.test.tsx"
      provides: "Smoke tests for settings sub-route renders"
      contains: "settings-routes"
  key_links:
    - from: "dashboard/app/(dashboard)/page.tsx"
      to: "GET /v1/scans?limit=10"
      via: "backendFetch (RSC, server-side Clerk JWT)"
      pattern: "backendFetch.*scans.*limit=10"
    - from: "dashboard/components/home/ScoreCard.tsx"
      to: "summary_json.score + summary_json.findings"
      via: "prop: scan ScanListItemResp"
      pattern: "summary_json"
    - from: "dashboard/components/home/ScoreSparkline.tsx"
      to: "dashboard/components/scans/Sparkline.tsx"
      via: "re-export or direct import of Sparkline SVG logic"
      pattern: "Sparkline"
    - from: "dashboard/app/(dashboard)/settings/layout.tsx"
      to: "/settings/members|billing|integrations"
      via: "shadcn Tabs with href-based active detection via usePathname"
      pattern: "usePathname"
---

<objective>
Deliver the home summary dashboard (D-04) and the Settings sub-routes (D-03), closing DSH-05.

Plan 07-05 (Wave 2) scaffolded `dashboard/app/(dashboard)/page.tsx` as a stub redirect. This plan rewrites it with the real home dashboard: latest score+grade card, score sparkline, top-3 critical findings, and recent-scans table. It also creates the `/settings/{members,billing,integrations}` sub-routes with their URL-routed shadcn Tabs layout, Clerk OrganizationProfile for members, billing stub card, and integrations stub cards.

Purpose: Without this plan DSH-05 (settings page) is unimplemented and the home `/` is a blank stub — both are part of the Phase 7 shipped product.
Output: 9 new/rewritten files in `dashboard/`, 2 test files, full test suite green.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/07-saas-dashboard-history-share/07-CONTEXT.md
@.planning/phases/07-saas-dashboard-history-share/07-UI-SPEC.md
@.planning/phases/07-saas-dashboard-history-share/07-RESEARCH.md

<!-- Prior plan summaries needed for interface contracts -->
@.planning/phases/07-saas-dashboard-history-share/07-02-SUMMARY.md
@.planning/phases/07-saas-dashboard-history-share/07-05-SUMMARY.md

<interfaces>
<!-- ScanListItemResp + ScanListResp (from backend/app/schemas/scan.py — Plan 07-01) -->
<!-- TypeScript mirror in dashboard/lib/types.ts (Plan 07-05 Task 2) -->
export interface ScanListItem {
  id: string
  team_id: string
  status: 'pending' | 'ready' | 'failed'
  size_bytes: number | null
  created_at: string                   // ISO 8601
  summary_json: ScanSummaryJson | null
  branch: string | null
  commit_sha: string | null
  source: string | null
}

export interface ScanSummaryJson {
  score: number                        // 0–100
  findings: Finding[]
  // ... other fields
}

export interface Finding {
  rule_id: string
  severity: 'critical' | 'high' | 'medium' | 'info'
  title: string
  resource_id: string
  description?: string
  remediation?: string
}

export interface ScanListResp {
  items: ScanListItem[]
  next_cursor: string | null
}

<!-- backendFetch helper (dashboard/lib/backend.ts — Plan 07-05 Task 2) -->
export async function backendFetch<T>(
  path: string,
  init?: RequestInit
): Promise<T>
// Attaches Clerk JWT Authorization header server-side (uses auth() from @clerk/nextjs/server)

<!-- Score-to-grade derivation (implement inline in ScoreCard — no shared utility exists yet) -->
// grade(score: number): string
// score >= 90 → 'A+'   (bg-green-100 text-green-700)
// score >= 85 → 'A'    (bg-green-100 text-green-700)
// score >= 80 → 'B+'   (bg-sky-100   text-sky-700)
// score >= 75 → 'B'    (bg-sky-100   text-sky-700)
// score >= 70 → 'C'    (bg-amber-100 text-amber-700)
// score >= 60 → 'D'    (bg-orange-100 text-orange-700)
// else        → 'F'    (bg-red-100   text-red-700)

<!-- Sparkline component (dashboard/components/scans/Sparkline.tsx — Plan 07-06 Task 1) -->
// Props: { points: number[] }  (array of scores, oldest→newest, max 10)
// Renders: handrolled SVG polyline stroke-slate-700 stroke-width:2,
//          circle at min (fill-sev-critical) + circle at max (fill-sev-clean)
// Import path: ../../components/scans/Sparkline  (from home/ subdir)

<!-- Clerk components (from @clerk/nextjs) -->
import { OrganizationProfile } from '@clerk/nextjs'
// appearance prop: { baseTheme: undefined, variables: { colorPrimary: '#f59e0b' } }

<!-- shadcn Tabs (from @radix-ui/react-tabs via shadcn) -->
import { Tabs, TabsList, TabsTrigger } from '@/components/ui/tabs'
// URL-routing pattern: use Next.js usePathname() to derive active tab;
// each TabsTrigger is an <Link> (or onClick + router.push) — not internal Tabs state
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Home dashboard components — ScoreCard, ScoreSparkline, TopFindings, RecentScansTable</name>
  <files>
    dashboard/components/home/ScoreCard.tsx,
    dashboard/components/home/ScoreSparkline.tsx,
    dashboard/components/home/TopFindings.tsx,
    dashboard/components/home/RecentScansTable.tsx,
    dashboard/__tests__/home-dashboard.test.tsx
  </files>
  <behavior>
    - ScoreCard(scan: ScanListItem): grade derivation — score>=90→'A+', 85→'A', 80→'B+', 75→'B', 70→'C', 60→'D', else 'F'; grade pill Tailwind classes per UI-SPEC color table
    - ScoreCard: displays score number text-[28px] font-semibold tabular-nums, finding counts (critical/high/drift) as 3 vertical pairs, scan metadata (created_at formatted, branch@commit_sha, Open scan → link)
    - ScoreSparkline(scans: ScanListItem[]): extracts scores from scans[].summary_json.score (skip null), passes array to existing Sparkline component; title "Score over last 10 scans"
    - TopFindings(scan: ScanListItem): filters scan.summary_json.findings[] where severity==='critical', sorts stable (rule_id asc), takes first 3; renders border-l-4 border-sev-critical cards with rule_id badge + title + resource_id + "Open scan →" link; if fewer than 3 critical: shows available count + green "0 critical findings — well done" pill at remaining slots
    - RecentScansTable(scans: ScanListItem[], latestScanId: string): renders bg-white border rounded-lg table with title bar "Recent scans" + "View all →" link to /scans; columns Date/Branch/Score/Crit/High; max 5 rows; click row → /scans/{id}
    - All four components: "use client" NOT set (they receive server-fetched props; no hooks needed except RecentScansTable uses next/link)
    - Test: ScoreCard grade derivation — score 95→'A+', 87→'B+', 72→'C', 55→'F'
    - Test: TopFindings sorts and limits to 3 critical; renders green pill when 0 critical
    - Test: home-dashboard integration — renders ScoreCard + sparkline + findings + table from fixture ScanListResp; empty state renders CLI hint card when items=[]
  </behavior>
  <action>
    Write tests first in `dashboard/__tests__/home-dashboard.test.tsx`:
    - Import ScoreCard, TopFindings from their component paths
    - Fixture: one ScanListItem with summary_json.score=87, findings=[3 critical, 2 high]
    - Test ScoreCard renders grade 'B+' and sky color classes
    - Test TopFindings renders exactly 3 critical finding titles, sorted by rule_id
    - Test TopFindings renders green pill when findings=[]
    - Fixture: empty ScanListResp (items=[]) — test that home page shell renders CLI hint text

    Then implement components:

    1. `dashboard/components/home/ScoreCard.tsx` — per UI-SPEC §"/ Summary dashboard" item 1:
       - 3-column flex, gap-8, items-center
       - Col 1: grade pill (28×28 rounded-md, Tailwind bg/text per grade table) + score number (text-[28px] font-semibold tabular-nums) + "Score" label (text-xs text-slate-500)
       - Col 2: Critical/High/Drift counts as 3 vertical pairs (text-sev-critical/etc font-semibold + text-xs text-slate-500 label)
       - Col 3 ml-auto: created_at formatted (e.g. "Apr 28, 2026 · 14:32 UTC"), branch@commit_sha (font-mono), "Open scan →" next/link to /scans/{id}
       - Derive drift count from summary_json.findings.filter(f => f.severity === 'info').length (proxy until drift field exists)
       - Export `gradeInfo(score: number): { grade: string; bgClass: string; textClass: string }` — used by tests

    2. `dashboard/components/home/ScoreSparkline.tsx` — per UI-SPEC §"/ Summary dashboard" item 2:
       - Import Sparkline from `../../components/scans/Sparkline` (the handrolled SVG from Plan 07-06)
       - Extract points: scans.map(s => s.summary_json?.score).filter((s): s is number => s !== null && s !== undefined).slice(-10)
       - Wrap in bg-white border-slate-200 rounded-lg p-6 h-[120px] card
       - Title: "Score over last 10 scans" (text-base font-semibold)
       - Pass points array to <Sparkline/>

    3. `dashboard/components/home/TopFindings.tsx` — per UI-SPEC §"/ Summary dashboard" item 3:
       - Filter + sort: scan.summary_json?.findings?.filter(f => f.severity === 'critical').sort((a,b) => a.rule_id.localeCompare(b.rule_id)).slice(0,3) ?? []
       - Each finding card: border-l-4 border-sev-critical bg-red-50/40 p-4; rule_id badge (text-xs font-mono text-slate-500) + title (text-sm font-semibold) + resource_id (text-xs font-mono text-slate-500) + "Open scan →" link (text-xs text-amber-600 hover:underline) → /scans/{scan.id}
       - If criticalFindings.length < 3: fill remaining slots with green pill: bg-green-100 text-green-700 text-sm px-3 py-1.5 rounded-full "0 critical findings — well done"
       - Wrap in bg-white border-slate-200 rounded-lg p-6; title "Top 3 critical findings" (text-base font-semibold)

    4. `dashboard/components/home/RecentScansTable.tsx` — per UI-SPEC §"/ Summary dashboard" item 4:
       - Props: { scans: ScanListItem[] } — show first 5
       - Outer: bg-white border-slate-200 rounded-lg overflow-hidden
       - Title bar: px-6 py-4 border-b flex justify-between; "Recent scans" (text-base font-semibold) + next/link "View all →" to /scans (text-sm text-amber-600)
       - Table: thead with Date/Branch/Score/Crit/High columns (text-xs text-slate-500 uppercase)
       - Each row: click → router.push(`/scans/${scan.id}`) or next/link; Date formatted (relative if ≤7d old per UI-SPEC §"Date + time formatting"), Branch (font-mono text-sm, "—" if null), Score pill (24×24 rounded-sm + number tabular-nums), Crit count (text-sev-critical), High count (text-orange-600)
       - This component DOES use next/navigation for row click → mark 'use client'
  </action>
  <verify>
    <automated>cd dashboard && npx vitest run __tests__/home-dashboard.test.tsx --reporter=verbose 2>&1 | tail -30</automated>
  </verify>
  <done>All home-dashboard.test.tsx tests green. Four component files exist. ScoreCard exports gradeInfo(). TopFindings renders ≤3 critical finding cards + green pill for missing slots. RecentScansTable capped at 5 rows.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Home RSC page rewrite + Settings sub-routes + settings-routes tests</name>
  <files>
    dashboard/app/(dashboard)/page.tsx,
    dashboard/app/(dashboard)/settings/layout.tsx,
    dashboard/app/(dashboard)/settings/members/page.tsx,
    dashboard/app/(dashboard)/settings/billing/page.tsx,
    dashboard/app/(dashboard)/settings/integrations/page.tsx,
    dashboard/__tests__/settings-routes.test.tsx
  </files>
  <behavior>
    - page.tsx RSC: awaits backendFetch<ScanListResp>('/v1/scans?limit=10') server-side; no searchParams needed; passes data to home components
    - page.tsx empty state: if items.length === 0, render single centered card with "No scans yet" heading + CLI install hint per UI-SPEC §"Empty state for /"
    - settings/layout.tsx: URL-routed tabs — usePathname to derive active; three TabsTrigger items as next/link hrefs: /settings/members, /settings/billing, /settings/integrations; active tab = border-b-2 border-amber-400 (amber active indicator per D-03)
    - settings/members/page.tsx: renders Clerk <OrganizationProfile/> with appearance={{ variables: { colorPrimary: '#f59e0b' } }}
    - settings/billing/page.tsx: stub card — no backend call in v1; "Open billing portal" amber CTA button that shows a toast "Coming soon" (not a real POST yet; backend portal endpoint deferred)
    - settings/integrations/page.tsx: Slack card (MessageSquare icon + heading + helper text + Input + Save button) + GitHub card (Github icon + heading + helper + disabled Connect GitHub button per UI-SPEC copywriting)
    - Test settings/members: renders without crash (smoke — OrganizationProfile mocked)
    - Test settings/billing: "Open billing portal" button present; "Coming soon" on click
    - Test settings/integrations: Slack + GitHub cards present; GitHub button disabled
  </behavior>
  <action>
    Write tests first in `dashboard/__tests__/settings-routes.test.tsx`:
    - Mock @clerk/nextjs OrganizationProfile as a div with data-testid="org-profile"
    - Test members page renders org-profile
    - Test billing page has button with text "Open billing portal"
    - Test integrations page has disabled button containing "Connect GitHub"

    Then implement:

    1. `dashboard/app/(dashboard)/page.tsx` — REWRITE the stub from Plan 07-05:
       - RSC (no 'use client')
       - `const data = await backendFetch<ScanListResp>('/v1/scans?limit=10')` at top
       - If data.items.length === 0: return empty state card per UI-SPEC §"Empty state for /"
         ```tsx
         <div className="max-w-md mx-auto p-8 bg-white border border-slate-200 rounded-lg text-center mt-20">
           <p className="text-base font-semibold">No scans yet</p>
           <p className="text-sm text-slate-500 mt-2">Run a scan from the CLI to see your infrastructure here.</p>
           <code className="block font-mono text-sm bg-slate-100 px-4 py-3 rounded-md mt-4">
             $ pip install infracanvas{'\n'}$ infracanvas scan ./terraform --upload
           </code>
         </div>
         ```
       - Otherwise: render vertical stack inside `max-w-7xl mx-auto px-8 py-12` with `gap-12` flex-col:
         ```tsx
         const latestScan = data.items[0]
         <ScoreCard scan={latestScan} />
         <ScoreSparkline scans={data.items} />
         <TopFindings scan={latestScan} />
         <RecentScansTable scans={data.items} />
         ```
       - Import all four from `../../components/home/...`

    2. `dashboard/app/(dashboard)/settings/layout.tsx` — settings sub-nav:
       - 'use client' (needs usePathname)
       - Import usePathname from 'next/navigation'; Link from 'next/link'
       - Tabs: three links — Members (/settings/members), Billing (/settings/billing), Integrations (/settings/integrations)
       - Active detection: `pathname.startsWith('/settings/members')` etc.
       - Active class: `border-b-2 border-amber-400 text-slate-900 font-medium` (D-03 amber active)
       - Inactive class: `text-slate-500 hover:text-slate-700`
       - Outer: `max-w-7xl mx-auto px-8 py-8`; heading `Settings` (text-base font-semibold mb-6); tab list flex border-b border-slate-200 mb-8; {children} below
       - Do NOT use shadcn Tabs internal state — each tab is a next/link so URL drives active state

    3. `dashboard/app/(dashboard)/settings/members/page.tsx`:
       - RSC (no 'use client' needed — OrganizationProfile is a Clerk component that handles its own state)
       - `import { OrganizationProfile } from '@clerk/nextjs'`
       - Return `<OrganizationProfile appearance={{ variables: { colorPrimary: '#f59e0b' } }} />`

    4. `dashboard/app/(dashboard)/settings/billing/page.tsx`:
       - 'use client' (button with onClick)
       - Card: `bg-white border border-slate-200 rounded-lg p-6 max-w-md`
       - Heading: "Billing & invoices" (text-base font-semibold)
       - Body text: "Manage your subscription, view invoices, and update payment methods in the Stripe portal." (text-sm text-slate-500 mt-2)
       - Button: `[Open billing portal]` — amber CTA styling (`bg-amber-400 hover:bg-amber-500 text-slate-900 font-medium px-4 py-2 rounded-md mt-4`)
       - onClick: import toast from shadcn sonner (or use alert as fallback if sonner not installed); show "Billing portal — coming soon" message
       - Add data-testid="billing-portal-btn" for test targeting

    5. `dashboard/app/(dashboard)/settings/integrations/page.tsx`:
       - RSC (static content, no state)
       - Two integration cards (gap-3, flex-col):
         Card 1 — Slack: `bg-white border border-slate-200 rounded-lg p-4`; MessageSquare icon (lucide-react, 16px text-slate-400) + "Slack" heading (text-sm font-semibold inline ml-2); helper "Send Critical findings to a Slack channel (Phase 8)." (text-xs text-slate-500 mt-1); Input placeholder="https://hooks.slack.com/..." + [Save webhook URL] button (secondary style, mt-2) — wired to a TODO stub handler
         Card 2 — Github: same card chrome; Github icon + "GitHub" heading; helper "Connect a repo and scan on push (coming in 7.5)."; disabled button "Connect GitHub (coming in 7.5)" with `disabled className="opacity-50 cursor-not-allowed ..."`; data-testid="github-connect-btn" disabled={true}

    Run TypeScript check last:
    ```bash
    cd dashboard && npx tsc --noEmit 2>&1 | grep -E "settings|home|page" | head -20
    ```
  </action>
  <verify>
    <automated>cd dashboard && npx vitest run __tests__/settings-routes.test.tsx --reporter=verbose 2>&1 | tail -30</automated>
  </verify>
  <done>All settings-routes.test.tsx tests green. page.tsx rewritten (stub removed). Five settings files exist. npx tsc --noEmit exits 0 for these files.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| RSC → GET /v1/scans?limit=10 | Server-side fetch with Clerk JWT; team_id resolved by backend RLS |
| /settings/billing → Stripe portal URL | Backend endpoint not implemented in v1; UI shows stub — no URL exposed |
| /settings/integrations Slack webhook URL | User-supplied URL stored to backend stub; validated as HTTPS URL before save |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-11-01 | Information Disclosure | Home dashboard RSC — fetches latest 10 scans | mitigate | RSC uses server-side Clerk auth(); backendFetch attaches JWT; backend RLS returns only team-scoped rows — no cross-team data possible |
| T-07-11-02 | Information Disclosure | TopFindings renders finding titles from summary_json | accept | summary_json already stored team-scoped in R2; backend /v1/scans endpoint enforces team isolation from Plan 07-02 |
| T-07-11-03 | Tampering | Slack webhook URL field — user can supply arbitrary HTTPS URL | mitigate | Validate URL is https:// before saving; backend stub rejects non-HTTPS; full Phase 8 delivery adds signature verification |
| T-07-11-04 | Elevation of Privilege | /settings/members renders OrganizationProfile — Clerk admin UI | accept | Clerk's own OrganizationProfile enforces role-based actions (only admins can add/remove members); no custom invitation code from us |
| T-07-11-05 | Information Disclosure | /settings/billing stub — "coming soon" state | accept | No portal URL generated in v1; no Stripe session minted; no sensitive data exposed |
| T-07-11-06 | Denial of Service | Home RSC fetches 10 scans on every page load — no cache | accept | RSC runs server-side with Clerk auth; Next.js 15 uncached-by-default is intentional for SaaS (per D-17); limit=10 is a bounded query |
</threat_model>

<verification>
After both tasks complete:

1. `cd dashboard && npx vitest run __tests__/home-dashboard.test.tsx __tests__/settings-routes.test.tsx --reporter=verbose` — all tests green
2. `grep -c 'ScoreCard\|ScoreSparkline\|TopFindings\|RecentScansTable' dashboard/app/(dashboard)/page.tsx` — outputs 4
3. `grep -rn 'export default' dashboard/components/home/` — lists 4 exports (ScoreCard, ScoreSparkline, TopFindings, RecentScansTable)
4. `grep -c 'OrganizationProfile' dashboard/app/(dashboard)/settings/members/page.tsx` — outputs >= 1
5. `grep -c 'Open billing portal' dashboard/app/(dashboard)/settings/billing/page.tsx` — outputs >= 1
6. `grep -c 'Connect GitHub' dashboard/app/(dashboard)/settings/integrations/page.tsx` — outputs >= 1
7. `cd dashboard && npx tsc --noEmit 2>&1 | grep -c "error TS"` — outputs 0
</verification>

<success_criteria>
- `/` RSC page renders full home dashboard (score card + sparkline + top findings + recent scans table) from real API data
- `/` empty state renders CLI install hint — no crash, no 500
- `/settings/members` renders Clerk OrganizationProfile restyled with amber primary
- `/settings/billing` renders billing card with "Open billing portal" amber CTA (stub — coming soon)
- `/settings/integrations` renders Slack webhook card + disabled GitHub card
- Settings sub-nav uses URL-persisted active tab with amber-400 active indicator (D-03)
- All new component files pass TypeScript strict mode (npx tsc --noEmit exits 0)
- home-dashboard.test.tsx and settings-routes.test.tsx both pass in vitest
- DSH-05 requirement covered: settings page with members/billing/integrations sub-routes ships
</success_criteria>

<output>
After completion, create `.planning/phases/07-saas-dashboard-history-share/07-11-SUMMARY.md` per `~/.claude/get-shit-done/templates/summary.md`.
</output>
