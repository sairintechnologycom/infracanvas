---
phase: 02-canvas-v1-0
plan: 00
type: execute
wave: 0
depends_on: []
files_modified:
  - cli/tests/test_azure_parser.py
  - cli/tests/test_shadow.py
  - cli/tests/test_staleness.py
  - cli/tests/test_policy.py
  - cli/tests/test_cost.py
  - viewer/src/__tests__/ResourceNode.test.tsx
  - viewer/src/__tests__/DetailPanel.test.tsx
autonomous: true
requirements: [AZR-01, SHD-01, RST-01, RST-02, POL-01, CST-03, AZR-02, PLN-03]

must_haves:
  truths:
    - "All Wave 0 test stubs exist before any implementation plan runs"
    - "Each stub file imports its target module (will fail until implementation)"
    - "Test stubs document expected behavior via test names and docstrings"
  artifacts:
    - path: "cli/tests/test_azure_parser.py"
      provides: "Azure parser test stubs"
      contains: "def test_"
    - path: "cli/tests/test_shadow.py"
      provides: "Shadow detector test stubs"
      contains: "def test_"
    - path: "cli/tests/test_staleness.py"
      provides: "Staleness check test stubs"
      contains: "def test_"
    - path: "cli/tests/test_policy.py"
      provides: "Policy engine test stubs"
      contains: "def test_"
    - path: "viewer/src/__tests__/ResourceNode.test.tsx"
      provides: "Azure icon rendering test stubs"
      contains: "test("
    - path: "viewer/src/__tests__/DetailPanel.test.tsx"
      provides: "ChangesTab test stubs"
      contains: "test("
  key_links: []
---

<objective>
Create all test stub files required by the Nyquist validation strategy before any implementation begins.

Purpose: The validation contract (02-VALIDATION.md) requires Wave 0 test stubs to exist BEFORE the implementation tasks they test. This ensures every implementation task has a pre-existing test file to run against, enforcing the Nyquist sampling rule that automated verification commands reference real test files.

Output: Test stub files for all new test modules (Python + TypeScript). Stubs contain test function signatures with `pytest.skip("Wave 0 stub")` or equivalent markers. Implementation plans (01-08) will replace stubs with real test logic.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-canvas-v1-0/02-VALIDATION.md
@.planning/phases/02-canvas-v1-0/02-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Python test stub files</name>
  <files>
    cli/tests/test_azure_parser.py,
    cli/tests/test_shadow.py,
    cli/tests/test_staleness.py,
    cli/tests/test_policy.py,
    cli/tests/test_cost.py
  </files>
  <action>
    Create test stub files with `pytest.mark.skip("Wave 0 stub — implementation in Plan NN")` on each test. Each file must:
    1. Import pytest
    2. Define test classes/functions matching the names that implementation plans expect
    3. Mark every test with `@pytest.mark.skip` so the suite stays green

    **cli/tests/test_azure_parser.py** (for Plan 02):
    ```python
    """Tests for Azure resource extraction and attribute normalisation (AZR-01). Wave 0 stubs."""
    import pytest

    @pytest.mark.skip("Wave 0 stub — implementation in Plan 02")
    class TestAzureParser:
        def test_vnet_extracted(self): ...
        def test_nsg_extracted(self): ...
        def test_provider_set_to_azurerm(self): ...
        def test_location_mapped_to_region(self): ...
        def test_storage_account_extracted(self): ...
        def test_aks_extracted(self): ...
    ```

    **cli/tests/test_shadow.py** (for Plan 04):
    ```python
    """Tests for shadow infrastructure detector (SHD-01, SHD-02). Wave 0 stubs."""
    import pytest

    @pytest.mark.skip("Wave 0 stub — implementation in Plan 04")
    class TestShadowDetectorImport:
        def test_missing_boto3_raises_runtime_error(self): ...
        def test_no_credentials_raises_runtime_error(self): ...

    @pytest.mark.skip("Wave 0 stub — implementation in Plan 04")
    class TestShadowDetection:
        def test_no_shadow_when_all_managed(self): ...
        def test_shadow_ec2_flagged(self): ...
        def test_shadow_s3_flagged(self): ...
    ```

    **cli/tests/test_staleness.py** (for Plan 03):
    ```python
    """Tests for runtime staleness checks (RST-01, RST-02). Wave 0 stubs."""
    import pytest

    @pytest.mark.skip("Wave 0 stub — implementation in Plan 03")
    class TestLambdaStaleness:
        def test_eol_runtime_flagged(self): ...
        def test_current_runtime_not_flagged(self): ...
        def test_finding_has_framework_ids(self): ...

    @pytest.mark.skip("Wave 0 stub — implementation in Plan 03")
    class TestEksStaleness:
        def test_old_eks_flagged(self): ...

    @pytest.mark.skip("Wave 0 stub — implementation in Plan 03")
    class TestAksStaleness:
        def test_old_aks_flagged(self): ...

    @pytest.mark.skip("Wave 0 stub — implementation in Plan 03")
    class TestResourceLocks:
        def test_unlocked_critical_resource_flagged(self): ...
        def test_locked_resource_not_flagged(self): ...
    ```

    **cli/tests/test_policy.py** (for Plan 06):
    ```python
    """Tests for custom policy engine (POL-01, POL-02). Wave 0 stubs."""
    import pytest

    @pytest.mark.skip("Wave 0 stub — implementation in Plan 06")
    class TestPolicyLoader:
        def test_loads_yaml_from_directory(self): ...
        def test_empty_dir_returns_empty(self): ...

    @pytest.mark.skip("Wave 0 stub — implementation in Plan 06")
    class TestPolicyEvaluation:
        def test_policy_source_injected(self): ...
        def test_policy_finding_has_correct_rule_id(self): ...
        def test_security_and_policy_findings_coexist(self): ...
    ```

    **cli/tests/test_cost.py** — If this file already exists, APPEND new stub classes. If not, create it:
    ```python
    # APPEND to existing test_cost.py:
    @pytest.mark.skip("Wave 0 stub — implementation in Plan 05")
    class TestRegionMultiplier:
        def test_us_east_1_no_change(self): ...
        def test_eu_west_1_higher(self): ...
        def test_unknown_region_defaults_to_1(self): ...

    @pytest.mark.skip("Wave 0 stub — implementation in Plan 05")
    class TestGroupCostAggregation:
        def test_group_costs_in_metadata(self): ...
    ```

    IMPORTANT: Check if test_cost.py already exists first. If it does, only append the new classes. Do not overwrite existing test content.
  </action>
  <verify>
    <automated>cd /Users/bhushan/Documents/Projects/Infracanvas/cli && python -m pytest tests/test_azure_parser.py tests/test_shadow.py tests/test_staleness.py tests/test_policy.py -x -q --tb=short 2>&1 | tail -5</automated>
  </verify>
  <done>All Python test stub files exist with skip markers. pytest collects and skips all stubs (exit 0).</done>
</task>

<task type="auto">
  <name>Task 2: Create viewer test stub files</name>
  <files>
    viewer/src/__tests__/ResourceNode.test.tsx,
    viewer/src/__tests__/DetailPanel.test.tsx
  </files>
  <action>
    Create TypeScript test stub files for viewer components.

    **viewer/src/__tests__/ResourceNode.test.tsx** (for Plan 07):
    ```typescript
    import { describe, test, expect } from 'vitest'

    describe('ResourceNode', () => {
      test.skip('renders Azure resource with correct icon color', () => {
        // Wave 0 stub — implementation in Plan 07
        expect(true).toBe(true)
      })

      test.skip('strips azurerm_ prefix from type label', () => {
        // Wave 0 stub — implementation in Plan 07
        expect(true).toBe(true)
      })

      test.skip('uses getAzureServiceConfig for azurerm provider', () => {
        // Wave 0 stub — implementation in Plan 07
        expect(true).toBe(true)
      })

      test.skip('applies correct drift border colors per D-15', () => {
        // Wave 0 stub — implementation in Plan 07
        expect(true).toBe(true)
      })
    })
    ```

    **viewer/src/__tests__/DetailPanel.test.tsx** (for Plan 07):
    ```typescript
    import { describe, test, expect } from 'vitest'

    describe('DetailPanel ChangesTab', () => {
      test.skip('renders Changes tab alongside Findings tab', () => {
        // Wave 0 stub — implementation in Plan 07
        expect(true).toBe(true)
      })

      test.skip('shows before/after attribute diff for changed resources', () => {
        // Wave 0 stub — implementation in Plan 07
        expect(true).toBe(true)
      })
    })

    describe('FindingCard', () => {
      test.skip('renders POLICY source pill for policy findings', () => {
        // Wave 0 stub — implementation in Plan 07
        expect(true).toBe(true)
      })

      test.skip('renders compliance framework tags when framework_ids present', () => {
        // Wave 0 stub — implementation in Plan 07
        expect(true).toBe(true)
      })

      test.skip('hides framework tags in gate mode', () => {
        // Wave 0 stub — implementation in Plan 07
        expect(true).toBe(true)
      })
    })
    ```

    Create the `viewer/src/__tests__/` directory if it does not exist.
  </action>
  <verify>
    <automated>cd /Users/bhushan/Documents/Projects/Infracanvas/viewer && npx vitest run --reporter=verbose 2>&1 | tail -15</automated>
  </verify>
  <done>Viewer test stub files exist. Vitest collects and skips all stubs.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| N/A | Test stubs contain no runtime logic |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| N/A | N/A | N/A | N/A | No threats — test-only files with skip markers |
</threat_model>

<verification>
- `cd cli && python -m pytest tests/ -x -q` — all existing tests pass, new stubs skipped
- `cd viewer && npx vitest run` — all existing tests pass, new stubs skipped
</verification>

<success_criteria>
- All 5 Python test stub files exist and are collected by pytest
- All 2 TypeScript test stub files exist and are collected by vitest
- No test failures introduced (all stubs are skipped)
- Wave 0 requirements in VALIDATION.md are satisfiable
</success_criteria>

<output>
After completion, create `.planning/phases/02-canvas-v1-0/02-00-SUMMARY.md`
</output>
