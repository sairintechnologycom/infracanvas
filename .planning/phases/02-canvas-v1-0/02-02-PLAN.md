---
phase: 02-canvas-v1-0
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - cli/infracanvas/parser/azure.py
  - cli/infracanvas/graph/builder.py
  - cli/infracanvas/security/rules/azure/network.yaml
  - cli/infracanvas/security/rules/azure/storage.yaml
  - cli/infracanvas/security/rules/azure/compute.yaml
  - cli/infracanvas/security/rules/azure/identity.yaml
  - cli/infracanvas/security/rules/azure/database.yaml
  - viewer/src/icons/azureServiceConfig.ts
  - cli/tests/test_azure_parser.py
  - cli/tests/fixtures/azure/vnet.tf
  - cli/tests/fixtures/azure/storage.tf
  - cli/tests/fixtures/azure/compute.tf
autonomous: true
requirements: [AZR-01, AZR-02, AZR-03]

must_haves:
  truths:
    - "infracanvas scan on a directory with azurerm_ resources extracts all 10 supported Azure types"
    - "Azure resources get provider='azurerm' and region mapped from location attribute"
    - "10 Azure security rules (AZ-001 through AZ-010) evaluate correctly"
    - "Viewer shows Azure resource types with correct colors and labels"
  artifacts:
    - path: "cli/infracanvas/parser/azure.py"
      provides: "Azure attribute normalisation layer"
      contains: "normalize_azure_attrs"
    - path: "cli/infracanvas/security/rules/azure/network.yaml"
      provides: "AZ-001, AZ-003, AZ-009 rules"
      contains: "AZ-001"
    - path: "viewer/src/icons/azureServiceConfig.ts"
      provides: "Azure icon color and label config"
      contains: "AZURE_SERVICE_CONFIG"
  key_links:
    - from: "cli/infracanvas/graph/builder.py"
      to: "cli/infracanvas/parser/azure.py"
      via: "normalize_azure_attrs call for azurerm provider"
      pattern: "normalize_azure_attrs"
    - from: "viewer/src/icons/azureServiceConfig.ts"
      to: "viewer/src/components/ResourceNode.tsx"
      via: "getAzureServiceConfig import"
      pattern: "getAzureServiceConfig"
---

<objective>
Add Azure Terraform resource support: parser normalisation, 10 security rules, and viewer icon config.

Purpose: Per D-06, `infracanvas scan ./tf` must auto-detect azurerm_ provider resources without any flag. This plan adds the Azure parsing layer, all 10 Azure security rules (AZ-001 through AZ-010), test fixtures, and the viewer icon configuration. The existing HCL parser already extracts azurerm_ blocks — this plan adds attribute normalisation and the Azure-specific analysis.

Output: Azure parser module, 5 Azure rule YAML files, Azure icon config, test fixtures, passing tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-canvas-v1-0/02-CONTEXT.md
@.planning/phases/02-canvas-v1-0/02-RESEARCH.md
@.planning/phases/02-canvas-v1-0/02-PATTERNS.md
@.planning/phases/02-canvas-v1-0/02-UI-SPEC.md

<interfaces>
<!-- Current builder.py _create_nodes provider detection (line 30): -->
```python
provider = res.resource_type.split("_")[0] if "_" in res.resource_type else "unknown"
# "azurerm" -> provider = "azurerm"  (already works)
region = str(res.attributes.get("region", ""))
```

<!-- awsServiceConfig.ts pattern to mirror (viewer/src/icons/awsServiceConfig.ts): -->
```typescript
export interface ServiceConfig {
  color: string;
  label: string;
}
export const SERVICE_CONFIG: Record<string, ServiceConfig> = { ... };
export function getServiceConfig(resourceType: string): ServiceConfig { ... }
```

<!-- Existing YAML rule schema (cli/infracanvas/security/rules/aws/s3.yaml): -->
```yaml
- id: SEC-001
  title: "S3 Bucket Publicly Accessible"
  severity: critical
  resource_types: ["aws_s3_bucket"]
  condition:
    attribute: "acl"
    operator: "in"
    values: ["public-read", "public-read-write"]
  remediation: "Set acl to 'private'..."
  description: "S3 bucket has public ACL..."
```

<!-- Azure resource types (from RESEARCH.md): -->
10 supported types:
- azurerm_virtual_network, azurerm_subnet, azurerm_network_security_group
- azurerm_virtual_machine, azurerm_linux_virtual_machine, azurerm_windows_virtual_machine
- azurerm_storage_account, azurerm_kubernetes_cluster, azurerm_app_service
- azurerm_mssql_server, azurerm_key_vault, azurerm_application_gateway
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Azure parser, builder integration, and test fixtures</name>
  <read_first>
    - cli/infracanvas/graph/builder.py
    - cli/infracanvas/parser/hcl.py
    - cli/tests/test_parser.py
    - cli/tests/fixtures/
  </read_first>
  <files>
    cli/infracanvas/parser/azure.py,
    cli/infracanvas/graph/builder.py,
    cli/tests/test_azure_parser.py,
    cli/tests/fixtures/azure/vnet.tf,
    cli/tests/fixtures/azure/storage.tf,
    cli/tests/fixtures/azure/compute.tf
  </files>
  <action>
    1. **Create `cli/infracanvas/parser/azure.py`** — Azure attribute normalisation module:
       ```python
       """Normalize azurerm resource attributes for the InfraCanvas graph model."""
       from __future__ import annotations
       from typing import Any

       # Azure VNet/subnet grouping: map resource_group_name to group field
       AZURE_GROUP_ATTRS = ("resource_group_name", "virtual_network_name")

       def normalize_azure_attrs(resource_type: str, attrs: dict[str, Any]) -> dict[str, Any]:
           """Map Azure-specific attribute names to InfraCanvas canonical form."""
           normalized = dict(attrs)
           # Azure uses 'location' instead of 'region'
           if "location" in normalized and "region" not in normalized:
               normalized["region"] = normalized["location"]
           return normalized
       ```

    2. **Modify `cli/infracanvas/graph/builder.py`** `_create_nodes()` — After the existing `provider = ...` line (line 30) and `region = ...` line (line 31), add Azure normalisation:
       ```python
       if provider == "azurerm":
           from infracanvas.parser.azure import normalize_azure_attrs
           attrs = normalize_azure_attrs(res.resource_type, res.attributes)
           region = str(attrs.get("region", ""))
       else:
           attrs = res.attributes
       ```
       Then use `attrs` instead of `res.attributes` when creating the ResourceNode (change `attributes=res.attributes` to `attributes=attrs`).

    3. **Create `cli/tests/fixtures/azure/vnet.tf`**:
       ```hcl
       resource "azurerm_resource_group" "example" {
         name     = "example-resources"
         location = "East US"
       }

       resource "azurerm_virtual_network" "main" {
         name                = "main-vnet"
         address_space       = ["10.0.0.0/16"]
         location            = "East US"
         resource_group_name = "example-resources"
       }

       resource "azurerm_subnet" "internal" {
         name                 = "internal"
         resource_group_name  = "example-resources"
         virtual_network_name = "main-vnet"
         address_prefixes     = ["10.0.1.0/24"]
       }

       resource "azurerm_network_security_group" "web" {
         name                = "web-nsg"
         location            = "East US"
         resource_group_name = "example-resources"

         security_rule {
           name                       = "allow-all"
           priority                   = 100
           direction                  = "Inbound"
           access                     = "Allow"
           protocol                   = "Tcp"
           source_port_range          = "*"
           destination_port_range     = "*"
           source_address_prefix      = "*"
           destination_address_prefix = "*"
         }
       }
       ```

    4. **Create `cli/tests/fixtures/azure/storage.tf`**:
       ```hcl
       resource "azurerm_storage_account" "example" {
         name                     = "examplestorage"
         resource_group_name      = "example-resources"
         location                 = "East US"
         account_tier             = "Standard"
         account_replication_type = "LRS"
         allow_blob_public_access = true
         min_tls_version          = "TLS1_0"
       }

       resource "azurerm_key_vault" "example" {
         name                = "example-vault"
         location            = "East US"
         resource_group_name = "example-resources"
         tenant_id           = "00000000-0000-0000-0000-000000000000"
         sku_name            = "standard"
         soft_delete_retention_days = 7
         purge_protection_enabled   = false
       }
       ```

    5. **Create `cli/tests/fixtures/azure/compute.tf`**:
       ```hcl
       resource "azurerm_kubernetes_cluster" "example" {
         name                = "example-aks"
         location            = "East US"
         resource_group_name = "example-resources"
         dns_prefix          = "exampleaks"
         kubernetes_version  = "1.24"
         role_based_access_control_enabled = false

         default_node_pool {
           name       = "default"
           node_count = 1
           vm_size    = "Standard_D2_v2"
         }
       }

       resource "azurerm_mssql_server" "example" {
         name                         = "example-sqlserver"
         resource_group_name          = "example-resources"
         location                     = "East US"
         version                      = "12.0"
         administrator_login          = "sqladmin"
         administrator_login_password = "P@ssw0rd123!"
         public_network_access_enabled = true
       }
       ```

    6. **Create `cli/tests/test_azure_parser.py`**:
       ```python
       """Tests for Azure resource extraction and attribute normalisation (AZR-01)."""
       from pathlib import Path
       from infracanvas.graph.builder import build_graph
       from infracanvas.parser.hcl import parse_directory

       FIXTURES = Path(__file__).parent / "fixtures" / "azure"

       def _scan_azure_fixture(name: str):
           parsed = parse_directory(FIXTURES / name)
           return build_graph(parsed)

       class TestAzureParser:
           def test_vnet_extracted(self):
               """AZR-001-A: azurerm_virtual_network resource extracted."""
               graph = _scan_azure_fixture("vnet")
               types = {n.type for n in graph.nodes}
               assert "azurerm_virtual_network" in types

           def test_nsg_extracted(self):
               """AZR-001-B: azurerm_network_security_group resource extracted."""
               graph = _scan_azure_fixture("vnet")
               types = {n.type for n in graph.nodes}
               assert "azurerm_network_security_group" in types

           def test_provider_set_to_azurerm(self):
               """AZR-001-C: provider field set to 'azurerm'."""
               graph = _scan_azure_fixture("vnet")
               node = next(n for n in graph.nodes if n.type == "azurerm_virtual_network")
               assert node.provider == "azurerm"

           def test_location_mapped_to_region(self):
               """AZR-001-D: location attribute normalised to region field."""
               graph = _scan_azure_fixture("vnet")
               node = next(n for n in graph.nodes if n.type == "azurerm_virtual_network")
               assert node.region != ""

           def test_storage_account_extracted(self):
               """AZR-001-E: azurerm_storage_account extracted from storage fixture."""
               graph = _scan_azure_fixture("storage")
               types = {n.type for n in graph.nodes}
               assert "azurerm_storage_account" in types

           def test_aks_extracted(self):
               """AZR-001-F: azurerm_kubernetes_cluster extracted from compute fixture."""
               graph = _scan_azure_fixture("compute")
               types = {n.type for n in graph.nodes}
               assert "azurerm_kubernetes_cluster" in types
       ```
       Note: Each fixture directory name must match (vnet/ has vnet.tf, etc.) — OR put all .tf files in a single `azure/` directory and parse from there. Simplest: put all 3 .tf files directly in `cli/tests/fixtures/azure/` (no subdirs) and use `_scan_azure_fixture(".")` with `FIXTURES` pointing to `fixtures/azure`. Adjust the test helper:
       ```python
       def _scan_azure():
           parsed = parse_directory(FIXTURES)
           return build_graph(parsed)
       ```
       Then all tests call `_scan_azure()`.
  </action>
  <verify>
    <automated>cd /Users/bhushan/Documents/Projects/Infracanvas/cli && python -m pytest tests/test_azure_parser.py -x -q --tb=short 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - cli/infracanvas/parser/azure.py exists and contains `def normalize_azure_attrs`
    - cli/infracanvas/graph/builder.py contains `normalize_azure_attrs` import for azurerm provider
    - cli/tests/fixtures/azure/vnet.tf exists with `azurerm_virtual_network` resource
    - cli/tests/fixtures/azure/storage.tf exists with `azurerm_storage_account` resource
    - cli/tests/fixtures/azure/compute.tf exists with `azurerm_kubernetes_cluster` resource
    - cli/tests/test_azure_parser.py passes all 6 tests
    - All existing tests still pass
  </acceptance_criteria>
  <done>Azure resources are parsed and normalised. Provider is correctly set to "azurerm". Location attribute maps to region. All 6 Azure parser tests pass.</done>
</task>

<task type="auto">
  <name>Task 2: Azure security rules (AZ-001 through AZ-010) and viewer icon config</name>
  <read_first>
    - cli/infracanvas/security/rules/aws/s3.yaml
    - cli/infracanvas/security/rules/aws/networking.yaml
    - viewer/src/icons/awsServiceConfig.ts
    - cli/tests/test_security.py
  </read_first>
  <files>
    cli/infracanvas/security/rules/azure/network.yaml,
    cli/infracanvas/security/rules/azure/storage.yaml,
    cli/infracanvas/security/rules/azure/compute.yaml,
    cli/infracanvas/security/rules/azure/identity.yaml,
    cli/infracanvas/security/rules/azure/database.yaml,
    viewer/src/icons/azureServiceConfig.ts
  </files>
  <action>
    1. **Create `cli/infracanvas/security/rules/azure/` directory** with 5 YAML files. Each follows the exact schema from existing aws/ rules but with `framework_ids` added (per SEC-06):

       **network.yaml** (AZ-001, AZ-003, AZ-009):
       ```yaml
       - id: AZ-001
         title: "NSG Allows Unrestricted Inbound Access"
         severity: critical
         resource_types: ["azurerm_network_security_group"]
         framework_ids: ["CIS-6.1", "NIST-SC-7", "SOC2-CC6.6"]
         condition:
           attribute: "security_rule.source_address_prefix"
           operator: "equals"
           value: "*"
         remediation: "Replace wildcard source with specific IP ranges or service tags"
         description: "NSG rule allows inbound access from any source address"

       - id: AZ-003
         title: "NSG Allows SSH from Any Source"
         severity: high
         resource_types: ["azurerm_network_security_group"]
         framework_ids: ["CIS-6.2", "NIST-AC-17", "SOC2-CC6.6", "PCI-DSS-1.2"]
         condition:
           attribute: "security_rule.destination_port_range"
           operator: "any_equals"
           value: "22"
         remediation: "Restrict SSH source to known IP ranges or use Azure Bastion"
         description: "NSG allows SSH access from any source"

       - id: AZ-009
         title: "VNet Missing DDoS Protection Plan"
         severity: medium
         resource_types: ["azurerm_virtual_network"]
         framework_ids: ["NIST-SC-5", "SOC2-CC6.6"]
         condition:
           attribute: "ddos_protection_plan"
           operator: "not_exists"
         remediation: "Enable DDoS Protection Standard plan on production VNets"
         description: "Virtual network does not have DDoS protection plan configured"
       ```

       **storage.yaml** (AZ-002, AZ-006, AZ-007):
       ```yaml
       - id: AZ-002
         title: "Storage Account Allows Public Blob Access"
         severity: high
         resource_types: ["azurerm_storage_account"]
         framework_ids: ["CIS-3.5", "NIST-SC-28", "PCI-DSS-3.4"]
         condition:
           attribute: "allow_blob_public_access"
           operator: "equals"
           value: true
         remediation: "Set allow_blob_public_access = false"
         description: "Storage account allows anonymous public access to blobs"

       - id: AZ-006
         title: "Storage Account Does Not Enforce HTTPS"
         severity: high
         resource_types: ["azurerm_storage_account"]
         framework_ids: ["CIS-3.1", "NIST-SC-8", "SOC2-CC6.7", "PCI-DSS-4.1"]
         condition:
           attribute: "https_traffic_only_enabled"
           operator: "equals"
           value: false
         remediation: "Set https_traffic_only_enabled = true"
         description: "Storage account allows unencrypted HTTP traffic"

       - id: AZ-007
         title: "Storage Account Minimum TLS Version Too Low"
         severity: medium
         resource_types: ["azurerm_storage_account"]
         framework_ids: ["NIST-SC-8", "PCI-DSS-4.1"]
         condition:
           attribute: "min_tls_version"
           operator: "not_equals"
           value: "TLS1_2"
         remediation: "Set min_tls_version = 'TLS1_2'"
         description: "Storage account accepts connections with TLS version below 1.2"
       ```

       **compute.yaml** (AZ-004, AZ-005):
       ```yaml
       - id: AZ-004
         title: "Virtual Machine Managed Disk Not Encrypted"
         severity: high
         resource_types: ["azurerm_virtual_machine", "azurerm_linux_virtual_machine", "azurerm_windows_virtual_machine"]
         framework_ids: ["CIS-7.2", "NIST-SC-28", "SOC2-CC6.7", "PCI-DSS-3.4"]
         condition:
           attribute: "os_disk.disk_encryption_set_id"
           operator: "not_exists"
         remediation: "Enable disk encryption using Azure Disk Encryption or customer-managed keys"
         description: "Virtual machine OS disk is not encrypted with a customer-managed key"

       - id: AZ-005
         title: "Virtual Machine Has Public IP"
         severity: medium
         resource_types: ["azurerm_virtual_machine", "azurerm_linux_virtual_machine", "azurerm_windows_virtual_machine"]
         framework_ids: ["CIS-7.1", "NIST-SC-7", "SOC2-CC6.1"]
         condition:
           attribute: "public_ip_address"
           operator: "exists"
         remediation: "Remove public IP and use Azure Bastion or VPN for access"
         description: "Virtual machine is directly exposed to the internet via public IP"
       ```

       **identity.yaml** (AZ-008):
       ```yaml
       - id: AZ-008
         title: "AKS Cluster RBAC Not Enabled"
         severity: high
         resource_types: ["azurerm_kubernetes_cluster"]
         framework_ids: ["CIS-8.5", "NIST-AC-3", "SOC2-CC6.1"]
         condition:
           attribute: "role_based_access_control_enabled"
           operator: "equals"
           value: false
         remediation: "Set role_based_access_control_enabled = true"
         description: "AKS cluster does not have RBAC enabled, allowing unrestricted API access"
       ```

       **database.yaml** (AZ-010):
       ```yaml
       - id: AZ-010
         title: "SQL Server Allows Public Network Access"
         severity: high
         resource_types: ["azurerm_mssql_server"]
         framework_ids: ["CIS-4.1.1", "NIST-SC-7", "SOC2-CC6.1", "PCI-DSS-1.2"]
         condition:
           attribute: "public_network_access_enabled"
           operator: "equals"
           value: true
         remediation: "Set public_network_access_enabled = false and use private endpoints"
         description: "SQL Server is accessible from the public internet"
       ```

    2. **Create `viewer/src/icons/azureServiceConfig.ts`** — Mirror awsServiceConfig.ts structure:
       ```typescript
       export interface AzureServiceConfig {
         color: string
         label: string
       }

       export const AZURE_SERVICE_CONFIG: Record<string, AzureServiceConfig> = {
         azurerm_virtual_network:         { color: '#0078D4', label: 'VNet' },
         azurerm_subnet:                  { color: '#0078D4', label: 'NET' },
         azurerm_network_security_group:  { color: '#DD344C', label: 'NSG' },
         azurerm_virtual_machine:         { color: '#FF9900', label: 'VM' },
         azurerm_linux_virtual_machine:   { color: '#FF9900', label: 'VM' },
         azurerm_windows_virtual_machine: { color: '#FF9900', label: 'VM' },
         azurerm_storage_account:         { color: '#3F8624', label: 'STG' },
         azurerm_kubernetes_cluster:      { color: '#2E73B8', label: 'AKS' },
         azurerm_app_service:             { color: '#FF9900', label: 'APP' },
         azurerm_mssql_server:            { color: '#2E73B8', label: 'SQL' },
         azurerm_key_vault:               { color: '#DD344C', label: 'KV' },
         azurerm_application_gateway:     { color: '#8C4FFF', label: 'AGW' },
       }

       export function getAzureServiceConfig(resourceType: string): AzureServiceConfig {
         if (AZURE_SERVICE_CONFIG[resourceType]) return AZURE_SERVICE_CONFIG[resourceType]
         return { color: '#94a3b8', label: resourceType.replace(/^azurerm_/, '').slice(0, 4).toUpperCase() }
       }
       ```
       Colors are locked by UI-SPEC: Azure blue #0078D4, security red #DD344C, compute orange #FF9900, storage green #3F8624, orchestration blue #2E73B8, networking purple #8C4FFF, fallback grey #94a3b8.
  </action>
  <verify>
    <automated>cd /Users/bhushan/Documents/Projects/Infracanvas/cli && python -m pytest tests/test_security.py tests/test_azure_parser.py -x -q --tb=short 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - cli/infracanvas/security/rules/azure/network.yaml contains `AZ-001`
    - cli/infracanvas/security/rules/azure/storage.yaml contains `AZ-002`
    - cli/infracanvas/security/rules/azure/compute.yaml contains `AZ-004`
    - cli/infracanvas/security/rules/azure/identity.yaml contains `AZ-008`
    - cli/infracanvas/security/rules/azure/database.yaml contains `AZ-010`
    - All 10 Azure rules (AZ-001 through AZ-010, with AZ-003 included in network.yaml) have `framework_ids` arrays
    - viewer/src/icons/azureServiceConfig.ts contains `AZURE_SERVICE_CONFIG` with 12 entries
    - viewer/src/icons/azureServiceConfig.ts contains `getAzureServiceConfig` function
    - All existing security tests pass
  </acceptance_criteria>
  <done>10 Azure security rules with compliance tags are loadable by the engine. Azure viewer icon config provides correct colors per UI-SPEC. Azure fixture files validate parsing pipeline.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Azure .tf files -> parser | User's Terraform files with azurerm_ resources (untrusted input) |
| Azure YAML rules -> engine | Package-internal rule definitions (trusted) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-04 | Tampering | parser/azure.py normalize_azure_attrs | accept | Only reads dict values and copies; no file I/O or exec. Low risk. |
| T-02-05 | Information Disclosure | Azure resource attributes in graph | accept | All attributes come from user's own .tf files — no secret leakage. Same as existing AWS path. |
</threat_model>

<verification>
- `cd cli && python -m pytest tests/test_azure_parser.py -v` — all Azure parser tests green
- `cd cli && python -c "from infracanvas.security.loader import load_rules; rules = load_rules(); azure_rules = [r for r in rules if r.id.startswith('AZ-')]; print(f'{len(azure_rules)} Azure rules loaded'); assert len(azure_rules) == 10"` — 10 Azure rules loaded
- `cd viewer && npx tsc --noEmit` — TypeScript compiles with new Azure config
</verification>

<success_criteria>
- Azure resources auto-detected (no --provider flag, per D-06)
- 10 Azure resource types parsed with provider="azurerm" and region mapped from location
- 10 Azure security rules loaded and evaluatable
- All Azure rules have framework_ids (SEC-06 compliance)
- Viewer has azureServiceConfig.ts with correct colors per UI-SPEC
- All existing + new tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/02-canvas-v1-0/02-02-SUMMARY.md`
</output>
