---
phase: 7.1
slug: phase-7-ui-contract-remediation
status: draft
nyquist_compliant: false
wave_0_complete: false
created: 2026-04-29
---

# Phase 7.1 — Validation Strategy

> Per-phase validation contract for feedback sampling during execution.

> **Note:** This file is a draft scaffold. The planner will fill the Per-Task Verification Map after PLAN.md files are written. Acceptance criteria for UI remediation work are largely **grep-able** (no `text-xl`, no `bg-amber-500`, etc.) per RESEARCH.md §6.

---

## Test Infrastructure

| Property | Value |
|----------|-------|
| **Framework** | vitest 4.1.x + @testing-library/react 16.3.x (dashboard); pytest (backend, only if RMD-04 needs new endpoint) |
| **Config file** | `dashboard/vite.config.ts` (test config) — none for backend |
| **Quick run command** | `cd dashboard && npm run test -- --run` |
| **Full suite command** | `cd dashboard && npm run test -- --run && npm run build` |
| **Estimated runtime** | ~30s (tests) + ~60s (next build) = ~90s |

---

## Sampling Rate

- **After every task commit:** `cd dashboard && npm run test -- --run` (Vitest single-shot, no watch)
- **After every plan wave:** `cd dashboard && npm run test -- --run && npm run build` (full)
- **Before `/gsd-verify-work`:** Full suite green AND grep checklist passes
- **Max feedback latency:** ~90s

---

## Per-Task Verification Map

> Filled in by `gsd-planner` after each PLAN.md is written. Each task with `<automated>` must have an entry here. Grep-style checks (e.g., `grep -r "text-xl" dashboard/app | wc -l == 0`) count as automated commands.

| Task ID | Plan | Wave | Requirement | Threat Ref | Secure Behavior | Test Type | Automated Command | File Exists | Status |
|---------|------|------|-------------|------------|-----------------|-----------|-------------------|-------------|--------|
| _to be populated by planner_ | | | | | | | | | ⬜ pending |

---

## Wave 0 Requirements

- [ ] `npx shadcn@latest init` — initialize shadcn/ui in `dashboard/` (research §1)
- [ ] `dashboard/components.json` — created by init
- [ ] 17 shadcn blocks added per UI-SPEC (button, card, dialog, dropdown-menu, input, label, select, separator, sheet, sonner, table, tabs, toast, tooltip, alert-dialog, badge, collapsible)
- [ ] `dashboard/__tests__/` test setup intact for Vitest + Testing Library

---

## Manual-Only Verifications

| Behavior | Requirement | Why Manual | Test Instructions |
|----------|-------------|------------|-------------------|
| Visual fidelity to UI-SPEC mockups | RMD-01..06 | Pixel match against the design contract is a human judgment call (the UI-REVIEW score 16→24 is itself a manual rubric) | Run dashboard locally, side-by-side with `07-UI-SPEC.md` mockups and `07-UI-REVIEW.md` warnings, confirm each warning is closed |
| Toast firing on copy/revoke/fail (RMD-03) | RMD-03 | Sonner DOM rendering is hard to assert reliably in jsdom; visual confirmation is faster | Trigger share-link copy, revoke, and a forced 500 — confirm three different toast variants render |
| AlertDialog destructive-confirm flow (RMD-04) | RMD-04 | Multi-step interaction across portal — unit-test asserts the wiring, manual confirms the UX | Click `[Revoke]`, confirm dialog, verify list updates |

---

## Validation Sign-Off

- [ ] All tasks have `<automated>` verify or Wave 0 dependencies
- [ ] Sampling continuity: no 3 consecutive tasks without automated verify
- [ ] Wave 0 covers shadcn init + the 17 blocks before any migration plan starts
- [ ] No watch-mode flags
- [ ] Feedback latency < 90s
- [ ] `nyquist_compliant: true` set in frontmatter

**Approval:** pending
