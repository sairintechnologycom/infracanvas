---
phase: 01-canvas-mvp
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - cli/infracanvas/security/scorer.py
  - cli/infracanvas/export/scorecard.py
  - cli/tests/test_scorer.py
autonomous: true
requirements:
  - SEC-01
  - SEC-02
  - SEC-03
  - SEC-04
  - SCR-01
  - SCR-02
  - SCR-03

must_haves:
  truths:
    - "Score dimensions are exactly: Security, Encryption, IAM Hygiene, Cost Efficiency, Tagging"
    - "Score card HTML has large letter grade at top, 5 horizontal progress bars, footer CTA, attribution"
    - "Score card links to infracanvas.dev/founding for upgrade CTA"
    - "Grade colors follow the UI-SPEC: A=#22c55e, B=#4ade80, C=#f59e0b, D=#f97316, F=#ef4444"
  artifacts:
    - path: "cli/infracanvas/security/scorer.py"
      provides: "5 correct score dimensions"
      contains: "IAM Hygiene"
    - path: "cli/infracanvas/export/scorecard.py"
      provides: "D-08 layout score card HTML"
      contains: "infracanvas.dev/founding"
  key_links:
    - from: "cli/infracanvas/export/scorecard.py"
      to: "cli/infracanvas/graph/models.py"
      via: "ScoreCard model"
      pattern: "from infracanvas.graph.models import ScoreCard"
---

<objective>
Realign the scorer to the required 5 dimensions (Security, Encryption, IAM Hygiene, Cost Efficiency, Tagging) per SCR-02, and redesign the score card HTML export to match D-08 layout (letter grade prominent, horizontal progress bars, footer CTA, attribution).

Purpose: The scoring engine and score card are standalone subsystems with no dependency on viewer, parser, or CLI command changes. They use the existing ScoreCard Pydantic model. This plan makes them spec-compliant.

Output: Corrected scorer.py with 5 dimensions, redesigned scorecard.py with D-08 layout per UI-SPEC.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-canvas-mvp/01-RESEARCH.md
@.planning/phases/01-canvas-mvp/01-PATTERNS.md
@.planning/phases/01-canvas-mvp/01-UI-SPEC.md

<interfaces>
From cli/infracanvas/graph/models.py:
```python
class CategoryScore(BaseModel):
    name: str
    score: int
    grade: str
    finding_count: int

class ScoreCard(BaseModel):
    overall: int
    overall_grade: str
    categories: list[CategoryScore]
    top_issues: list[Finding]
    resource_count: int
    estimated_monthly_cost: float
    scan_id: str
    project: str
    scanned_at: str
```

From cli/infracanvas/security/scorer.py (CURRENT — to be modified):
```python
CATEGORY_RULES: dict[str, set[str]] = {
    "Security": {"SEC-001", "SEC-003", "SEC-004", "SEC-005", "SEC-007", "SEC-008"},
    "Encryption": {"SEC-002", "SEC-006", "SEC-009"},
    "Networking": {"SEC-010", "SEC-011", "SEC-012", "SEC-013", "SEC-014"},
    "IAM": {"SEC-007", "SEC-008", "SEC-015", "SEC-016"},
    "Tagging": {"SEC-010"},
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Realign scorer dimensions to SCR-02 spec</name>
  <files>cli/infracanvas/security/scorer.py, cli/tests/test_scorer.py</files>
  <read_first>
    cli/infracanvas/security/scorer.py,
    cli/tests/test_scorer.py,
    .planning/phases/01-canvas-mvp/01-RESEARCH.md
  </read_first>
  <behavior>
    - Test 1: Scorer.build() returns exactly 5 categories named "Security", "Encryption", "IAM Hygiene", "Cost Efficiency", "Tagging"
    - Test 2: "Networking" and "IAM" category names do NOT appear in output
    - Test 3: Category "Security" maps to rule IDs SEC-001, SEC-003, SEC-004, SEC-005
    - Test 4: Category "Encryption" maps to SEC-002, SEC-006, SEC-009
    - Test 5: Category "IAM Hygiene" maps to SEC-007, SEC-008
    - Test 6: Category "Cost Efficiency" has 0 findings (empty rule set) and scores 100 by default
    - Test 7: Category "Tagging" maps to SEC-010
    - Test 8: GRADE_MAP thresholds: A>=80, B>=65, C>=50, D>=35, F<35
  </behavior>
  <action>
Replace `CATEGORY_RULES` in `cli/infracanvas/security/scorer.py` with:
```python
CATEGORY_RULES: dict[str, set[str]] = {
    "Security":        {"SEC-001", "SEC-003", "SEC-004", "SEC-005"},
    "Encryption":      {"SEC-002", "SEC-006", "SEC-009"},
    "IAM Hygiene":     {"SEC-007", "SEC-008"},
    "Cost Efficiency": set(),
    "Tagging":         {"SEC-010"},
}
```

Update `GRADE_MAP` to match UI-SPEC grade boundaries:
```python
GRADE_MAP = [(80, "A"), (65, "B"), (50, "C"), (35, "D"), (0, "F")]
```

The Scorer.build() method logic stays the same — the `Cost Efficiency` category with an empty rule set will score 100 by default (no penalty). All other logic (penalty calculation, weight mapping, top issues sorting) is correct.

Write tests in `cli/tests/test_scorer.py` as a new `TestDimensions` class verifying:
- Exact 5 category names in output
- Grade thresholds at boundary values (80 -> A, 79 -> B, 65 -> B, 64 -> C, 50 -> C, 49 -> D, 35 -> D, 34 -> F)
- Cost Efficiency scores 100 with no findings
  </action>
  <verify>
    <automated>cd /Users/bhushan/Documents/Projects/Infracanvas/cli && python -m pytest tests/test_scorer.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - cli/infracanvas/security/scorer.py contains `"IAM Hygiene"` as a CATEGORY_RULES key
    - cli/infracanvas/security/scorer.py contains `"Cost Efficiency": set()`
    - cli/infracanvas/security/scorer.py does NOT contain `"Networking"` as a key
    - cli/infracanvas/security/scorer.py does NOT contain `"IAM":` (without "Hygiene") as a key
    - cli/infracanvas/security/scorer.py contains `(80, "A"), (65, "B"), (50, "C"), (35, "D")`
    - `cd cli && python -m pytest tests/test_scorer.py -x -q` exits 0
  </acceptance_criteria>
  <done>Scorer produces exactly 5 dimensions matching SCR-02 spec. Grade thresholds match UI-SPEC. All scorer tests pass.</done>
</task>

<task type="auto">
  <name>Task 2: Redesign score card HTML to D-08 layout</name>
  <files>cli/infracanvas/export/scorecard.py, cli/tests/test_scorer.py</files>
  <read_first>
    cli/infracanvas/export/scorecard.py,
    .planning/phases/01-canvas-mvp/01-UI-SPEC.md,
    .planning/phases/01-canvas-mvp/01-CONTEXT.md
  </read_first>
  <action>
Rewrite `export_scorecard()` in `cli/infracanvas/export/scorecard.py` to produce the D-08 layout from the UI-SPEC. The function signature stays the same: `export_scorecard(card: ScoreCard, output_path: Path) -> None`.

The HTML must be self-contained (inline CSS, no external dependencies) and follow this structure:

1. **Background**: `#0a0e17` (canvas-bg). Card surface: `#111827`, border-radius 12px, border `1px solid #1e293b`, padding 24px, max-width 480px, margin auto.

2. **Header**: "InfraCanvas" at 14px semibold `#e2e8f0`, project name at 12px `#94a3b8`, scan date at 12px `#64748b`.

3. **Grade display**: Letter grade at 72px semibold, colored by grade (A=`#22c55e`, B=`#4ade80`, C=`#f59e0b`, D=`#f97316`, F=`#ef4444`). Below: "{N} / 100" at 14px semibold, same color as grade.

4. **Dimension progress bars** (5 rows, 8px gap between):
   - Label: dimension name 12px regular `#94a3b8` left-aligned
   - Score: "{N}/100" 12px mono `#e2e8f0` right-aligned
   - Bar track: 4px height, border-radius 4px, background `#1e293b`
   - Bar fill: 4px height, border-radius 4px, width `{score}%`
   - Dimension-specific fill colors: Security=`#3b82f6`, Encryption=`#a855f7`, IAM Hygiene=`#f97316`, Cost Efficiency=`#22c55e`, Tagging=`#64748b`

5. **Stats footer**: "{N} resources  ·  {N} findings" at 12px `#94a3b8`. Below: severity breakdown "Critical: N  High: N  Medium: N  Info: N" using severity colors.

6. **Upgrade CTA**: Button with background `#3b82f640`, border `1px solid #3b82f6`, color `#60a5fa`, text "Unlock finding details →" at 12px semibold, border-radius 6px, padding 8px 16px. Links to `https://infracanvas.dev/founding` with `target="_blank" rel="noopener noreferrer"`. Sub-copy below: "Founding member pricing — $49/mo locked forever" at 12px `#64748b`.

7. **Attribution footer**: "Generated by InfraCanvas · infracanvas.dev" at 12px `#64748b` center-aligned. "infracanvas.dev" links to `https://infracanvas.dev`.

8. **OG meta tags** for social sharing: `og:title` = "InfraCanvas Score Card — {grade} ({score}/100)", `og:description` = "{resource_count} resources, {finding_count} findings", `og:type` = "website".

9. **Font**: Use Inter from Google Fonts CDN for the HTML file. Monospace: system `ui-monospace, monospace` for score numbers.

Replace the `_score_color()` helper with a `_grade_color()` function that maps grade letters to the UI-SPEC colors: A=`#22c55e`, B=`#4ade80`, C=`#f59e0b`, D=`#f97316`, F=`#ef4444`.

Add a `DIMENSION_COLORS` dict: `{"Security": "#3b82f6", "Encryption": "#a855f7", "IAM Hygiene": "#f97316", "Cost Efficiency": "#22c55e", "Tagging": "#64748b"}`.

Add a test `TestScorecardHtml` in `test_scorer.py` that creates a ScoreCard fixture, calls `export_scorecard()`, reads the output HTML, and verifies key strings exist.
  </action>
  <verify>
    <automated>cd /Users/bhushan/Documents/Projects/Infracanvas/cli && python -m pytest tests/test_scorer.py::TestScorecardHtml -x -q</automated>
  </verify>
  <acceptance_criteria>
    - cli/infracanvas/export/scorecard.py contains `infracanvas.dev/founding`
    - cli/infracanvas/export/scorecard.py contains `Generated by InfraCanvas`
    - cli/infracanvas/export/scorecard.py contains `#22c55e` (grade A color)
    - cli/infracanvas/export/scorecard.py contains `#a855f7` (Encryption dimension color)
    - cli/infracanvas/export/scorecard.py contains `og:title`
    - cli/infracanvas/export/scorecard.py contains `Unlock finding details`
    - cli/infracanvas/export/scorecard.py does NOT contain SVG circle gauge (old design)
    - `cd cli && python -m pytest tests/test_scorer.py -x -q` exits 0
  </acceptance_criteria>
  <done>Score card HTML renders D-08 layout with letter grade prominent, 5 progress bars with dimension-specific colors, footer CTA linking to infracanvas.dev/founding, attribution footer, OG meta tags. Old SVG circle design is removed. Tests verify HTML content.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| score data -> HTML | ScoreCard data interpolated into HTML string |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-05 | Tampering | export/scorecard.py | accept | Score card data comes from local scan pipeline; no untrusted input. HTML is f-string interpolated with numeric scores and enum strings only — no user-controlled text injection vector |
| T-01-06 | Information Disclosure | export/scorecard.py | accept | Score card intentionally shows aggregate scores and finding counts. Finding details are NOT included in score card HTML |
</threat_model>

<verification>
- `cd /Users/bhushan/Documents/Projects/Infracanvas/cli && python -m pytest tests/test_scorer.py -x -q` passes
- `python -c "from infracanvas.security.scorer import CATEGORY_RULES; print(list(CATEGORY_RULES.keys()))"` prints `['Security', 'Encryption', 'IAM Hygiene', 'Cost Efficiency', 'Tagging']`
</verification>

<success_criteria>
Scorer produces exactly 5 dimensions matching SCR-02. Grade thresholds match UI-SPEC. Score card HTML matches D-08 layout with correct colors, CTA, and attribution. All tests pass.
</success_criteria>

<output>
After completion, create `.planning/phases/01-canvas-mvp/01-02-SUMMARY.md`
</output>
