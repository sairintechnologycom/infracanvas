---
phase: 07.2
plan: 03
status: complete
date: 2026-05-03
requirements: [D-03, D-04]
---

# Plan 07.2-03 — /settings 404 + sparkline ovals (D-03 + D-04 P0)

## What was built

Bundled two tiny P0 fixes:

**Task 1 (D-03):** Created `dashboard/app/(dashboard)/settings/page.tsx` that redirects `/settings` to `/settings/members`, eliminating the bare-`/settings` 404 surfaced in the live UI review.

**Task 2 (D-04):** Removed `preserveAspectRatio="none"` from the SVG element inside `dashboard/components/scans/Sparkline.tsx` so dots render as round circles (not horizontal ovals), and raised `dashboard/components/home/ScoreSparkline.tsx` from `h-[60px]` to `h-[80px]` to match the score-card spec.

## Files changed

- `dashboard/app/(dashboard)/settings/page.tsx` (new)
- `dashboard/components/scans/Sparkline.tsx` (preserveAspectRatio removed)
- `dashboard/components/home/ScoreSparkline.tsx` (height raised)

## Verification

- `cd dashboard && npm test` → 171/171 passing
- `/settings` now resolves to `/settings/members` instead of 404
- Sparkline dots are circular at all viewport widths

## Commits

- `2b17b1d` feat(07.2-03): add /settings redirect to /settings/members (D-03)
- `990f46e` fix(07.2-03): remove preserveAspectRatio="none"; raise ScoreSparkline to h-[80px] (D-04)

## Notes

Originally dispatched to a parallel executor agent that completed both code commits but hit a tool-permission gate before writing/committing this SUMMARY.md. Orchestrator wrote and committed it; both code changes were already in place.

## Self-Check: PASSED
