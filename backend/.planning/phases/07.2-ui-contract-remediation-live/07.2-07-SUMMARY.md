---
phase: 07.2
plan: 07
status: complete
date: 2026-05-03
requirements: [D-08]
---

# Plan 07.2-07 — ShareModal shadcn Select + label + amber warning (D-08)

## What was built

Migrated `dashboard/components/share/ShareModal.tsx` away from a native `<select>` to the shadcn `<Select/>` primitive (landed in plan 07.1-01), updated the expiry label copy to spec, and changed the warning paragraph color to `text-amber-600`.

## Files changed

- `dashboard/components/share/ShareModal.tsx`:
  - Added imports: `Select, SelectTrigger, SelectValue, SelectContent, SelectItem` from `@/components/ui/select`
  - Replaced `<select id="share-expiry">` block with shadcn `<Select value={expiryChoice} onValueChange={…}>` — same option values, same default
  - Label `Expires in` → `Link expires in` (htmlFor linkage preserved)
  - Warning `<p>` className `text-xs text-slate-700 mt-1` → `text-xs text-amber-600 mt-1`
- `dashboard/__tests__/share-modal.test.tsx` — "Never warning" test rewritten to drive the Radix Select via `fireEvent.pointerDown` + click trigger + `findByRole('option')` + click; pattern from Phase 7.1 plan 03 (memory 1127)
- `dashboard/__tests__/setup.ts` — stubbed `hasPointerCapture` / `releasePointerCapture` / `setPointerCapture` / `scrollIntoView` on `Element.prototype` so Radix primitives don't TypeError under jsdom
- `dashboard/__tests__/polish-drift.test.ts` — D-08 ShareModal added to the spec-mandated `text-amber-600` exemption list

## Verification

- `! grep -q "<select" dashboard/components/share/ShareModal.tsx` → 0 hits
- `grep -c "Link expires in" dashboard/components/share/ShareModal.tsx` → 1
- `grep -c "from '@/components/ui/select'" dashboard/components/share/ShareModal.tsx` → 1
- `grep -c "text-amber-600" dashboard/components/share/ShareModal.tsx` → 1
- `cd dashboard && npm test` → 183/183 passing

## Commits

- `f3b482d` fix(07.2-07): ShareModal native select → shadcn + label + amber warning (D-08)

## Deviations

1. **`__tests__/setup.ts` modified** — not in `files_modified`, but the Radix Select migration triggers `Element.prototype.hasPointerCapture` calls that jsdom doesn't implement. Added a project-wide polyfill so future shadcn Select migrations also work without per-test boilerplate.
2. **`__tests__/share-modal.test.tsx` modified** — not in `files_modified`, but the "Never warning" test used `fireEvent.change(select, …)` which doesn't drive Radix Selects. Updated to the click-trigger + click-option pattern.
3. **`__tests__/polish-drift.test.ts` modified** — not in `files_modified`, but the RMD-06 amber-overuse rule (relaxed in plan 07.2-06) needed ShareModal added to its allowlist; otherwise the test fails with the new `text-amber-600` warning.

## Self-Check: PASSED
