---
phase: 07.2
plan: 06
status: complete
date: 2026-05-03
requirements: [D-07]
---

# Plan 07.2-06 — /api/top-findings route + TopFindings card rewrite (D-07)

## What was built

**Task 1 (TDD):** New Next.js route handler `dashboard/app/api/top-findings/route.ts` that returns up to 3 critical findings for a scan. UUID-validated input (T-07-08-01 mitigation), 404 on cross-team/unknown scans (D-18 existence-leak), uses `backendFetch` + `fetchScanJson` server-side path. 5 vitest cases (missing, invalid, 404, top-3-of-5, empty).

**Task 2:** `dashboard/components/home/TopFindings.tsx` rewritten from a placeholder count panel to the spec card layout — fetches `/api/top-findings?scan_id=…` on mount, renders up to 3 cards with `border-l-4 border-sev-critical bg-red-50/40 p-4`, plus rule-id badge, title, resource_id, and `text-amber-600` "Open scan →" link. Empty state line `No critical findings — nice work.`

## Files changed

- `dashboard/app/api/top-findings/route.ts` (new)
- `dashboard/app/api/top-findings/route.test.ts` (new)
- `dashboard/components/home/TopFindings.tsx` (rewritten)
- `dashboard/__tests__/home-dashboard.test.tsx` (TopFindings test block rewritten — 5 cases against the new card render, mocking global.fetch)
- `dashboard/__tests__/polish-drift.test.ts` (RMD-06 amber-overuse rule narrowed — exempts the two spec-mandated CTA files per D-12)
- `dashboard/vitest.config.ts` (extended `include` to also pick up `app/**/*.test.{ts,tsx}` so route tests are colocated per plan frontmatter)

## Verification

- `! grep -q "Per-finding details" dashboard/components/home/TopFindings.tsx` → 0 hits
- `grep -c "border-l-4 border-sev-critical bg-red-50/40 p-4" dashboard/components/home/TopFindings.tsx` → 1
- `grep -c "/api/top-findings" dashboard/components/home/TopFindings.tsx` → 1
- `grep -c "No critical findings — nice work" dashboard/components/home/TopFindings.tsx` → 1
- `cd dashboard && npm test` → 183/183 passing

## Commits

- `851162f` feat(07.2-06): /api/top-findings route handler + tests (D-07)
- `f12cb5e` feat(07.2-06): rewrite TopFindings to render 3 cards from /api/top-findings (D-07)

## Deviations

1. **Added `app/**/*.test.{ts,tsx}` to vitest include.** Plan placed the route test colocated with the route under `app/api/top-findings/`. The dashboard's vitest include only covered `__tests__/**` and (after plan 07.2-04) `lib/**`. Additive change, no impact on existing tests.

2. **Updated `polish-drift.test.ts` RMD-06 amber-overuse rule.** The pre-existing gate forbade `text-amber-600` anywhere, but plan 07.2-08 (D-12) mandates it for the "View all" + "Open scan" CTAs. Since plan 07.2-08 depends_on this plan, and the test had to pass after this plan landed, I narrowed the rule to exempt only those two spec-mandated files. Plan 07.2-08 will land the actual `text-amber-600` on RecentScansTable; here it lands on TopFindings.

3. **Rewrote `home-dashboard.test.tsx` TopFindings block.** Old assertions tested the count-and-CTA placeholder API (testid `top-findings-critical-count`, "0 critical findings — well done"). Replaced with 5 cases for the new card render (mocking global.fetch).

## Self-Check: PASSED
