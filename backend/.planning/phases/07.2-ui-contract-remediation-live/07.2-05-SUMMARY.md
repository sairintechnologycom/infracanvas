---
phase: 07.2
plan: 05
status: complete
date: 2026-05-03
requirements: [D-06]
---

# Plan 07.2-05 — Sidebar dead zone removal (D-06)

## What was built

Removed the `xl:`-gated icon-only collapsed state of the dashboard sidebar at 768–1279px viewports. The sidebar now renders full 220px width at all md+ viewports; mobile (<768px) still relies on the TopBar hamburger via the existing `hidden md:flex` pattern.

## Files changed

- `dashboard/components/layout/Sidebar.tsx` — root `w-12` + `xl:w-[220px]` collapsed pair → `w-[220px]`; removed `xl:inline hidden` / `xl:block hidden` from label spans + org-switcher; nav item alignment `xl:justify-start justify-center` → `justify-start`
- `dashboard/__tests__/responsive.test.tsx` — updated 2 assertions that pinned the old broken contract; replaced with positive `w-[220px]` assertion + negative `w-12` / `xl:w-[220px]` assertions

## Verification

- `grep -c "w-12" dashboard/components/layout/Sidebar.tsx` → 0
- `grep -c 'xl:w-\[220px\]' dashboard/components/layout/Sidebar.tsx` → 0
- `grep -c 'xl:inline hidden\|xl:block hidden' dashboard/components/layout/Sidebar.tsx` → 0
- `grep -c 'w-\[220px\]' dashboard/components/layout/Sidebar.tsx` → 1
- `grep -c 'md:flex' dashboard/components/layout/Sidebar.tsx` → 1
- `cd dashboard && npm test` → 178/178 passing

## Commits

- `64f1b00` fix(07.2-05): remove xl-breakpoint sidebar collapse + always show labels (D-06)

## Deviations

- `__tests__/responsive.test.tsx` was not in `files_modified` but contained 2 assertions that pinned the old broken contract (`xl:w-[220px]` and `w-12`). Updated them to assert the new spec instead (1 positive, 1 negative). Mechanical and isolated to this plan.

## Self-Check: PASSED
