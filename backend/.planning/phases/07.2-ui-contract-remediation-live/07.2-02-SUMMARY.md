---
phase: 07.2
plan: 02
status: complete
date: 2026-05-03
requirements: [D-02]
---

# Plan 07.2-02 — Singleton-store → factory hook migration (D-02 P0)

## What was built

Migrated all 10 viewer components that still imported the singleton `useStore` to the `useViewerStoreOrSingleton` factory hook so they bind to the per-scan store provided by the dashboard's `ViewerStoreProvider` while remaining unaffected in the standalone HTML export (where the singleton IS the context-bound store).

## Files changed

**Task 1 — main panels (4 files + 1 test):**
- `viewer/src/components/FilterPanel.tsx` — 10 call sites
- `viewer/src/components/DetailPanel.tsx` — 4 call sites
- `viewer/src/components/SummaryBar.tsx` — 6 call sites
- `viewer/src/components/SearchBar.tsx` — 3 call sites
- `viewer/src/__tests__/FreeGate.test.tsx` — mock now targets `useViewerStoreOrSingleton`

**Task 2 — FlowMap variants + node components (6 files):**
- `viewer/src/components/flowmap/FlowMapFilterPanel.tsx`
- `viewer/src/components/flowmap/PathDetailPanel.tsx`
- `viewer/src/components/flowmap/FlowMapCanvas.tsx`
- `viewer/src/components/flowmap/nodes/RouterNode.tsx`
- `viewer/src/components/flowmap/nodes/CloudHubNode.tsx`
- `viewer/src/components/flowmap/nodes/FirewallNode.tsx` (untouched aliased `useStore as useReactFlowStore` from @xyflow/react)

## Verification

- `grep -rn "useStore" viewer/src/components/ | grep -v useViewerStoreOrSingleton | grep -v useReactFlowStore` returns 0 hits
- Each migrated file contains ≥1 `useViewerStoreOrSingleton` reference
- `cd viewer && npm test` → 143/143 passing

## Commits

- `6869c0e` fix(07.2-02): migrate FilterPanel/DetailPanel/SummaryBar/SearchBar to useViewerStoreOrSingleton (D-02)
- `3babeb3` fix(07.2-02): migrate FlowMap variants and nodes to useViewerStoreOrSingleton (D-02)

## Notes

Originally dispatched to a parallel executor agent that hit a tool-permission gate after partially completing Task 1 (4/5 files). Orchestrator finished the remaining FreeGate.test.tsx edits (lines 37, 84, 86) and the 6 flowmap files via Python regex substitution, then committed atomically. No deviations from the plan; same edits the executor would have made.

## Self-Check: PASSED
