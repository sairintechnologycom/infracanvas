---
phase: 07.2
plan: 04
status: complete
date: 2026-05-03
requirements: [D-05]
---

# Plan 07.2-04 — Canonical gradeInfo() helper + adoption (D-05)

## What was built

Extracted the score → grade letter mapping into `dashboard/lib/grade.ts` and adopted it across the 4 surfaces that previously inlined their own thresholds. Eliminates the bug where a scan scoring 82 showed `B` on ScoreCard but `B+` in ScansTable/MetadataHeader (different ladders).

Canonical 7-tier ladder (locked): `A+ ≥95 / A ≥90 / B+ ≥85 / B ≥80 / C ≥70 / D ≥60 / F <60`.

## Files changed

**Task 1 (TDD):**
- `dashboard/lib/grade.ts` (new) — `gradeInfo(score) → { letter }`
- `dashboard/lib/grade.test.ts` (new) — 14 boundary cases
- `dashboard/vitest.config.ts` — extended `include` to also pick up colocated `lib/**/*.test.{ts,tsx}` (additive)

**Task 2 (adoption — 4 plan-named files):**
- `dashboard/components/home/ScoreCard.tsx` — removed local `gradeInfo` export; now imports from `@/lib/grade` and derives pill style from letter
- `dashboard/components/scans/ScansTable.tsx` — `ScoreGradePill` uses canonical letter
- `dashboard/components/scans/MetadataHeader.tsx` — `ScoreGradePill` uses canonical letter
- `dashboard/components/share/ShareViewer.tsx` — `ScoreGradePill` uses canonical letter

**Collateral migrations (consequence of removing ScoreCard's local export):**
- `dashboard/components/home/RecentScansTable.tsx` — was `import { gradeInfo } from './ScoreCard'`; now `from '@/lib/grade'`, with a small inlined `pillClasses(letter)` helper
- `dashboard/__tests__/home-dashboard.test.tsx` — deleted the `describe('ScoreCard.gradeInfo')` block (redundant with `lib/grade.test.ts` and tests an API that no longer exists)

## Verification

- `grep -nE "score\s*>=\s*(95|90|85|80|70|60)" components/home/ScoreCard.tsx components/scans/ScansTable.tsx components/scans/MetadataHeader.tsx components/share/ShareViewer.tsx` → 0 hits
- All 4 plan-named files import `gradeInfo` from `@/lib/grade`
- `cd dashboard && npm test` → 178/178 passing (was 171 + 14 new boundary cases − 7 removed redundant tests)

## Commits

- `04dab07` test(07.2-04): RED — grade.test.ts with 14 boundary cases (D-05)
- `4e16f69` feat(07.2-04): GREEN — gradeInfo() helper in dashboard/lib/grade.ts (D-05)
- `c31de3d` feat(07.2-04): adopt canonical gradeInfo() across 4 surfaces (D-05)

## Deviations

1. **vitest.config.ts modified** — plan didn't anticipate that the dashboard's vitest `include` was scoped to `__tests__/**` only. To honor the plan's frontmatter location (`dashboard/lib/grade.test.ts`), I extended `include` to `['__tests__/**/*.test.{ts,tsx}', 'lib/**/*.test.{ts,tsx}']`. Additive, no impact on existing tests.

2. **RecentScansTable.tsx + home-dashboard.test.tsx migrated** — not in `files_modified` but were importing `gradeInfo` from ScoreCard. Removing the ScoreCard export forced these updates. Mechanical: same letter source, `.grade` → `.letter` field rename, redundant test block removed.

## Self-Check: PASSED
