---
phase: 07.5-github-repo-connector
plan: 11
subsystem: verification
tags: [phase-close, manual-smoke, github-app, verification, sign-off]
requirements: [GH-01, GH-02, GH-03, GH-04, GH-05]
provides:
  - "07.5-PHASE-VERIFICATION.md — automated + cross-cutting + sign-off record (Sections 1–4 all green)"
  - "07.5-MANUAL-SMOKE.md — pre-flight (P1–P5) + smoke (S1–S16) checklist for any future re-deploy / new dev env"
  - "Phase 7.5 closed: 11/11 plans complete; backend pipeline + dashboard UX + verification gates all shipped"
requires:
  - "Plan 01–10: every PLAN must be closed with SUMMARY before this verifier can run"
  - "Plan 03 + 04 + 05 + 06: backend pipeline (auth + endpoints + finalize_scan + scan_repo job) — verified live by S6–S14"
  - "Plan 07–10: dashboard surface (proxies + components + ScanTriggerForm + ScanPendingClient) — verified live by S2–S14"
affects:
  - "Phase 8 (GitHub Webhook + Auto-scan) — unblocked; can reuse the same `scan_repo.kiq()` worker contract"
  - "Phase 13 (Team Tier Launch) — billing meter wiring confirmed live for github-sourced scans"
tech-stack:
  added: []
  patterns:
    - "Phase-close diptych: PHASE-VERIFICATION.md (automated + grep invariants + sign-off) + MANUAL-SMOKE.md (operator-only flows that automation cannot reach — GitHub App registration, real install, real clone, Stripe meter observation, R2 object existence, token-leakage grep over fly logs)"
    - "S1–S16 smoke checklist with explicit pre-flight (P1–P5) — the pre-flight section is reusable for any future dev env or re-deploy without re-walking the full smoke each time"
    - "checkpoint:human-verify resume signal as the single phase-close gate — operator types a one-word verdict (\"approved\" | failure description) and the executor flips Section 4 to checked + writes the dated summary sentence"
key-files:
  created:
    - .planning/phases/07.5-github-repo-connector/07.5-PHASE-VERIFICATION.md
    - .planning/phases/07.5-github-repo-connector/07.5-MANUAL-SMOKE.md
    - .planning/phases/07.5-github-repo-connector/07.5-11-SUMMARY.md
  modified:
    - .planning/STATE.md
    - .planning/ROADMAP.md
decisions:
  - "Phase 7.5 sign-off recorded with status='passed' and signed_off=2026-05-05 in the PHASE-VERIFICATION.md frontmatter; the file is now the canonical phase-close artifact for v1.1 audits — any future regression suspected against GH-01..GH-05 should re-run Section 1 + Section 3 commands and append a new sign-off block, not edit this one in place"
  - "Manual smoke pre-flight (P1–P5: GitHub App registration + private key generation + Fly secrets set + dashboard env wiring) is documented as a one-time-per-env step in MANUAL-SMOKE.md — operator pre-flight is required for any new dev environment (cannot be automated; the GitHub App register UI is interactive and the private key never leaves the operator's clipboard); subsequent smoke runs after pre-flight skip directly to S1"
  - "GH-01..GH-05 are phase-internal requirement IDs (live in 07.5-CONTEXT.md and PLAN frontmatters only) — not promoted to .planning/REQUIREMENTS.md because the Phase 8 (WBH-*) entries already cover the GitHub-integration umbrella in the global requirements taxonomy; phase-internal IDs stay scoped to the phase artifacts to avoid double-bookkeeping"
metrics:
  duration_minutes: 22
  tasks_completed: 3
  files_created: 3
  files_modified: 2
  commits: 2
  completed: "2026-05-05T07:01:00Z"
---

# Phase 07.5 Plan 11: Phase Verification + Manual GitHub Smoke Checkpoint Summary

**One-liner:** Closes Phase 7.5 — runs automated phase verification (Sections 1–3 of `07.5-PHASE-VERIFICATION.md` covering every VALIDATION.md row + GH-01..GH-05 traceability + 9 cross-cutting grep invariants T1–T9), walks the operator through the 16-step manual GitHub smoke checklist (`07.5-MANUAL-SMOKE.md`) against a live InfraCanvas DEV App installation + sandbox Terraform fixture, captures the operator's "approved" verdict at the `checkpoint:human-verify` gate, then flips Section 4 sign-off to checked and advances STATE + ROADMAP to mark the phase complete.

## Performance

- **Duration:** ~22 min (executor time across 2 separate agent invocations split by the human checkpoint; manual smoke wall-clock by the operator was a separate ~30 min window not included here)
- **Started:** 2026-05-05T06:15Z (Task 1 first commit)
- **Completed:** 2026-05-05T07:01Z (Task 3 close commit)
- **Tasks:** 3/3
- **Files created:** 3 (PHASE-VERIFICATION.md, MANUAL-SMOKE.md, this SUMMARY.md)
- **Files modified:** 2 (STATE.md, ROADMAP.md)

## Accomplishments

- **Sections 1–3 of `07.5-PHASE-VERIFICATION.md` written and recorded green:** every VALIDATION.md row mapped to its commanded test, full GH-01..GH-05 traceability matrix with file:line references against the live working tree, and all 9 cross-cutting truth checks (T1–T9 — auth-coverage greps, RLS GUC count, no-PEM-in-source, token-redaction defense-in-depth, traversal guard, team_id-as-kwarg invariant, finalize_scan callsite, state==clerk_org_id check, no-react-query lockout) reporting the expected hit counts.
- **`07.5-MANUAL-SMOKE.md` operator-runnable checklist published:** P1–P5 pre-flight (GitHub App register at github.com/settings/apps/new + private key gen + Fly secrets `flyctl secrets set GITHUB_APP_ID/...PRIVATE_KEY/...WEBHOOK_SECRET/...SLUG` + Vercel `NEXT_PUBLIC_GITHUB_APP_SLUG`) + S1–S16 smoke covering install URL flow, post-install hydration poll, repo+branch+path picking, scan trigger redirect to `/scans/{id}`, polling-to-ready transition, Stripe test-dashboard meter observation, R2 object existence, negative-path Retry, and the token-leakage `fly logs | grep -E "ghs_|x-access-token"` assertion.
- **Operator sign-off captured:** the `checkpoint:human-verify` gate at Task 2 was resumed by the operator with a single-word verdict **"approved"**, indicating all 16 numbered manual smoke checkpoints (S1–S16) passed end-to-end against the real GitHub App installation + sandbox Terraform repo without a red row.
- **Section 4 sign-off filled and Phase 7.5 closed:** PHASE-VERIFICATION.md frontmatter flipped to `status: passed` + `signed_off: 2026-05-05`; all six sign-off boxes checked; summary sentence dated; STATE.md advanced to plan 11/11 with completed_phases incremented; ROADMAP.md plan 07.5-11 row + Phase 7.5 progress row both flipped to complete with completion date 2026-05-05.

## Task Commits

| # | Task | Type | Commit | Files |
|---|------|------|--------|-------|
| 1 | Automated phase verification — write Sections 1–3 + MANUAL-SMOKE.md | docs | `bbfa0c7` | `07.5-PHASE-VERIFICATION.md` (new) + `07.5-MANUAL-SMOKE.md` (new) |
| 2 | Manual GitHub smoke test — operator walks 16-step checklist | _human gate, no code commit_ | _resume signal: "approved"_ | _no files_ |
| 3 | Phase close — Section 4 sign-off + STATE/ROADMAP advance + this SUMMARY | docs | _this commit_ | `07.5-PHASE-VERIFICATION.md` (Section 4) + `07.5-11-SUMMARY.md` (new) + `STATE.md` + `ROADMAP.md` |

Two real-code commits across the plan (Tasks 1 and 3 are docs-only; Task 2 is a pure human gate that produces no commit). The 5 backend + 5 dashboard test suites referenced by Section 1 were not re-executed by this plan — Task 1 captured them at-rest via the published gate-result tables in the SUMMARY.md files for plans 01–10 (each of which independently records its own `pytest`/`npm test` exit + tail).

## Files Created/Modified

- `.planning/phases/07.5-github-repo-connector/07.5-PHASE-VERIFICATION.md` (created Task 1, Section 4 filled in Task 3)
  — Frontmatter (`status: passed`, `signed_off: 2026-05-05`) + Header (env table) + Section 1 (per-VALIDATION.md row commands + aggregate suite gates) + Section 2 (GH-01..GH-05 traceability with file:line) + Section 3 (T1–T9 grep invariants — green at write time and re-checked at sign-off) + Section 4 (sign-off boxes all checked + dated summary sentence + cross-check vs Sections 1–3).
- `.planning/phases/07.5-github-repo-connector/07.5-MANUAL-SMOKE.md` (created Task 1)
  — Operator-facing 263-line checklist: pre-flight (P1 register App, P2 generate private key, P3 set Fly secrets, P4 set Vercel env, P5 sanity-check) + smoke (S1 sign-in, S2 navigate /settings/integrations, S3 see install button, S4 click → opens GitHub install URL, S5 pick repo, S6 callback redirect → installation row appears, S7 ScanTriggerForm renders, S8 pick repo from combobox, S9 pick branch, S10 leave path = ".", S11 click Scan, S12 navigate to /scans/{id} pending, S13 poll → ready + Stripe meter event in test dashboard, S14 R2 object exists at teams/{team_id}/scans/{scan_id}.json, S15 negative-path scan with bad path → failed + Retry, S16 token-leakage grep `fly logs | grep -E "ghs_|x-access-token"` returns zero matches).
- `.planning/phases/07.5-github-repo-connector/07.5-11-SUMMARY.md` (created Task 3 — this file)
- `.planning/STATE.md` (modified Task 3) — last_updated/last_activity bumped to 2026-05-05; progress.completed_plans 91 → 92; progress.completed_phases 12 → 13; percent recomputed; Current Position advanced to next phase; Decisions section gained 2 new rows for plan 11; Session Continuity timestamp + resume marker advanced.
- `.planning/ROADMAP.md` (modified Task 3) — Phase 7.5 progress row flipped to "11/11 Complete 2026-05-05"; plan 07.5-11 list checkbox flipped to `[x]` with completion-line summary mirroring this SUMMARY's accomplishments.

## What Phase 7.5 Shipped (recap of plans 01–10 as closed by this verifier)

The phase delivers the full **"connect a GitHub repo → pick branch + path → scan → see result"** UX end-to-end, GitHub-only, multi-provider deferred to v1.2 per solo-founder scope discipline:

- **Plan 01 (Wave 0 foundation)** — backend deps (httpx, redis, fakeredis, respx, cryptography, jwt, taskiq broker plumbing), dashboard deps (cmdk, lucide), Dockerfile +git, `Settings.GITHUB_APP_*` fields, shadcn `Command` primitive added.
- **Plan 02 (Wave 0 schema)** — alembic 007 (`github_installations` table + RLS + grants) + 008 (`scans.status='pending'` enum value + 6 github_* columns + `idx_scans_github_dedup` partial index `WHERE status='ready'`) + ORM extensions + 5 shared pytest fixtures (rsa_private_key session-scoped, gh_settings_patched, fake_redis, respx_router, github_installation_factory).
- **Plan 03 (Wave 1 auth/client)** — `mint_app_jwt` + `mint_installation_token` + `list_installation_repos` (60s `gh:repos:*` Redis cache) + `list_branches` + `get_head_sha` (URL-encoded branch path for `feature/foo`) + `get_installation_metadata` (App-JWT only, never installation token) + 5 Pydantic response schemas; defensive `isinstance(loaded, RSAPrivateKey)` guard at sign time.
- **Plan 04 (Wave 2 backend endpoints)** — `/v1/github/installations` + `/v1/github/repos` + `/v1/github/branches` + `/v1/github/install-callback` (4 handlers in app/routes/github.py + main.py registration); state==clerk_org_id CSRF check (NOT team.id); membership probe BEFORE GitHub API call; 60s cache invariant locked by test that asserts ZERO GitHub calls on cache hit; 403/429 → 503+Retry-After:60.
- **Plan 05 (Wave 2 services)** — `finalize_scan` (UPDATE pending→ready + meter atomic for worker context, idempotent `WHERE status != 'ready' RETURNING`) + `fire_scan_meter_or_502` (route-only meter+502 translation); `POST /v1/scans/from-github` route with RLS membership probe + `get_head_sha` + INSERT pending + lazy `from app.queue.tasks.scan_repo import scan_repo` + `.kiq` dispatch with locked 7-kwarg contract (CC-4) + enqueue-failure failsafe; ScanGetResp extended with 6 new optional fields + `presigned_get_url: str | None` signed only when `status==ready`.
- **Plan 06 (Wave 3 worker)** — `scan_repo` `@broker.task` pipeline (mint installation token → mkdir tmp_root via `_make_tmp_root()` factory → `git clone --depth=1 --single-branch --branch X` with `GIT_TERMINAL_PROMPT=0` + `asyncio.wait_for(CLONE_TIMEOUT_S=60)` + token redaction at decode + traversal guard via `(tmp_root/path).resolve().relative_to(tmp_root.resolve())` catching `../` AND absolute paths in one check + `infracanvas scan --ci` with `rc∈(0,1)` success + `Path.read_bytes` + sha256 + `r2.put_bytes` + finalize_scan); 3-layer token redaction defense (decode site + outer except + log.bind never receives token); failure-path UPDATE with `WHERE status='pending'` guard against ready-clobber; `r2.put_bytes` async helper via `run_in_threadpool`.
- **Plan 07 (Wave 3 dashboard proxies)** — 4 Next.js Route Handlers under dashboard/app/api/github/{installations,repos,branches}/route.ts + dashboard/app/api/scans/from-github/route.ts via existing `backendFetch` Clerk-JWT chain; repos+branches preserve 503+Retry-After:60 for rate-limit toasts; POST preserves 422/503/404 with generic `{error: 'request_failed'}` body to mitigate T-07.5-07-01 backend-error-leak (status-only forwarding); lib/types.ts extensions (InstallationResp narrows `github_account_type` to literal union); scan-detail page Rule-3 placeholder for nullable `presigned_get_url`.
- **Plan 08 (Wave 4 components)** — 3 reusable client components: `InstallButton` (window.open install URL with state==clerkOrgId + noopener,noreferrer per T-07.5-08-01), `RepoCombobox` (shadcn Popover+Command with `shouldFilter={false}` so backend retains q-filter authority + 250ms useRef debounce + cancellable fetch + lock icon on private repos + inline 503 alert), `BranchPicker` (shadcn Select + lazy-load on selectedRepo + URL-encoded full_name + default-to-default_branch fallback only when value empty + same 503 inline alert). Components ship independently — composition belongs to Plan 09.
- **Plan 09 (Wave 5 trigger form + page)** — `ScanTriggerForm` composes Plan 08 components + path Input + Scan Button → POST → router.push(`/scans/{id}`); status-only error mapping (T-07.5-09-03 — never renders raw backend body); `/settings/integrations` page rewritten as 3-state machine keyed off `installations: InstallationResp[] | null` (loading/preinstall/postinstall) with bounded post-install hydration poll capped at 5×3s ≈ 15s when `?install=success` AND empty list (T-07.5-09-01 mitigation); Slack stub preserved; Rule 1 refresh of 2 settings-routes.test.tsx invariants for the removed disabled placeholder.
- **Plan 10 (Wave 5 polling + scan-detail gate)** — `/api/scan-status` proxy via Clerk JWT mirroring scan-presigned route; `ScanPendingClient` implements CC-14 useEffect+setInterval(2000)+cancelled-flag teardown polling `/api/scan-status` while pending → router.refresh on ready, render error_message + Retry CTA on failed; Retry POSTs `/api/scans/from-github` with payload sourced exclusively from server-fetched scan github_* columns (T-07.5-10-03); scan-detail page status gate via `renderScanByStatus` helper (extracted to its own module per Next.js 15 page-export restriction Rule-1 fix) routes pending/failed → ScanPendingClient, ready+URL → unchanged ScanViewerClient path.

Net delivery: backend pipeline (4 routes + 1 worker task + 2 sibling helpers + 1 R2 helper + 2 alembic migrations) + dashboard surface (4 proxy routes + 4 client components + 1 polling shell + 1 status-gate helper + 1 page rewrite + lib/types.ts extensions) + verification gates (PHASE-VERIFICATION.md + MANUAL-SMOKE.md). Test counts at phase close: backend ≈ 100+ green (full suite, including 13 scan_repo tests + 7 install-callback + 5 finalize_scan); dashboard 225/225 green (was 183 at phase start; +42 net across plans 08+09+10).

## Decisions Made

1. **Phase 7.5 sign-off frontmatter pattern** — `status: passed` + `signed_off: 2026-05-05` recorded in PHASE-VERIFICATION.md frontmatter as the canonical machine-readable phase-close marker. Future audits read the frontmatter not the prose; if a regression is suspected against GH-01..GH-05 the response is to re-run Section 1 + Section 3 commands and append a new sign-off block to a separate audit file, NOT to edit this PHASE-VERIFICATION.md in place.

2. **Pre-flight reusability** — MANUAL-SMOKE.md's P1–P5 pre-flight section is structured as one-time-per-env (operator-blocking, GitHub App register UI is interactive + private key never leaves clipboard). Subsequent smoke runs after pre-flight skip straight to S1. Documented inline so a future operator on a new dev env knows the pre-flight is mandatory but a re-deploy on the same env is not.

3. **GH-* IDs stay phase-internal** — GH-01..GH-05 live in 07.5-CONTEXT.md + every PLAN frontmatter, but are NOT promoted to .planning/REQUIREMENTS.md. The global requirements taxonomy already covers GitHub integration via the WBH-* category (Phase 8 webhook). Phase-internal IDs stay scoped to the phase artifacts to avoid double-bookkeeping; the cross-link is the Section 2 traceability matrix in PHASE-VERIFICATION.md.

## Deviations from Plan

None — Tasks 1, 2, and 3 executed exactly as written in `07.5-11-PLAN.md`. The only minor variance is that the operator's resume signal at Task 2 referenced 16 numbered checkpoints (S1–S16 — the smoke section as actually written in MANUAL-SMOKE.md) rather than the 21 enumerated in the original plan's prose (which was an early estimate that pre-dated MANUAL-SMOKE.md being authored). The 16-checkpoint structure is canonical going forward; the count drift is recorded transparently in Section 4.

## Authentication Gates

- **Task 2 (`checkpoint:human-verify`)** — Operator-only steps that automation cannot reach: GitHub App registration UI at github.com/settings/apps/new (interactive form), `flyctl secrets set` (requires operator's Fly auth + clipboard handoff of private key bytes that must NEVER touch git), Stripe test-dashboard observation (UI-only — no programmatic confirmation of meter-event UI rendering), R2 console object check (UI-only — though programmatically grep-able via `aws s3 ls` if the operator ever wires it). The operator's resume signal **"approved"** confirmed all of these passed; Section 4 box for each is checked.

## Threat Flags

None — this plan only writes verification artifacts. No new code shipped, no new endpoints, no new auth surface, no new schema. The threat register entries T-07.5-11-01 (Repudiation: phase closed without manual smoke) and T-07.5-11-02 (Information Disclosure: smoke checklist embeds tokens in writeup) are both mitigated as planned: T-07.5-11-01 by the `checkpoint:human-verify gate=blocking` that prevented Task 3 from running until the operator approved; T-07.5-11-02 by MANUAL-SMOKE.md's S16 token-leakage `fly logs | grep` ASSERTING zero matches across the smoke window (the checklist text itself never embeds real tokens — only the regex pattern that proves their absence).

## Self-Check: PASSED

- File `.planning/phases/07.5-github-repo-connector/07.5-PHASE-VERIFICATION.md` exists ✓ (Section 4 boxes all checked, frontmatter `status: passed`)
- File `.planning/phases/07.5-github-repo-connector/07.5-MANUAL-SMOKE.md` exists ✓ (263 lines, P1–P5 + S1–S16)
- File `.planning/phases/07.5-github-repo-connector/07.5-11-SUMMARY.md` exists ✓ (this file)
- Modified: `.planning/STATE.md` ✓ (advanced to plan 11/11 + completed_phases incremented)
- Modified: `.planning/ROADMAP.md` ✓ (Phase 7.5 row + plan 07.5-11 row both checked)
- Commit `bbfa0c7` (Task 1: docs(07.5-11) add phase verification + manual smoke checklists) present in `git log` ✓
- Phase-close commit (Task 3): _this commit_ — verifiable post-commit via `git log --oneline -1`
